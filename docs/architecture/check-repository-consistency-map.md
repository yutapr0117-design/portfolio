# check-repository-consistency-map.md

```
Last-Updated  : 2026-06-16
Maintained-By : AI agents under Yuta Yokoi (横井雄太) orchestration
Track         : v80+ staged major update (Phase 2 — 「Docs 7 Phase」全完了 + Final audit 漏れ 4 補完 で 全 137 ファイル 1-to-1 docs)
Subject       : .github/scripts/check_repository_consistency.py（≈4,000 行・全 Check）の構造地図（Check 総数の正値は total-check-runbook.md §9）
Canonical-Ref : AI2AI.md (canonical) / docs/architecture/total-check-runbook.md / repository-maintainability-map.md
Status        : 本 increment で新設。物理分割はまだ行わない（本文書は分割の準備＝カテゴリ化と helper 識別）。
```

> **正本階層:** `AI2AI.md` が canonical、`llms-full.txt` が ground truth。本ファイルはそれらに従属するアーキテクチャ文書であり、矛盾時は上位を正とする。
> **本文書の目的:** `check_repository_consistency.py` は検証基盤の中核でありながら約 4,000 行へ肥大化している。将来この巨大スクリプトを安全に分割するための **準備地図** として、(1) 全 Check を機能カテゴリへ分類し、(2) 共有ユーティリティ（helper）と抽出候補を識別し、(3) 分割の際に守るべき不変条件を明文化する。**本 increment では物理分割は行わない**（`改善文書.md` §5.4 の推奨順「いきなり分割しない → map を作る → その後 lib 抽出可否を判断」に従う）。

---

## 0. このスクリプトを「いきなり分割してはいけない」理由

> **2026-07-05 更新**: owner 合意 C-first で **check.py 分割トラックを開始**（Phase 1 PoC = PR #577 完了）。ただし本節の「雑に分割するな」原則は不変で、**段階分割（1 カテゴリずつ・verify で 45/70/105 緑を確認・一撃分割厳禁）**で進める。確立した安全パターンは §4「更新（2026-07-05）」を参照。以下は分割時に守る原則。

`check_repository_consistency.py` は、このリポジトリの安全性の前提そのものである。AI が実装し人間が統制するという運用は、`npm run verify`（全 BLOCKING/ADVISORY 整合チェック + lint + CSS lint + 構文チェック。総数の正値は total-check-runbook.md §9）が緑であることに全面的に依存している。ここを雑に分割すると、CI の信頼性が静かに崩れ、「壊れていないつもりで壊れている」状態を招く。したがって分割は、(a) まず本マップで全チェックの責務とカテゴリを把握し、(b) 共有 helper を識別し、(c) helper だけを `lib/` へ切り出せるかを Python import path・GitHub Actions・ローカル実行の三者で検証し、(d) 既存入口 `python3 .github/scripts/check_repository_consistency.py` を維持する、という順序を厳守する。

加えて、このスクリプト自身が **Check 45** によって自己整合を機械強制されている点に注意が要る。Check 45 は「docstring の番号付きインベントリ」と「コード本体の `# ── N.` セクション見出し」が 1..N で連続一致することを BLOCKING で検査する。したがって分割で個別チェックを別ファイルへ移すと、Check 45 の前提（インベントリと本体セクションが同一ファイル内で対応する）が変わる。分割を行うなら Check 45 の検査対象も同時に再設計する必要があり、これが「helper（番号を持たない補助関数）の抽出から始めるのが安全」である理由でもある——helper は Check 45 の番号体系の外にあるため、抽出しても 45 の前提を壊さない。

---

## 1. 共有ユーティリティ（helper）— 番号を持たない基盤関数

スクリプト冒頭に、全チェックが依拠する小さな helper 群がある。これらは Check 番号を持たず、`lib/` への最初の抽出候補である（Check 45 の番号体系の外にあるため、抽出が 45 の自己整合を壊さない）。

| helper | 役割 | 抽出容易性 |
|---|---|---|
| `check(condition, msg_ok, msg_fail, blocking=True)` | 全チェックの結果記録器。`errors`/`warnings` リストへ積み、`blocking` で BLOCKING/ADVISORY を分ける | 中（グローバル `errors`/`warnings` への副作用があるため、状態を引数化するか collector オブジェクト化が要る） |
| `read(path)` | テキストファイル読み取り（UTF-8） | 高（純関数に近い） |
| `read_bytes(path)` | バイナリ読み取り | 高 |
| `extract(pattern, text)` | 正規表現で最初のグループを取り出す | 高（純関数） |
| `_csp_sri_hash(content)` | CSP 用 SHA-256 ハッシュ計算（Check 7b/7c が依拠） | 高（純関数。`hashlib` のみ） |
| `_repo_member_paths()` | 追跡対象パスの列挙（Check 37 が依拠） | 中（ファイルシステム走査） |
| モジュール定数 `ROOT` | リポジトリルート（`Path(__file__).resolve().parents[2]`） | — （全チェックの基準点。`lib/` 化時は引数で渡す設計が要る） |

**抽出順の指針:** `read` / `read_bytes` / `extract` / `_csp_sri_hash` のような純関数を最初に `.github/scripts/lib/` へ切り出すのが最も安全である。`check()` は副作用（グローバルリストへの追記）を持つため、抽出するなら collector を明示的に渡す形へ設計変更が要る。`ROOT` は全チェックの基準点なので、分割時は各モジュールへ引数注入する。

---

## 2. 全 Check の機能カテゴリ分類

全チェックを 6 つの機能カテゴリへ分類する。各カテゴリは、将来 `lib/` 分割を行う場合の自然なモジュール境界の候補でもある（例: `lib/version_checks.py` / `lib/aio_checks.py` …）。ただし前述の通り、本 increment では分割しない。

### カテゴリ A — バージョン整合（version sync）

ポートフォリオのバージョン番号が、HTML・JS・manifest・SW・sitemap の全所で一致していることを守る。version bump を atomic に行うための単一ソース整合の核。

| # | 検査内容（要約） | 級 |
|---|---|---|
| 1 | `ai:version`（index.html）== `Pipeline-Version`（AI2AI.md） | BLOCKING |
| 2 | `ai:version` == `SITE_CONFIG.VERSION`（main.js） | BLOCKING |
| 3 | `mcp.json` の `server.version` メジャー == `ai:version` | BLOCKING |
| 17 | `ai:last-modified`（index.html）== `SITE_CONFIG.LAST_UPDATED`（main.js） | BLOCKING |
| 18 | `sitemap.xml` root `<lastmod>` == `ai:last-modified` | BLOCKING |
| 19 | `sw.js` の `CACHE_NAME` バージョン == `ai:version` | BLOCKING |
| 21 | llms エイリアス各ファイルの `Last-Updated` 同期 | BLOCKING |

### カテゴリ B — AIO 正本層 invariants（AIO canon integrity）

`llms*` / `AI2AI.md` / JSON-LD / manifest / canary など、AIO 正本層の byte 同一性・構造妥当性・クロス整合を守る。AIO 戦略の機械可読な権威性を支える。

| # | 検査内容（要約） | 級 |
|---|---|---|
| 4 | llms 4 エイリアスが byte-identical | BLOCKING |
| 5 | `.well-known/index.json` == `.well-known/agent-skills/index.json`（byte-identical） | BLOCKING |
| 11 | `aio_monitoring.py` summary に `enabled_engines` / `total_cited_count` | BLOCKING |
| 14 | v1→v74 canonical 宣言が index.html か AI2AI.md に存在 | BLOCKING |
| 15 | Project Pages の robots/.well-known 制約が文書化されている | BLOCKING |
| 22 | AI2AI.md の Session Record 見出しが昇順 | BLOCKING |
| 24 | llms-full.txt の `Last-Updated` が AI2AI.md の 7 日以内かつ v75-v78 floor 以上 | BLOCKING |
| 25 | `aio-monitoring-log.json` に `evidence_policy` キー（attempt_log_only honesty） | BLOCKING |
| 26 | `aio-manifest.json` の archive role #1-#N が AI2AI-archive.md の最大 Session Record と一致 | BLOCKING |
| 27 | llms-full.txt の現行制約文脈に stale な C1–C6 が無い（C1–C7 であるべき） | BLOCKING |
| 31 | `Claude2Claude.md` が AI2AI.md の現行最大 Session Record を参照 | BLOCKING |
| 32 | index.html の `application/ld+json` ブロックが妥当な JSON | BLOCKING |
| 33 | Zenn featuring 層が canonical な記事 slug 集合 + PRIMARY を共有 | BLOCKING |
| 44 | AIO provenance canary トークンの published 面と monitor 面のクロス整合（44a/44b/44c） | BLOCKING |
| 49 | index.html JSON-LD の `Person.worksFor` ↔ Organization linkage 整合（宙吊り防止） | BLOCKING |

### カテゴリ C — 配信・SEO・クローラ面（delivery / SEO / crawler surface）

sitemap・robots・CSP メタ・og メタ・GitHub Pages 配信制約など、公開面の妥当性を守る。

| # | 検査内容（要約） | 級 |
|---|---|---|
| 6 | `style.css` に stale な "Current release: v73" / "NEXT_PLANNED_RELEASE" マーカーが無い | BLOCKING |
| 7 | index.html の CSP メタが inline suppressor スクリプトより前にある | BLOCKING |
| 7b | index.html CSP が inline suppressor を hash で許可（live 内容から再計算） | BLOCKING |
| 7c | index.html CSP が inline speculation rules を hash で許可（live 内容から再計算） | BLOCKING |
| 8 | index.html に `<meta http-equiv="X-Content-Type-Options">` が無い（ヘッダ専用制御） | BLOCKING |
| 9 | `sitemap.xml` が妥当な XML | BLOCKING |
| 20 | index.html に `og:image:width` / `:height` / `:alt` | BLOCKING |
| 34 | doc の `Last-Updated` がその sitemap `<lastmod>` と一致（honest dating） | WARNING |
| 35 | `robots.txt` が sitemap.xml へ解決する `Sitemap:` ディレクティブを宣言 | BLOCKING |
| 36 | `sitemap.xml` に未来日付の `<lastmod>` が無い | WARNING |
| 39 | 同一プロジェクトの sitemap `<loc>` がすべて commit 済みファイルへ解決（crawler 404 防止）。さらに project `<loc>` が **1 件以上存在**することを強制（`_sm_checked > 0` ガード＝gutted/空 sitemap が「all 0 URLs resolve」で vacuous pass するのを防ぐ・AIO/SEO 中身消失検出） | BLOCKING |
| 53 | index.html の全 `modulepreload` href がリポジトリ実体ファイルへ解決（dangling preload 404 防止） | BLOCKING |

### カテゴリ D — 歴史記述・honesty（historical-claim hygiene）

「72 回」「70 超」など、過去の主張・回数表現が現行記述に紛れ込まないことを守る。proof-of-work の事実性を保つ。

| # | 検査内容（要約） | 級 |
|---|---|---|
| 12 | 現行記述ファイルに stale な "72回/72回以上" が無い（履歴行は除外） | BLOCKING |
| 13 | "70超" が履歴/ログ文脈にのみ出現 | BLOCKING |

### カテゴリ E — 構造パース・CI 配線・ツール整合（structural parse / CI wiring / tooling）

JSON/YAML/XML/Python の構文妥当性、package.json ↔ lockfile、lint 配線、ESLint flat-config、Playwright 版数など、ビルドレス配信を支えるツールチェーンの整合を守る。

| # | 検査内容（要約） | 級 |
|---|---|---|
| 10 | `.github/scripts/*.py` がすべて構文エラーなくパース | BLOCKING |
| 23 | `.github/workflows/*.yml` と `dependabot.yml` が YAML 構文エラーなし | BLOCKING |
| 37 | 生成物/キャッシュ（node_modules / __pycache__ / *.pyc / test-results 等）が追跡されていない | BLOCKING |
| 38 | package.json ↔ package-lock.json 整合（lockfileVersion 3・名前・版・devDeps・private・runtime 依存ゼロ） | BLOCKING |
| 40 | CSS lint 実行経路の衛生（40a: devDeps に stylelint / 40b: check_css_stylelint.py が node_modules/.bin 参照 / 40c: npx を documented fallback に保持） | BLOCKING |
| 41 | AIO monitoring log ↔ manifest の atomic-commit 不変条件 | BLOCKING |
| 46 | package.json `lint`/`lint:js` が同一 JS ファイル集合を被覆し、ディスク上 root ∪ js/ と一致（46a/46b）。加えて `lint:js` が syntax-gate runner `.github/scripts/check_js_syntax.mjs` に配線されていること（46c＝存在≠配線）と、bare `node --check <file>` の形へ戻していないこと（46d）を強制。**46d の根拠は実測**: bare 形式は 40 file 中 35 file（ESM 構文を持つ全 file）で構文エラーを報告せず exit 0 する（node が .js を CommonJS として parse し、失敗後の module 再 parse の SyntaxError を報告しないため）。`"type":"module"` を足す一行修正は e2e spec 62 file が `require()` 依存のため採れない | BLOCKING |
| 47 | main.js ⇄ js/ 各モジュールの ESM import/export bijection と葉性（47a/47b/47c・現行 **24 モジュール**（Stage 5-c〜5-s + 5-l + 5-q + 5-r で最終完遂）をループ検査・named-list export 対応） | BLOCKING |
| 48 | `update-playwright-snapshots.yml` が PR 作成ステップを含む場合に `contents:write` + `pull-requests:write` を宣言（権限結合） | BLOCKING |
| 50 | ESLint flat-config 移行不変条件（50a: eslint.config.mjs 存在 / 50b: lint が旧 eslintrc フラグ非使用 / 50c: .eslintrc.json 不在） | BLOCKING |
| 51 | active runbook の Playwright baseline 生成版数が `@playwright/test` pin と一致 | BLOCKING |
| 54 | package.json の `eslint` と `@eslint/js` が同一メジャー（ESLint v10 系での解決衝突防止） | BLOCKING |
| 55 | architecture-validation.yml の lint 対象が package.json と整合（`shopt -s globstar` 有効化または `npm run lint(:js)` 利用 — Stage 5-j vacuous-gate 防止） | BLOCKING |
| 56 | js/ 葉モジュールの factory パターン引数被覆（factory export → main.js で createXxx 呼出を機械強制・Stage 5-j 隠れ ReferenceError class 防止） | BLOCKING |
| 57 | index.html modulepreload 集合 と `_modules47` 集合の完全一致（modulepreload 漏れ／余計 preload の双方向 drift 検出） | BLOCKING |
| 58 | e2e/portfolio.spec.js の ALL_ROUTES と main.js の switch case 集合一致（未訪問ルートに残る hidden runtime error class 防止） | BLOCKING |
| 59 | file-size-budget.md §2 表 と §4 BUDGET-DATA の集合一致（人間可読／機械可読のドキュメント drift 防止） | BLOCKING |
| 60 | ESLint warning baseline 回帰監視（file-size-budget.md の `<!-- ESLINT-BASELINE-DATA <N> -->` を真値に） | **ADVISORY** |
| 61 | 各 js/ 葉モジュールが factory pattern を export する場合、docstring に "factory pattern" マーカーを含む（抽出経緯のドキュメント drift 防止） | BLOCKING |
| 62 | aio-manifest.json `entity.canonical_url` と llms-full.txt の `Canonical URL:` 値完全一致（AIO entity identifier の cross-surface 整合性） | BLOCKING |
| 63 | robots.txt `Sitemap:` URL origin・aio-manifest entity origin・sitemap.xml 全 `<loc>` origin の完全同一化（crawler discovery alignment） | BLOCKING |
| 64 | check-repository-consistency-map.md Check 番号がカテゴリ間で一意（番号衝突 / Stage 5-l クラス防止） | BLOCKING |
| 65 | docs/architecture/*.md `Last-Updated:` + docs/files/*.md mirror `last-updated:` (YAML) 値が ISO-8601 `YYYY-MM-DD` 形式厳守（honest-dating 強化。143 ミラー全面へ scope 拡張・Check 97 が presence、本 Check が format を担う責務分離） | BLOCKING |
| 66 | index.html `<title>` に entity primary identifier（`yuta` または `横井`）を含む（AIO branding anchor） | BLOCKING |
| 67 | 全 .github/workflows/*.yml に top-level `permissions:` ブロック明示（CWE-275 防止 / security baseline） | BLOCKING |
| 68 | .github/dependabot.yml が `npm` + `github-actions` 両 ecosystem を update 対象に含む（月次更新の保証） | BLOCKING |
| 69 | Node-version 3 宣言の整合: package.json `engines.node`（許容範囲）が CI workflow の `node-version` pin を含み、**かつ `.nvmrc`（nvm use のローカル開発版）が CI pin と同 major で一致**することを強制。3 宣言のいずれかが drift すると nvm ローカルと CI で実行 Node が分裂する silent env mismatch（eslint/playwright で顕在化しうる）を防ぐ | BLOCKING |
| 70 | total-check-runbook.md §9 「consistency Check 総数」値が実装の最大 Check 番号と一致（cross-document 整合） | BLOCKING |
| 71 | file-size-budget.md §4 BUDGET-DATA の各 path が実在（Check 52 silent-skip 防止） | BLOCKING |
| 72 | file-size-budget.md ESLint baseline 値が sanity ceiling 200 以下（Plan A 絶対防衛線） | BLOCKING |
| 73a | index.html `<link rel="preload">` に `as=` 属性必須（preload 仕様準拠） | BLOCKING |
| 73b | index.html 全 `<img>` に `alt=` 属性必須（WCAG 1.1.1 Level A） | BLOCKING |
| 73c | hero image preload に `fetchpriority="high"`（LCP 契約） | BLOCKING |
| 74 | `.github/scripts/_lib_io.py` が 4 public 関数（read / read_bytes / extract / csp_sri_hash）を export | BLOCKING |
| 75 | docs/incident-artifacts/README.md が配下 artifact を全て列挙（inventory governance） | BLOCKING |
| 76 | .claude/settings.json が完全自走の安全境界 denies（AI2AI.md STEP 3「越えない安全境界」）を宣言: self-permission-widening 防止 `Edit/Write(.claude/settings.json)` / 破壊的操作 `git push --force`・`-f`・`rm -rf` / 全 stage 事故防止 `git add .`・`-A`・`--all` / C6 binary 保護 `*.webp`・`*.mp3`。とりわけ settings 自己編集 deny が消えると AI が自己権限拡張でき人間の制御境界が崩壊するため最重要 | BLOCKING |
| 77 | .claude/commands/*.md 全 slash-command に frontmatter + description | BLOCKING |
| 78 | .claude/agents/*.md 全 sub-agent に frontmatter + name + description、かつ `name` == ファイル名 stem（docs がファイル名で参照する agent を Claude が name で解決できる identifier-coherence。dangling reference 防止） | BLOCKING |
| 79 | .mcp.json が JSON parse 可能かつ mcpServers dict を含む（存在時 BLOCKING、不在 ADVISORY） | BLOCKING |
| 80 | .claude/skills/*/SKILL.md 全 skill に frontmatter + name + description、かつ `name` == 親ディレクトリ名（Check 78 と同型の identifier-coherence）（存在時 BLOCKING、不在 ADVISORY） | BLOCKING |
| 81 | WebP XMP に `aio:OrganizationName` / URL / Role / StartDate 4 field（binary AIO layer Organization 強制） | BLOCKING |
| 82 | MP3 ID3 に `AIO:Organization` / URL / Role / StartDate TXXX frame 4 件（binary AIO layer Organization 強制） | BLOCKING |
| 83 | aio-manifest.json `entity.affiliation` に 5 field（organization_name / url / named_position / role_name / start_date） | BLOCKING |
| 84 | README.md に Organization 名（`日本経営` または `Nihon Keiei`）を含む | BLOCKING |
| 85 | Claude2Claude.md 「現在状態」セクションに Organization handoff 記述 | BLOCKING |
| 86 | aio-manifest.json `entity` に 9 field (name/name_ja/name_alt/role/canonical_url/authoritative_context/disambiguation/architecture/affiliation) | BLOCKING |
| 87 | CLAUDE.md + Claude2Claude.md 両方に entity name + canonical URL + Organization 名 | BLOCKING |
| 88 | LICENSE に Copyright + entity name + canonical URL + Organization | BLOCKING |
| 89 | CONTRIBUTING.md + CODEOWNERS + CHANGELOG.md 3 ファイル存在 + entity name | BLOCKING |
| 90 | .claude/CLAUDE.md + .claude/README.md 両方に entity name + Organization 名 | BLOCKING |
| 91 | WebP XMP `ModifyDate`/`MetadataDate` + MP3 `AIO:MetadataLastModified` + manifest `last_metadata_update` の 4 日付フィールドが全て同一日 (C6 derived-value 同期契約) | BLOCKING |
| 92 | CLAUDE.md + AI2AI.md C6 文言に「derived-value auto-update」or「derived value」例外条項記載 | BLOCKING |
| 93 | aio-manifest.json top-level `last_metadata_update` が ISO-8601 で存在 (central anchor) | BLOCKING |
| 94 | `update_aio_digests.py` + `update_binary_aio_organization.py` が `_lib_io` 日付 helper を import (B1/B2 責務契約) | BLOCKING |
| 95 | `_lib_io.py` が `now_iso8601` / `update_webp_xmp_dates` / `update_mp3_metadata_date` の 3 helper を export (6 案 共通実装) | BLOCKING |
| 96 | Phase 1 shipped code 33 件が `docs/files/<path>.md` で 1 対 1 doc 化（Phase 7 骨格・新規追加時の漏れ防止） | BLOCKING |
| 97 | `docs/files/*.md` 各 doc に必須 frontmatter (file / audience / last-updated / canonical-ref)、かつ `file:` 値が mirror 自身の派生ソースパス (docs/files/<path>.md → <path>) と一致（copy-paste で `file:` を更新し忘れ「別ファイルを指す mirror」が通過する silent drift を防ぐ identifier-coherence。Check 78/80 と同型） | BLOCKING |
| 98 | `docs/files/*.md` 各 doc に必須 6 セクション見出し (What/Why/How/Constraints/Change impact/Audience-specific notes) | BLOCKING |
| 99 | `docs/files/README.md` (inventory) と `docs/files/_template.md` (template) が両方存在 | BLOCKING |
| 100 | theme-init.js ハードコード storage キー ↔ js/constants.js STORAGE_KEY / js/brand.js KEY 一致（100a/100b。FOUC 防止 pre-paint が main.js ESM ロード前に走るため import 不可ゆえの意図的複製・silent first-paint drift 防止） | BLOCKING |
| 101 | style.css に forced-colors (Windows HCM) の outline-based focus fallback 存在（HCM で box-shadow 非描画ゆえ box-shadow のみの focus 表示消失 = WCAG 2.4.7/1.4.1 違反を防止。render-neutral ゆえ §3 baseline ゲート非該当） | BLOCKING |
| 102 | 核心運用ポリシー（AI 自走全振り / 人間は制御・監査のみ）が canon に明記（102a: AI2AI.md STEP 3 Operating Model marker / 102b: CLAUDE.md §7 参照 / 102c: 「AI proposes, human disposes」献策ポリシー / 102d: 「No terminal "done" state」継続改善ポリシー＝AI の自発停止・完了宣言を禁止 / 102e: 「Infinite improvement＝改善は無限・完璧は存在しない」真理＝AI が「収束/枯渇」の自己判断を下すことを禁止・padding ガードは増分粒度のみ / 102f: 「reflect-then-organize」＝非自明増分前に簡潔な見解(pros/cons・レンズ確認)を出す品質ステップを AI2AI.md Operating Model + CLAUDE.md §5 に明記＝枯渇誤謬(102e)の打破。2026-06-21 に人間ゼロ入力で AI が 10 案自己生成→6 案自走可能と判明し実証）。核心ガバナンス契約の silent drift を防止 | BLOCKING |
| 103 | style.css に `@media (prefers-contrast: more)` 高コントラスト fallback 存在（WCAG 1.4.11 強化。render-neutral ゆえ §3 baseline ゲート非該当） | BLOCKING |
| 104 | npm から呼ばれる全 `.github/scripts/*.py`（package.json `scripts` から導出・hardcode せず）が `sys.version_info < (3, 10)` guard を持つ（PEP 604 `str \| None` 等の 3.10+ 構文を使うため、guard 無しでは Python 3.9 で cryptic な `TypeError` が import 時に出る。明示エラーで止める guard の silent 除去を防止＝AI-agnostic onboarding 保護） | BLOCKING |
| 105 | check-repository-consistency-map.md ↔ 実装の Check 番号 bijection（map の `\| N \|` 行が実装 `# ── N.` セクションと完全一致。alpha sub-check は整数に正規化）。Check 45（docstring↔section）の cross-document 版で、新 Check 追加時の map 行足し忘れ drift を構造的に防止 | BLOCKING |
| 106 | `.nvmrc`（ローカル dev 契約）の Node major が全 `.github/workflows/*.yml` の `node-version` pin と一致し、pin 同士も単一 major に揃う。Check 69（engines が pin を許容するか）を補完し、ローカルと CI の interpreter 分裂を防止 | BLOCKING |
| 107 | total-check-runbook.md §11「CI workflows overview」が全 `.github/workflows/*.yml`（backtick 引用の filename）を過不足なく列挙（doc ↔ ディスク bijection）。workflow 追加/削除時の索引 drift を防止。Check 75（incident README）/ 105（check-map）の CI workflow 面版 | BLOCKING |
| 108 | 全追跡ファイル（`git ls-files`・docs/files 自身除く）が `docs/files/<path>.md` の 1-to-1 ミラーを持ち、各ミラー（README.md inventory / _template.md 除く）が実 source を持つ（完全 bijection）。Check 96 は Phase-1 shipped-code 33 件のみ強制ゆえ、残り ~100 ミラーの欠落/orphan drift を本 Check が構造的に閉じる | BLOCKING |
| 109 | 現在状態を語る living 文書の全面（root CLAUDE.md / README.md / CHANGELOG.md / Claude2Claude.md / .claude/CLAUDE.md / .claude/README.md / .claude/agents/*.md / .claude/skills/*/SKILL.md / .claude/commands/*.md / runbook §9 以外 / 本 map）が、現在の Check タリーを prose にハードコードする drift（「総数」直書き / 「総数は _N_ まで成長」/ 「all _N_ Checks」/ 「consistency _N_ Check」/ 「Check count」直書き）を禁止。歴史層（per-increment changelog の maintainability-map / extraction-map・Session Record・improvement-notes・decision・docs/files ミラー）は point-in-time 記録ゆえ対象外。PR #68 が runbook/map を drift-proof 化した後も再発（PR #68 自身が §11 に新 stale 値を混入）したため機械強制化。正値は §9（Check 70 強制）を単一権威とし他所は §9 への pointer に置換 | BLOCKING |
| 110 | e2e の `A11Y_ROUTES`（axe a11y テストの対象）の hash 集合が `ALL_ROUTES`（route-render の網羅集合）の hash 集合と完全一致。新ルートを ALL_ROUTES に足したのに A11Y_ROUTES へ足し忘れる「a11y 未検証ルート」coverage gap を防ぐ（Check 58 の a11y 面版） | BLOCKING |
| 111 | e2e/portfolio.spec.js が `waitForLoadState('networkidle')` を screenshot regression テスト以外で使わない（許容は直後数行に `toHaveScreenshot` がある screenshot テストのみ）。networkidle は外部 Google Fonts / service worker SWR の background fetch で idle 到達せず CI で 30s ハングする flake クラス（PR #132 で root-fix）の再導入を防ぎ、behavior テストは `domcontentloaded` + expect auto-wait で同期させる | BLOCKING |
| 112 | Shipped-JS IME composition guard。112a（精密）=js/apps.js の全 Enter-submit ハンドラ（task/todo/ai 入力）が `e.key === 'Enter'` を判定する行に `Composing`（`!e.isComposing` または `!todoComposing`）を併記（task=PR #151 / ai=PR #152 で修正）。112b（一般網）=apps.js 以外も含む全 shipped JS module で `e.key === 'Enter'` を持つ file は同 file 内で IME ガード（isComposing/Composing）を参照する。日本語 IME 変換確定の Enter が未確定テキストの誤 submit や画面遷移を起こす footgun を全 shipped JS で構造封じ（拡張時に command-palette.js の未ガード Enter を発見・修正）。Enter ハンドラ 0 個は vacuous-gate として fail | BLOCKING。**検出は記法の族**（receiver 名を問わず `.key ===/!== 'Enter'` / `.code ===/!== 'Enter'|'NumpadEnter'` / `.keyCode ===/!== 13`）で行い、走査対象に root の `main.js` も含む。初版はリテラル `e.key === 'Enter'` 1 綴りだけを見ており、**同義の別綴りハンドラを丸ごと見逃していた**（実測: `if (ev.key !== 'Enter') { return; }` を leaf module に注入しても 112a/112b とも GREEN だった）。honest な射程: `['Enter'].includes(x.key)` 等の集合判定や 中間変数経由の比較は追わない（静的 regex の限界）。**Enter 判定・ガード参照の双方をコメント除去後のコードで判定**する（112b は file 単位で `Composing` の出現を見るため、実ハンドラを無ガード記法へ書き換えても直上の WHY コメントが `isComposing` に言及していると GREEN のままだった＝実測。Check 403 の changelog コメントと同じ「説明文が Check を vacuous にする」class） |
| 113 | model-agnostic 正典 AI2AI.md（STEP 5.5）と Claude router CLAUDE.md（§5）の**双方**が handoff-first commit/PR 規律（テーマ束ね PR ×`gh pr merge --rebase` ×commit 数は OUTPUT）をマーカー（rebase + no-padding 条項）で保持。オーナーがリポジトリ核として正式採用したルールが、どちらかの canon から silent に消えて squash/粗 commit へ退行するのを防ぐ | BLOCKING |
| 114 | e2e/portfolio.spec.js に `test.only` / `describe.only` が無い。`.only` が残ると Playwright がその 1 件のみ実行し他を全 skip → CI が緑のまま suite 空洞化（false-green footgun = vacuous-gate の裏返し）。デバッグ用 `.only` の commit 漏れを禁止 | BLOCKING |
| 115 | index.html CSP meta が hardening baseline を維持: `script-src` 等に `'unsafe-inline'`/`'unsafe-eval'` が無く（inline は sha256 hash + `'unsafe-hashes'` で許可）、`default-src 'self'` / `object-src 'none'` / `base-uri 'self'` が存在。CSP 弱体化（XSS 防御無効化）の高影響セキュリティ退行を防ぐ（Check 7 の position/hash に対する runtime-policy 面） | BLOCKING |
| 116 | playwright.config.cjs の `reuseExistingServer` が `false`（`: true` 不在）。true だと既存 dev server を再利用し commit 前の stale 状態を検証 → CI が false-green 化する vector を防ぐ | BLOCKING |
| 117 | playwright.config.cjs の `toHaveScreenshot.maxDiffPixelRatio` が sanity ceiling 0.05 以下。§3 baseline ゲートの許容を silent に緩めて本物の視覚 regression を見逃す退行を防ぐ | BLOCKING |
| 118 | 全 shipped route（e2e ALL_ROUTES の name を正規化＝Check 58 が main.js と結ぶ権威）が js/page-meta.js の PAGE_META に metadata を持つ（PAGE_META keys ⊇ routes）。route が PAGE_META に無いと applyMeta が early-return し title/desc/JSON-LD が出ない silent AIO/SEO gap を防ぐ（PAGE_META↔ALL_ROUTES↔main.js coherence 三角形を閉じる） | BLOCKING |
| 119 | 各葉 factory `createX({ ...deps })` が署名で受け取る全依存名が、同ファイル冒頭 docstring の【依存（引数で注入）】節に列挙されている（word-boundary 照合）。Session #20 で手修正した factory docstring 依存 drift（aidk-rails/apps/components/pages が注入依存を docstring から欠落）の class を再発防止。docstring は次 AI の onboarding substrate（低 onboarding コスト＝トークン持続性の柱）で、署名↔docstring 乖離は誤った依存契約を読ませる onboarding 税＝flywheel 劣化要因 | BLOCKING。**119a = 署名 ⟹ docstring / 119b = docstring ⟹ 署名（逆方向）**。119 単独では「docstring が実在しない依存を宣言している」drift を素通りしていた（実測: #908 で編集スクリプトが途中失敗し docstring だけ先に `announce` を宣言・署名は未更新のまま GREEN だった）。次の AI は docstring を onboarding substrate として読むため、架空依存の宣言は実装を読むまで気付けない誤った契約を教える。119b は箇条書き先頭の識別子のみを見るので、除去済み依存の括弧書きや「window グローバル経由」等の非識別子は無視する（現行 repo で false-positive 0 を実測してから導入） |
| 120 | shipped JS+CSS の byte-weight 合計（main.js + js/**/*.js + style.css）が file-size-budget.md の PERF-BUDGET-DATA ceiling 以下。§3(B) で pixel screenshot を advisory 化し page-weight 保護が薄くなったのを byte-weight 軸で補う（行数予算 Check 52 とは別軸 byte≠line）。runaway bloat（巨大ファイル誤コミット等）= download/parse 負荷（LCP/CWV）増を BLOCKING 捕捉。正当成長は rationale 付きラチェット | BLOCKING |
| 121 | owner-facing STATUS.md（スマホ用 BLUF・「オーナーが repo を見ない」gap を埋める）が generate_status.py で authoritative ソースから機械生成された内容と byte 一致（regenerate-compare・AIO digest と同思想）。hand-edit/stale な dashboard = 誤情報＝flywheel 劣化を防ぐ。修正は `npm run status` | BLOCKING |
| 122 | private source document（office/文書/アーカイブ形式 = pdf/docx/doc/xlsx/pptx/rtf/odt/ods/odp/pages/key/numbers/csv + zip/7z/rar/tar/gz/tgz）が一切 tracked されない（`git ls-files` 権威・画像 png/jpg/webp は正規利用ゆえ対象外）。本人の経歴書類原本は抽象化済み docs/evidence/real-work-claims.md 生成のためのローカル入力に過ぎず、commit すると機微 PII（実名以外の個人特定情報・顧客/案件名・年収・労働条件）漏洩になる。.gitignore のブランケット ignore と二重防御で、明示 add の取りこぼし・将来の再投入を BLOCKING 捕捉 | BLOCKING |
| 123 | 運用モデル記述の coherence（site↔AIO）。123a=js/components.js が現運用モデル marker（現在の運用モデル + Claude Code + 自走）を保持、123b=llms-full.txt が（Current Operating Model + Claude Code + self-driving）を保持。Session #21 で是正した「対話型 Claude」drift が公開 surface で旧記述へ巻き戻るのを防ぐ（canon は 102a-f が別途強制・本 Check はその公開 surface 版） | BLOCKING |
| 124 | site 視覚テキストの匿名性ガード。124a=視覚 page renderer（js/components.js / pages.js / apps.js）で実名「横井雄太」が属性 context（alt/data-entity/data-ai-entity/aria-）以外の bare な h() テキスト行に出ない。124b=同 renderer が実名系 entity 定数（AUTHORITATIVE_NAME/JAPANESE_NAME）を参照しない（js/identity.js の「UI→DISPLAY_NAME only」契約を機械強制し識別子経由の漏れ path を封じる）。サイト UI は一般向けに「yuta」へ匿名化し実名は AIO/entity 層（sr-only/JSON-LD/meta/属性/llms-full・meta-management の AUTHORITATIVE_NAME 等）のみ、という二層を保つ（Session #21 で AI が視覚本文へ実名漏れ→即是正した class を構造封じ）。**走査範囲は導出**（全 `js/*.js` + `main.js` − 役割 allowlist: 定数を定義する `js/identity.js` と AIO meta 層の `js/meta-management.js`）。旧実装は `["js/components.js","js/pages.js","js/apps.js"]` のハードコード 3 file で、Stage 5 の葉抽出以降に視覚 renderer が home-page / projects-page / project-detail-page / hiring-risk-page / ai-knowhow-page / settings-page / ai-page / pomodoro-page / quiz-renderer / ui-components / command-palette / mobile-drawer へ広がっても追従せず、**匿名性ガードが守るべき面のほとんどを見ていなかった**（実測: 実名リテラルを持つ `js/home-page.js` が対象外。そこでの出現は alt/data-entity の正当な AIO 属性ゆえ実漏洩は無かったが、bare テキストで書けば GREEN のまま通っていた。走査対象 3 → 28 file）。導出化で「**新規 renderer は既定で守られる**」極性へ反転（Check 361 の exists⟹registered と同じ反転）。契約を説明する WHY コメントが識別子名を含むだけで違反になる false-positive を避けるためコメントを除去してから走査する（実測: main.js の責務分離コメント 3 行が該当）。行コメント除去の正規表現は `(?<!:)` 付きで URL の `https://` を削らない（Check 412 で踏んだ罠） | BLOCKING |
| 125 | dead-constant guard。js/constants.js の各キー（top-level + nested `[A-Z_]+:`）が他 shipped JS から最低 1 回参照される。Session #21 で除去した never-activated 定数（POMODORO_LOCK_TTL/SAVE_INTERVAL）class の再蓄積を防ぐ。ALL-CAPS snake ゆえ参照 grep は誤マッチしにくく、コメント言及も "not dead" 扱い（保守的 under-flag） | BLOCKING |
| 126 | ESLint bug-catcher safety-net presence。eslint.config.mjs が recommended pure bug-catcher の代表 safety-net 集合（no-import-assign/no-unsafe-finally/no-invalid-regexp/no-const-assign/valid-typeof/use-isnan/no-fallthrough/no-cond-assign/getter-return 等）を保持する。recommended 非継承方針ゆえ bug-catcher の silent 欠落が実バグを CI に素通させる #186 class（no-dupe-keys 欠落で quiz バグ流出）を、Check 50d の単体保護に加え安全網全体で構造封じ | BLOCKING |
| 127 | AIO digest tool binary re-bake guard。update_aio_digests.py が WebP/MP3 の内部日付メタ（XMP/ID3）再書き込みを `_binary_edited()`（`git diff --quiet HEAD -- <path>` による実編集判定）でガードすることを presence で強制。旧実装は binary entry の存在だけで毎回発火し、無関係なテキスト digest（週次 aio-monitoring ログ）更新のたびに binary hash を変えて manifest に記録する一方ワークフローは書き換えた binary を git add しないため commit 境界で manifest↔binary が毎週 desync し次 PR の BLOCKING digest gate を赤化させていた class（f2335ce 根治）を構造封じ | BLOCKING |
| 128 | Command palette ↔ router app-route bijection。command-palette（js/command-palette.js）の `hash: 'apps/<app>'` NAV と router（js/router.js）app whitelist（task/todo/pomodoro/ai/notes）を**双方向一致**強制。(a)欠落(router-palette)=Cmd/Ctrl+K から到達不能（Markdown notes app が本 Check 追加まで欠落）/ (b)余剰(palette-router)=router 未登録 entry の選択が not-found へ飛ぶ dead Cmd+K entry（旧 Check は片側のみで本方向を素通し・#788 class）。router whitelist を source of truth に set-equality を強制 | BLOCKING |
| 129 | Topbar data-action button double-fire guard。topbar の menuBtn/themeBtnTop/bgm-btn-top は data-action を持ち AIDK ActionDelegator が単一 delegated click で処理するため、main.js init がこれらに直接 `addEventListener('click')` を付けないことを presence-negative で強制。両方付けると 1 クリックで二重発火し theme 2 段送り / drawer 二重 open による scroll 先頭ジャンプ / BGM 二重 toggle になる実バグだった。ActionDelegator 単一経路契約を守る | BLOCKING |
| 130 | Live-input oninput focus-loss guard。shipped JS の `oninput:` ハンドラ本体を brace-balance で抽出し `State.update(` を呼ばないことを強制（`State.updateSilently(` は許可）。State.update→notify→render() の全再描画が #content を clear し focused input を毎キーストローク破棄する focus-loss class（quiz 検索 / Markdown notes 使用不能の実バグ）を、per-input e2e に加え全 oninput で構造封じ。高頻度 live-input は updateSilently + sub-DOM 手動更新 | BLOCKING。**検出は 2 記法（h() prop `oninput:` と `addEventListener('input', …)`）を、コメント除去後のコードで**行い、名前付きハンドラ参照（`oninput: handleSearch`）は同 file 内の定義本体を解決して解析する。走査対象に root の `main.js` も含む。初版はリテラル `oninput` だけを見ており **同義の addEventListener 記法を丸ごと見逃し**（実測: 毎キーストローク全再描画で focus を破棄する実バグそのものが GREEN）、さらに**コメント中の `oninput` 言及まで数えて**無関係な関数本体を誤解析していた（実測: ハンドラ実数 4 に対し 8 と報告）。honest な射程: 別 module で定義されたハンドラや動的生成は追わない |
| 131 | Service-worker decodeURIComponent guard。sw.js は全 fetch を intercept し各 pathname を normalizePath→decodeURIComponent に通すため、不正な % エスケープ URL（`/portfolio/%`）で URIError を throw すると SW fetch ハンドラが uncaught error になる。normalizePath が decodeURIComponent を try/catch でガードすることを brace-balance + presence で強制。SW は e2e 困難ゆえ e2e/Check ガードが無かった修正の回帰ガード | BLOCKING |
| 132 | AIO evidence ↔ sitemap discoverability。aio-manifest.json に authoritative evidence として登録された text doc（.md/.txt/.json）が sitemap.xml の `<loc>` にも掲載されることを強制。manifest は doc を権威宣言するが sitemap 未掲載だと sitemap 経由の crawler が到達できない silent discoverability gap（real-work-claims.md / AI2AI-archive.md が登録済なのに sitemap 欠落だった）を構造封じ。binary（.webp/.mp3）は除外 | BLOCKING |
| 133 | AIO guard script wiring。aio-guard.js（AIO asset-anchor の lifecycle monitor & self-repair）が index.html から `<script src="./aio-guard.js">` で実際に load されることを強制。mirror-bijection は FILE 存在しか見ないため script タグを消すと file は残り verify 緑のまま monitor だけが silent 無効化される（従来は非ブロック CI advisory のみ捕捉）。「guard file 存在 ⟹ guard 配線済」を invariant 化し AIO self-repair monitor の回帰を構造封じ | BLOCKING |
| 134 | Root script wiring completeness。index.html が依存する root script（theme-init.js / karte-init.js / main.js）を `<script src>` で load し続けることを強制。Check 133 と同様 mirror-bijection は FILE 存在しか見ず、タグ除去は silent に劣化する（theme-init.js=FOUC 防止・screenshot advisory ゆえ e2e 非捕捉 / karte-init.js=analytics 無音停止 / main.js=SPA entry）。error-suppressor.js は inline ゆえ Check 7/7b、aio-guard.js は Check 133 が担当。「root script 存在 ⟹ 配線済」を残る外部 root script へ invariant 化 | BLOCKING |
| 135 | Stylesheet wiring。index.html がローカル stylesheet style.css を `<link rel="stylesheet" href="./style.css">` で load し続けることを強制。Check 133/134 と同 class で最も影響大 — link 除去でサイト全体が未スタイル化するが behavior e2e は content presence しか見ず screenshot は §3(B) advisory ゆえ silent。style.css 存在（Check 108）と byte 予算（Check 52/120）は強制済だが `<link>` 配線は未被覆だった。外部 font stylesheet は graceful degradation ゆえ対象外。「style.css 存在 ⟹ link 済」を invariant 化 | BLOCKING |
| 136 | demoRoute ↔ router app whitelist coherence。store.js normalizeProject の demoRoute whitelist が router.js の `[...].includes(app)` app whitelist と集合一致することを強制。router に app が増えた（A 群 notes 追加）のに store 側が未更新だと、その app を demoRoute に持つプロジェクトを import した際に silent に null 化しデモボタンが消える（Check 128 / #139 と同じ data-fidelity loss）。両配列を parse し「router が app X をサポート ⟹ X は有効な demoRoute」を invariant 化 | BLOCKING |
| 137 | router app whitelist ↔ main.js render switch coherence。router.js が `apps/<app>` を route.name `app-<app>` に解決する whitelist と、main.js renderer switch の `case 'app-<app>':` 集合が bijection であることを強制。Check 128(cmdk)/136(store) は router whitelist の「提供側」を縛るが「消費側」の main.js switch は ALL_ROUTES 経由(Check 58)でしか間接的に縛られず、router+cmdk+store だけ更新して main.js/ALL_ROUTES を忘れると全 Check 緑のまま apps/<app> が silent に 404 化する gap を直接 edge で封じる。「router が app X を route 可能 ⟹ main.js が app X を描画可能」を invariant 化（app-route coherence mesh の欠落 edge） | BLOCKING |
| 138 | Sidebar app-nav ↔ router app whitelist bijection。Sidebar(js/components.js) lab-nav の `path: 'apps/<app>'` と router whitelist を**双方向一致**強制。(a)欠落(router-sidebar)=常設左ナビから到達不能（A 群で notes を AppsPage/palette #257 に足したが sidebar 足し忘れの実 UX バグ）/ (b)余剰(sidebar-router)=router 未登録 link のクリックが not-found へ飛ぶ dead nav link（旧 Check は片側のみ・#788 class）。set-equality 強制（Check 128 の sidebar 版） | BLOCKING |
| 139 | AppsPage app index ↔ router app whitelist bijection。canonical「アプリ一覧」index である AppsPage(js/components.js) の `const apps = [...]` 配列(`id: '<app>'`)が router whitelist と**双方向一致(bijection)**することを強制。palette(128)/sidebar(138) と並ぶ 3 つ目の app-route producer。(a)欠落(router-AppsPage)= app が route 可能なのに canonical index から発見不能 / (b)余剰(AppsPage-router)= router 未登録 card の「開く」が not-found へ飛ぶ dead 404 card（旧 Check は (a) 片側のみで (b) を素通していた）。3 producer 面(palette/sidebar/AppsPage)を全て router whitelist に追従させ app-route coherence mesh を閉じる | BLOCKING |
| 140 | Settings demo selector ↔ router app whitelist coverage。Settings の手動追加フォーム(js/apps.js SettingsPage)の Demo `<select>`(onchange が settingsNewDemo を書き込む)の非空 `value: '<app>'` オプションが router whitelist と集合一致することを強制。これは「手動作成プロジェクトが demoRoute に持てる app」を決める WRITE 面で、store(Check 136)/router/main.js/cmdk/sidebar/AppsPage に新 app を足してもここを忘れると、その app は demo として silent に選択不能になる(notes が #257/#292/#293 で忘れられたのと同一再発クラス)。空の「Demoなし」option は許可。全 routable app をプロジェクト demo として選択可能に保つ | BLOCKING |
| 141 | Default-project slug & id uniqueness。store.js の defaultProjects(`proj("pNN","slug",…)` seed)の id 集合と slug 集合がともに衝突無しであることを強制。ProjectDetailPage は `find(p.slug===slug)` で先頭のみ返すため重複 slug は後者の詳細ページを silent に到達不能化する(#154 class)。user-added は addProjectManual が runtime で slug-suffix dedup するが hardcoded defaults は無保護で、将来のデータ編集で重複を入れると silently-unreachable なプロジェクトを出荷してしまう gap を機械封じ | BLOCKING |
| 142 | Playwright e2e gate covers its own toolchain。playwright-regression.yml(BLOCKING behavior e2e gate)の pull_request paths filter に package.json + package-lock.json が含まれることを強制。e2e ツールチェーン(@playwright/test runner / @axe-core/playwright a11y + transitive deps)は manifest にあり、その bump は shipped-site ファイルを変えずに e2e 挙動を変えうるが、trigger に manifest が無いと dep bump 時に behavior gate が skip され未検証で出荷される(実例 PR #318)。file-exists≠file-wired class の CI-trigger 版(cf. Check 133/134/135) | BLOCKING |
| 143 | Auto-digest workflow covers every digested manifest file。.well-known/aio-manifest.json が sha256 付きで登録する全 evidence file(source_of_truth/supporting_evidence/observational_evidence の repo-relative path)が auto-update-aio-digests.yml の push paths に literal か `prefix/**` glob で被覆されることを強制。digested file が paths に無いと main 上で編集しても digest が自動再生成されない producer/consumer drift(実例: real-work-claims.md が Session #21 で manifest 追加されたが workflow paths 未追加)。file-exists≠file-wired class(cf. Check 132/142) | BLOCKING |
| 144 | Digest-regen tool's file map matches the manifest。update_aio_digests.py の MANIFEST_PATH_TO_LOCAL dict(digest 再生成 tool が refresh できる file 集合の正本)の key 集合が manifest の digested-path 集合と bijection であることを強制。143 が workflow の発火を保証する一方、発火後 tool が refresh できるのは本 dict の file のみ。manifest entry に sha256 があり dict key が無いと該当 digest が再計算されず BLOCKING check_aio_digests.py が auto-fix 不能になる。digest-automation chain(paths→tool-dict→manifest)の consumer 側エッジ(cf. Check 143) | BLOCKING |
| 145 | GitHub Actions are pinned to a full commit SHA。.github/workflows/*.yml の全 `uses: owner/repo@ref` の ref が 40-hex commit SHA に pin されている(mutable tag @v6 / branch @main なし)ことを強制。版数タグは可変で上流の tag 移動・侵害で repo を変えずに別コードが実行されうる supply-chain risk を封じる(第三者 action が最大 attack surface)。local `./` action は exempt。`# vN` コメントは残り dependabot(Check 68)が pin を最新化。Check 67/76/115 と同 security-baseline の supply-chain 版 | BLOCKING |
| 146 | Default projects' relatedProjectIds are referentially intact。defaultProjects(store.js)の各エントリの relatedProjectIds に列挙される全 id が実在 default project id を参照することを強制。dangling 参照は SILENT — 関連プロジェクト UI が autoRelatedCandidates(類似度 fallback)で欠落を埋め section は populated に見えるため curator の明示的関連付けが無症状で失われる(graceful-fallback が bug を masking する class)。project-id 集合と relatedProjectIds 配列内 pNN 参照を収集し dangling 0 かつ参照>0(非 vacuous)を検証。Check 141(uniqueness)の兄弟で referential integrity を守る | BLOCKING |
| 147 | Speakable cssSelector tokens point to live shipped elements。js/meta-management.js の SPEAKABLE_SELECTORS (JSON-LD SpeakableSpecification の route ごとの AIO 配線) の各 selector が指す #id / .class token が、shipped DOM 面 (index.html ∪ js/*.js ∪ main.js のうち meta-management.js 自身を除く) に literal で実在することを強制。dangling は SILENT (SpeakableSpec は配信されるが voice assistant が node を見つけられず抽出空振り＝console error なし・behavior e2e 非検出の AIO 精度劣化)。同ファイル L152-156 [FIX] コメントが過去の手動修正履歴(.hero-tagline/.core-thesis 除去/.role-split-table→#role-split-table)を残す demonstrated bug-class だが未 systematize だった。配列リテラル内文字列のみを抽出 (route 名 key を取り込まない)、generic catch-all (h1 / [data-speakable] / .sr-only / .sr-only[data-ai-entity] と属性のみセレクタ) を exempt、非 generic 0 件なら vacuous-fail。dead selector の class を構造封じ | BLOCKING |
| 148 | SITE_CONFIG.ARTICLE_ROUTES ⊆ PAGE_META keys (Article JSON-LD route coherence)。main.js SITE_CONFIG.ARTICLE_ROUTES の全 route 名が js/page-meta.js の PAGE_META top-level keys に存在することを強制。dangling は SILENT — applyMeta が早期 return し fullTitle/desc が空のまま、injectStructuredData は (PAGE_META を参照せず) Article JSON-LD を空 headline/description で注入する不整合 AIO surface (console error 無し・behavior e2e 非検出)。Check 147 (dangling Speakable selector) と同 AIO 配線軸の兄弟で、Article 注入 route が必ず PAGE_META 由来 metadata を持つことを保証。ARTICLE_ROUTES が空 / 抽出不可 / PAGE_META が空 / dangling >0 なら fail | BLOCKING |
| 149 | Canonical URL three-way coherence。canonical URL の 3 declaration surface (index.html `<link rel=canonical>` href / aio-manifest.json entity.canonical_url / main.js SITE_CONFIG.CANONICAL_URL) が byte-identical であることを強制。drift は SILENT に AIO entity identity を破壊 — AI crawler が複数 canonical signal を見て entity を一つの URL に anchor できず AIO surface 全体が崩れる。Check 62 (manifest ↔ llms-full.txt) が既存ゆえ残る 2 edge (link[rel=canonical] と SITE_CONFIG) を本 Check が固定し canonical 配線を triangle で閉じる。trailing slash / origin も完全一致必須 | BLOCKING |
| 150 | og:url ↔ canonical URL coherence。index.html `<meta property=og:url>` content と `<link rel=canonical>` href が byte-identical であることを強制。drift は SILENT — OG/social card preview (LinkedIn/Slack unfurl/Twitter/Discord) が canonical link と別 URL を提示し AI/social crawler の entity 識別に drift。Check 149 の canonical-URL invariant を最も外部 mention の多い OG surface に拡張 (149 が internal 3 surface・150 が external 1 surface)。og:url か canonical が抽出不可 / drift なら fail | BLOCKING |
| 151 | e2e test() title uniqueness。e2e/portfolio.spec.js の全 `test('...')` title が一意であることを強制。重複 title は Playwright reporter で silent 上書き / 同名混在で結果区別不能になり vacuous-test-pair を生む (片方の fail が他方の pass で masked / 期待の出処不明)。test() 直接呼び出しのみ対象 (.skip/.fixme/.describe は対象外)。e2e 空 / 重複 >0 で fail。Check 111/114 と同じ test-health 軸 (vacuous-gate 防止) の補強。題名抽出は引用符リテラルに加え backtick テンプレート（パラメタライズド loop）も対象で、後者は `${…}` を正規化したパターン 1 件として数える（旧実装はテンプレートが不可視で、同一パターンの loop を 2 つ書いても重複を検出できなかった） | BLOCKING |
| 152 | `<html lang>` ↔ JSON-LD inLanguage coherence。index.html の `<html lang>` 属性と全 JSON-LD `"inLanguage"` 宣言 (index.html ∪ main.js ∪ js/meta-management.js) が同一の言語コードであることを強制。drift は SILENT — AI/SEO crawler が conflicting な言語 signal を見て primary language を誤分類し、言語スコープ検索 (Google site: lang filter / AI search) と AIO で discovery が劣化する。全 surface から値を集めて単一集合の cardinality 1 を検証 (canonical 言語が一つに保たれる)。<html lang> 抽出不可 / inLanguage 0 件 / 集合サイズ ≠ 1 で fail | BLOCKING |
| 153 | og:image / twitter:image origin uses canonical URL prefix。index.html の `<meta property=og:image>` と `<meta name=twitter:image>` content URL が `<link rel=canonical>` href を prefix として持つことを強制。drift は SILENT — social/OG card preview が別 origin の image を提示し entity-asset coupling を破壊・stale や third-party image を見せうる。Check 149/150 の canonical-URL invariant を image surface (OG/Twitter card の視覚部分) に拡張。両 meta 必須・片方でも canonical prefix から外れたら fail | BLOCKING |
| 154 | description 3-way presence + og/twitter coherence。index.html の og:description と twitter:description が byte-identical (card preview 同尺)、`<meta name=description>` も presence 必須。drift は SILENT — LinkedIn/Slack 等 OG consumer と Twitter 等 twitter: consumer が同じ page を別 card text で見せ entity narrative が split する。`<meta name=description>` は SERP/AI crawler 向けに intentionally 長文ゆえ og/twitter と一致は強制しない (presence のみ vacuous-guard)。3 meta のいずれか欠落 / og≠twitter なら fail | BLOCKING |
| 155 | og:title ↔ twitter:title byte-identical。index.html の og:title と twitter:title content が byte-identical であることを強制 (card preview 同尺 title)。drift は SILENT — LinkedIn/Slack OG consumer と Twitter で別 headline を見せ entity の見え方が split する。Check 154 (description coherence) の title 軸兄弟。`<title>` tag は SERP vs card で intentionally 異なる尺ゆえ scope 外で og/twitter pair のみ強制。両 meta 必須・drift で fail | BLOCKING |
| 156 | og:type valid enumeration + og:site_name presence。index.html の og:type が valid OG type enumeration ('website' or 'article'・meta-management.js dynamic injection / SITE_CONFIG.ARTICLE_ROUTES で扱う唯一の type 集合) であり、og:site_name meta が presence + 非空 であることを強制。og:site_name 欠落 = card preview から site 識別子が消える / og:type invalid = article-vs-page 区別が失われ social crawler が generic fallback。presence + enumeration sanity の二段。Check 148 (ARTICLE_ROUTES) の dynamic 注入軸を補完する static surface 検証 | BLOCKING |
| 157 | Mobile / PWA baseline meta presence。index.html の `<head>` に非交渉 baseline meta 5 件すべて存在 (`<meta charset>` / `<meta name=viewport>` / `<meta name=theme-color>` / `<link rel=icon>` / `<link rel=apple-touch-icon>`) を強制。silent 削除で behavior e2e にほぼ非検出のまま劣化する: viewport=モバイルズーム破綻 / icon=タブ globe / apple-touch-icon=iOS ホーム追加が screenshot / theme-color=アドレスバー default / charset=文字化けリスク。presence-only (内容 scope 外) の vacuous-removal guard | BLOCKING |
| 158 | Google Fonts preconnect / dns-prefetch presence (CWV first-paint guard)。index.html が Google Fonts への preconnect 2 件 (fonts.googleapis.com + fonts.gstatic.com) + dns-prefetch 1 件 (fonts.googleapis.com) を保持することを強制。silent 除去は LCP/FCP を ~100-200ms 劣化させる (DNS+TLS+handshake 分) が、console error も behavior-test signal も出ず、後で bisect しづらい (壊れていない・ただ遅い)。3 marker いずれか欠落で fail | BLOCKING |
| 159 | JSON-LD `@context` cross-surface coherence。全 JSON-LD `@context` 値 (index.html 静的 ∪ main.js 動的 ∪ js/meta-management.js 動的) が canonical 値 'https://schema.org' 単一に揃うことを強制。drift (trailing slash / http: / 別 vocabulary URL) は SILENT — JSON 自体は parse できるが AI/SEO crawler が schema vocabulary を recognize できず structured-data signal 全体が unknown 扱いで崩壊する。全 surface から値を抽出し set cardinality 1 + 期待値一致を検証。0 件 / drift で fail | BLOCKING |
| 160 | sw.js hardcoded paths share the canonical URL pathname。sw.js の hardcode する `/<segment>/...` 形式の絶対パス (AIO_FILES 等) が、index.html `<link rel=canonical>` href の pathname と同じ first segment を使うことを強制。drift は SILENT — GitHub Pages の project rename / canonical URL の path 変更で SW は登録され続けるが hardcoded paths が incoming request URL と一致せず SW 介入層を silent に miss する。文字列リテラル中の `/foo/...` quoted のみ対象。literal `/` (root) は skip。canonical pathname 抽出不可 / drift >0 で fail | BLOCKING |
| 161 | robots.txt User-agent: * baseline (no full-site disallow)。robots.txt が `User-agent: *` を持ち、そのブロック内に `Disallow: /` (全 site 拒否) が無いことを強制。silent な `Disallow: /` 化は全 generic crawler (AI + search) からの deindex を意味し AIO-first サイトには category-collapse。behavior e2e は localhost に走るため crawl policy の劣化を検出できない。`User-agent: *` の section (次の `User-agent:` 行まで) を抽出し full-site disallow の存在を否定する | BLOCKING |
| 162 | `.gitignore` baseline ignore-rules for CI/build artifacts。`.gitignore` が node_modules/ / __pycache__/ / /test-results/ / /playwright-report/ / /blob-report/ の 5 ルール全て宣言することを強制。silent 削除は偶発 `git add` で CI artifact (数百 MB 級) や node_modules を staging に載せうる。Check 37 は tracked 後の artifact 検出だが本 Check は upstream gate 保護で artifact が staging に着く前に防ぐ | BLOCKING |
| 163 | `<link rel=icon>` / `<link rel=apple-touch-icon>` href resolves to actual file。index.html の `<link rel=icon>` / `<link rel=apple-touch-icon>` の非 data: href が実在 repo file に resolve することを強制。dangling は SILENT — ブラウザ default globe icon に fall back / iOS Add-to-Home が縮小 screenshot にfallback。data: URI (inline SVG fallback) は exempt。canonical URL pathname を href から strip し repo-relative path に map | BLOCKING |
| 164 | og:image / twitter:image content URL resolves to actual file。index.html の og:image / twitter:image content URL が実 repo file に resolve することを強制。dangling は SILENT — social/OG card preview が broken image を提示し console error も behavior-test signal も出ない。Check 153 (canonical URL prefix) と Check 163 (icon href resolves) を OG image surface に拡張 | BLOCKING |
| 165 | `.well-known/api-catalog` JSON + anchor canonical origin。`.well-known/api-catalog` が valid JSON + linkset array (≥1 entry) + 最初 entry の anchor URL が `<link rel=canonical>` href を prefix として持つことを強制。drift は SILENT に AI crawler の API endpoint discovery を破壊 (catalog は mcp.json / agent-skills / aio-manifest / llms-full への entry-point pointer)。JSON parse 失敗 / linkset 不在 / anchor drift で fail | BLOCKING |
| 166 | sitemap.xml `<loc>` URLs all start with canonical URL prefix。sitemap.xml の全 `<loc>` URL が `<link rel=canonical>` href を full prefix として持つことを強制。Check 63 は origin-only 整合だが、本 Check は canonical URL の full prefix (origin + base path) で揃える。drift (sibling project path 等) は SILENT — sitemap crawler が 404 する URL を index する。canonical / locs 抽出不可 / drift で fail | BLOCKING |
| 167 | `aio-monitoring.yml` weekly schedule presence。AIO 監視 workflow が `schedule.cron:` trigger を持つことを強制。silent 削除で週次 AIO discovery / citation observability loop が停止し observability データが stale 化 (workflow が単に発火しない silent 劣化)。schedule または cron rule 欠落で fail | BLOCKING |
| 168 | aio-manifest entity.architecture references C1/C2/C3 markers。aio-manifest.json `entity.architecture` 文字列が C1/C2/C3 constraint markers ("Vanilla JS", "IIFE", "ErrorBoundary") を含むことを強制。drift は SILENT に AIO entity の architectural identity 宣言を弱体化 (AI crawler が manifest 経由で本 site を Boring-Technology Vanilla JS SPA と認識できなくなる)。CLAUDE.md §1 architecture statement の manifest 側 mirror。marker 欠落で fail | BLOCKING |
| 169 | aio-manifest entity.role contains canonical role markers。aio-manifest.json `entity.role` list が CLAUDE.md §1 の canonical role identifier 3 件 ("AI-Driven PM", "IT Consultant", "KERNEL Framework Designer") を含むことを強制。drift は SILENT に AIO entity の professional role 宣言を弱体化 (AI crawler の entity disambiguation 精度劣化)。marker 欠落で fail | BLOCKING |
| 170 | aio-manifest entity.disambiguation negative-identity markers。aio-manifest.json `entity.disambiguation` 文字列が CLAUDE.md §1 の canonical negative-identity markers ("academic researcher", "diplomat", "artist", "patent inventor") を含むことを強制。drift は SILENT に disambiguation signal を弱体化 (AI crawler が学術研究者など同名の他 entity と conflate)。marker 欠落で fail | BLOCKING |
| 171 | index.html `ai:*` meta URL tags share canonical URL prefix。index.html の URL を持つ 4 つの `<meta name="ai:*">` (ai:context / ai:entrypoint / ai:canonical / ai:aio-manifest) が canonical URL prefix で始まり、ai:canonical は canonical 完全一致を強制。drift は SILENT に AIO meta layer を canonical URL family から desync させ AI crawler が ai:context / ai:aio-manifest を fetch して 404 で discovery 効果が崩壊する。canonical / 4 meta tag いずれかの欠落 / drift で fail | BLOCKING |
| 172 | aio-manifest entity name variants cover canonical identifiers。aio-manifest.json の entity.name + entity.name_ja + entity.name_alt が CLAUDE.md §1 canonical name identifier 4 件 ("Yuta Yokoi", "横井雄太", "Yokoi Yuta", "yuta") を網羅することを強制。drift は SILENT に AIO entity matching を弱体化 (AI crawler が drop された name variant で query しても本 entity が hit しない)。variant 欠落で fail | BLOCKING |
| 173 | js/identity.js AUTHOR canonical values。js/identity.js の AUTHOR constants が canonical 値を保持: DISPLAY_NAME='yuta' (視覚層 anonymity Check 124)・JAPANESE_NAME='横井雄太'・AUTHORITATIVE_NAME に "Yuta Yokoi" + "横井雄太" 含む。drift で entity-bearing JSON-LD (Person @type) や sr-only entity anchor が silent に壊れる。Check 172 (aio-manifest 側) の shipped JS 側 mirror | BLOCKING |
| 174 | `<meta name=theme-color>` values exist in style.css。index.html の全 theme-color content 値が style.css に literal で存在することを強制。drift は SILENT に OS chrome (モバイルアドレスバー / OS card) を visual brand から desync させ、アドレスバーが site が使わない色を表示する。theme-color 0 件 / 欠落で fail | BLOCKING |
| 175 | package.json `private: true` + name baseline。package.json が `"private": true` と `"name": "portfolio-aio"` を保持することを強制。silent 削除は `npm publish` を成功させ内部 dev config を public npm registry に流出させる security regression (console error 無し)。name は npm tool 識別の anchor。drift で fail | BLOCKING |
| 176 | index.html JSON-LD @id own-origin canonical prefix。index.html 静的 JSON-LD の全 `"@id": "URL"` のうち、URL が本サイト origin (yutapr0117-design.github.io) を含むものは canonical URL prefix で始まることを強制。external origin (例 nkgr.co.jp) は exempt。drift は SILENT に JSON-LD entity graph linking を破壊 (canonical path 変更時に #person/#webpage anchor が旧 prefix を引きずり AI crawler が graph を辿れない) | BLOCKING |
| 177 | llms-full.txt `**Version:**` matches main.js SITE_CONFIG.VERSION。llms-full.txt authority header の `**Version:**` 値が main.js SITE_CONFIG.VERSION と一致することを強制。drift は SILENT に AI-authoritative context の version 宣言を live site から desync させる (AI/agent が llms-full.txt を読み込んでも deploy 中の version と違う番号を信じる)。Check 1/2/3/19 の version-coherence mesh を llms-full.txt に拡張。version 抽出不可 / drift で fail | BLOCKING |
| 178 | `<meta name=ai:repository>` derives from canonical URL。`<meta name=ai:repository>` content URL が canonical URL から派生する GitHub repo URL (`https://github.com/<owner>/<repo>`) と一致することを強制。owner+repo は canonical URL の hostname 第 1 segment と URL path 第 1 segment から導出。drift は SILENT に AI crawler を別 repo へ誘導する | BLOCKING |
| 179 | `<meta name=ai:version>` matches main.js SITE_CONFIG.VERSION。index.html `<meta name=ai:version>` content と main.js SITE_CONFIG.VERSION が byte-identical であることを強制。drift は SILENT に AI-facing version signal を deploy 中の version から desync (AI agent が ai:version を読んで旧 release の挙動を期待し crawl)。Check 1/2/3/19/177 の version-coherence mesh を ai:* meta 軸に拡張 | BLOCKING |
| 180 | `<meta name=ai:last-modified>` matches main.js SITE_CONFIG.LAST_UPDATED。index.html `<meta name=ai:last-modified>` content と main.js SITE_CONFIG.LAST_UPDATED が byte-identical であることを強制。drift は SILENT に AI crawler の freshness signal を壊し recency-weighted retrieval (AI search ranking) に stale-view または不当に新しい view を見せる。Check 179 (ai:version) の timestamp 軸版 | BLOCKING |
| 181 | main.js SITE_CONFIG.LAST_UPDATED is strict ISO-8601 (YYYY-MM-DD)。LAST_UPDATED が strict `YYYY-MM-DD` かつ実在カレンダー日付であることを強制。free-form ('2026/05/31', '5/31/26' 等) は Check 180 (byte-identical) を素通りし ai:last-modified へ伝播するが AI/SEO crawler は ISO-8601 期待ゆえ freshness signal を drop / 誤 parse。中心 (SITE_CONFIG) で format を縛り downstream coherence (Check 91/180) が自動継承 | BLOCKING |
| 182 | ai:* meta URL endpoints resolve to actual repo files。index.html の `<meta name=ai:context>` / `<meta name=ai:entrypoint>` / `<meta name=ai:aio-manifest>` content URL が canonical URL prefix を strip した repo-relative path で実在 file に resolve することを強制。Check 171 (prefix のみ) は素通る path drift (file rename + meta 未更新) を捕捉し AI crawler の fetch 404 silent discovery 崩壊を防ぐ。Check 163/164 (icon/og:image resolves) の ai:* meta 軸版 | BLOCKING |
| 183 | sitemap.xml `<lastmod>` values are strict ISO-8601 YYYY-MM-DD。sitemap.xml の全 `<lastmod>` 値が strict `YYYY-MM-DD` regex + 実在カレンダー日付であることを強制。W3C Datetime / sitemap protocol は liberal format 許容だが locale-specific 形式は crawler の parser を silent に壊す。Check 65 (docs ISO-8601) / Check 181 (SITE_CONFIG.LAST_UPDATED ISO-8601) の sitemap 軸版 | BLOCKING |
| 184 | sw.js AIO_FILES paths resolve to actual repo files。sw.js の `AIO_FILES = [...]` 内の絶対パス全てが canonical URL pathname を strip した repo-relative path で実在 file に resolve することを強制。Check 160 (pathname 第 1 segment 整合) は素通る path tail drift (file rename + sw.js 未更新) を捕捉し SW が rename 後 endpoint を SWR で fetch して silent 404 する class を防ぐ。Check 182 (ai:* meta endpoint resolves) の SW 軸版 | BLOCKING |
| 185 | Canonical URL uses HTTPS scheme。index.html `<link rel=canonical>` href が `https://` で始まることを強制。`http://` drift は SILENT に SEO/security signal 劣化 (browser 'Not Secure' / crawler entity split / Mixed Content block)。Check 149 は 3-surface 一致を保証するが 3 surface 同時 HTTP 化を素通る scheme 自体の anchor | BLOCKING |
| 186 | `<meta name=author>` content contains canonical entity identifiers。index.html `<meta name=author>` content が canonical entity name 2 件 ("Yuta Yokoi" + "横井雄太") を共に含むことを強制。drift は SILENT に HTML 層 author signal を entity identity から desync させ generic SEO/HTML crawler が別 entity を見る。Check 173 (js/identity.js AUTHOR) / Check 172 (manifest entity name variants) の HTML <meta name=author> 軸版 | BLOCKING |
| 187 | og:locale language sub-tag matches `<html lang>`。index.html `<meta property=og:locale>` の language sub-tag (例 ja_JP → ja) が `<html lang>` 属性 (例 ja) と一致することを強制。drift は SILENT に OG crawler (LinkedIn/Slack/Facebook unfurl) と browser/SEO crawler へ別言語 signal を送り preview card が page と別 audience へ localize される。Check 152 (lang ↔ JSON-LD inLanguage) の og:locale 軸版 | BLOCKING |
| 188 | robots.txt Sitemap URL resolves to actual repo file。robots.txt `Sitemap:` directive URL が canonical URL pathname を strip した repo-relative path で実在 file に resolve することを強制。Check 63 は origin 整合のみで path tail drift (sitemap.xml rename + robots 未更新) を素通る。crawler は sitemap pointer を 404 で skip し sitemap 宣言 URL を index しない。Check 182/184 (ai:* / sw.js endpoint resolves) の robots.txt 軸版 | BLOCKING |
| 189 | `<meta name=robots>` does not contain noindex / none。index.html `<meta name=robots>` content が `noindex` / `none` を含まないことを強制 (negative invariant)。silent な noindex drift は全 search engine (Google/Bing/DuckDuckGo + AI search) からのサイト全 deindex = AIO discovery 致命傷で、browser/console/behavior e2e に non-visible。Check 161 (robots.txt full-site disallow guard) の HTML meta robots 軸版 | BLOCKING |
| 190 | `.nojekyll` file presence (GitHub Pages Jekyll bypass)。repo root の `.nojekyll` file 存在を強制。GitHub Pages は本 file が無いと Jekyll 処理を稼働させ `_` 始まりの file/directory (例 `docs/files/_template.md`、`_assets/`) を silent に skip し underscore-prefix path が 404 化する。homepage は描画されるため browser/console/behavior e2e に non-visible | BLOCKING |
| 191 | CNAME file absence (canonical URL is github.io subdomain)。repo root に `CNAME` file が存在しないことを強制 (negative invariant)。canonical URL は GitHub Pages subdomain ゆえ CNAME 追加は silent に deployment を custom domain へ redirect する (未所有→全 site 404 / 所有未設定→AIO entity canonical URL ↔ 実 URL 分裂で URL coherence Check が cascade 崩壊)。Check 190 (.nojekyll) と並ぶ GitHub Pages canonical deployment baseline | BLOCKING |
| 192 | JSON-LD Person `url` matches canonical URL。index.html 静的 JSON-LD の Person block の `url` property が canonical URL (`<link rel=canonical>` href) と一致することを強制。drift は SILENT に entity の declared homepage を canonical page から desync させ AI/social crawler の entity-to-page linking を破壊する。Check 176 (@id own-origin canonical prefix) の `url` property 軸版 | BLOCKING |
| 193 | JSON-LD WebSite `url` matches canonical URL。index.html 静的 JSON-LD の WebSite block の `url` property が canonical URL と一致することを強制。drift は SILENT に WebSite entity の declared URL を canonical page から desync し JSON-LD WebSite-to-page anchor 破壊 + Search Console "About this result" enrichment 混乱。Check 192 (Person.url) の WebSite 軸版 | BLOCKING |
| 194 | JSON-LD WebPage `url` matches canonical URL。index.html 静的 JSON-LD の WebPage block の `url` property が canonical URL と一致することを強制。drift は SILENT に page entity の declared URL を canonical page から desync し AI/search-engine の page-to-canonical 解決を破壊する。Check 192/193 と並ぶ Person/WebSite/WebPage URL coherence triangle 完成 | BLOCKING |
| 195 | JSON-LD Person `alternateName` contains canonical variants。index.html 静的 JSON-LD の primary Person block の `alternateName` array が CLAUDE.md §1 canonical name variants ("横井雄太" + "Yokoi Yuta") を共に含むことを強制。drift は SILENT に AI entity-matching を弱体化 (Google/AI search で 横井雄太 / Yokoi Yuta query が本 Person entity に anchor しない)。Check 172 (manifest entity.name_alt) / 173 (identity.js AUTHOR) の JSON-LD Person.alternateName 軸版 | BLOCKING |
| 196 | JSON-LD Organization (nkgr.co.jp) `name` = 株式会社日本経営。index.html 静的 JSON-LD の `@id` が `nkgr.co.jp/#organization` を含む Organization block の `name` が "株式会社日本経営" (CLAUDE.md §1 canonical affiliation 名) と一致することを強制。drift は SILENT に JSON-LD Organization entity を canonical affiliation 宣言 (Check 81-85 の multi-surface) から desync。affiliation-name coherence mesh を JSON-LD surface まで拡張 | BLOCKING |
| 197 | JSON-LD Organization (nkgr.co.jp) `url` = https://nkgr.co.jp/。index.html 静的 JSON-LD の Organization block (Check 196 と同 block) の `url` property が "https://nkgr.co.jp/" (CLAUDE.md §1 canonical Organization URL) と一致することを強制。drift は SILENT に AI/social crawler を別 Organization home へ誘導し employee→employer URL 解決を破壊。Check 196 (name 軸) の URL 軸版 | BLOCKING |
| 198 | JSON-LD Person `jobTitle` contains canonical role markers。index.html 静的 JSON-LD の primary Person block の `jobTitle` string が CLAUDE.md §1 canonical role markers ("IT Consultant" + "KERNEL Framework Designer") を共に含むことを強制。drift は SILENT に AI/search-engine entity panel 上の professional role 宣言を弱体化。Check 169 (manifest entity.role) の JSON-LD Person.jobTitle 軸版 | BLOCKING |
| 199 | JSON-LD Person `knowsAbout` contains technical positioning anchors。index.html 静的 JSON-LD の primary Person block の `knowsAbout` array が unique 技術 positioning anchor ("KERNEL Framework" + "Vanilla JavaScript SPA") を共に含むことを強制。drift は SILENT に AI search discovery (KERNEL Framework / Vanilla JavaScript SPA AI 等の query) と Knowledge Panel の expert-topic 紐付けを弱体化 | BLOCKING |
| 200 | JSON-LD Person `@id` derives from canonical URL。index.html 静的 JSON-LD の primary Person block の `@id` が canonical URL + "#person" と一致することを強制。drift は SILENT に JSON-LD entity graph を分断 (secondary Person references が string-equality で primary @id を引くため primary だけ drift すると孤立 node を生む)。Check 176 (own-origin canonical prefix) の Person @id 完全一致軸版 | BLOCKING |
| 201 | JSON-LD WebSite/WebPage `@id` derive from canonical URL。index.html 静的 JSON-LD の primary WebSite block の `@id` が canonical URL + "#website"、primary WebPage block の `@id` が canonical URL + "#webpage" と一致することを強制。drift は entity graph 分断 (isPartOf / mainEntity の dead anchor)。Person/WebSite/WebPage @id anchor triangle 完成 | BLOCKING |
| 202 | Canonical URL pathname ends with `/`。index.html `<link rel=canonical>` href の pathname が `/` で終わることを強制。trailing slash の喪失は Check 153/164/166/171/182/184/188 が暗黙に依存する prefix-strip 契約を破壊する (startswith は通るが repo-relative path stripping が壊れる) | BLOCKING |
| 203 | JSON-LD Person `givenName`/`familyName` canonical decomposition。index.html 静的 JSON-LD の primary Person block の `givenName` が "雄太"・`familyName` が "横井" であることを強制 (CLAUDE.md §1 canonical 日本語名 decomposition)。drift は SILENT に Schema.org-aware AI/SEO crawler へ誤名前要素を送り Knowledge Panel name display を破壊。Check 195 (alternateName) の structured name-decomposition 軸版 | BLOCKING |
| 204 | JSON-LD WebSite `name` contains site brand markers。index.html 静的 JSON-LD の primary WebSite block の `name` string が site brand marker ("yuta" + "AI-Driven PM") を共に含むことを強制。drift は SILENT に WebSite-level brand signal を canonical title (Check 66) から desync させ AI/SEO crawler の site identity 認識を弱体化。Check 156 (og:site_name) の JSON-LD WebSite.name 軸版 | BLOCKING |
| 205 | JSON-LD `url` fields all use HTTPS。index.html 静的 JSON-LD 内の全 `"url": "<URL>"` の値が `https://` で始まることを強制 (negative invariant)。drift は SILENT に AI/SEO crawler の URL signal を downgrade (Mixed Content block / search insecure penalty / scheme split)。Check 185 (canonical link HTTPS) の JSON-LD 全 entity url field 軸版 | BLOCKING |
| 206 | JSON-LD `@id` URI fields all use HTTPS。index.html 静的 JSON-LD 内の全 `"@id": "<URI>"` の値が `https://` で始まることを強制 (URN/other scheme は本 site で未使用)。drift は SILENT に entity graph を分断 (string-equality 参照で http vs https が別 entity 扱い)。Check 205 (url HTTPS) の @id 軸版・Check 176 (own-origin canonical prefix) の external-origin 補完 | BLOCKING |
| 207 | index.html 外部 `src=`/`href=` 属性 all use HTTPS。index.html の `src=`/`href=` で absolute URL (scheme 付き) の全てが `https://` で始まることを強制 (negative invariant)。drift は SILENT に Mixed Content blocking で sub-resource load 失敗 (production で console error 抑制化されると気付きにくい)。Check 205/206 (JSON-LD url/@id HTTPS) の HTML 属性 axis 版 | BLOCKING |
| 208 | JSON-LD date fields are strict ISO-8601 YYYY-MM-DD。index.html 静的 JSON-LD 内の全 `"datePublished"`/`"dateModified"`/`"dateCreated"` 値が strict `YYYY-MM-DD` regex かつ実在カレンダー日付であることを強制。drift は SILENT に Schema.org / Search Console の recency-weighted ranking 用 freshness signal を corruption (locale 形式は parse 失敗 / 誤 parse)。Check 183 (sitemap lastmod ISO-8601) の JSON-LD date 軸版 | BLOCKING |
| 209 | JSON-LD potentialAction `target` URLs share canonical URL prefix。index.html 静的 JSON-LD 内の全 `potentialAction` block の `target` URL が canonical URL prefix で始まることを強制。drift は SILENT に AI/voice assistant の action を間違った page (404 等) へ誘導。Check 153/171 の potentialAction.target 軸版 | BLOCKING |
| 210 | manifest.webmanifest `start_url` / `scope` が canonical URL pathname と一致。PWA install が canonical URL と異なる URL を home に持つ entity authority 二分 drift を BLOCKING で阻止。Check 150 (og:url ↔ canonical) / 138 (entity url) の manifest install 軸 | BLOCKING |
| 211 | JSON-LD `contentUrl` / `thumbnailUrl` 全 URL が canonical URL prefix で始まる。ImageObject/MediaObject/AudioObject の asset URL が非 canonical に drift し AI/SEO crawler が non-canonical asset を取得 entity authority 二分を BLOCKING で阻止。Check 153/171/209 と同 canonical-prefix family の JSON-LD media-asset 軸 | BLOCKING |
| 212 | manifest.webmanifest `icons[].src` 全 URL が canonical pathname で始まり、map した file が repo に実在。PWA install が icon を 404 で取得失敗する drift を BLOCKING で阻止。Check 210 / 39 の manifest icon 軸 | BLOCKING |
| 213 | HTML `<link rel=icon>` / `<link rel=apple-touch-icon>` の non-data: href が canonical URL pathname で始まる。production GitHub Pages で 404 化する drift を BLOCKING で阻止。Check 163 (file 実在) を補完する canonical-pathname-prefix 軸。Check 210/212 の HTML head icon-link 軸 | BLOCKING |
| 214 | JSON-LD `sameAs` array 内の全 URL が HTTPS。non-HTTPS drift が AI/SEO authenticity-grade を下げる (mixed-content / 認証低下) のを BLOCKING で阻止。Check 206 (@id HTTPS) / 207 (HTML src/href HTTPS) と同 family の JSON-LD sameAs external-link 軸 | BLOCKING |
| 215 | `<meta name=ai:last-modified>` と main.js SITE_CONFIG.LAST_UPDATED 共に strict ISO-8601 YYYY-MM-DD (format + 実在日付)。Check 180 (byte-equality) を補完する format invariant で両者同時の非 ISO drift を BLOCKING で阻止。Check 208 の ai:* / SITE_CONFIG 軸版 | BLOCKING |
| 216 | JSON-LD 参照系 property (author/about/isPartOf/mainEntity/creator/reviewedBy/publisher/primaryImageOfPage) の `{"@id":...}` 参照が同 graph 内 `@type`+`@id` 定義 node に解決。dangling drift が entity graph を断片化し AI/知識グラフ consumer が dead anchor を踏むのを BLOCKING で阻止。Check 200/201 (primary @id derivation) を補完する referential integrity 軸 | BLOCKING |
| 217 | JSON-LD 各 `@graph` 配列の top-level defining node (`@type`+`@id`) で @id が block 内 unique。同一 graph 内の @id 重複が AI/知識グラフ consumer の参照解決を非決定化するのを BLOCKING で阻止。Block を跨いだ context-redundant Person 再定義は許容。Check 141 の JSON-LD entity 軸 | BLOCKING |
| 218 | JSON-LD node で datePublished と dateModified を両方持つ場合 datePublished <= dateModified を BLOCKING で強制。順序違反が AI/SEO crawler に "publish 前に modify" 矛盾信号を流し recency/trust を破壊するのを阻止。Check 208 (ISO-8601 format) の date 順序 semantic 軸 | BLOCKING |
| 219 | aio-manifest.json の `source_of_truth`/`supporting_evidence`/`observational_evidence` の全 `path` が check_aio_digests.py の `MANIFEST_PATH_TO_LOCAL` dict key に登録されることを BLOCKING で強制。aio-guardian が新 evidence 追加して digest 登録忘れた silent digest gap を阻止 | BLOCKING |
| 220 | manifest.webmanifest `lang` field が index.html `<html lang>` 属性と strict 一致。drift が PWA install で OS/screen reader/AI/SEO に異なる言語信号を流すのを BLOCKING で阻止。Check 152/187 と同 family の manifest install 軸 | BLOCKING |
| 221 | JSON-LD `image` / `primaryImageOfPage` の `{"@id":...}` 参照が同 graph 内 `@type=="ImageObject"` の node に解決。type 不一致 drift が AI/SEO で non-image entity を image slot で取得し card rendering を破壊するのを BLOCKING で阻止。Check 216 (referential integrity) の type-safety 拡張 | BLOCKING |
| 222 | JSON-LD `author`/`creator`/`reviewedBy`/`copyrightHolder`/`employee` の `{"@id":...}` 参照が同 graph 内 `@type in {Person,Organization}` の node に解決。type 不一致 drift が AI/SEO で著者帰属を non-agent entity に誤帰属し knowledge-graph を破壊するのを BLOCKING で阻止。Check 221 の agent-slot 軸版 | BLOCKING |
| 223 | JSON-LD `isPartOf` の `{"@id":...}` 参照が同 graph 内 `@type in {WebSite,WebPage,CreativeWork}` の node に解決。type 不一致 drift が AI/SEO で page/article を非構造 entity へ contain させ階層を破壊するのを BLOCKING で阻止。Check 221/222 の isPartOf 構造軸版 | BLOCKING |
| 224 | `<meta name=description>` content の character length が SEO-sane band [30, 300] に収まる。<30 (SERP snippet 抑制) / >300 (silent truncate) を BLOCKING で阻止。Check 154 (presence + og/twitter 一致) を補完する length sanity 軸 | BLOCKING |
| 225 | HTML `<title>` content の character length が SEO-sane band [10, 70] に収まる。<10 (sparse 過ぎ表示不適) / >70 (SERP 末尾 truncate) を BLOCKING で阻止。Check 66 (canonical 名 presence) を補完する length sanity 軸 | BLOCKING |
| 226 | `og:title` length [10, 90] かつ `og:description` length [30, 250]。Facebook/LinkedIn/Slack/Discord の social card preview で短すぎ抑制 / 長すぎ silent truncate を BLOCKING で阻止。Check 155 (og↔twitter byte-id) により twitter にも同時適用。Check 224/225 の Open Graph 軸版 | BLOCKING |
| 227 | JSON-LD Person.name が canonical entity name (Yuta Yokoi / 横井雄太 / Yokoi Yuta) のいずれかに一致。drift が AI/SEO entity identity を block 跨ぎで断片化するのを BLOCKING で阻止。Check 195 (alternateName) / 203 (givenName/familyName) の primary name 軸 | BLOCKING |
| 228 | sitemap.xml 全 `<changefreq>` 値が Sitemap Protocol 規定 7 値 (always/hourly/daily/weekly/monthly/yearly/never) のいずれか。typo/不正値で crawler freshness hint が silent 無視されるのを BLOCKING で阻止 | BLOCKING |
| 229 | sitemap.xml 全 `<priority>` 値が float [0.0, 1.0]。spec 外値で crawler が default 0.5 へ fallback し priority hint 喪失するのを BLOCKING で阻止。Check 228 の priority 軸 | BLOCKING |
| 230 | sitemap.xml で `<priority>1.0</priority>` を持つ `<url>` がちょうど 1 件、かつその `<loc>` が canonical URL に一致。複数 1.0 や non-canonical 1.0 で primary entry point signal が分散し canonical authority が希釈するのを BLOCKING で阻止 | BLOCKING |
| 231 | main.js SITE_CONFIG.ROLE_TITLE が canonical role (AI-Driven PM / IT Consultant / KERNEL Framework Designer) のいずれかに一致。drift で SPA renderer 出力 (title/meta) の entity 役割誤表現を BLOCKING で阻止。Check 169 (aio-manifest entity.role) の SITE_CONFIG 軸 | BLOCKING |
| 232 | `<meta name=ai:*>` content の absolute URL が全て https:// で始まる。http:// drift で AI crawler が Mixed Content blocking で AIO routing 喪失するのを BLOCKING で阻止。Check 207 (HTML src/href HTTPS) の ai:* content 軸 | BLOCKING |
| 233 | `<meta name=asset:*>` content の absolute URL が全て https:// で始まる。http:// drift で AI/SEO crawler が Mixed Content blocking で asset fetch 失敗・authenticity 劣化を BLOCKING で阻止。Check 232 の asset:* 軸 | BLOCKING |
| 234 | `<meta name=asset:*>` content の absolute URL が canonical URL prefix で始まる。非 canonical drift で AI/SEO crawler が non-canonical asset を正規扱いし entity authority 二分を BLOCKING で阻止。Check 171 (ai:* canonical prefix) の asset:* 軸 | BLOCKING |
| 235 | JSON-LD Article/TechArticle の `@id` 付き full definition が Schema.org 必須 field (headline+author+datePublished) を持つ。silent 欠落で Google rich-result 失格 + AI search Article snippet 劣化を BLOCKING で阻止。subjectOf/citation 外部参照 (`@id` 無し) は exempt | BLOCKING |
| 236 | aio-manifest.json `generated_at` が RFC 3339 (YYYY-MM-DDTHH:MM:SSZ) かつ `entity.affiliation.start_date` が YYYY-MM-DD で実在 datetime/date。drift で recency/雇用 timeline 信号 corruption するのを BLOCKING で阻止。Check 93 (last_metadata_update format) の generated_at+start_date 軸補完 | BLOCKING |
| 237 | js/*.js (および js/quiz/*.js) 全 leaf module が top-level `import` を 0 (zero coupling)。leaf↔leaf 依存で循環依存の入口・hidden coupling・bundling 複雑化が起こるのを BLOCKING で阻止。main.js のみ orchestrator | BLOCKING |
| 238 | index.html head 内 singleton tag (`<title>` / canonical link / meta description / og:url / og:title) がそれぞれ exactly 1 件。複数 instance で browser/crawler が非決定選択し canonical signal が希釈するのを BLOCKING で阻止 | BLOCKING |
| 239 | shipped JS (main.js + sw.js + js/**/*.js + root scripts) に `eval(` / `new Function(` / `Function(` 呼び出しが 0。drift で CSP `unsafe-eval` を要求し runtime 破壊する security baseline gap を BLOCKING で阻止。Check 115 (CSP anti-weakening) の JS source-level 軸 | BLOCKING |
| 240 | shipped JS に setTimeout/setInterval の文字列第 1 引数が 0。eval-equivalent semantics で CSP unsafe-eval 要求する security gap を BLOCKING で阻止。Check 239 の timer-callback 軸 | BLOCKING |
| 241 | shipped JS に document.write/writeln 呼び出しが 0。deprecated/HTML5 parser sniff 破壊/XSS vector の legacy API 混入を BLOCKING で阻止。Check 239/240 の document-write 軸 | BLOCKING |
| 242 | index.html の全 inline on*= 属性 (comment 外) が CSP unsafe-hashes 許可の 1 パターン onload="this.media='all'" のみ。drift で CSP script-src bypass の XSS entry vector を BLOCKING で阻止。Check 239/240/241 と同 family の inline handler 軸 | BLOCKING |
| 243 | main.js SITE_CONFIG.LAST_UPDATED と ai:last-modified が共に today より未来でない。未来日 drift で AI/SEO recency-weighted retrieval が「未来 content」誤認 ranking corruption するのを BLOCKING で阻止。Check 36 (sitemap lastmod 未来 WARNING) の canonical-version date 軸 (BLOCKING 化) | BLOCKING |
| 244 | index.html JSON-LD <script> block の top-level `@graph` 配列の全 element に非空 `@type` がある (anonymous node 不在)。drift で AI/SEO が無 type node 無視し Schema.org graph traversal 破壊を BLOCKING で阻止。Check 217 (top-level @id uniqueness) の @type presence 軸 | BLOCKING |
| 245 | JSON-LD FAQPage の mainEntity 配列が Schema.org Q&A 構造 (Question+name+acceptedAnswer(Answer+text)) を満たす。drift で Google FAQ rich-result 失格 + AI FAQ ingestion 破壊を BLOCKING で阻止。Check 235 の FAQPage 軸 | BLOCKING |
| 246 | JSON-LD BreadcrumbList の itemListElement が Schema.org 構造 (ListItem + position int + name 非空 + item) を満たす。drift で Google breadcrumb rich-result 失格 + AI site-structure ingestion 破壊を BLOCKING で阻止。Check 245 と同 family の BreadcrumbList 軸 | BLOCKING |
| 247 | JSON-LD `@type in {ImageObject, AudioObject, VideoObject}` の node が name + (contentUrl OR url) を持つ。drift で Google Image/Audio rich-result 失格 + AI/SEO entity-asset linkage 破壊を BLOCKING で阻止。Check 245/246 と同 family の MediaObject 軸 | BLOCKING |
| 248 | index.html `<meta charset>` 値が utf-8 (case-insensitive)。drift で Japanese mojibake → canonical entity 名表示破壊を BLOCKING で阻止。Check 157 (presence) を補完する value canonicality 軸 | BLOCKING |
| 249 | index.html `<meta name=viewport>` content が `width=device-width` AND `initial-scale=1` を含む。drift で mobile rendering 破壊 (zoom 固定/content cropped) を BLOCKING で阻止。Check 157 (presence) の content 軸補完 | BLOCKING |
| 250 | index.html `<html lang="...">` 値が BCP-47 regex `^[a-zA-Z]{2,3}(?:-[a-zA-Z0-9]{1,8})*$` に一致。drift (jp/JAPANESE/ja_JP 等) で browser/screen reader/AI/SEO 言語信号破壊を BLOCKING で阻止。Check 152/187/220 (inter-surface 一致) の syntactic 軸補完 | BLOCKING |
| 251 | JSON-LD `potentialAction` block が `@type` (Schema.org Action subclass) + `target` を持つ。drift で AI/voice assistant action invocation 破壊を BLOCKING で阻止。Check 209 (target canonical prefix) の required-fields 軸 | BLOCKING |
| 252 | sw.js が `install` / `activate` / `fetch` 3 event handler を全て登録。silent 欠落で SW lifecycle (precache/cleanup/offline+SWR) 破壊を BLOCKING で阻止。Check 19 (CACHE_NAME version) の SW handler presence 軸 | BLOCKING |
| 253 | main.js が `navigator.serviceWorker.register('./sw.js'...)` 呼び出しを持つ。silent 欠落で sw.js handler は存在しても install されず offline+SWR 機能停止を BLOCKING で阻止。Check 252 の register call-site 軸 | BLOCKING |
| 254 | .well-known/index.json の `skills[]` 各 entry が name 非空+block 内 unique かつ digest が `sha-256:<64-hex>` regex に一致。drift で agent-skills discovery 破壊を BLOCKING で阻止。Check 5 (byte-identical mirror) の schema structural validity 軸 | BLOCKING |
| 255 | index.html が `<!DOCTYPE html>` (HTML5 declaration、case-insensitive、leading BOM/whitespace 無視) で始まる。drift で browser quirks mode → CSS box model 退行 / layout 破壊を BLOCKING で阻止。Check 157 の document-mode declaration 軸 | BLOCKING |
| 256 | primary JSON-LD WebPage node (`@id == canonical+#webpage`) が `dateModified` + `inLanguage` + `isPartOf` を持つ。drift で recency/language/hierarchy 信号 silent 喪失を BLOCKING で阻止。Check 235 (Article 必須 fields) の primary WebPage 軸版 | BLOCKING |
| 257 | primary JSON-LD Person node (`@id == canonical+#person`) が jobTitle + image + sameAs + worksFor + description 5 fields を持つ。drift で entity-rich profile 喪失 → knowledge-graph card 縮小を BLOCKING で阻止。Check 256 の primary Person 軸版 | BLOCKING |
| 258 | primary JSON-LD WebSite node (`@id == canonical+#website`) が inLanguage + potentialAction を持つ。drift で site-level 言語 / action descriptor 喪失を BLOCKING で阻止。Check 256/257 の primary WebSite 軸版 | BLOCKING |
| 259 | primary JSON-LD Organization node (`@id == https://nkgr.co.jp/#organization`) が name + url + alternateName + description + employee 5 fields を持つ。drift で employer-rich data 喪失 → knowledge-graph Organization card 縮小を BLOCKING で阻止。Check 257 の Organization 軸版 | BLOCKING |
| 260 | primary hero ImageObject node (`@id == canonical+#hero-image`) が caption (str) + width (numeric) + height (numeric) + encodingFormat (str) 4 fields を持つ。drift で Google Image rich-result + CWV LCP preload + accessibility 劣化を BLOCKING で阻止。Check 247 の hero-image 軸版 | BLOCKING |
| 261 | primary BGM AudioObject node (`@id == canonical+#portfolio-bgm`) が encodingFormat (str) + creator (dict/str) を持つ。drift で AI search audio classification + attribution 喪失を BLOCKING で阻止。Check 260 の audio 軸版 | BLOCKING |
| 262 | shipped JS (main.js + sw.js + js/**/*.js + root scripts) に `console.log(` 呼び出しが 0 (console.error/warn/info/debug は許可)。production debug-statement leak を BLOCKING で阻止。Check 239/240/241 の production-cleanliness 軸 | BLOCKING |
| 263 | shipped JS に `debugger;` statement / `alert(` 呼び出しが共に 0 (confirm() は許可)。dev-debug pattern leak で production UX-break を BLOCKING で阻止。Check 262 の dev-debug 軸 | BLOCKING |
| 264 | shipped JS の comment 内に dev-cruft marker (TODO/FIXME/HACK/XXX) が 0 (string literal の "TODO" は exempt)。incomplete dev-time note leak を BLOCKING で阻止。Check 262/263 と同 family の comment-cruft 軸 | BLOCKING |
| 265 | shipped JS が strict equality (===/!==) のみ使用 (loose `==`/`!=` 0、string/comment 内は exempt)。type coercion bugs (`'0'==0===true` 等) を BLOCKING で阻止。Check 262/263/264 の equality-strictness 軸 | BLOCKING |
| 266 | JSON-LD で Person/Organization/ImageObject/CreativeWork node の description 値長が [20, 1000] 内。too brief (AI/SEO 不足) / too long (copy-paste 過剰) を BLOCKING で阻止。Check 224 (meta description length) の JSON-LD 軸版 | BLOCKING |
| 267 | JSON-LD で @id + name 両備の entity (Person/Org/Image/WebSite/WebPage/TechArticle/CreativeWork/AudioObject) の name 値長が [3, 200] 内。<3=stub/empty / >200=copy-paste over-long を BLOCKING で阻止。Check 266 の entity name 軸版 | BLOCKING |
| 268 | JSON-LD Article/TechArticle/NewsArticle/BlogPosting の headline 値長が [10, 110] 内 (Schema.org / Google rich-result spec)。drift で SERP card truncate / sparse を BLOCKING で阻止。Check 235 (Article 必須 fields) の headline length 軸版 | BLOCKING |
| 269 | canonical hero.webp <= 200_000 bytes AND BGM.mp3 <= 1_000_000 bytes (CWV LCP / mobile bandwidth budget)。drift で LCP/CWV 劣化を BLOCKING で阻止。Check 120 (JS byte weight) の binary asset 軸版 | BLOCKING |
| 270 | style.css <= 100_000 bytes AND index.html <= 200_000 bytes。silent file bloat (copy-paste / dead-code 累積) を BLOCKING で阻止。Check 120 (JS) / 269 (binary) の text asset 軸版 | BLOCKING |
| 271 | main.js <= 100_000 / sw.js <= 20_000 / 4 root scripts each <= 10_000 bytes。silent script bloat を BLOCKING で阻止。Check 120 (JS 総 weight) の per-file 軸版 | BLOCKING |
| 272 | js/**/*.js 全 leaf module が <= 100_000 bytes。leaf bloat で Stage 5 factory 抽出 goal 崩壊を BLOCKING で阻止。Check 271 の leaf 軸版 | BLOCKING |
| 273 | JSON-LD 全 `datePublished`/`dateModified`/`dateCreated` 値が today 以下。未来日で AI/SEO recency-weighted retrieval が corrupt するのを BLOCKING で阻止。Check 243/218 の JSON-LD date not-future 軸版 | BLOCKING |
| 274 | .well-known/aio-manifest.json entity.name が index.html JSON-LD primary Person.name (`@id == canonical+#person`) と strict 一致。両者 drift で entity identity split を BLOCKING で阻止。Check 172 (name variants combined) の direct-equality 軸版 | BLOCKING |
| 275 | .well-known/aio-manifest.json entity.affiliation.organization_name が index.html JSON-LD primary Organization (`@id == https://nkgr.co.jp/#organization`) name と strict 一致。両者 drift で employer identity split を BLOCKING で阻止。Check 274 の Organization 軸版 | BLOCKING |
| 276 | .well-known/aio-manifest.json entity.affiliation.organization_url が index.html JSON-LD primary Organization url と strict 一致。両者 drift で canonical URL split を BLOCKING で阻止。Check 275 の url 軸版 | BLOCKING |
| 277 | .well-known/aio-manifest.json entity.authoritative_context が canonical URL + "llms-full.txt" と strict 一致。drift で AI/agent authoritative-context ingestion 誤 route を BLOCKING で阻止。Check 274/275/276 の authoritative-context 軸版 | BLOCKING |
| 278 | sitemap.xml 全 `<loc>` URL が `https://` で始まる。http:// drift で crawler が insecure transport fetch するのを BLOCKING で阻止。Check 206/207/214 と同 family の sitemap loc HTTPS 軸 | BLOCKING |
| 279 | robots.txt 全 `Sitemap:` directive URL が `https://` で始まる。http:// drift で crawler が insecure transport で sitemap fetch を BLOCKING で阻止。Check 278 の robots.txt sitemap-directive HTTPS 軸版 | BLOCKING |
| 280 | main.js SITE_CONFIG CANONICAL_URL + REPO_URL 両 URL literal が `https://` で始まる。JS 側 URL emission が insecure に drift するのを BLOCKING で阻止。Check 279 の JS SITE_CONFIG URL-scheme 軸版 | BLOCKING |
| 281 | main.js SITE_CONFIG.REPO_URL 値と index.html `<meta name=ai:repository>` content が strict 一致。両者 drift で JS 側 code URL と AI 側 discovery が split するのを BLOCKING で阻止。Check 280 の direct-equality 軸版 | BLOCKING |
| 282 | main.js SITE_CONFIG.CANONICAL_URL 値と index.html `<meta name=ai:canonical>` content が strict 一致。両者 drift で JS 側 canonical と AI 側 discovery が split するのを BLOCKING で阻止。Check 281 の canonical URL 軸版 | BLOCKING |
| 283 | index.html `<meta name=ai:aio-manifest>` content が canonical URL + `.well-known/aio-manifest.json` と strict 一致。drift で AIO discovery が canonical .well-known/ 外を参照するのを BLOCKING で阻止。Check 184 (path resolves) の exact derivation 軸版 | BLOCKING |
| 284 | index.html `<meta name=ai:context>` == canonical+"llms-full.txt" AND `<meta name=ai:entrypoint>` == canonical+"llms.txt" と strict 一致。drift で AI/agent discovery 経路が canonical exact location から drift するのを BLOCKING で阻止。Check 283 の ai:context/entrypoint 軸版 | BLOCKING |
| 285 | main.js SITE_CONFIG.VERSION が `^v\d+$` regex (小文字 v + 数字) に一致。V74/v74.1/v-74 等 format drift で downstream regex/parser 破壊を BLOCKING で阻止。Check 2 (ai:version parity) の format 軸版 | BLOCKING |
| 286 | sw.js CACHE_NAME が `^portfolio-aio-v\d+$` regex に一致。portfolio-cache-v74 等 semantic contract 崩壊 drift を BLOCKING で阻止。Check 19 (version parity) の format 軸版 | BLOCKING |
| 287 | .well-known/aio-manifest.json manifest_version が `^\d+\.\d+$` regex (major.minor) に一致。非 semver drift で schema consumer parse 失敗を BLOCKING で阻止。Check 285/286 の manifest_version 軸版 | BLOCKING |
| 288 | main.js SITE_CONFIG.ARTICLE_ROUTES 配列の全 string 要素が js/router.js の `case '<route>':` に出現。ghost route (og:type=article dead pointer) を BLOCKING で阻止。Check 137 (router↔switch case coverage) の ARTICLE_ROUTES 軸版 | BLOCKING |
| 289 | aio-manifest.json source_of_truth (>=3) + supporting_evidence (>=1) + observational_evidence (>=1) 各 minimum count + list 内 path 一意性。accidental shrinkage / duplicate ingestion を BLOCKING で阻止。Check 219 (path ⊆ MANIFEST_PATH_TO_LOCAL) の structural axis 版 | BLOCKING |
| 290 | .well-known/aio-manifest.json entity.role が canonical 3 role の set (AI-Driven PM / IT Consultant / KERNEL Framework Designer) と strict 一致。extras/missing/duplicates を BLOCKING で阻止。Check 169 (substring presence) の strict set-equality 軸版 | BLOCKING |
| 291 | .well-known/aio-manifest.json entity.name_alt が canonical variant set (Yokoi Yuta / yuta) と strict 一致。extras/missing/duplicates を BLOCKING で阻止。Check 172 (combined name-variants) の strict set-equality 軸版 | BLOCKING |
| 292 | .well-known/aio-manifest.json entity.name_ja が canonical Japanese 名 '横井雄太' と strict 一致。drift を BLOCKING で阻止。Check 290/291 の name_ja strict-equality 軸版 | BLOCKING |
| 293 | .well-known/aio-manifest.json entity.disambiguation が 5 academic domains (agriculture/chemistry/medicine/entomology/computer science) を全て含む。学術系 Yuta Yokoi との分離弱化 drift を BLOCKING で阻止。Check 170 の academic-domain 軸版 | BLOCKING |
| 294 | .well-known/aio-manifest.json entity.disambiguation が 4 non-academic markers (diplomat/artist/musician/patent inventor) を全て含む。非学術系 Yuta Yokoi との分離弱化 drift を BLOCKING で阻止。Check 293 の non-academic 軸版 | BLOCKING |
| 295 | index.html `<meta name=publisher>` content が 両 canonical name ("Yuta Yokoi" AND "横井雄太") を含む。drift で publisher meta entity 帰属が弱まるのを BLOCKING で阻止。Check 186 (author meta) の publisher 軸版 | BLOCKING |
| 296 | index.html に `<link rel=alternate>` for both `./llms.txt` AND `./llms-full.txt` 両方存在。silent 削除で AIO discovery entrypoint 喪失を BLOCKING で阻止。Check 283/284 (ai:* exact URL derivation) の alternate link 軸版 | BLOCKING |
| 297 | sitemap.xml で `<priority>1.0</priority>` を持つ canonical entry が loc/lastmod/changefreq/priority + `<image:image>` 全 5 要素を含む。structural completeness 崩壊 drift を BLOCKING で阻止。Check 230 (canonical priority=1.0 uniqueness) の structural 軸版 | BLOCKING |
| 298 | index.html `<meta property=og:image:width>` / `og:image:height` content が positive integer に parse できる。numeric drift で social card layout collapse を BLOCKING で阻止。Check 20 (presence) の numeric-value 軸版 | BLOCKING |
| 299 | index.html `<meta name=twitter:card>` content が Twitter spec 4 値 (summary / summary_large_image / player / app) のいずれか。drift で Twitter link-preview default fallback を BLOCKING で阻止。Check 155 の twitter:card 軸版 | BLOCKING |
| 300 | index.html `<meta property=og:image:alt>` content が "横井雄太" AND "AI-Driven PM" 両 marker を含む。accessibility alt-text から entity attribution / role signal 喪失を BLOCKING で阻止。Check 20 (presence) の content-value 軸版 | BLOCKING |
| 301 | index.html に `<link rel=preconnect href=https://fonts.googleapis.com>` AND `href=https://fonts.gstatic.com>` 両方が存在。silent 削除で CWV LCP / font waterfall 劣化を BLOCKING で阻止。Check 73a (preload as=) の preconnect 軸版 | BLOCKING |
| 302 | index.html `<body data-canonical>` 属性が `<link rel=canonical>` href と strict 一致。JS reader hydration の canonical drift を BLOCKING で阻止。Check 149 の body attribute 軸版 | BLOCKING |
| 303 | index.html `<html data-theme>` == "system" AND `<html data-brand>` ∈ {"indigo","classic"}。FOUC-prevention initial paint / brand fallback drift を BLOCKING で阻止。Check 302 の html root attribute 軸版 | BLOCKING |
| 304 | index.html 全 `<meta name=theme-color>` content が `^#[0-9a-fA-F]{6}$` regex に一致。mobile browser chrome default fallback drift を BLOCKING で阻止。Check 174 (style.css literal) の value-format 軸版 | BLOCKING |
| 305 | index.html に `<meta name=theme-color media="(prefers-color-scheme: light)">` AND `dark` 両 media variant 存在。OS-level light/dark mode 遷移で inconsistent 化を BLOCKING で阻止。Check 304 の media-coverage 軸版 | BLOCKING |
| 306 | index.html の trailing 空白行を除いた最終行が `</html>` で終わる。truncated HTML / build error による incomplete markup を BLOCKING で阻止。Check 255 (DOCTYPE opening) の structural-closure 軸版 | BLOCKING |
| 307 | sitemap.xml が `<?xml version="1.0" encoding="UTF-8"?>` で始まり `</urlset>` で終わる。structural malformation で crawler 全 sitemap drop を BLOCKING で阻止。Check 306 (index.html) の sitemap.xml structural 軸版 | BLOCKING |
| 308 | sitemap.xml の `<urlset>` tag が `xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"` AND `xmlns:image="http://www.google.com/schemas/sitemap-image/1.1"` 両 namespace 宣言を含む。欠落で `<image:image>` block が unknown parse し Google Image sitemap coverage 崩壊を BLOCKING で阻止 | BLOCKING |
| 309 | .well-known/aio-manifest.json に `http://` URL が 0 (HTTPS-only)。AIO discovery transport downgrade drift を BLOCKING で阻止。Check 232/233/234 の aio-manifest.json 軸版 | BLOCKING |
| 310 | 全 shipped asset (index.html + style.css + all root JS + all leaf JS + hero.webp + BGM.mp3) 合計 byte size が <= 2_000_000。per-file 通過でも total で mobile cell-network 圧迫する drift を BLOCKING で阻止。Check 269/270/271/272 の total 軸版 | BLOCKING |
| 311 | sitemap.xml の全 `<lastmod>` が strict `YYYY-MM-DD` 形式かつ future date を含まない。malformed date silent accept / crawl priority 不正操作 drift を BLOCKING で阻止。Check 208/273/243 の sitemap.xml `<lastmod>` 軸版 | BLOCKING |
| 312 | sitemap.xml の全 `<loc>` URL が unique (重複無し)。copy-paste drift / lastmod・priority silent overwrite を BLOCKING で阻止。Check 217 (@graph @id uniqueness) の sitemap.xml `<loc>` 軸版 | BLOCKING |
| 313 | aio-manifest.json の `generated_at` + `last_metadata_update` が today 以下 (未来日付なし)。TZ ずれ / 誤編集による未来 timestamp を BLOCKING で阻止。Check 243/273/311 の aio-manifest 日付軸版 | BLOCKING |
| 314 | manifest.webmanifest の `theme_color` が index.html の `<meta name="theme-color">` 集合に含まれる。PWA install screen と in-browser address bar の色 drift を BLOCKING で阻止。Check 304/305 の webmanifest ↔ meta cross-surface 軸版 | BLOCKING |
| 315 | manifest.webmanifest の `display` が W3C spec enum `{fullscreen,standalone,minimal-ui,browser}` かつ `background_color` が 6-digit hex。typo による PWA install prompt degradation / splash screen OS-default fallback を BLOCKING で阻止。Check 304/210 の webmanifest 構造正当性軸版 | BLOCKING |
| 316 | manifest.webmanifest の `icons[].purpose` トークンが W3C spec enum `{any,maskable,monochrome}` 内かつ `icons[].sizes` が `any` or `<W>x<H>` 形式。typo による adaptive-icon 使用不能 / UA icon reject を BLOCKING で阻止。Check 315/212 の webmanifest icons 構造正当性軸版 | BLOCKING |
| 317 | .well-known/aio-manifest.json の全 `sha256` field (source_of_truth / supporting_evidence / observational_evidence) が strict `^[0-9a-f]{64}$` (lowercase 64-hex)。truncated / uppercase / space-embedded digest 形式違反を BLOCKING で阻止。Check 42/236 の aio-manifest digest 構造正当性軸版 | BLOCKING |
| 318 | .well-known/aio-manifest.json の全 evidence entry (source_of_truth / supporting_evidence / observational_evidence) が `{path, role, sha256}` 3 field 完備 (present + non-empty)。missing role による unlabeled evidence / missing path による digest chain resolution 破損を BLOCKING で阻止。Check 289/317 の aio-manifest evidence-entry 構造完全性軸版 | BLOCKING |
| 319 | .well-known/aio-manifest.json の全 evidence entry の `path` が実際に ROOT/<path> に存在する。whitelist (Check 219) の drift / rename・deletion による phantom evidence を BLOCKING で阻止。Check 219/42 の evidence-path existence 軸版 | BLOCKING |
| 320 | robots.txt の `Sitemap:` directive 行が exactly 1 件。0=AIO discovery 喪失 / 2+=crawler 挙動不定 の drift を BLOCKING で阻止。Check 35/279 の robots.txt Sitemap-directive cardinality 軸版 | BLOCKING |
| 321 | style.css に CSS `@import` statement が 0 件 (Boring Technology 契約 C1 遵守)。外部 CSS load による render-blocking 依存 / CSP 拡張 drift を BLOCKING で阻止。Check 1 (no external framework CSS) の CSS 面 no-external-load 軸版 | BLOCKING |
| 322 | index.html に inline `<style>` element block が 0 件 (single-stylesheet 契約遵守)。style 断片が HTML に漏れると Check 52/174 が bypass され CSP style-src 'unsafe-inline' 化を招く drift を BLOCKING で阻止。Check 321/52 の CSS 単一 SSoT 軸版 | BLOCKING |
| 323 | index.html に `style="..."` HTML attribute が 0 件 (per-element inline style 完全 0-tolerance)。style.css SSoT 破綻 / CSP style-src 'unsafe-inline' 要求 drift を BLOCKING で阻止。Check 322 (`<style>` block) / Check 242 (`on*=` handler) の trio で HTML inline-CSS zero-tolerance 完成 | BLOCKING |
| 324 | .well-known/aio-manifest.json の `entity.affiliation.start_date` が today (JST) 以下 (未来日付なし)。TZ ずれ / typo による「未来から来た所属」矛盾を BLOCKING で阻止。Check 313/236 の affiliation start_date recency 軸版 | BLOCKING |
| 325 | index.html の `<meta name="referrer">` が present かつ content が W3C Referrer Policy enum 内 (no-referrer 系 8 値)。missing/typo による browser default fallback → 古い browser で full URL leak リスクを BLOCKING で阻止。Check 115/249 の HTTP header 相当セキュリティ meta 軸版 | BLOCKING |
| 326 | index.html の全 `<link rel="preload" as="X">` の as 値が HTML5 preload destination enum 内 (audio/document/embed/fetch/font/image/object/script/style/track/video/worker の 12 値)。typo による preload spec 違反 → browser silent ignore → first-paint 回帰を BLOCKING で阻止。Check 73a (as= presence) の value 軸版 | BLOCKING |
| 327 | index.html に `<meta http-equiv="refresh">` element が 0 件。SEO 有害 (PageRank 転移損失) / a11y 有害 (WCAG 2.2.1 timing 違反) / AI crawler URL 混乱の drift を BLOCKING で阻止。Check 325 (referrer) / Check 322 (inline style) の HTML negative-invariant 軸版 | BLOCKING |
| 328 | index.html に `<base>` element が 0 件。SPA hash router / `./asset` 参照 / canonical semantics すべてが `<base href>` で silent 破綻する drift を BLOCKING で阻止。Check 327 (meta refresh) の HTML SPA-integrity negative-invariant 軸版 | BLOCKING |
| 329 | index.html に HTML4 deprecated tag (`<frame>` `<frameset>` `<applet>` `<font>` `<center>` `<blink>` `<marquee>` `<big>` `<strike>`) が 0 件。HTML5 validator 破綻 / UA 描画差 / Boring Technology 矛盾 / `<applet>` Java 攻撃面を BLOCKING で阻止。Check 328/327 の HTML modernity + hygiene 軸版 | BLOCKING |
| 330 | index.html に `<iframe>` / `<object>` / `<embed>` 要素が 0 件。click-jacking / CSP frame-src 拡張 / render 差 / SPA narrative 崩壊 drift を BLOCKING で阻止。Check 329/115 の HTML external-embed 攻撃面軸版 | BLOCKING |
| 331 | index.html に `attribute="javascript:..."` (href/src/formaction 等の JS URL scheme) が 0 件。CSP script-src bypass の XSS 永続 vector を BLOCKING で阻止。Check 239/242/323 の HTML JS-URL-scheme zero-tolerance 軸版 | BLOCKING |
| 332 | root classic script (aio-guard/theme-init/karte-init/error-suppressor.js) に `import`/`export` statement が 0 件。<script type="module"> でなく classic script として load されるため ESM 文は silent parse error → theme FOUC / error suppress 破綻 / AIO monitor 停止 の drift を BLOCKING で阻止。Check 239/43d の shipped-JS module-boundary integrity 軸版 | BLOCKING |
| 333 | manifest.webmanifest の `name`/`short_name`/`description` に実名 (横井雄太/Yokoi Yuta/Yuta Yokoi) が漏れない (PWA 匿名契約)。PWA install prompt / app launcher は一般向け UI 面ゆえ実名は home screen に露出禁止。Check 124 (視覚 renderer anonymity) の PWA-surface 匿名軸版 | BLOCKING |
| 334 | manifest.webmanifest の `orientation` が W3C spec enum `{any,natural,landscape,landscape-primary,landscape-secondary,portrait,portrait-primary,portrait-secondary}` 内 (or 未設定)。typo による UA silent fallback を BLOCKING で阻止。Check 315/316 の webmanifest 構造正当性軸版 | BLOCKING |
| 335 | index.html の `<link rel="manifest">` が存在し href が実 manifest.webmanifest file に解決する。link tag 除去 / href drift で PWA install capability が全 gate に silent 無効化 (install prompt/icon/standalone 喪失) する drift を BLOCKING で阻止。Check 135/134 の webmanifest link-wiring 軸版 | BLOCKING |
| 336 | index.html の `<meta property="og:image">` と `<meta name="twitter:image">` の content URL が byte-identical。2 つの異なる valid image への drift で Twitter card と OG (LinkedIn/Slack/FB) が別画像を表示し entity visual identity が SNS ごとに分裂する drift を BLOCKING で阻止。Check 155 (og:title↔twitter:title) の social-card image-coherence 軸版 | BLOCKING |
| 337 | canonical binary assets (hero.webp / BGM.mp3) の magic bytes が拡張子と一致 (webp=RIFF....WEBP / mp3=ID3 or frame-sync)。拡張子を保ったまま中身が別 format に差し替わり Check 269 (byte budget) を通過する drift を BLOCKING で阻止。Check 269/42 の binary-asset format-integrity 軸版 | BLOCKING |
| 338 | `<meta property="og:image:width/height">` の宣言値が実 hero WebP の実寸 (VP8X/VP8/VP8L chunk から parse) と一致。hero 再エクスポートで実寸が変わったのに meta 未更新 → social-card 誤 aspect ratio/reject の drift を BLOCKING で阻止。Check 298/337 の hero-image dimension-truth 軸版 | BLOCKING |
| 339 | JSON-LD の hero ImageObject が宣言する `width`/`height` が実 hero WebP の実寸と一致。og:image (Check 338) は更新したのに JSON-LD が stale → AI crawler/knowledge graph が誤寸法を ingest する drift を BLOCKING で阻止 (2026-07-04 実 drift 1200×630→1536×1024 を C6 承認で是正して誕生)。Check 338 の JSON-LD ImageObject dimension-truth 軸版 | BLOCKING |
| 340 | JSON-LD の ImageObject/AudioObject の `encodingFormat` MIME が実 binary の magic-byte 由来 format と一致 (webp→image/webp, mp3→audio/mpeg)。JSON-LD が実バイナリと異なる MIME を宣言する drift を BLOCKING で阻止。Check 337/339 の binary-asset declaration-truth 軸版 | BLOCKING |
| 341 | index.html の全 `<meta property="og:*">` / `<meta name="twitter:*">` の content が非空。空 content で該当 social-card field (title/image/description) が silent 破壊される drift を BLOCKING で阻止。Check 155/336 の social-card field-presence 軸版 | BLOCKING |
| 342 | robots.txt に破滅的 Disallow (`Disallow: /` 全サイト block / llms.txt・llms-full.txt・sitemap.xml・.well-known を狙う Disallow) が無い。AIO-first 戦略の生命線 (最大 crawl 可能性) を殺す drift を BLOCKING で阻止。Check 35/161 の robots.txt crawl-permission integrity 軸版 | BLOCKING |
| 343 | 全 `.well-known/**/*.json` (現在 4 件 + 将来追加) が valid JSON として parse できる。個別 check が触らない新規 discovery JSON の構文エラーを包括的に BLOCKING で阻止。Check 32/79 の discovery-layer JSON-parse-integrity 軸版 | BLOCKING |
| 344 | style.css の全 `@layer name {` ブロックが top-level `@layer a,b,c;` 宣言文に含まれる。未宣言 layer が first-use 位置 (末尾) で生成され cascade 順序が壊れ style precedence 回帰する drift を BLOCKING で阻止。Check 321 の CSS cascade-integrity 軸版 | BLOCKING |
| 345 | package.json の `verify` aggregate script が全 4 検証層 (`check`/`lint:css`/`lint`/`lint:js`) を `npm run <layer>` で連結する。1 link 欠落で該当層が npm run verify で silent skip される vacuous-gate drift を BLOCKING で阻止。Check 46/50b の verify-chain completeness 軸版 | BLOCKING |
| 346 | .github/workflows/architecture-validation.yml が `python3 .github/scripts/check_repository_consistency.py` を run: step で実行する。この 1 行が消えると全 consistency Check が silent 無効化される ultimate vacuous-gate を BLOCKING で阻止 (guard を守る meta-guard)。Check 55/345 の CI-invokes-the-guard 軸版 | BLOCKING |
| 347 | .github/workflows/playwright-regression.yml が behavior e2e (`grep-invert "screenshot regression"`) を run: step で実行し、かつその step が continue-on-error でない (=BLOCKING) こと。behavior gate の除去 or advisory 化で機能性 gate が silent に無効化される drift を BLOCKING で阻止 (Check 346 の behavior-gate 双子)。Check 142/346 の CI-invokes-the-guard 軸版 | BLOCKING |
| 348 | 両 BLOCKING gate workflow (architecture-validation.yml + playwright-regression.yml) が `on:` に `pull_request`→`main` トリガを宣言する。トリガ除去で workflow が PR 発火せず run: step があっても実行されず PR が un-gated merge される drift を BLOCKING で阻止。Check 346/347 の CI-triggers-the-guard 軸版 | BLOCKING |
| 349 | icon.svg (favicon/PWA icon, `type="image/svg+xml"` 宣言) が well-formed SVG (先頭 `<?xml`/`<svg` + `</svg>` close + SVG namespace)。非 SVG を icon.svg 名で保存し declared MIME が実体と食い違う drift を BLOCKING で阻止。Check 337 の icon-asset format-integrity 軸版 | BLOCKING |
| 350 | inline event handler `onload="this.media='all'"` (font 非同期ロード) の実内容 SHA-256 hash が CSP `script-src` に存在する (unsafe-hashes)。handler 編集後 hash 未更新で Chrome が block しフォントロードが破綻する drift を BLOCKING で阻止。Check 7b/7c/242 の inline-CSP-hash integrity 軸版 | BLOCKING |
| 351 | sitemap.xml の全 `<url>` block が `<loc>` を厳密 1 個持つ。loc 欠落 block は crawler に silent drop され discovery から消える / 複数 loc は undefined drift を BLOCKING で阻止。Check 312/307 の sitemap url-block 構造完全性軸版 | BLOCKING |
| 352 | js/ui-components.js の h() が fail-closed innerHTML 禁止を保持 (`'html'` key branch が throw + `.innerHTML=` 代入が存在しない)。h() は全 render 経路の単一 DOM builder = XSS 境界ゆえ throw 除去で全 call site が XSS sink 化する drift を BLOCKING で阻止。Check 239/43 の shipped-JS XSS-boundary integrity 軸版 | BLOCKING |
| 353 | js/ui-components.js の実コード (コメント除く) に DOMParser 不在。createIcon が DOMParser.parseFromString に戻ると Trusted Types createHTML handler を呼び require-trusted-types-for CSP 違反を silent 再導入する drift を BLOCKING で阻止 (main.js の保護 sanitizer は正当使用ゆえ scope 外)。Check 352/43c の createIcon Trusted-Types-boundary 軸版 | BLOCKING |
| 354 | index.html の全外部 `<script src="https://...">` host (現在 KARTE cdn-edge.karte.io) が CSP `script-src` で authorize される。host が script-src から消え loader が Chrome に CSP-block され analytics が silent 死する drift を BLOCKING で阻止 (C7 契約の機械強制)。Check 63/115 の external-script CSP-authorization 軸版 | BLOCKING |
| 355 | index.html の全外部 `<script src="https://...">` host が CSP `connect-src` にも存在する。loader が LOAD 許可 (script-src) でも CONNECT 不許可だと XHR/fetch が CSP-block され analytics 半壊する drift を BLOCKING で阻止 (Check 354 の connect leg twin)。Check 354 の external-script connect-authorization 軸版 | BLOCKING |
| 356 | Google Fonts CSP pair: 外部 `<link rel=stylesheet href=https://host>` host が `style-src`、woff2 host `fonts.gstatic.com` が `font-src` で authorize される。style-src から消えると @font-face 全滅 / font-src から gstatic 消えると woff2 block で system font fallback する drift を BLOCKING で阻止 (354/355 の font 版)。Check 301 の external-font CSP-authorization 軸版 | BLOCKING |
| 357 | index.html の全 local `<link rel=preload>` href (相対/canonical、非 cross-origin) が実 file に解決する。hero asset rename 等で preload href が取り残され 404 preload=LCP miss する drift を BLOCKING で阻止。Check 53 (modulepreload resolution) の plain preload 版 | BLOCKING |
| 358 | sitemap.xml の全 `<image:loc>` が実 file 解決 + og:image content URL が image:loc 集合に含まれる (hero cross-surface 一致)。hero rename で og:image/image:loc の片方が取り残され Google Images が 404/別画像を指す drift を BLOCKING で阻止。Check 164/297 の image-sitemap resolution+coherence 軸版 | BLOCKING |
| 359 | index.html に `<audio id="bgm-audio">` が存在し src が実 mp3 file に解決する。id 除去で BGM manager (getElementById) が null→toggle silent no-op、src drift で playback 404 する drift を BLOCKING で阻止。Check 357/335 の BGM audio element-wiring 軸版 | BLOCKING |
| 360 | 全 `<meta name="asset:*:canonical">` content URL (asset:image:canonical→hero webp / asset:audio:canonical→BGM mp3) が実 file に解決する。Check 234 (canonical prefix) は通るが rename で 404 な AIO canonical asset を declare し AI crawler が dead link を fetch する drift を BLOCKING で阻止。Check 234/164 の AIO-asset-canonical resolution 軸版 | BLOCKING |
| 361 | 全 shipped JS leaf module (`js/*.js` ∪ `js/quiz/*.js`) が file-size-budget.md §4 BUDGET-DATA に行数予算として登録済みであることを強制。Check 71 の対称 (71=registered⟹exists / 361=exists⟹registered) で js-leaf 面の bijection を成す。bloat-reduction 抽出で生まれる新 leaf が silent に未予算化し Check 52 advisory を素通りして無制限成長する gap (file-size-budget.md §5 の deferred 拡張候補) を封じる。owner 受諾 1,000 行しきい値の機械強制 | BLOCKING |
| 362 | mutation_samples.py の全 mutation (MUTATIONS ∪ E2E_MUTATIONS) の `find` anchor が対象 file に実在することを強制。mutation-probe/-e2e は CI 非実行 (完全性検証は手動) ゆえ、leaf 抽出/リファクタで anchor の対象コードが別 file へ移動/消滅すると手動 probe まで silent に orphan 化し completeness-critic の網が穴だらけになる (実例 #558: PomodoroPage 分離で pomodoro E2E anchor が orphan 化)。anchor 整合を verify 時 BLOCKING gate へ昇華 | BLOCKING |
| 363 | shipped JS *ロジック* leaf module (`js/*.js`・非再帰) の行数が JS-LEAF-CEILING marker (file-size-budget.md・現行 1,000) のハード上限を越えないことを強制。Check 52 (BUDGET-DATA) が per-file loose ADVISORY 予算で超過を warning するのみなのに対し、本 Check は owner 受諾 1,000 行しきい値を BLOCKING gate 化し越えたロジック leaf を merge 前に分割させる (Check 60 と同型の advisory 早期警告 + BLOCKING ハードゲート二層設計)。除外: js/quiz/*.js (純データ・設問追加は価値ある成長ゆえ advisory 観測) と main.js (保護 kernel・js/ 直下でない)。肥大化を「生じないように」する防止層で AI 無限改善自走を behavior-code 面で守る | BLOCKING |
| 364 | store.js の正規化子 (validateAndNormalize / normalizeAppsData / normalizeProject) に unsafe `(X \|\| []).<throwing array-method>` idiom が無いことを強制。これらは import/cross-tab/snapshot/load の untrusted 外部データを正規化する総関数で、X が非配列 truthy (文字列等) だと `\|\| []` が置換せず `.filter/.map/...` が TypeError を投げ全 ingestion 経路が FatalPage crash する (#568 ai/pomodoro history・#572 project tech/tags/links・#573 task.tags が実バグ)。安全形 `(Array.isArray(X) ? X : [])` を強制し per-instance fix を class 構造防止へ昇華 (肥大化 Check 363 と同じ「解消+再発防止」規律の ingestion-safety 版) | BLOCKING |
| 365 | Capstone: git ls-files でスキャンした全 committed text file（A 群 style.css/index.html/main.js・AIO/C6 層 llms-full.txt 等・npm lockfile package-lock.json・純データ js/quiz/*・.well-known/・binary 拡張子・e2e snapshot dirs を除く）が ≤1,000 行を BLOCKING 強制。check.py 分割（920行）・e2e spec 分割（max 647行）・B-track trim（AI2AI.md 951行・ChatGPT2ChatGPT.md 970行）が完了した 2026-07-08 に初めて通過できるようになった最上位の肥大化防止 gate。Check 363 (js/*.js) の全ファイル版として、AI 無限改善自走の持続可能性を守る Capstone 層 | BLOCKING |
| 366 | shipped JS (js/*.js 非再帰) の h() props で `target: '_blank'` を含む行の ±2 行以内に `noreferrer` が現れることを BLOCKING 強制。runtime は ui-components.js:h() が全 a[target=_blank] に noopener+noreferrer を付与する二重防御だが、source レベルの省略は「意図的に noreferrer を省いた」と誤読されるコード drift を生む（実例: ContactPage LinkedIn の rel:'noopener' のみ push・#322）。secureExternalLinks の mutation test とは独立した静的ソース軸の防止層 | BLOCKING |
| 367 | shipped JS (js/*.js 非再帰) の h('select', ...) 呼び出しで attrs オブジェクトに `value:` キーが現れないことを BLOCKING 強制。`<select>` 要素には HTML 仕様上 `value` content attribute が存在しない。h('select', { value: x }) は el.setAttribute('value', x) を呼び選択状態に一切反映されない (#7cbc4d9 class)。正しい実装は各 `<option>` に `selected: val === cur ? true : undefined` を付与すること。apps.js / settings-page.js / projects-page.js の全 h('select') を #668〜#670 + 本 increment で修正済、本 Check は再発防止の構造封じ | BLOCKING |
| 368 | shipped JS の apps.js と store.js が notes 上限 20000 をマジックナンバーとして直接持たないことを BLOCKING 強制。NotesPage oninput (apps.js) と validateAndNormalize (store.js) の両方が `20000` を hardcode しており、片方だけ変更すると silently drift する class (#672)。`CONSTANTS.LIMITS.NOTES_TEXT` に定数化して単一ソースへ集約した。本 Check は `slice(0, 20000)` パターンの再注入を構造封じ | BLOCKING |
| 369 | shipped JS の AI/pomodoro 履歴保持件数上限をマジックナンバーで持たないことを BLOCKING 強制。AI 履歴の 80 は store.js (normalize) + ai-page.js (add) の 2 箇所、pomodoro 履歴の 200 は store.js (normalize) + pomodoro-page.js (complete) の 2 箇所で同じ値を参照し、片方だけ変更すると silently drift する (Check 368 の NOTES_TEXT と同型 class)。`CONSTANTS.LIMITS.AI_HISTORY` / `POMODORO_HISTORY` に定数化して各ペアを単一ソースへ集約。本 Check は `.slice(-80)` / `.slice(-200)` マジックの再注入を構造封じ | BLOCKING |
| 370 | shipped JS の pomodoro 既定状態 (settings {work:25,short:5,long:15} + runtime remainingSec 1500) を state.js (clone fallback) と store.js (default + normalize clamp fallback) がマジックリテラルで重複せず `CONSTANTS.POMODORO_DEFAULT_SETTINGS` / `POMODORO_DEFAULT_REMAINING_SEC` 経由で参照することを BLOCKING 強制。両ファイルが同じ既定値を独立に持つと片方だけ変更で既定状態が silently drift する (Check 369 と同型の cross-file default-object drift class)。定数化して単一ソースへ集約後、`work: 25` / `remainingSec: 1500` / `\|\| 1500` (runtime) および settings normalize clamp fallback `settings.work) \|\| 25` / `settings.short) \|\| 5` / `settings.long) \|\| 15` マジックの再注入を構造封じ (runtime remainingSec は定数参照するのに settings fallback だけ magic が残っていた非対称 gap を後から閉じた)。利用側は参照共有 mutation 回避のため spread 必須 | BLOCKING |
| 371 | docs/files mirror doc の Check 52 制約行が volatile な「現在行数」(`**Check 52**: N 行 ≤ M`) を hardcode しないことを BLOCKING 強制。現在行数はファイル編集ごとに drift するが未強制で、25 mirror doc + 17 「module (N 行)」self-ref が実際に stale 化していた drift-magnet。行数の権威は file-size-budget.md §4 BUDGET-DATA (Check 52/71) と `wc -l` であり mirror doc はそれを指すべき。`**Check 52**: 行数予算 ≤ M 行（現在値は file-size-budget.md / wc -l が権威）` 形式へ正規化し、volatile 現在行数引用の再注入を構造封じ | BLOCKING |
| 372 | 各 `js/*.js` factory `createXxx({...})` の全注入依存名 (単一文字 h を除く) が対応 mirror doc `docs/files/js/<x>.js.md` に whole-word 言及されることを BLOCKING 強制。Check 119 は docstring の【依存】節を強制するが mirror doc の依存記述は未強制で、#674 (pomodoro-page に CONSTANTS 注入 → mirror silent stale) 含め 9 mirror doc が stale/不完全 signature を持っていた (store.js.md `createStore({Storage})` ← 実 8 依存 / theme.js.md `createTheme({Storage})` ← 実 {State, Toast} 等)。Check 119 の docstring 面に対する mirror-doc 面 (Check asymmetry 是正) で、依存再注入時に mirror doc も同期させる | BLOCKING |
| 373 | `js/store.js` の `defaultAppsData` 全 top-level フィールドが `normalizeAppsData(data)` で `data.<field>` として読み戻されることを BLOCKING 強制。normalizeAppsData は全 ingestion 経路 (load/import/cross-tab/snapshot/settings 正規化) が通るチョークポイントで、`deepClone(defaultAppsData)` から再構築し各ユーザーフィールドを再適用する。あるフィールドが default (永続化 shape) にあるのに読み戻されないと、呼び出し側が永続化していても reload 毎に default へ silent リセットされる — QuizPage が検索語を `State.updateSilently(s => s.appsData.quizSearch = val)` で永続化書き込みし init で読み戻して復元するのに、normalizeAppsData が tasks/todos/pomodoro/ai/notes だけ preserve し `quizSearch` を drop していた実バグ (#294/#568 と同 producer/consumer drift class・「永続化された検索語を反映」が毎 reload silent 失敗) を封じる。defaultAppsData の top-level key を brace-parse し各 key の `data.<key>` 参照を normalizeAppsData 本体 (行コメント除去=comment-only vacuous pass 防止) に強制 | BLOCKING |
| 380 | `mutation_samples.py` の全 mutation（`MUTATIONS` ∪ `E2E_MUTATIONS`）の `replace` が `find` と**異なる**ことを BLOCKING 強制。find==replace の mutation は no-op で、適用しても何も変わらず gate が GREEN のままゆえ mutation_probe は SURVIVED（偽 coverage gap）と報告するが verify 時は完全 silent（Check 362 は find anchor 存在のみ、379 は test-field のみ）。no-op mutation は「バグ class を叩いているように見えて何も test しない」偽カバレッジを silent に提供し、編集ミスで容易に混入する（本 Check の契機も Check 379 の self-referential mutation 除去中に一時的に find==replace を作った実例）。mutation-integrity mesh（362=find anchor 解決 / 379=test-field 解決 / 380=replace が実 mutate）を閉じる | BLOCKING |
| 382 | command-palette NAV の**静的 top-level route** と router（js/router.js `_parseRoute` の `case '<name>':` = projects/apps/settings/about/resume/contact/quiz/hiring-risk/ai-knowhow/role-split）を**双方向一致（bijection）**強制。(a)欠落(router-palette)=新静的ページを router に足して palette への `hash: '<name>'` を忘れ Cmd/Ctrl+K から到達不能 / (b)余剰(palette-router)=palette の top-level `hash: '<x>'`（home '' と `apps/<x>` は除外）が router case に無く選択で not-found へ飛ぶ dead entry（旧 Check は片側のみ・#788/#789 class）。router case を source of truth に set-equality 強制（Check 128 の静的 route 版・両方向） | BLOCKING |
| 381 | `main.js` が `from './js/X.js'` で静的 import する全 js/ モジュールが `checks_esm.py` の `_modules47` 登録集合の部分集合であることを BLOCKING 強制。Check 47 は `_modules47` を反復して「`_modules47` ⟹ main.js import」方向を強制するが逆方向（main.js の js/ 静的 import ⟹ `_modules47` 登録）は未強制だった。この非対称ゆえ葉抽出で新モジュールを main.js import + lint + modulepreload に足しても `_modules47` への追加を忘れると Check 57（preload↔`_modules47`）/ 61（factory doc）が新モジュールを被覆せず、modulepreload 漏れ（初期ロード waterfall 遅延）が silent 発生する（#706 で 9 モジュール home-page/projects-page/project-detail-page/ai-knowhow-page/hiring-risk-page/ai-page/pomodoro-page/settings-page/command-palette がこの drift で漏れていた実バグ）。main.js の `from './js/X.js'` 集合 ⊆ `_modules47` を強制し、Check 57 の「登録 ⟹ modulepreloaded」と合わせ「import ⟹ preloaded」loop を構造的に閉じ #706 drift class の再発を防ぐ | BLOCKING |
| 383 | `style.css` に `@media (prefers-reduced-motion: reduce)` の **global reset**（universal selector `*` で `animation-duration` + `transition-duration` を ~0 に潰す規則）が存在することを BLOCKING 強制。前庭障害配慮（WCAG 2.3.3 Animation from Interactions）の CSS-layer 主防御で、main.js の View Transition API バイパス（reduced-motion 時）と対をなす dual-layer defense（改善文書b §13.1）。JS 層は VT のみ抑止し、サイト全体の他アニメ/トランジションを実際に無効化するのは CSS の universal-selector global reset。これが silently strip されると reduced-motion ユーザーに動きが戻るが behavior e2e は動きを検査せず screenshot は advisory ゆえ無検出。Check 101（forced-colors）/ 103（prefers-contrast）と同型で、当該 OS 設定が非アクティブな通常描画では inert（render-neutral・§3 baseline gate 非該当）。将来編集での silent strip を封じる | BLOCKING |
| 384 | `style.css` の @media ブロックを除去した top-level CSS に `:focus-visible { ... outline: <非 none/非 0> ... }` ルール（通常モードのキーボード focus indicator・WCAG 2.4.7 Focus Visible）が存在することを BLOCKING 強制。Check 101（forced-colors）/ 103（prefers-contrast）は @media 内の HCM/高コントラスト変種のみを守り、@media の外にある **base の focus outline** 自体は無保護だった。base はキーボード利用者の大半が依存する主 focus 指標で、silently strip されると通常描画でキーボード focus が不可視になり WCAG 2.4.7 違反になるが、behavior e2e は focus ring スタイルを検査せず screenshot advisory ゆえ無検出。Check 383/101/103 と同じ a11y-CSS presence class（base 指標は変種より重要） | BLOCKING |
| 385 | 分割 check module（`.github/scripts/checks_*.py`）が bare `warnings.append(...)` / `errors.append(...)` を error/skip パスで使うのに `warnings = ctx.warnings` / `errors = ctx.errors` を `run(ctx)` で unpack していないと、そのパス到達時（target file 不在 / PyYAML 不在 等）に NameError で consistency script 全体が traceback で crash する。全ファイル/依存が揃う通常 verify では error 枝が走らず休眠する latent crash。dependabot.yml 削除で Check 68 が NameError 化した実バグ（5 module: aio_config/governance_sync/repo_hygiene/structural_ci が warnings、misc_governance が errors の unpack 漏れ）を修正し、各 module の bare append + unpack 漏れを parse して BLOCKING 強制 | BLOCKING |
| 386 | `sitemap.xml` の全ページ `<loc>` URL（`<url><loc>`。`<image:loc>` は Check 358 が担当ゆえ除外）が canonical origin + `/portfolio/` base を剥がしたローカル path で実在 file に解決することを BLOCKING 強制。Check 311 は `<lastmod>` の format、Check 312 は `<loc>` の一意性のみを見てファイルシステムに触れず、Check 358 は `<image:loc>` のみを解決していたため、通常ページ `<loc>`（llms-full.txt / AI2AI.md / .well-known/* / docs/evidence/* 等 19 URL）が実 file に解決するかは無検証だった。file rename/deletion で sitemap.xml の更新を忘れると、sitemap が存在しない URL を advertise し AI crawler へ 404 を返す＝AIO discovery surface が壊れる（sitemap の存在目的そのものを毀損）。Check 358（image:loc 解決）/ Check 319（aio-manifest evidence.path 解決）/ Check 133-135（file-exists ⟹ wired）と同型の sitemap page-loc existence 軸 | BLOCKING |
| 387 | AIO discovery pointer files（`.well-known/api-catalog`＝RFC 9727 linkset ＋ `.well-known/mcp.json`）の全 same-origin（canonical origin + `/portfolio/`）URL 参照（href / service-meta / resource URL）が実在ローカル file に解決することを BLOCKING 強制。両者は AI エージェントが実際に **dereference** してサイトの機械可読リソースを discover する entry point で、宣言された URL が rename/deletion で dangling すると agent は 404 を受け取り agent-facing AIO discovery が silent に壊れる。Check 165 は api-catalog の構造（JSON/linkset/anchor canonical）のみを見て href 存在は無検証、Check 386 は sitemap（search-crawler 面）のみで、この agent 面 pointer の存在は盲点だった。各 JSON を parse し全 string 値を再帰走査、canonical origin の URL を実 file 解決する。Check 386（sitemap page-loc）/ 165（api-catalog 構造）/ 319（evidence.path）と同型の AIO agent-discovery pointer existence 軸 | BLOCKING **387b (2026-08-23 追加)**: 同一 origin の **SPA ルート fragment**（`#/role-split` 等）は file ではないので 387 の file 解決から分離し、**Check 439 と同じ router.js 由来の導出**で実ルートへ解決することを検証する。ただ除外すると**ルートの正しさが誰にも検証されなくなり**、agent が typo ルートを dereference して NotFound へ落ちる「淡々と緑になる壊れ方」（#96-99 の vacuous-hash class）を素通しする。動機: WebMCP ツールの `canonicalRoute` を mcp.json へ足した瞬間、387 が**正しい pointer を「404 する」と誤報告**した —— 「同一 origin URL = ファイル」という前提が SPA では狭すぎた。 |
| 388 | `sw.js` の `isBotRequest`（`BOT_UA_PATTERNS`）が、実際に URL を dereference する既知 AI エージェント UA（GPTBot / ChatGPT-User / OAI-SearchBot / ClaudeBot / Claude-User / Claude-SearchBot / PerplexityBot / Googlebot / Bingbot / Applebot / CCBot）を全て bot 判定することを BLOCKING 強制。sw.js は AIO ファイル（llms.txt/llms-full.txt）を bot には `no-store` で fresh、humans には SWR で提供するため、AI エージェント UA が BOT_UA_PATTERNS のどれにも substring 一致しないと SWR の stale なキャッシュ本文を受け取り AIO 戦略が silent に劣化する。BOT_UA_PATTERNS を parse し isBotRequest の substring 判定を再現、curated AI-fetch UA が全て検出されることを強制。**発見 drift**: `Claude-User`（Anthropic の on-demand fetch UA・robots.txt 宣言済）が旧パターンのどれにも非一致（'claudebot'⊄'claude-user' / bare 'claude' なし / 'bot' 部分文字列なし）で stale AIO を受け取っていた（→ 'claudebot'→'claude' で是正）。Check 252（sw.js handler 存在）の AI-agent freshness 軸 | BLOCKING |
| 390 | `js/router.js` の `_parseRoute()` が emit しうる全 route.name（`route.name = '<name>'` の literal 代入 + 初期 `{ name: 'home' }` + `['task','todo',...].includes(app)` whitelist を `app-<x>` に展開）が `js/page-meta.js` の PAGE_META top-level key に存在することを BLOCKING 強制。Check 118 は route 集合を e2e ALL_ROUTES から導くが、ALL_ROUTES は param 必須の動的 route（`project-detail`）を構造的に含まないため、project-detail の PAGE_META エントリはどの Check にも守られない盲点だった。project-detail が PAGE_META から落ちると applyMeta が early-return し、全プロジェクト詳細ページ（Case Study = ポートフォリオ #1 mission）が title/desc/JSON-LD なしで ship される silent AIO/SEO 回帰になる（#773 で announceRouteForAccessibility を唯一の a11y announcer にしたため route アナウンス完全欠落も意味する）。router↔PAGE_META を ALL_ROUTES 非経由で直結し param route を含む全 route を被覆、page-meta.js:15 の「不一致は MetaMgmt で silent fallback」を機械強制化。Check 377（router→main.js case）の PAGE_META twin | BLOCKING |
| 389 | theme toggle の accessible name `テーマを切り替える（現在: <state>）` が js/theme.js（topbar `#themeBtnTop` の `themeToggleAriaLabel`）と js/components.js（sidebar ボタン inline）で**独立に複製**されているのを coherence 強制。葉契約（Check 47c: import ゼロ）ゆえ共有できず in-code コメントのみで保護＝機械強制なしだった。片方の文言が drift すると同一機能ボタンが topbar/sidebar で異なる accessible name を announce する WCAG 4.1.2（Name/Role/Value）違反（behavior e2e は両面の name 文言を diff せず screenshot は advisory ゆえ silent）。両葉を parse し (a) aria-label 接頭辞 `テーマを切り替える（現在:` が両方に存在、(b) 3 state ラベル（システム設定/ダーク/ライト）が各葉の aria-label 近傍（接頭辞 ±window）に存在、を強制。Check 100（theme-init storage-key 重複）の theme cross-leaf string-duplication coherence 軸 | BLOCKING |
| 379 | `mutation_samples.py` の全 `E2E_MUTATIONS` entry の `test` フィールドが `e2e/*.spec.js` の実在 test タイトルの substring であることを BLOCKING 強制。E2E_MUTATIONS の `test` は mutation_probe `--e2e` runner が Playwright `-g` grep として渡し「mutated バグを捕捉すべき behavior test」を選ぶ。typo / rename・削除で `playwright -g "<typo>"` が **0 test マッチ**すると baseline も mutated も pass（0 test 実行）になり、その mutation の非 vacuity 検証が silent 無効化する。Check 362（`find` anchor 解決）は検証するが `test`-名参照は未強制で、mutation-probe-e2e は CI 非実行ゆえ typo は手動実行まで silent。「mutation が behavior test を名指す ⟹ その test が存在する」を invariant 化（Check 362 の find-anchor resolution の e2e-title twin）。**題名抽出は 2 記法**: 引用符リテラルに加え backtick テンプレート題名 (パラメタライズド loop) も解決する。literal のみの旧実装ではテンプレートが完全に不可視で、**リポジトリで最も重要な安全網**（security-proxy.spec.js の ALL_ROUTES route-render loop / a11y-axe の route loop）に mutation を一切登録できなかった（解決不能で false RED = safe-fail だが、カバレッジが最も効く場所に meta-QA の穴が残る）。テンプレートは `${…}` 境界で静的セグメントへ分解し、いずれかの部分文字列なら解決とみなす | BLOCKING |
| 378 | `CONSTANTS.MOBILE_BREAKPOINT`（JS・sidebar→drawer 切替判定）と `style.css` の唯一の `@media (max-width: Npx) { .sidebar { display: none } }` の N が一致することを BLOCKING 強制。sidebar→drawer レスポンシブ切替は 2 レイヤに二重エンコードされる: js/mobile-drawer.js が `matchMedia('(max-width: ${MOBILE_BREAKPOINT}px)')` で isMobile 判定し mobile topbar（drawer メニューボタン）を表示、style.css が `@media (max-width: N)` で desktop sidebar を隠す。両者が drift すると（JS 定数を 960 にしたが CSS を 920 のまま等）、JS が topbar を出すのに CSS が sidebar を残す（両表示=broken responsive layout）viewport 幅帯が生じ、behavior e2e も screenshot(advisory) も捕捉しない。magic-number single-source Check 368/369/370 の JS↔CSS cross-layer twin | BLOCKING |
| 377 | `js/router.js` の `_parseRoute()` が生成する全 literal route.name（`route.name = '<name>'` 代入 + 初期 `name: 'home'`。動的 `app-${app}` は Check 137 の担当ゆえ除外）が `main.js` の renderer switch に `case '<name>':` を持つことを BLOCKING 強制。Check 137 は app-route（`app-<app>`）版のこの edge だが、非 app route.name は Check 58（main.js cases ⟺ e2e ALL_ROUTES）経由でしか間接的に縛られない。router に新 route（例 `route.name = 'blog'`）を足して main.js の case と e2e ALL_ROUTES の両方を忘れると全 Check 緑（Check 58 は旧集合で set-equal）なのに /#/blog が switch default→NotFoundPage へ落ち silent 404 化する。「router が route X を解決 ⟹ main.js が X を描画可能」を非 app route に直接 invariant 化（route coherence mesh 58/137 の欠けた非 app edge） | BLOCKING |
| 376 | index.html + shipped JS の全 `data-action="X"` producer が `js/aidk-rails.js` の ActionDelegator `_handlers` キー（+ `register('X', ...)` 動的登録）に解決することを BLOCKING 強制。ActionDelegator は document 単一 click 委譲器で、未登録 action の click は `_handlers[X]` が undefined ゆえ **silent no-op**（throw も console error も e2e 失敗も無い）。typo した `data-action="drawr:open"` 等は menu/theme/BGM の critical control を無反応化するのに verify も behavior e2e も advisory screenshot も素通りする。Check 375（icon）/133/134 と同じ silent-wiring class を data-action 面「action が使われる ⟹ handler が定義済」へ適用。逆方向（handler ⟹ producer）は非強制（`drawer:close` は意図的 symmetric unused handler・drawer は直接 onclick/Escape/nav-click で閉じる）＝used⟹defined ガードで bijection ではない | BLOCKING。**producer の検出は記法族**（属性リテラル `data-action="X"` / h() prop `'data-action': 'X'` / DOM API の `setAttribute('data-action','X')` / `el.dataset.action = 'X'`）で行い、走査対象に root の `main.js` も含む。初版は前 2 綴りのみで、**DOM API 経由で typo した action を丸ごと見逃していた**（実測: 両 DOM-API 記法で typo 名を注入しても GREEN ＝ まさに本 Check が防ぐはずの silent no-op ボタンが素通り）。honest な射程: テンプレートリテラル/変数で動的合成される action 名は追わない |
| 393 | shipped JS（main.js + js/*.js）の全 `CONSTANTS.<KEY>`（および `CONSTANTS.LIMITS.<M>` / `CONSTANTS.POMODORO_DEFAULT_SETTINGS.<M>`）参照が `js/constants.js` の定義済み key に解決することを BLOCKING 強制。typo（例 `CONSTANTS.LIMITS.MAX_TASSK` / `CONSTANTS.DEBOUNCE_DELY`）は**合法な property access** ゆえ throw せず静かに `undefined` へ評価され、ESLint（定義済みオブジェクトへの property access は合法）も node --check も素通りする。`undefined` を `.slice(0, undefined)` bound に使うと切り詰めが無効化し（配列全体を返す=**DoS/bloat 切り詰めガード沈黙**）、`setTimeout(fn, undefined)` は即発火する **silent bug**。Check 375（icon）/376（data-action）/377（route→case）/391（getElementById）/392（aria idref）と同じ used⟹defined wiring レンズの CONSTANTS-access 面。top-level CONSTANTS key（4-space-indent `KEY:`）と object-valued key（LIMITS / POMODORO_DEFAULT_SETTINGS）の member を balanced-brace 抽出し、seg1（top key）は常に・seg2 は seg1 が既知 object key の時だけ検証する（scalar 定数への method chain `CONSTANTS.STORAGE_KEY.slice(...)` を誤検出しない）。値が IIFE/式（TAB_ID / DEBUG=先頭が `(` で `{` でない）の top key は object 扱いせず内部が valid-key を汚染しない。cross-namespace typo（`CONSTANTS.LIMITS.work`）は非対象＝主要な misspelled-key class のみ強制 | BLOCKING。**分割代入経由の参照も辿る**（`const { LIMITS } = CONSTANTS` / `const { LIMITS: L } = CONSTANTS` の alias を top key に対応付けて `alias.MEMBER` を検証）。初版は `CONSTANTS.` で始まる dotted access しか見ておらず、**分割代入すれば typo が素通り**した（実測: `const { LIMITS } = CONSTANTS; LIMITS.MAX_TASSK` を注入しても GREEN）。コメントは除去して説明文中の例示を実参照と誤認しない |
| 394 | pomodoro settings clamp *range*（work=[1,180] / short=[1,60] / long=[1,120]）が pomodoro-page.js（UI input onchange の即時 clamp）と store.js（normalizeAppsData の ingestion clamp）の 2 レイヤで field 毎に一致することを BLOCKING 強制。Check 370 は既定値（25/5/15）のマジック再注入を禁止するが range（180/60/120）は magic literal のまま両ファイルに重複しており未カバーだった。片方だけ range を変えると（例: UI work max を 240 にしたが store は 180 のまま）UI で入力できる値が reload/import 後に store normalize で再 clamp され silently 変わる（2 層 clamp-range drift）。behavior e2e は特定の境界値しか検査せず中間帯の range 差は捕捉しない。UI/store 双方から work/short/long の (min,max) を抽出し一致を強制する（Check 370 default-object drift の clamp-range twin・Check 378 の JS↔CSS cross-layer coherence と同じ 2 レイヤ一致軸） | BLOCKING |
| 397 | `mutation_samples.py` の全 `E2E_MUTATIONS` `test` フィールドが **厳密に 1 つ**の e2e test title の substring であること（Check 379 の ≥1 を ==1 へ tighten）を BLOCKING 強制。mutation_probe `--e2e` は `test` を `playwright -g "<field>"` に渡し substring 一致する**全 test を実行**するため、2+ title に一致すると複数 test が走り mutation の非 vacuity 帰属が曖昧化する（意図 test が green でも co-match test の fail で「caught」誤報告 / 意図 test の fail が masked され false SURVIVED）。Check 379（≥1）と対で ambiguous-anchor 面を閉じ mutation-integrity mesh（362/379/380）を完成。実例: `"unique slugs"` は settings same-name test と resilience colliding-import test の 2 件に一致していた（本 Check 導入時に `"same name yields unique slugs"` へ限定）。self-reference trap（mutation_samples.py を検証する Check ゆえ consistency mutation 登録不可）は手動 revert→RED で非 vacuity 担保。題名抽出は Check 379 と共通の両記法対応。backtick テンプレートは **1 loop = 1 test** として数える（実行時は N ルートへ展開されるが本体は同一 assertion であり、co-match しても帰属は曖昧化しないため） | BLOCKING |
| 398 | `check_repository_consistency.py` の Result block が `warnings` を反復し各本文を `::warning::{w}`（GitHub Actions annotation）形式で印字することを BLOCKING 強制。従来は errors 側だけが `::error::{e}` で本文を列挙し、warnings 側は `passed with N warning(s)` と**件数のみ**を出して本文を捨てていた（「1 ケースだけ処理・他を忘れる」asymmetry）。ADVISORY Check は 56 箇所・13 module に及ぶが、drift を検出しても 本文が出ないためローカル実行でも CI ログでも「どの invariant が緩んでいるか」を読めず対処の起点が存在しない — **読めない advisory は実質 vacuous な助言層**で、BLOCKING でない Check 群（52 予算 / 60 ESLint baseline / 121 STATUS 鮮度 等）の早期警告価値がゼロになる。検証は loop 変数名に束縛して行う（`for w in warnings:` → `print(f"...::warning::{w}")`）— literal の有無だけを見るとループと無関係な固定文字列で vacuous に PASS しうるため。Check 385（advisory/error パスの latent crash 防止）と対で「助言層が実際に機能する」軸を守る | BLOCKING |
| 399 | `mutation_probe.py` の consistency mode が catch を **Check 362 (anchor orphan) 以外の error** で帰属することを BLOCKING 強制（`ANCHOR_ORPHAN_MARKER = "Check 362:"` 定数 + `caught_by_real_check()` の存在、かつ旧判定 `if run_gate() == 0:` の非残存）。probe は mutation を適用してから gate を走らせるが、適用によりその mutation 自身の find-anchor が対象 file から消えるため **Check 362 が必ず RED になる**。ゆえに gate の exit code だけで caught を判定すると、意図した Check が 1 つも発火しなくても全 mutation が Check 362 の副作用で自動的に caught と報告され、「安全網が本当に回帰を捕捉するか」を検証するはずの meta-QA が何も検証しない vacuous なツールへ退行する（Check 362 導入以降 実際にそうなっていた）。実証: どの Check も見ない inert prose を対象にした対照 mutation で gate は RED になり error は Check 362 の 1 件のみだった。`run_gate()` が (exit code, 出力) tuple を返すため旧判定の残存は常に False = 全件 SURVIVED へ静かに反転する — その退行実体も禁止する。Check 362/379/380/397（mutation データの整合）に対し本 Check は **probe の判定そのものの健全性**を守る（mutation-integrity mesh の runner 面） | BLOCKING |
| 400 | `check_repository_consistency.py` の module 直下（indent 0）に try/except 非保護の `json.loads(...)` / `yaml.safe_load(...)` が無いことを BLOCKING 強制。対象 file が壊れた瞬間に traceback で **suite 全体**が停止し、その破損を検出するために書かれた Check 自身を含む全 Check が未実行のまま skip される。exit 1 ゆえ merge は止まるが (a) 診断が Python traceback で actionable でなく (b) crash 地点以降の Check が一切走らず他の drift を全て masking する — Check としては死んでいる。実測（Check 399 で mutation-probe の catch 帰属を正した直後に検出）: `.well-known/mcp.json` に構文エラーを入れると module-level の `mcp_data = json.loads(read(...))` が JSONDecodeError を送出し、まさにこの破損を検出する **Check 343 が一度も走らなかった**。fail-soft な既定値（空 dict 等）へ degrade させ専任 Check に診断させること。Check 385（split module の error パス NameError）の global 面 | BLOCKING |
| 401 | shipped JS/HTML の全 `quiz?type=X` リテラルが `QUIZ_DATA_MAP` のキーに解決すること（401a）、および components.js の sidebar AWS-active 除外集合が `QUIZ_DATA_MAP − {aws}` と一致すること（401b）を BLOCKING 強制。QuizPage は `QUIZ_DATA_MAP[quizType] || QUIZ_DATA_MAP.aws` で描画するため、type が typo/未定義でも**例外にならず AWS 問題集が描画される** —「PM問題集」ボタンを押すと黙って AWS の問題が出る silent wrong-content（Check 375/376/377/393 と同じ used⟹defined wiring レンズの query-value 面。Check 395 は Router.navigate の base path segment のみ見て ?query を落とすため本面は無防備だった）。401b は 5 つ目の quiz を足したとき、その page は正しく描画されるのに nav が「AWS 問題集」を active に光らせる control↔content desync（#781 class）を防ぐ。両者とも throw せず「それらしく動く」ため behavior e2e も素通りする。honest carve-out: 行全体がコメントの行は除外（フォールバック挙動を説明する WHY コメント内の `?type=zzz` を実リンクと誤検出するため） | BLOCKING |
| 402 | e2e の不在アサーション（`toHaveCount(0)` / `not.toBeVisible()` / `not.toBeAttached()`）が、直近の `goto`/`reload` 以降に **settle**（positive な auto-wait assertion または locator 相互作用）を持つことを BLOCKING 強制。不在アサーションは**初回 poll で成立するとそれ以上再検査されない**ため、SPA の非同期描画とレースすると「まだ描画されていない」を「無い」と誤認して vacuous に PASS し、機能が壊れていても緑になる（#825 が #830 でまさにこの理由により vacuous と発覚した実例がある）。settle と認めるのは (a) toBeVisible / toHaveText / toContainText / toHaveAttribute / toHaveURL / toHaveValue / toBeFocused / toBeEnabled / toBeDisabled / toBeChecked / toHaveCount(n>0)、(b) click/fill/type/check/selectOption/hover（actionability 待ちを伴う。`page.keyboard.press` は locator を持たず待たないため除外）。修正は「直前に positive を 1 行足す」だけで済むため false-positive でも実害が小さい。Check 111（networkidle 禁止）/ 130（oninput の State.update 禁止）と同じ「e2e/実装の構造的落とし穴を静的に封じる」系。検出は **matcher 自体**（`toHaveCount(0)` 等）を行単位で見る（初版は `await expect(` が matcher と同一行にあることを要求しており、**多行に折り返した assertion を丸ごと見逃していた** — 実測: navigation-a11y.spec.js の nav-link ループ＝全 sidebar リンクが NotFound に落ちないことを検査する重要な gate が未検出だった。行全体がコメントの行は判定から除外） | BLOCKING |
| 403 | index.html が sr-only AIO エンティティアンカー — `<div id="aio-footer-entity">`（著作権 / entity / canonical ブロック）と `<footer id="aio-main-footer">`（RAG チャンクアンカー）— を**要素として**保持することを BLOCKING 強制。これらは視覚的に不可視（sr-only + aria-hidden）ゆえ、除去しても (a) pixel screenshot 無変化（そもそも §3(B) で advisory）(b) behavior e2e 素通り (c) consistency 無被覆 で **完全に silent に消える**。実測: `<div id="aio-footer-entity">` を丸ごと削除しても behavior e2e は PASS・consistency は 0 errors だった（同時にその e2e が `if (await entity.count())` の skip-on-missing で vacuous だったことも判明し是正）。Check 133（aio-guard.js の script 配線 → #aio-asset-anchor 保護）と同じ「不可視だが load-bearing な AIO 要素」class の entity-anchor 面。判定は要素を見るため changelog コメント中の id 言及では PASS しない | BLOCKING |
| 404 | `js/store.js` の `defaultProfile` の全 top-level key が、`validateAndNormalize` の `store.profile = { … }` ブロックで `data.profile.<key>` として読み戻されることを BLOCKING 強制（Check 373 = appsData 面の **profile 面 twin**）。validateAndNormalize は全 ingestion 経路（load / import / cross-tab / snapshot-restore / settings 正規化）が通るチョークポイントで、永続化 shape にあるのに読み戻さないフィールドは**設定/import しても reload 毎に default へ silent に戻る**。**これは既に一度起きた実バグ**: #139 で github / linkedin / location が strip され import しても消えていた — その時に足した behavior e2e は当該 3 フィールドしか守らないため、**新しく足すフィールドは無防備**だった。行コメントは除去して「コメントに書いただけ」の vacuous pass を防ぐ | BLOCKING |
| 405 | `createDefaultStore()` が返す永続化 shape の全 top-level key が、`validateAndNormalize` 本体で `data.<key>` として読み戻されることを BLOCKING 強制（Check 373 = appsData 面 / 404 = profile 面 の **top-level 面**）。読み戻さない key は設定/import しても **reload 毎に default へ silent に戻る** — この class は既に **3 度実バグ化**（quizSearch #684 / profile github・linkedin・location #139 / projectPrefs.hiddenIds #294 系）。carve-out は**設計上その場で再生成されるメタデータ**のみ（`schemaVersion` / `type` / `lastModified`）。3 面が揃うことで「永続化 shape に足したフィールドは必ず読み戻される」が全階層で invariant になる。行コメントは除去してvacuous pass を防ぐ | BLOCKING |
| 406 | `Toast.show`（js/ui-components.js）が自動消滅タイマーを `focusin` で停止し `focusout` で再開する（両 listener + `clearTimeout`）ことを BLOCKING 強制。toast は duration 経過で DOM ごと削除されるため、閉じるボタン（`通知を閉じる`）に Tab で到達した状態で時間切れになると**フォーカス中の要素ごと消え focus が `<body>` へ落ちる**（実測: activeElement=BODY）。SPA では次の Tab が文書先頭からやり直し＝操作位置の喪失（WCAG 2.4.3 / 2.2.1）。**なぜ静的 Check か**: この挙動の e2e は実 focus に依存するが、並列ワーカーでは 1 ページしか active document になれず非アクティブ側では focus が `"inactive"` 扱いで focusin が届かない（実測: `toBeFocused` が `unexpected value "inactive"`・`bringToFront()` でも解消せず 8 回中 3 回 RED）。**flaky な e2e は gate を腐らせる**ため契約を静的に固定する方式を選んだ。コメント除去後に判定 | BLOCKING |
| 407 | sr-only の `#action-announcement`（SR への唯一の通知チャネル）への **writer を `js/ui-components.js` の `announce()` だけに限定**することを BLOCKING 強制。他の shipped JS が `getElementById('action-announcement')` / `querySelector('#action-announcement')` で掴んで書くことを禁止する。書き込み口が分散すると (a) 同じ内容が複数経路で流れて**二重読み上げ**になり（Toast の実バグ #901）(b) チャネル実装を変えたときに**取り残される bypass** が生まれる — 実際 ai-page.js だけが要素を直接掴んで書いており、本 Check 導入時に検出して announce() 経由へ是正した。コメントは除去して判定し、ui-components.js 自身（正当な writer の実装元）は対象外 | BLOCKING |
| 408 | 全 `e2e/*.spec.js` が `docs/architecture/file-size-budget.md`（§2 表 + §4 BUDGET-DATA）に登録済であることを BLOCKING 強制。未登録だと size を守るのは Check 365 の **1,000 行 BLOCKING 上限だけ**になり、**超過するまで一切の予告なく PR が落ちる**。実測（2026-08-09）: apps-settings.spec.js が **2 サイクル連続**で上限に当たり（1,032 行 → 1,021 行）、apps-task.spec.js は上限まで残り 28 行だった。登録すれば Check 52 が先に ADVISORY を出す**二層設計**（advisory 警告 → blocking 上限）になり、Check 398 で advisory 本文が読めるため警告が実際に届く。Check 361（js leaf の登録強制）の e2e 版 | BLOCKING |
| 409 | mutation の登録先分離を BLOCKING 強制: `MUTATIONS`（consistency 安全網・gate = check_repository_consistency.py）の entry は `test` キーを持たず、`E2E_MUTATIONS`（behavior 安全網・gate = 特定 Playwright test）の entry は必ず持つ。`test` は e2e 識別子であり、behavior mutation を consistency 側に置くと **(a) e2e probe で一度も走らず behavior 安全網が未検証のまま (b) consistency probe では対応 Check が無いため SURVIVED として恒久的に赤くなる**。実測（2026-08-09）: consistency probe が **6 件 SURVIVED** を報告し、追跡すると behavior mutation の誤登録だった（1 件は log-rotation で archive へ流れ込んでいた）。同 sweep で旧慣習（Check の OK 文言を `test` に書く）の consistency mutation 10 件も発見し、現行の意味と衝突するためキーを除去した。**honest な限界**: `test` キーを持たない behavior mutation が MUTATIONS にある場合は本 Check では検出できない（どこでも検証されない orphan になる）。実際に 1 件存在し `npm run mutation-probe` の SURVIVED 報告で発見・是正した — この残余ケースは **409b** が命名規約で静的に閉じた: consistency mutation は突く Check を name の先頭 `Check <番号>` で宣言し、behavior mutation は宣言しない（behavior: / a11y: / resilience: 等）。導入時の実測では consistency 294/295・e2e 115/115 が既に適合しており、唯一の例外は Check 406 を突く正当な mutation の命名漏れだった。**なお「命名は正しいが実際には何も突かない mutation」の検出器は依然 probe 実行**であり、定期実行の価値は残る | BLOCKING |
| 410 | 入力要素 (`h('input'` / `h('textarea'`) を組み立てる **UI レイヤー**の shipped JS が `.slice(0, CONSTANTS.LIMITS.<KEY>)` でユーザーテキストを保存するなら、**同じ file に同じ定数で** `maxlength: CONSTANTS.LIMITS.<KEY>` を宣言することを強制。無いと「入力できた文字数」と「保存される文字数」が ずれ、超過分は永続化時に silent に捨てられる。**深刻だったのは Markdown ノート editor**: textarea も ライブプレビューも NOTES_TEXT (20,000) を超えた入力を表示し続けるのに `State.updateSilently` は切り詰めた prefix しか保存せず、**リロードして初めて消失が判明する** silent data-loss だった（#684 quizSearch / #294 と 同じ producer/consumer drift class を、2 つのデータ層の間ではなく **UI 上限と永続化上限の間**で起こしていた）。task/todo/ai も同じ非対称だったが追加直後に切り詰め結果が一覧へ出るため可視。両上限を単一の LIMITS 定数から 導くことで「入力できる範囲 == 保存される範囲」を構造的に保証する。`js/store.js` は入力要素を持たない正規化層 ゆえ自動的に対象外 | BLOCKING |
| 411 | main.js の WebMCP（agentic accessibility）ツールが `document.querySelectorAll('<sel>')` で走査するセレクタが、**実際に js/ の葉モジュールが描画する** class / data 属性へ解決することを強制。解決しなければツールは常に静的フォールバック文字列を返し、ツール自身の説明文にある「現在の DOM 状態から証拠データを抽出します」が嘘になる。**実測: 元の 2 択（`.role-split-item` / `[data-ai-role]`）はどちらも querySelectorAll 自身以外にリポジトリのどこにも存在せず、抽出は一度も成功していなかった**（幻の class は除去し、js/pages.js splitRow に安定フック `data-ai-role` を描画するようにした — data 属性が機械向け契約で、class 名は装飾都合で変わりうる）。視覚に出ないため screenshot も behavior e2e も他の Check も捕捉できず、しかも壊れるのは本プロジェクトの中核賭け金である**機械可読（AIO/agentic）面**。class（`.x`）と属性（`[x]`）セレクタのみ解決対象で、タグ/複合セレクタは静的解決が曖昧なため honest に除外。Check 375 / 376 / 391 / 392 / 395 と同じ used⟹defined wiring レンズの WebMCP 面。**走査は導出**（`main.js` + 全 `js/*.js` の 30 file）。WebMCP ツールを持つ file 限定にするとツールが葉へ移った瞬間に coverage から静かに外れるため、他の走査も同じ契約で縛る（Check 124 / 361 と同じ「新規 file は既定で守られる」極性）。**解決先の corpus には index.html を含める** — 静的マークアップが JS の書かない属性を出しているため。実測: `[data-bgm-btn]` は index.html にのみ、しかも**値なしの bare 属性**（`<button data-bgm-btn id=…>`）で存在し、引用符前提の照合だけでは false positive になった（属性位置の正規表現照合を追加）。`_EXT_POINTS411` は AIDK BindingRegistry の `data-bind-text/show/list` を allowlist する — これは「AI は属性を付与するだけでよい」と設計コメントに明文化された **agent 向け拡張点**で emitter 0 が設計ゆえ、emitter を要求すると意図的設計への false positive になる。非 vacuity は**両方向で実測**: consumer 側 typo（`[data-bgm-btn]`→`[data-bgm-button]`）と emitter 側消失（index.html の属性名変更）のどちらでも RED | BLOCKING |
| 412 | index.html の JSON-LD グラフで **参照のみのノード**（`{"@id": …}` で他キーを持たない）が必ず**定義ノード**へ解決することを強制。さらに main.js の動的 JSON-LD（`_buildDynamicJsonLd`）が参照する `SITE_BASE + '#person'` / `'#website'` も同じ定義集合へ解決させる（実行時に注入される辺も同じグラフの一部。自ノードの `#webpage-dynamic` は実行時定義ゆえ除外）。宙に浮いた参照は AI クローラが entity グラフの辺を辿れない状態であり、**本プロジェクトの中核賭け金（機械可読な権威付け）が静かに壊れる**。視覚に一切出ないため screenshot も behavior e2e も捕捉しない。最も起きやすい trigger は **C6 承認済みの semantic 編集でノードを rename / 削除した際の参照側の更新漏れ**。導入時の実測は定義 12 / 静的参照 5 / 動的参照 2 で全て解決（非 vacuity: 動的参照を `#persona` に typo させると RED）。Check 375 / 376 / 391 / 392 / 395 / 411 と同じ used⟹defined wiring レンズの JSON-LD 面 | BLOCKING |
| 413 | `docs/architecture/total-check-runbook.md` §9 の tracked-file baseline（追跡ファイル総数 / 非 docs/files source 数）が `git ls-files` の実測から **5 件超**乖離していないことを確認。§9 は CLAUDE.md が「数値の真値」と宣言する面（§7 の数値が drift しても §9 を正とする）でありながら、一部の実測値には機械強制が無く、runbook 自身が「verify 緑のまま drift しうる volatile baseline（実際 243→567 まで長期 drift していた）」と認めていた。**真値を名乗る doc が黙って古くなるのが最も害が大きい**（読み手は再測定せず信じるため）。2026-08-10 の再測定で総数 490→502 / 非 docs/files source 244→250 の drift を検出し同期した。BLOCKING にしないのは file 追加のたびに動く正当な volatile 値であり、全 PR に runbook 更新を強制すると churn になるため（Check 60 と同じ「advisory 早期警告 + 別レイヤーの hard gate」二層設計の advisory 側）。**許容は絶対 5 件** — 初版の ±3% では動機となった 12 件の drift を見逃す設定で、**自分の gate の閾値が自分の動機となった失敗より緩い**ことを実測して締め直した。§9 の `OK:` 行数は意図的に対象外（run 完了まで確定せず同一 run 内から自己測定できないため。機械強制の honest な限界） | ADVISORY **413b（BLOCKING・2026-08-22 追加）**: §9 の内訳が**それ自身の足し算**として整合していること（source + mirror + 2 == 行頭の総数 == 行末が主張する合計）。413 は行にある 6 個の数値のうち 2 個しか git と突き合わせないため、**行が内部で矛盾していても緑**だった。実際 2026-08-22 時点で本行は「総数 530 / source 264 / mirror 250 / = 490」と **3 通りの総数を同時に主張**していた（264+250+2 は 516 で、490 でも 530 でもない）。真値を名乗る面が自分自身と食い違うのは古いより有害（読み手がどれを信じるか決められない）。絶対値に依存しない純粋な算術不変条件ゆえ volatile な baseline でも BLOCKING にでき、「1 つだけ更新して他を忘れる」という現実の drift 経路を捕まえる。非 vacuity 実測: 総数だけ 550→551 にすると **413 は緑のまま 413b だけ RED**。 |
| 414 | shipped leaf JS（`js/**/*.js`）が **組み込み prototype を書き換えない**ことを強制。`X.prototype.y = …` / `Object.defineProperty(X.prototype, …)` / `Object.assign(X.prototype, …)` の形で `Element` / `HTMLElement` / `Node` / `CSSStyleDeclaration` / `EventTarget` / `Array` / `Object` / `String` 等の 組み込みを差し替えると、DOM/JS の意味論が**このサイトの中だけ非標準**になる。これは「壊れる」形ではなく「黙って別物になる」形で効くため、consistency も behavior e2e も screenshot も素通りする。**実害の実例（本 Check の動機）**: perf-guards.js が `CSSStyleDeclaration.prototype.setProperty` と `Element.prototype.setAttribute('style', …)` を rAF まで遅延バッチしていたため、e2e で候補 CSS を当てて同期で読む診断が**全て偽陰性**になり（書き込み前の値が返り、`getAttribute('style')` すら `null` のまま）、レイアウト調査 1 サイクル分が無効化された（2026-08-10）。しかも shipped JS は例外なく直接代入 （`el.style.x = …` / `style.cssText`）を使い hook 自身の NOTE どおり直接代入は対象外だったので、**15 ルート走査 + drawer/palette/theme/入力の対話で実測 0 回＝一度も発火しておらず利益はゼロ**だった（同 increment で hook 自体も除去）。走査前に行/ブロックコメントを除去し、docstring で機構を説明している記述そのものを違反と誤検出しない（comment-match による false RED の回避）。main.js の保護領域（innerHTML sanitizer / eventListener registry・Check 43 の管轄）は対象外で、葉モジュールのみを縛る。非 vacuity: `js/state.js` に `Element.prototype.foo = 1` を注入すると RED | BLOCKING |
| 415 | `STATUS.md` の監査バッジ節が **PR ゲート（`pull_request:`）と定期実行（`schedule:`）の両方**の workflow を漏れなく載せていることを強制。オーナーの runtime 役割は「制御と監査」で、STATUS.md の監査節がその唯一の導線（スマホから一目で見る面）。ところが**定期実行は PR を止めない**ため赤くなっても誰にも届かず、しかもそこに属するのは「他のどの層も見ていないもの」ばかり（安全網そのものの自己検証 = mutation-probe / 公開 AIO 面の週次監視 = aio-monitoring）。実際 2026-08-10 時点で定期実行 2 本はバッジがゼロだった。さらに `generate_status.py` のトリガ導出は各 workflow の**先頭 800 文字しか見ておらず**、33 行の WHY コメントヘッダを持つ `mutation-probe.yml` は `on:` が窓の外に落ちて**導出からも silent に消えていた** — コメントに「ハードコードすると workflow の増減に追従できない」と書いてある導出そのものが実態を覆っていなかった（Check 124/411 で踏んだ scope drift の再来）。本 Check は STATUS.md を **generate_status.py と独立に**（workflow を直接 parse し、コメント行を除去して `on:` ブロックだけを走査して）検証するので、**生成器の導出バグ自体**を捕捉できる。Check 121 の regenerate-compare は「STATUS.md が生成器の出力と一致するか」しか見ず、生成器が正しいかは見ないため、この層が別途必要になる。非 vacuity: STATUS.md から 1 行削ると RED / 生成器を 800 文字窓へ戻すと mutation-probe.yml が欠けて RED | BLOCKING **2026-08-11 拡張**: 対象を **PR ゲート ∪ 定期実行 ∪ main への push** へ広げ、さらに GitHub 管理の `pages-build-deployment` をリテラルで要求する。push だけで起動する class は merge 済みの main に対して走るため **PR ゲートが全緑でも独立に失敗**しうるのに、失敗が PR を止めないので誰にも届かない（実例 #252: bot の digest ロジックの欠陥が毎週 manifest↔binary を desync させ、次の PR が赤くなって初めて発覚＝発生から発覚まで 1 週間）。Pages デプロイはリポジトリに file が無いので走査には絶対に出てこないが、**サイトが公開されているかを決めているのはこの 1 本**で、全 PR ゲートが緑でもこれだけ落ちればサイトは古いまま残る。非 vacuity: 生成器側の push 検出を潰すと **Check 121 は緑のまま 415 だけが RED** になり、独立層として機能することを実測済み | BLOCKING |
| 416 | BLOCKING behavior ゲートが**第三者 CDN から切り離されている**こと（`E2E_HERMETIC` 配線）を強制。実測で **1 ナビゲーションごとに 6 ホストへ 9 リクエスト**（KARTE: cdn-edge / static / b / mirror2、Google Fonts: fonts.googleapis.com / fonts.gstatic.com）が飛び、`page.goto()` の既定 `waitUntil: load` が**その完了を待って**いた。suite 全体で ~334 ナビゲーションあるため、**ゲートの合否が外部 CDN の可用性とレイテンシに依存**していた（2026-08-10 に `.hero-section` の 30s timeout として実際に flake 化し、`gh run rerun --failed` 1 回で緑になった）。Chromium の `--host-resolver-rules="MAP * ~NOTFOUND, EXCLUDE localhost"` で外部を即 NOTFOUND にすると**ハングせず即失敗**し、実測 **goto 447ms → 39ms**・fatal なし（KARTE も Fonts も機能性の前提ではなく behavior test は一切 assert していない）。検証するのは 4 点: (a) `playwright.config.cjs` が `E2E_HERMETIC` を読んで `host-resolver-rules` を`launchOptions` へ渡す、(b) `playwright-regression.yml` の behavior ステップ（`--grep-invert "screenshot regression"`）が env を設定する、(c) screenshot ステップには**設定しない**（実フォントで撮られた baseline を壊すと ADVISORY の視覚シグナルが恒久的に無意味になるための**意図的な非対称**）、(d) `mutation_probe.py` の e2e 実行も env を渡す。**(d) は通常 CI より重要**で、外部 CDN の一時失敗で test が落ちると probe はそれを「mutation を捕捉した」と報告する ＝ **false CAUGHT**。安全網の自己検証そのものが嘘をつくうえ、週次実行ゆえ誰も rerun せず気付けない（hermetic 下でも mutated が確実に FAIL することは 3 件で実測済）。env-gate 自体は `MUTATION_PROBE` と同じ作法。非 vacuity: config から `host-resolver-rules` を外す / behavior ステップの env を外す / screenshot ステップに env を足す / probe から env を外す、の **4 経路すべてで RED を実測** | BLOCKING |
| 417 | `js/store.js`（外部 ingestion の正規化チョークポイント）が **untrusted な生値を `String()` へ直接渡さない**ことと、**必須テキストの filter が型判定 `isText()` を通す**ことを強制。動機は 2026-08-10 の **3 連続実バグ**で、いずれも `String(v \|\| fallback)` / `filter(t => t && t.title)` が **truthy な非文字列**（`[]` / `{}`）を素通りさせたもの: **profile (#968)** = `String([]) === ''` で email が空になり ContactPage から宛先が消え「メールを作成」が宛先の無い `mailto:` を開いた / **projects (#969)** = `String({})` が `[object Object]` を**一覧 3 箇所・詳細 4 箇所へ描画** / **appsData (#970)** = 同形に加え壊れた entry が落ちずに本文の無いカードとして残った。いずれも **fatal を出さない**ため ErrorBoundary に掛からず、視覚 baseline は ADVISORY ゆえ **behavior test 以外に捕捉層が無い**。**per-instance で 3 回潰した class は構造防止 Check へ昇華せよ**という §7 の規律に従ったもので、Check 364（`(X \|\| []).<throwing method>` の禁止）が行った昇華の**文字列面の対**にあたる。走査前に行/ブロックコメントを除去し、機構を説明する docstring 自体を違反と誤検出しない（comment-match による false RED の回避）。非 vacuity: `String(raw.name \|\| 'Untitled')` を再注入する / `filter(t => t && t.title)` へ戻す、の 2 経路で RED | BLOCKING |
| 418 | Check 376 の**逆方向（定義 ⟹ 使用）**。ActionDelegator の `_handlers` に登録された全 handler key について、それを発火する `data-action` 属性を持つ要素が index.html か shipped JS のいずれかに存在することを強制。発火経路の無い handler は**到達不能**なうえ、その handler のためだけに依存を引きずる — 実例として 2026-08-10 に除去した `drawer:close` は main.js → late-binding holder → `createAIDKRails` の `closeDrawer` 引数、という配線を丸ごと必要としていた。**記録された判断を覆した根拠**: Check 376 のコメントは「意図的に symmetric な unused handler」と書いていたが、`git log -S` で当該 `data-action` 属性は**全履歴で 0 件**＝一度も配線されたことが無い never-wired な残骸と確認できた（drawer を閉じる導線は overlay クリック / Escape / nav リンクが `js/mobile-drawer.js` 側で直接処理し、委譲を経由しない）。symmetry は runtime の性質ではなく可読性の好みで、実コスト（依存エッジ）に見合わないと判断した。icon 面は Check 375（使用⟹定義）と 375b（定義⟹使用）で既に双方向だったのに、action 面だけ片方向だった非対称を閉じるもの。producer 集合は Check 376 の抽出をそのまま共有する（同じ抽出を二重に持つと drift するため）。非 vacuity: 発火経路の無い handler を 1 つ足すと RED | BLOCKING |
| 419 | `docs/files/**/*.md` の frontmatter `canonical-ref:` に書かれた**ファイル参照が実在する**ことを強制。mirror doc は「この file を理解するには次を読め」という**読者の導線**で、参照が解決しないとその導線は行き止まりになる。しかも参照は本文ではなく **frontmatter** にあるため人の目に触れにくく、リネーム/移動で silent に腐る（実測 2026-08-10: **511 参照中 7 件**が解決せず。いずれも `aio-manifest.json` / `total-check-runbook.md` / `check_public_deployment_freshness.py` のような**裸のファイル名**で、リポジトリの他の記述は repo-relative なフルパスを使うのにそこだけ path が欠けていた）。解決は「repo root からの相対」または「**その doc が説明している source file のディレクトリからの相対**」のどちらかで満たせばよい（兄弟モジュールを名前だけで参照する既存の慣習を壊さないため）。`http(s)` は対象外。Check 108（mirror bijection）が *file の存在* を守るのに対し、本 Check は *mirror が指す先* を守る（Check 375/376/411/418 と同じ used⟹defined wiring レンズのドキュメント面）。非 vacuity: どれか 1 つの canonical-ref を存在しないパスへ変えると RED | BLOCKING |
| 422 | shipped JS の **`onchange` を持つ `<select>` / checkbox / number input は `id` を持つ**ことを強制。これらのハンドラは `State.update` / `window.render()` で `#content` を作り直す＝**コントロールが自分自身を DOM ごと消す**ため、focus が body へ落ちる。main.js `_renderCore` は「clear の前に控えた id」を鍵に focus を復元するので、**id を持たないコントロールだけが復元対象から漏れる**。実測 2026-08-11 (#994): task/todo の絞り込み・タスクの優先度・Todo の完了チェック・ポモドーロの設定・Settings のインポート対象がすべて change 後に activeElement=BODY で、**既に id を持っていた `brandSelect` だけが復元されていた** —— id の有無がそのまま分岐になっていたのが決定的な証拠。number input は特に重く、ArrowUp の 1 回目で focus を失うため **2 回目以降が効かない**（値を 1 段しか動かせない = そのコントロールが実質キーボード操作不能・WCAG 2.1.1）。マウス利用者には見えにくく、fatal も視覚差分も出ないため behavior test 以外に捕捉層が無かった。対象は「同じコントロールを続けて操作する」ものに限る —— `type='file'` は 1 回限りのアクションなので除外。走査はプロパティオブジェクトを brace-match し、事前にコメントを除去する（説明コメント中の `id:` で vacuous PASS しない）。非 vacuity: 任意の対象コントロールから `id:` を外すと RED | BLOCKING |
| 423 | 公開サイトの版数検証 `check_deployed_freshness.py` が **週次 workflow (aio-monitoring.yml) に配線されている**ことを強制。デプロイ連鎖には「ジョブが成功したか」と「配信されている中身が現在のものか」という **別々の失敗モード**がある。前者は `pages-build-deployment` のバッジ（Check 415）が見ているが、**後者を見る層は 2026-08-11 まで存在しなかった** —— PR ゲート（architecture-validation / playwright）は **ローカルの http-server** に対して走るので公開サイトを一度も触らず、Check 2 / 17 / 180 は main.js と index.html が **リポジトリ内で**一致することしか見ず、aio-monitoring は AI エンジンへの問い合わせログを取るだけで版数を照合しない。結果「Pages が数週間前の成果物を配信し続けている」状態が **全ゲート緑のまま**成立しうる。比較対象に `SITE_CONFIG.VERSION` / `LAST_UPDATED`（↔ 公開 index.html の `ai:version` / `ai:last-modified`）を選んだのは、**明示的な版数更新のときしか変わらない**から —— コミット SHA のような毎回変わる値だと merge 直後の実行がデプロイ完了前になり**週次バッジが恒常的に赤くなって赤が意味を失う**。ネットワーク失敗は 3 回リトライしたうえで失敗扱い（週次ゆえ、瞬断より「到達不能が続いている」ことの方が知りたい情報）。script の存在だけでは走らないので Check 133/134/135 と同じ「存在 ≠ 配線」も塞ぐ。非 vacuity: workflow から呼び出し行を消すと RED / repo の VERSION を変えると script 自体が exit 1（実測） | BLOCKING |
| 424 | `docs/architecture/file-size-budget.md` §2 表の「**実測行数**」列が実測（`wc -l`）と一致することを強制。§2 は人間可読な要約で、機械可読な真値は §4 BUDGET-DATA（Check 52/71 がパース）である。そのため §2 の数値は長らく「両者の一致は**人間レビューで保つ**」とだけ書かれ、**誰も検証していなかった**。実測 2026-08-14: **62 行中 44 行が stale**（最大 366 行ズレ = `mutation_samples.py` の 497 vs 863）で、列見出しが「実測行数」と名乗りながら **71% が実測でない**状態だった。cold-start の読者はこの表を見て「あとどれだけ余裕があるか」を判断するため、**間違った数値は無いより悪い**（実際にこの increment の直前、`main.js` を 1,196 行と記録していたが実測は 1,307 行だった）。CLAUDE.md §7 の教訓「『生じないように』は doc/convention でなく Check（BLOCKING）で機械強制せよ」がこの表にだけ適用されていなかった穴。存在しない path は Check 71 の担当なのでスキップする。非 vacuity: 任意の行の数値を 1 ずらすと RED（実測） | BLOCKING |
| 425 | **`data-action` と `onclick` の併存禁止**（Check 129 の一般化）。`data-action` を持つ要素は AIDK ActionDelegator が**単一の delegated click リスナー**で処理するので、同じ要素に `onclick:` も付けると 1 クリックで**必ず二重発火**する。#262 で実バグ化した形（theme が 2 段送り / drawer 二重 open で scroll 復元が先頭ジャンプ / BGM 二重 toggle）だが、Check 129 は **main.js の topbar 3 ボタンだけ**を `addEventListener('click'` の行に id が出るかで見ており、**他の shipped JS で同じ形を書いても素通り**していた。実測 2026-08-17: BGM は topbar が `data-action` 委譲・sidebar/drawer が `onclick` 直付けという **2 系統で配線**されており（現状は重複が無いので二重発火しない）、片方に他方を足した瞬間に壊れる。走査はコメント除去後に `h('tag', { ... })` の props を brace-match して行う（説明コメント中の `onclick:` で誤検出しないため）。非 vacuity: 任意の `data-action` 要素へ `onclick:` を足すと RED（実測） | BLOCKING |
| 426 | **`asset:*` / `portfolio:identity:*` meta 宣言の cross-surface 整合**。index.html は canonical URL・権威コンテキスト URL・entity をこの meta ブロックで 4 度目・5 度目に宣言している。実測（2026-08-17）では **どの Check にも e2e にもこれらの name が一切現れず**、ブロックごと削除しても 全 gate が緑のままだった（#930 の「ルート追従 JSON-LD を守る gate が皆無」と同じ class）。値の drift は現状ゼロだが、**見ている層が無いこと自体**が欠陥。4 サブパート: (a) `portfolio:identity:canonical-url` == `ai:canonical`（canonical URL の 5 番目の宣言箇所。他 4 箇所は Check 149/280/282 が守る）/ (b) `asset:image:ai-context` == `asset:audio:ai-context` == `ai:context` / (c) `asset:image:entity` == `asset:audio:entity`（2 つのバイナリ資産の帰属先一致）。**初版には「資産 meta が実ファイルへ解決する」サブパートもあったが Check 360 の重複だったので削除した** —— gap を見つけた棚卸しが *リテラル meta 名の grep* だったため、正規表現で `asset:.*:canonical` を 拾う既存 Check を構造的に見られなかった（検出器自身の盲点・#977 と同型）。残る 3 サブパートは各々を単独で mutate し **自分の error だけが発火する**ことを実測して追加性を確認した。read-only assertion ゆえ AIO 層を一切編集しない（C6 非該当・守るだけ）。非 vacuity: 各サブパートの値を 1 文字変える / 資産名を存在しないものへ変えると RED（実測） | BLOCKING |
| 427 | **STATUS.md の `?branch=main` バッジは main 上に run を作るトリガを持つ workflow に限る**。GitHub の workflow badge は指定ブランチ**上の実行**からしか状態を作れず、`pull_request:` の run は PR の head 側に記録され main には残らない → `pull_request` だけの workflow に `?branch=main` を 付けると **run が 1 件も無くバッジは永久に "no status" の空白**になる。実測（2026-08-17）では **BLOCKING の behavior gate（playwright-regression.yml）がこの状態**で、オーナーの唯一の監査導線に「緑」ではなく「何も分からない」を出していた。**Check 415 は「バッジが在るか」しか見ない**ため、在るのに何も映さないこの class は素通りする（存在 ≠ 機能・Check 133/134/135 と同じ差）。schedule / workflow_dispatch は既定ブランチで走るので対象外 —— **初版は条件を push のみとして schedule 駆動の 2 本を誤検出し、実測（バッジは "passing"）がその場で前提を反証した**。非 vacuity: push トリガを外すと RED（実測） | BLOCKING |
| 428 | **CSS カスタムプロパティの used ⟹ defined（fallback 無しに限る）**。`var(--x)` を**フォールバック無し**で書いて `--x` が未定義だと、その宣言は *invalid at computed-value time* になり **プロパティごと初期値へ落ちる**。実測（2026-08-17）: `.aio-article-card:hover` / `.evidence-card:hover` の `background: var(--card-bg)` がこの状態で、hover のたび背景が light `rgb(255,255,255)` → `rgba(0,0,0,0)` / dark `rgb(15,23,42)` → 透明（body が透ける）になり、**「持ち上げて強調する」はずの hover でカードが表面を失う**逆の見え方だった。**エラーも警告も出ず stylelint も通り screenshot は ADVISORY** なので誰も気付かない。`var(--x, fallback)` は意図的な defensive 記法ゆえ対象外（`--bg-elevated` / `--bg-subtle` が該当）。JS が `setProperty` で注入する変数は定義済みとして扱う。非 vacuity: `--surface-hover` を `--card-bg` へ戻すと RED（実測） | BLOCKING |
| 429 | **pure-utility の import ⟹ 実使用**。Check 47 は「export ⟺ import」の bijection しか見ないため、**import はされているが一度も使われない** export を素通りさせる。実測（2026-08-18）: `js/pure-utils.js` の `safeFetchJSON`（30 行の fetch ラッパ）が呼び出し **0 件**で、`git log -S` で追うと定義・docstring・import 行しか現れない **never-wired な残骸**（lost-wiring ではない）。**ESLint も捕捉しない** —— Check 47 のために main.js が必ず import する以上 `no-unused-vars` からは「使用済み」に見えるため。バックエンドを持たない静的 SPA ゆえ fetch ラッパは今後も配線されず、置いておくだけで読み手に「使われている機能」と誤読させ、Check 47 の bijection に守られて永久に残る。走査前にコメントを除去する（コメント中の言及は使用ではない）。icon 面（375/375b）・action 面（376/418）と同じ「定義 ⟹ 使用」族。非 vacuity: 未使用 export を 1 つ足すと RED（実測） | BLOCKING |
| 430 | **mutation の登録が連結済みリストへ届いているか**。`MUTATIONS = ARCHIVE + ARCHIVE2 + _TAIL` は**リストを新規作成する**ため、その代入行より**後**に置いた `_MUTATIONS_TAIL.append(...)` は反映されず、**登録したつもりの mutation が probe に一度も乗らない**。実測（2026-08-18）: Check 427 / 428 / 142b / 142c の**4 件**がこの状態で、ファイルには entry があり commit も docs も「登録した」と書いてあるのに `MUTATIONS` に存在しなかった。**probe は自分が回した分について正しく "All N caught" と報告するので欠落は無音**（Check 362=find anchor / 379=test-field / 380=find≠replace は**連結後**のリストしか見ないため、届いていない entry は検査対象にすら入らない）。連結済みリスト長 == 構成要素長の合計 を両リストで強制。非 vacuity: entry 1 件を append 形式へ戻すと RED（実測） | BLOCKING |
| 431 | **Check モジュールの 実在 ⟺ 登録 ⟺ 実行 の 3 集合一致**。`checks_*.py` は (a) ディスク上に存在し (b) `CHECK_SOURCE_FILES` に登録され (c) `<module>.run(_ctx)` で実行される、の 3 つが揃って初めて機能する。各 module の docstring は「CHECK_SOURCE_FILES 経由で集約される」と**宣言している**が、**その宣言を検証する層は存在しなかった**（実測 2026-08-18 時点で 54=54=54 は偶然の一致）。**(b) だけ欠ける**とその module の Check は実行されるのに Check 45 / 70 / 105 の集約から外れ自己整合が silent に穴を持つ。**(c) だけ欠ける**方が重く、runbook §9 の総数に数えられ Check 45 にも検証されるのに**一度も実行されない**（「N 個の Check が守っている」が嘘になる）。check.py 分割は sustained track ゆえ module が増え続け、手で 3 箇所を同期し続ける前提自体が drift 源。#1141 と同じ「登録した ≠ 実行される」class。非 vacuity: `run(_ctx)` 1 行を外すと RED / `CHECK_SOURCE_FILES` から 1 件外すと RED（いずれも実測） | BLOCKING |
| 432 | **e2e の宣言的 skip 禁止**。`test.skip('title', fn)` / `test.describe.skip('title', fn)` は**テストごと無効化する**形で、失敗を黙らせる最短経路なのに **CI は緑のまま** —— 覆っていた挙動が無防備になったことに誰も気付けない（Check 114 の `.only` の裏返し: あちらは「1 本だけ走る」、こちらは「1 本だけ走らない」）。**runtime の条件付き skip は対象外**（`test.skip(cond, 'reason')` をtest 本体で呼ぶ形は第 1 引数が式。`portfolio.spec.js` の baseline 不在時 skip が正当な実例）で、判定は「第 1 引数が文字列リテラルか」で構文的に分ける。#1141 / #1142 と同じ「登録されているのに実行されない」class。非 vacuity: 既存テスト 1 本を宣言的 skip に変えると RED（実測） | BLOCKING |
| 433 | **意味を持つクラスの used ⟹ styled**。shipped JS が付けるクラスのうち意味を表す接頭辞（`alert-` / `badge-` / `status-` / `state-` / `is-` / `has-` / `toast-`）を持つものは、style.css に対応する宣言が無ければならない。**付けているのに宣言が無い＝意味論だけが存在して見た目が伴わない**状態で、コードは「種別で描き分けている」ように見えるのに実際は全部同じに描かれる。実測 2 例: **aria-invalid の宣言が皆無で不正な入力欄が見分けられなかった**（#1160）/ **`.alert` 系が履歴上一度も存在せず、21 箇所が選び分ける通知種別が全て同一描画**（#1166）。**cosmetic utility（absolute/relative 等 ~30）は対象外** —— §7 に「オーナー裁可で現状維持」と記録済みのため、接頭辞を意味系に限定して境界を機械化。テンプレートリテラルは静的に解決できないのでe2e が担当。非 vacuity: `.alert-error` の宣言を削ると RED（実測） | BLOCKING |
| 434 | **verify が「不完全な視界」で緑にならない保証**。Check 108 / 122 / 37 は `git ls-files` を真値にするため、**未追跡ファイルは走査対象にすら入らない**。新規ファイルを add せずにローカル verify を回すと、そのファイルの invariant を**一つも検査しないまま緑**になり、CI（clean checkout）で初めて赤くなる。**実際に踏んだ**（2026-08-20 #1169: 新規 spec の mirror doc 欠落を Check 108 が CI でだけ検出）。mirror/bijection 系が統治するディレクトリ（`js/` `e2e/` `.github/scripts/` `docs/`）の未追跡を BLOCKING で止める。検査対象そのものではなく **verify の結果が信用できる状態かの前提条件**。使い捨て probe を置いたまま回すのも同じ理由で止まる。非 vacuity: `js/` に未追跡ファイルを 1 つ置くと RED（実測） | BLOCKING |
| 435 | **quiz の模範解答フォームが作る mailto URL が実行可能な長さに収まる**。日本語は percent-encode で 1 文字 9 バイトになり、URL は文字数の見た目より遥かに速く伸びる。Windows の mailto 実行は約 2,048 文字で切られ、**本文が黙って切られるかメールソフトが開かない** silent failure になる。上限自体は #1082 で実測して決めたが **その結果を守る層が無かった**。実測 (2026-08-20): 最長タイトル「品質・プロセス問題集」で **2,027 文字 = 余裕 21 文字**。タイトルを少し変えるか maxlength を上げるだけで silent に超える。4 種すべての最悪ケース (全 free-text を日本語で埋める) を再構成して検証する。**435b** は本 Check 自身の走査対象を守る: 435 は `js/quiz-renderer.js` を決め打ちするため、**新しい mailto 経路が増えても長さ検証されない** (Check 124/411/434b と同じ scope-drift)。mailto を組む shipped JS の集合を導出し既知集合との一致を強制する (ContactPage は subject/body が固定文で構造的に上限内ゆえ長さ計算の対象外だが集合には含める)。3 つ目が現れたら「可変長か」を明示的に判断させる | BLOCKING |
| 436 | **規範層 (`docs/architecture/`) に「オーナー裁可待ち」型の defer 理由を残さない**。canon (AI2AI.md STEP 3 / CLAUDE.md §7) は 2026-08-18 に「**オーナー裁可が要る項目なんか一切無い**」「C5 は『人間がコードを書かない』の意であって設計判断を defer する根拠ではない」「**「裁可待ち」という作業カテゴリは存在しない**」と明記した。しかし **canon を直しても、その canon を根拠に書かれた下流の規範文書は自動では直らない**。実際 2026-08-20 の棚卸しで `research-application-policy.md` が「適用条件: オーナーが配色変更を裁可した時」を現行ガイダンスとして保持しており、しかもその項目は既に #1158 で解決済みだった —— 読み手は**否定された規則を持ち帰る**。scope を規範層に限るのは、`docs/incident-artifacts/` が**性質上の歴史記録**で、そこへ注記を強制すると履歴を濁す圧力になるため (#977 の「書き換えれば履歴を偽る」判断と同じ線引き)。超越を明示する行 (SUPERSEDED / 否定された / 解決済み 等) は違反にしない | BLOCKING |
| 437 | **install の step timeout が単一ソースから引かれ、mirror doc とも一致する**。behavior gate の install step には `timeout-minutes` があり、赤の帰属 step は「何分待って中断したか」を読み手に伝える。2026-08-20 に**帰属メッセージだけ「8 分」のまま drift**していた（実際は 11）—— **赤の誤診を防ぐために作った層が誤った数値を出す**のは、読まれる場面（CI が赤いとき）を考えると特に害が大きい。値を job env `INSTALL_TIMEOUT_MIN` に一本化したので message と timeout は構造的に drift しない。残る面 = mirror doc の散文を本 Check が縛る | BLOCKING |
| 438 | **葉モジュールの docstring が宣言する export ⟺ 実際の return**。各 `js/*.js` 葉は docstring に `createXxx({ deps }) -> { a, b }` と自分の形を書いており、**次に読む AI が「この factory は何を返すか」を知る一次情報**になっている。だが誰も検証していなかった。実測 (2026-08-20・`js/settings-io.js` 抽出直後): settings-io の `lossParts` と settings-page の `getImportOptions` が **docstring に無いまま返されて**いた —— **drift はそれを作った増分の中で発生する**。宣言されていないメンバーは「これは再利用してよいのか」を判断する人から見えず、**散文は誰も読まないので放置され続ける**。宣言集合と factory 末尾の `return { … }` の一致を強制する | BLOCKING |
| 439 | **e2e の走査ルート一覧が実在ルートへ解決する**。a11y 等の「全ルートを舐める」gate は `const *ROUTES* = ['#/x', ...]` のリテラル一覧を持つが、存在しないハッシュが混ざるとその entry は NotFound へ解決し、**走っているように見えて実質何も検査しない**。実測 (2026-08-21・#1231): `LABEL_IN_NAME_ROUTES` の `'/#/apps/settings'` は router の apps whitelist (task/todo/pomodoro/ai/notes) に無く NotFound へ落ちており、**本物の Settings (操作要素 82 個・当該ルールの検査対象 22 個) が一度も走査されていなかった** (#96-99 の vacuous-hash class の a11y 版)。router の whitelist と非 app `case '<name>':` を単一ソースに parse し、e2e の `*ROUTES*` 定数中の **ハッシュ形式リテラル**がすべて解決することを強制する。route 名 (`'home'` / `'app-task'`) を並べる 定数は別形式ゆえ対象外、`'#/not-found'` は正当な到達先として許可。BLOCKING。 |
| 440 | **コード側から `docs/` へのパス参照が解決する**。shipped JS / e2e / `playwright.config.cjs` のコメントには「詳細はこの doc を読め」という**読者の導線**が書かれている（例: `docs/files/playwright.config.cjs.md` の落とし穴表）。リネームや移動で参照が腐ると**読者はどこにも辿り着けない**が、コメントなので lint も test も気付かない。Check 419 は `docs/files/**` の frontmatter だけを見ており **コード側は射程外**だった。実測（2026-08-21・#1250）: 対象 7 参照すべて解決＝誤検出ゼロ。prose 全般（#977 で「Check にしない」と判断済み・歴史的記述を壊す圧力になる）とは違い、**コードコメントの導線は常に「今そこにある doc」を指すべき**なので機械強制する。走査先が空になった場合も RED（vacuous 化の防止）。BLOCKING。 |
| 441 | **独自ライセンス `LICENSES/ACD-1.0.txt` (Autonomous Commons Dedication 1.0) の構造整合と配線**。本 repo は 2026-08-23 に独自ライセンスへ移行し、SPDX License List / OSI License Review への提出を計画している。SPDX の inclusion principles は「テキストが確定していること」「収録後に steward が改変しないこと」を definitive requirement に置くため、**本文は壊れてはならない成果物**である。しかも**ライセンス本文は CI のどの層にも読まれない** —— behavior e2e もサイトも lint も consistency の他 Check も触らないので、壊れても全部緑のまま提出まで到達しうる。動機となった実測（2026-08-23 起草中）: 条項を 1 つ挿入しただけで (a) §15.3 が重複し (b) §12.5 の相互参照が別条項を指し (c) §9.4 に `You must not say` という義務語が混入した —— **3 件とも目視では見落とし、機械検査だけが捕捉した**。5 面を BLOCKING で強制: 441a 条項番号の重複なし・節内連番 / 441b `Section N.M` 相互参照がすべて実在条項へ解決 / 441c §1 の定義語がすべて本文で使用されている（未使用の定義は起草途中の残骸か削除条項の痕跡）/ 441d **利用者への義務語（`You must` 等）が無い**（§10.1 が「利用者に一切の条件を課さない」と宣言しており、義務語の混入は**このライセンスの中核主張そのものを偽にする**ので他のどの drift より重い）/ 441e `LICENSE` が SPDX 識別子と全文 path を配線している（**存在 ≠ 配線** —— 全文があっても LICENSE が指していなければ受領者はどの条項に従うか判定できない）。非 vacuity は 5 面すべて個別に破って RED を実測済み。BLOCKING。 |
| 442 | **binary metadata が「書かれている」だけでなく「読める」こと**。442a: MP3 の ID3v2.4 frame walk が padding か tag 末尾まで完走する（malformed frame が無い）/ 442b: Check 91 が同期を強制する `AIO:MetadataLastModified` が **walk で到達可能な位置**にある / 442c: WebP の RIFF chunk walk が EOF ちょうどで完走する。**動機（2026-08-23 実測）**: `COMM` frame（offset 6540）の size 欄が実データより 3 バイト大きく、ID3v2.4 の frame は連結されているため **offset 8946 以降の 5 frame が標準リーダーから一切読めなかった** —— `AIO:Organization` / `OrganizationURL` / `OrganizationRole` / `OrganizationStartDate` / **`MetadataLastModified`**。つまり PR #49 の「Organization 情報を cross-surface に反映」は **MP3 面では一度も届いておらず**、しかも **Check 91 が「必ず存在せよ」と BLOCKING 強制している当の field が誰にも読めない位置にあった**。Check 91 は raw byte 検索なので walk の破綻を見抜けず、**「宣言はあるが実態が伴わない」class（#929 幻セレクタ / #930 route 追従 JSON-LD）の binary 面**として素通りしていた。修復で読める frame は 33 → 38。442c は同 increment で WebP の XMP chunk を resize したため、その構造健全性を対で守る。非 vacuity は **実在した欠陥そのもの**（COMM size を過大値へ戻す / RIFF size を 1 ずらす）で 3 面とも RED を実測済み。BLOCKING。 |
| 443 | **advisory 予算は hard ceiling より厳密に小さいこと**。advisory の存在意義は「BLOCKING を踏む前に気付かせる」ことなので、予算が hard ceiling (Check 365 の 1,000 行) 以上だと**その file の早期警告は構造的に一度も出ない** —— OK からいきなり BLOCKING へ飛ぶ。本リポジトリは「advisory は BLOCKING を踏む前に効かせる」を標準規律にしているのに、**その規律が効かない file が実在した**。実測（2026-08-23）: 5 file が advisory = 1,000 = hard ceiling に設定されており、うち `mutation_samples_archive.py` は **999 行（BLOCKING まで 1 行）**、`mutation_samples_e2e_archive2.py` は **971 行**で、どちらも一度も警告が出ていなかった。しかも §2 表の説明文が「ceiling は Check 365 に整合させ 1,000 とする」と**欠陥そのものを設計として記述**していた。さらに `check_repository_consistency.py` は分割完遂前（15,913 行時代）の予算 4,750 が残り、実測 869 行に対し 6 倍で永久に鳴らない状態だった。**射程は Check 365 と同じ除外集合から導出する**（main.js / style.css / index.html のような hard ceiling 対象外 file に 1,000 超の予算を置くのは正当なので、決め打ちすると正しい設定を RED にする —— 初版で実際に誤検出した）。非 vacuity は実在した設定の再現で RED を実測済。BLOCKING。 |
| 444 | **ライセンス宣言の cross-surface coherence**。444a `<link rel="license">` が index.html にあり実 file へ解決する / 444b JSON-LD の `license` が同一 URL を指す / 444c aio-manifest の top-level `license` の spdx_id と url が整合する / 444d sitemap に全文の `<loc>` がある / 444e llms.txt と llms-full.txt が SPDX 識別子に言及する。**動機（2026-08-23 実測）**: 独自ライセンス ACD-1.0 へ移行した時点で、**機械可読な 7 面すべてに宣言がゼロ**だった —— 「この著作物を学習に使ってよいか」に機械可読な答えが存在しなかった。単なる登録漏れではなく、**ACD-1.0 §6.5 自身が「自動化システムが判定できない許諾は、学習されるための著作物にとっては許諾ではない」と述べている**ので、本文が発見できない状態は**ライセンスが自分の主張を満たしていない**ことを意味する。AIO を最優先の賭け金に据えたリポジトリで、最も痛い形の「宣言はあるが実態が伴わない」（#929 / #930 / archive #14 と同じ class）。canonical URL と SPDX 識別子は **LICENSE の `Full text:` 行と `SPDX-License-Identifier:` 行から導出**する（決め打ちすると path を変えたとき Check だけが古い場所を指す）。非 vacuity は 5 面それぞれを個別に破って RED を実測済。BLOCKING。 |
| 445 | **SPDX 提出用 XML がライセンス本文と同期していること**。445a 本文から再生成した結果と byte 一致する / 445b well-formed かつ `licenseId` が LICENSE の SPDX 識別子と一致する / 445c 本文の全条項番号が XML に現れる。**動機**: SPDX License List への収録が受理されると提出者は **XML とテストテキストの作成**を求められるが、手書きすると**本文を改善するたび silent に古くなる** —— しかも XML は普段誰も読まないので drift に気付く経路が無い。`LICENSES/ACD-1.0.txt` を単一ソースとして `generate_spdx_license_xml.py` が導出し、本 Check が regenerate-compare で検証する（STATUS.md に対する Check 121 と同じ設計）。**非 vacuity の要点**: 手編集は 445a が先に吸収するので、445b / 445c は**生成器そのものを壊して**独立に RED を実測した（licenseId をリテラルへ / 段落分割が §12 を落とす）—— この 2 面の存在意義は「生成器が壊れたとき」にある。BLOCKING。 |
| 446 | **実行可能な WebMCP ツール ⟺ discovery 層の宣言（双方向）**。shipped JS が `navigator.modelContext.registerTool({name: ...})` で登録する**実行可能な**ツールと、`.well-known/mcp.json` の `runtime: webmcp` entry が一致すること。**446a 登録 ⟹ 宣言**（今日の欠落を捕捉する方向）/ **446b 宣言 ⟹ 登録**（幽霊ツール防止）/ **capabilities.tools が実態と一致**。**動機（2026-08-23 実測）**: サイトは実行可能な WebMCP ツールを 1 つ登録しているのに、**AIO 公開面のどこにも宣言が無かった** —— 記載は開発者向け doc だけ。しかも mcp.json は `capabilities.tools = false` と宣言しており、**「ツールは無い」と言いながら 1 つ動いている**状態だった（書かれた当時は正確で、ツール追加時に更新されなかった）。本セッションで繰り返し掘った「宣言と実態の乖離」の**逆向き** —— 届いているのに宣言が無い。**登録されているのに宣言が無いと、静的 discovery しかしない agent はそのツールの存在を知りようがない**。ツール名は **main.js から導出**する（決め打ちすると rename 時に Check だけが古い名前を持つ）。非 vacuity は 4 通り（宣言削除 / 幽霊ツール追加 / capabilities を false へ / main.js で rename）で RED を実測済 —— うち 2 件は**実在した状態の再現**。BLOCKING。 |
| 447 | **制約を「列挙する」機械可読面が正典の C1–C7 名を使う**。`.well-known/mcp.json` の `audit_architecture_constraints` prompt は**エージェントが展開して「このコードは制約に準拠しているか」を評価する**テンプレートなので、名前が正典とずれていると**存在しない制約を監査する**。**実測（2026-08-23）**: 「C1–C7: Vanilla JS / IIFE / ErrorBoundary / External Framework Independent / App Logic External Library Independent 等」と書かれており、後ろ 2 つは**正典に存在しない名前**、しかも **C5（Human Writes Zero Code）/ C6（AIO Integrity）/ C7（KARTE CDN SRI Non-Application）—— このリポジトリを最も特徴づける 3 つが完全に欠落**していた。原因は履歴に残っている ——「mcp.json audit_architecture_constraints description updated C1–C6 → C1–C7」で、**範囲の表記だけ更新して列挙の中身を更新しなかった**。**射程は「列挙を名乗る面」に限る**: `llms-full.txt` の「C1–C7 に違反する構文を拒否せよ」のような**参照**や、index.html / README.md の `Architecture-Keywords:`（アーキテクチャの説明であって制約の列挙ではない）は正当なので対象外 —— 広げると**正しい記述を RED にする**（実測で参照 3 箇所の誤検出ゼロを確認）。正典名は AI2AI.md の C1–C7 表から**導出**する。非 vacuity は実在した状態の再現と部分 drift（1 名だけ欠落）の両方で RED を実測済。BLOCKING。 |
| 448 | **Agent Skills Discovery 仕様への適合**。`.well-known/agent-skills/index.json` は宣言した `$schema` （schemas.agentskills.io/discovery/0.2.0）に適合していなければ、**その schema で検証する agent は index ごと拒否する** —— 公開して robots.txt で Allow しているのに使われない状態になる。仕様は各 skill entry に **name / type / description / url / digest の 5 つすべてを必須**と定める。**実測（2026-08-23・仕様原文で確認）**: **`description` が全 entry で欠落**し、digest 形式も仕様の `sha256:{hex}` ではなく `sha-256:{hex}`（ハイフン付き）だった。とくに description の欠落が痛いのは、仕様の設計が**「agent は起動時に name と description だけを読んで関連性を判断し、一致したときに初めて本体を取得する」progressive disclosure** だから —— description が無いと agent は中身を取ってみるまで用途が判らない。必須 field 名は本 Check にリテラルで持つ（**外部仕様なので repo から導出できない**）が、**仕様が変わったら本 Check も同一 commit で更新する**契約とする。非 vacuity は description 削除・仕様外 type・旧 digest 形式の 3 通りで RED を実測済（うち 2 件は**実在した状態の再現**）。BLOCKING。 |
| 449 | **`.well-known/api-catalog` の RFC 9727 関係型セマンティクス**。RFC 9727 §2 はカタログの**メンバーを `item`**（RFC 6573）で列挙すると定め、カタログ内部の `api-catalog` 関係は「**別の API カタログへの入れ子**」を意味すると明記している。**実測（2026-08-23・RFC 原文で確認）**: メンバー **7 件すべてが `api-catalog` 関係**で列挙されていた —— `llms-full.txt` / `Claude2Claude.md` / evidence 文書などカタログでないリソースを「これらは全てカタログだ」と偽って宣言している状態で、仕様に従う agent は**それらを linkset として parse しようとして失敗する**。mirror doc は「RFC 9727 準拠 / linkset 形式の厳格な遵守」と述べており**意図的な逸脱ではなく誤り**だと確定した。**なぜ長く残ったか**: Check 165 は JSON 妥当性と anchor origin しか見ず、関係型を検証する層が存在しなかった —— 視覚に一切出ない機械可読面なので screenshot にも behavior e2e にも現れない。449a は `api-catalog` 配下を実際のカタログ URL に限る（**正当な入れ子は許可**）、449b は `service-desc` / `service-meta` / `service-doc` / `status` を link object の attribute に混ぜない（RFC 9264 でこれらは**関係型**であり、対象リソースを `anchor` とする別 context として表現する）、449c は全 link object の文字列 `href` を要求する。非 vacuity は 3 部それぞれを単独で壊して RED を実測済。BLOCKING。 |
| 421 | shipped JS (`js/**/*.js` ∪ `main.js`) で **スクロールの `behavior` を `'smooth'` と明示する file は、同じ file で `prefers-reduced-motion` を問い合わせる**ことを強制。CSSOM-View では **`behavior` を明示した時点で CSS の `scroll-behavior` は参照されない**ため、style.css の `@media (prefers-reduced-motion: reduce) { scroll-behavior: auto !important }` は `scrollIntoView({behavior:'smooth'})` / `scrollTo({behavior:'smooth'})` には**効かない**。実測 2026-08-11 (#993): home の「ケースを見る →」ボタンが reduce 環境でも no-preference と**同一のアニメーション曲線**（t0=0 → t150≈475 → t600≈1075）で 1,000px 超をスクロールしていた —— 前庭障害のユーザーに影響する WCAG 2.3.3 の欠陥だが、**fatal も視覚差分も出ない**ので behavior test 以外に捕捉層が無い。誤解を強めていたのは、**CSS 側の reduce override 自体は正しく働いていた**こと（同じ実測で `window.scrollTo(0, 0)` は reduce のとき即時に完了した）。効かないのは behavior を明示した呼び出しだけである。検出は「file 内に `'smooth'` リテラルがあるか」で広く取る —— `behavior:` の直後だけを見る形にすると、正しく直した後の三項 `behavior: reduce ? 'auto' : 'smooth'` を見落として **vacuous PASS** する（自分の修正を守れない Check になる）。走査前にコメントを除去し、説明文が違反にも充足にもならないようにしてある（Check 112 が「コメント中の語で file 単位判定が GREEN 化する」で踏んだ失敗の回避）。非 vacuity: home-page.js から matchMedia 行を外すと RED | BLOCKING |
| 420 | **`E2E_MUTATIONS` の `find` が対象 file 内で一意**であることを強制。`mutation_probe` は `src.replace(find, replace, 1)` ＝ **先頭 1 件しか置換しない**ため、find が複数箇所にマッチすると**テストが通る経路とは別の場所が壊れ**、mutation は silent に SURVIVED する。これは「安全網に穴がある」のではなく「**穴を探す道具が的を外している**」状態で、probe の結論（All caught）が嘘になる。実例 2026-08-10: `Router.navigate(Router.getLastListPath ? … : 'projects')` は project-detail-page.js に **2 箇所**（not-found 分岐の「一覧へ戻る」と詳細本体の「← 一覧に戻る」）あり、mutation は前者だけを壊してテストは通り続けた — **週次 mutation-probe が SURVIVED として検出**（PR CI では走らないので、STATUS.md にバッジを出した #964 が無ければ誰も気付かなかった）。導入直後に 3 件目（h2 見出しが課題/アプローチ/アーキテクチャの 3 箇所）も検出し一意化した。**consistency 側 (`MUTATIONS`) は対象外** — あちらの Check は file 全体を走査するのでどの出現を壊しても捕捉され、非一意でも害が無い（sitemap.xml / index.html には正当な非一意 anchor が複数ある）。behavior 側だけが「特定の実行経路に当てる」性質を持つ。非 vacuity: 一意でない find を持つ mutation を 1 件足すと RED | BLOCKING |
| 396 | `js/router.js` `_parseRoute` が emit する全 route.name（literal `route.name = 'X'` + 初期 `{ name: 'home' }` default + `app-${app}` を app whitelist から展開）が `js/page-meta.js` の `PAGE_META` にエントリを持つことを BLOCKING 強制。`meta-management.js` `applyMeta` は `PAGE_META[routeName]` が無いルートで**早期 return** するため、登録漏れのルートは `<title>` 更新 / SEO meta（desc/og）/ route announcer title（**a11y 2.4.2**）を **silent に欠落**する（throw も console error も無い・behavior e2e は描画は見るが title/meta announcer は検査せず・screenshot advisory）。Check 377（route.name ⟹ main.js render case）の **consumer 面の双子** + Check 148（ARTICLE_ROUTES ⊆ PAGE_META）の補完で route mesh の meta 面を閉じる。新ルート（新 `app-*` subroute 含む）を PAGE_META 登録なしで追加すると title/meta 無しページを出荷する前に build を落とす。Check 375/376/377/391/392/393/395 と同じ used⟹defined wiring レンズの PAGE_META 面 | BLOCKING |
| 395 | shipped JS（main.js + js/*.js）の全 literal `Router.navigate('X')` の base path segment（X から `?query` を除き最初の `/` まで）が `js/router.js` `_parseRoute` の扱う top-level route segment（`case 'seg':` label、または空文字=home default）に解決することを BLOCKING 強制。router.js は未知の第1 segment を `name:'home'` として parse しホームを描画するため、typo した nav target（`Router.navigate('rolesplit')`・リネーム後の stale target）はユーザーを意図先でなく **silent にホームへ誤誘導**する（throw も console error も e2e 失敗も無い。behavior e2e は各ルートを直接訪問時の描画は見るが in-page nav ボタンが live ルートを指すかは検査せず・screenshot は advisory）。Check 377（route.name ⟹ main.js render case）の **producer 面の双子**（377 は consumer=parse 済ルートに renderer が在る、395 は producer=nav 呼び出しが parse 可能ルートを指す）。template literal target（`apps/${app}` / `projects/${slug}`）は構造上動的ゆえ除外し no-`$` の文字列 literal のみ強制、JS 行コメントは strip して doc 例の false-positive を防ぐ。Check 375（icon）/376（data-action）/391（getElementById）/392（aria idref）/393（CONSTANTS）と同じ used⟹defined wiring レンズの navigate 面（target ⊆ router-handled segments） **検出は 2 記法**: literal だけを見る旧実装は、本リポジトリでより一般的な**データ駆動記法**（`Router.navigate(<識別子>)` を呼ぶ file が nav 先をデータ配列の `path: '<route>'` で持つ — js/components.js のサイドバーと js/hiring-risk-page.js の CTA カードで、強制対象 19 種のうち 12 種）を見逃しており、そこでの typo（`path: 'setting'`）は GREEN のまま利用者を silent にホームへ送っていた。実測: `path: 'settings'` → `path: 'setting'` で本 Check が RED。Check 112 / 130 / 375 / 376 / 393 / 402 と同じ「静的 Check が 1 綴りしか見ていない」class。palette の宛先は `hash:` 記法で Check 128 / 382 の router bijection が既に被覆 | BLOCKING |
| 392 | 静的リテラルの a11y id 参照（`aria-labelledby` / `aria-describedby` / `aria-controls` / `aria-errormessage` + `<label for>`）が定義済み id に解決することを BLOCKING 強制。dangling な aria idref は Check 391（getElementById）より高 severity で、accessible name/description/control の関連付けが assistive tech 上で切れる実 **WCAG 1.3.1/4.1.2 欠陥**（screen reader が label 無しの control をアナウンス）。id を片方でリネームすると visual 無変化・behavior e2e も素通りで silent（#563 a11y 属性 leak / #728 label 関連付けと同 class）。Check 391 の DOM-id used⟹defined wiring の a11y-idref 面。動的 idref（`aria-activedescendant='cmdk-opt-'+i` template literal / setAttribute）は self-consistent ゆえ静的リテラルのみ強制、`for` は JS キーワード衝突を単一行 `[ \t]*` colon 固定で排除 | BLOCKING |
| 391 | shipped JS の全 `getElementById('X')` リテラル target が定義済み id（index.html の `id="X"` / shipped-JS の `id: 'X'` h() prop / 動的 `el.id = 'X'`・`setAttribute('id','X')`）に解決することを BLOCKING 強制。id を index.html や別モジュールでリネームしたのに `getElementById('old')` を残すと DOM lookup が null を返し button/feature が **silent no-op** 化する（behavior e2e は content 描画のみ・screenshot は advisory ゆえ全 gate 素通り。#257 palette NAV 欠落 / #262 topbar 二重発火と同じ wiring 系 discoverability 破壊 class）。Check 375（icon）/376（data-action）/377（route→case）の used⟹defined wiring レンズの DOM-id 面（target ⊆ defined）。逆方向（定義 ⟹ 使用）は非強制＝index.html には JS 未参照の a11y/構造 id が多数あり正当ゆえ used⟹defined ガードで bijection ではない | BLOCKING |
| 375 | shipped JS の全 literal icon 名（直接 `createIcon('X')` 呼び出し + `createIcon(item.icon)`/`createIcon(app.icon)` へ流れる data field `icon: 'X'`）が `js/ui-components.js` の `getIcons()` registry key に解決することを BLOCKING 強制。`createIcon(name)` は未定義 name で silent に空 text node を返す（throw も console error も e2e 失敗も無い）ため、typo した icon 名は icon-only ボタン（削除/閉じる/メニュー）のアイコンを不可視化するのに verify も behavior e2e も advisory screenshot も素通りする。Check 133/134 の「file 存在 ⟹ 配線済」silent-wiring class を icon 使用面「icon 名が使われる ⟹ 実際に定義済」へ適用。getIcons() body を brace-parse し registry key を、shipped JS 全体から createIcon へ届く literal 名を集めて突合する | BLOCKING。**第 1 引数式の全体**（top-level ',' まで）を見て三項の**両枝**を検証する（条件部＝top-level `?` より前のリテラルは除外し `state.theme === 'dark' ?` を icon 名と誤認しない）。初版は `createIcon(` 直後の単一リテラルしか見ておらず、**三項の片枝 typo を見逃していた**（実測: `createIcon(open ? 'chevronUp' : 'chevronDwn', 16)` を注入しても GREEN ＝ トグルの片状態だけアイコンが消える silent broken-icon が素通り）。コメント除去 + root の `main.js` も走査。**375b = 逆方向（定義 ⟹ 使用）**: registry に足したまま一度も使われないアイコンは**全ユーザーへ配信される dead weight**。導入時の実測で **50 key 中 16 件（1,996 bytes）が never-wired**（`git log -S "createIcon('<name>')"` が各々ゼロ commit ＝ lost-wiring でなく初版からの残骸）で、Check 120 の byte 予算を無言で圧迫していた。先行定義が必要になったら Check を落とさず、使用箇所を同 PR で足すか意図を明記して carve-out する | BLOCKING |
| 374 | `js/settings-page.js` の `importJSON` が外部 JSON を生のまま `State.update` で adopt せず、マージ結果を `Store.validateAndNormalize` してから単一 `State.set` で commit する (normalize-before-adopt) ことを BLOCKING 強制。生を State.update で commit すると notify→render() が正規化前の生データ (strict モードの `merged.projects = parsed.projects` 生代入が malformed entry を SettingsPage の p.name/p.id dereference へ通し crash させうる) を描画する。従来は render-abort ordering (State.set の 2 度目 render が 1 度目の生 render を SettingsPage 到達前に abort) で偶発的に守られていたが、data-safety を描画順に依存させず restoreSnapshot と同じ「外部 ingestion は adopt 前に正規化」(#295/#561) invariant へ整合させる。importJSON 関数本体を brace-parse し `State.update(` を含まず validateAndNormalize を通すことを強制 (Check 130 の oninput no-State.update と同型の ingestion 版) | BLOCKING |

### カテゴリ F — 自己統治・テスト健全性・保守ガバナンス（self-governance / test health / maintainability）

チェックスクリプト自身の自己整合、E2E テストの構造健全性、保守性アンカー文書の存在、肥大化予算など、リポジトリの自己統治機構を守る。

| # | 検査内容（要約） | 級 |
|---|---|---|
| 16 | `e2e/portfolio.spec.js` の screenshot テストに baseline-skip ガードがある | BLOCKING |
| 28 | `e2e/portfolio.spec.js` に `test()` のネストが無い | BLOCKING |
| 29 | Playwright baseline 生成リンク健全（snapshot workflow ↔ spec env signal） | BLOCKING |
| 30 | v80+ 保守性アンカー文書が存在（repository-maintainability-map / main-js-extraction-map） | BLOCKING |
| 42 | docs/ アーティファクト配置・命名衛生（42a: incident-artifacts 直下の命名規約 / 42b: decision・improvement-notes の配置） | BLOCKING |
| 43 | main.js AIDK Isolated Kernel の構造健全性（43a: ヘッダマーカー / 43b: startViewTransition proxy / 43c: Trusted Types default policy / 43d: 単一トップレベル IIFE） | BLOCKING |
| 45 | 本チェックファイルの docstring インベントリ ↔ コードセクション見出しの自己整合（45a/45b/45c） | BLOCKING |
| 52 | file-size budget advisory（BUDGET-DATA ブロックと現行行数の照合・main.js は strong-advisory） | **ADVISORY** |

---

## 2.9 意図的に Check にしない面（brittle gate を足さないための記録）

### docstring の行数記述 / Check 文言が名指しする js file（2026-08-20 実測・非強制）

**動機となった実バグはある**: `js/settings-io.js` を抽出したとき Check 374 の走査先だけを
新 file へ移し、**docstring・section header・失敗メッセージは `settings-page.js` を名乗った
まま**残った。失敗メッセージは「問題の場所を探しているまさにその時」に読まれるので、
古い file 名は**もうコードが無い file** へ読み手を送る。これは per-instance で是正した。

**だが機械化はしない。** 実際に Check を書いて走らせたところ、3 module が RED になり
**すべて正当な参照**だった:

| module | 名指し | 実態 |
| :-- | :-- | :-- |
| `checks_app_route.py` | `apps.js` | 「Settings の手動追加フォーム (js/apps.js SettingsPage)」= 抽出前の**歴史的経緯**の説明 |
| `checks_canon_config.py` | `ai-page.js` | 「実測: ai-page.js の実ハンドラを書き換えても…」= **実測例**の言及 |
| `checks_behavioral.py` | `ai-page.js` / `components.js` | 同上（cross-reference と実測例） |

「自分が読む file」と「文脈として挙げる file」の区別は**意味の判断**で、正規表現では
分けられない。Check 化すると **正当な cross-reference や歴史的経緯を消す圧力**が生まれ、
説明が痩せて実態より悪くなる（§2.9 冒頭の #977 と同じ結論）。

**docstring の行数記述も同様**: 30 葉を走査すると「抽出時 1,370 行だった」「837→461 行へ
縮小」等が大量に引っかかるが、**いずれも過去の記録として正しい**。現在値の主張は
`file-size-budget.md` §2 表が単一ソースで、そちらは Check 424 が `wc -l` 一致を強制している。

**代わりに何が守るか**: Check を retarget するときは走査先・docstring・section header・
失敗メッセージを**同じ commit で**直す、という規律（Check 45 が docstring↔section の
bijection を、Check 70/105 が総数と map の同期を強制するので、番号面の drift は機械が止める）。


新しい Check を足す前に読むこと。**「機械的に検査できる」と「機械的に強制すべき」は別**で、
ここに挙げた面は実測したうえで **Check にしないと決めた**。強制すると正しい記述を壊す。

### prose 中のパス参照（インラインコードで書かれた `path/to/file.ext`）

2026-08-10 に全 md 333 ファイルを走査し、パス様のインラインコード **494 箇所**のうち
**56 箇所が解決しない**ことを実測した。だが**その大半は正しい記述**である:

| 内訳 | 例 | なぜ正しいか |
| :-- | :-- | :-- |
| **歴史記述** | `js/quiz-data.js`（10 箇所） | Stage 3 で 1 ファイルへ抽出し Stage 3-b で 4 分割した**経緯**の記述。現存しないのは分割したからで、書き換えると**履歴を偽る** |
| **仮定の例示** | `lib/version_checks.py` / `lib/aio_checks.py` | 「将来 `lib/` 分割を行う場合の候補（例: …）」という**意図的に存在しない**名前 |
| **アーカイブ** | `docs/session-records/*` からの旧 incident-artifact 参照 | アーカイブは当時の状態を保存するもの |
| **プレースホルダ** | `docs/files/...md.md` | `...` は説明用の省略記号でパスではない |

**Check 化すると**、これらを「直す」圧力が生まれ、履歴の書き換え・例示の削除という
**実態より悪い状態**を招く。逆に例外リストで守ろうとすると、リストの保守コストが
守る価値を上回る（除外条件が「歴史か否か」という**意味の判断**で、機械には決められない）。

**では何を強制するか**: `docs/files/**/*.md` の frontmatter `canonical-ref:`（**Check 419**）。
こちらは「この file を理解するには次を読め」という**現在形の導線**しか書かれない面で、
歴史も仮定も混ざらないため、解決性を機械強制しても正しい記述を壊さない。

同じ走査で見つかった **1 件の genuine な不正確さ**（Check 5 の説明が
`.well-known/agent-skills/index.json` を `agent-skills/index.json` と prefix 落ちで書いていた）は
本 increment で修正済み。**「検査できる面すべてを gate にする」のではなく、
gate にして安全な面だけを選ぶ**のがこのリポジトリの方針。

## 3. 級別サマリ（BLOCKING / ADVISORY）

| 級 | チェック数 | 該当 |
|---|---:|---|
| BLOCKING（失敗で exit 1） | 114 | Check 52/60 を除く全カテゴリのチェック（Check 53〜59, 61〜118 を含む。サブチェックを持つものは全サブが BLOCKING） |
| ADVISORY/WARNING（exit に影響しない） | 4 | Check 34（honest dating）・Check 36（future-dated lastmod）・Check 52（file-size budget）・Check 60（ESLint warning baseline） |

注: Check 34/36 は元から WARNING 級として設計されている（公開面の軽微な日付ずれを観測するが、ビルドを止めない）。Check 52 は quiz-domain-split + bloat-governance increment で追加した advisory（肥大化予算は CI を止めるべきでないため）。Check 53（modulepreload 解決）・Check 54（eslint↔@eslint/js メジャー一致）は console-fix increment で追加（ともに BLOCKING）。Check 55（CI lint-target 整合）・Check 56（factory-pattern 引数被覆）は v80+ Stage 5-k CI hygiene fix で追加（Stage 5-j 隠れバグ再発防止・ともに BLOCKING）。Check 57（modulepreload ↔ _modules47 集合）・58（e2e ALL_ROUTES ↔ main.js switch 集合）・59（file-size-budget §2/§4 集合）・61（factory docstring marker）は post-Stage 5 「CI 更なる手厚化」増分で追加（structural coherence 強制・全 BLOCKING）。Check 60（ESLint warning baseline 回帰）は同 increment で追加した advisory。Check 62（AIO entity canonical_url cross-surface 一致）・63（crawler discovery origin alignment）・64（map table 昇順）・65（Last-Updated ISO-8601 形式）・66（title entity-identifier）・67（workflow permissions 明示）・68（dependabot 二 ecosystem）・69（engines.node ↔ CI pin）・70（runbook §9 cross-reference）・71（BUDGET-DATA path 実在）は post-Stage 5 「CI 更なる手厚化 v2」増分で追加した 10 件の構造整合性 BLOCKING（全て exit に影響）。残るチェックはすべて BLOCKING である。

---

## 4. 将来の `lib/` 分割に向けた指針（本 increment では実行しない）

第一段階として、§1 の純 helper（`read` / `read_bytes` / `extract` / `_csp_sri_hash`）を `.github/scripts/lib/io_utils.py` のような単一モジュールへ切り出す案が最も安全である。これらは Check 番号を持たず副作用も小さいため、Check 45 の自己整合（番号体系）を壊さない。切り出し後は、(a) `python3 .github/scripts/check_repository_consistency.py` をローカルで実行して同一結果になること、(b) GitHub Actions の `architecture-validation.yml` でも同じ import path が解決すること、の両方を確認する。Python の import path は、スクリプトが `.github/scripts/` 配下で実行される前提に依存するため、`sys.path` への追加か相対 import の設計を慎重に行う。

第二段階以降として、カテゴリ単位（A〜F）でのチェック本体の分割が考えられるが、これは Check 45 の前提（docstring インベントリとコードセクションが同一ファイル内で対応する）の再設計を伴うため、helper 抽出が安定し、かつ Check 45 を「複数ファイルにまたがるインベントリ」に対応させる設計が固まってから着手する。本マップは、その判断のための地図である。

### 更新（2026-07-05）— 分割トラック開始（owner 合意 C-first）

上記の前提（Check 45 の複数ファイル対応）は **`CHECK_SOURCE_FILES` list + `_aggregate_check_numbers()`**（#253 で敷いた横断集約 foundation）で既に満たされており、owner との議論タイム合意（`docs/incident-artifacts/decision-v80-phase4-bloat-reduction-1000-line-threshold.md` の Addendum）を受けて **check.py 分割トラックを開始した**。

- **Phase 1 PoC 完了（PR #577）**: maintainability/test-health 系の Check 361-364 を `.github/scripts/checks_maintainability.py` へ **`run(ctx)` 明示 context 注入方式**で抽出（monolith 15,913→15,749 行）。#253 が net-negative とした `exec` 方式を使わず、`ctx` に check/errors/warnings/ROOT を同一オブジェクト参照で渡すことで挙動 byte-equivalent を実証。自己整合 Check 45/70/105 が monolith + module を横断して成立。
- **確立した分割パターン**（Phase 2+ で踏襲）: ①孤立カテゴリ選定（monolith の共有 computed 変数に依存せず ctx 提供物のみ使う Check 群）→ ②`checks_<cat>.py` に `run(ctx)`（section は +4 indent・docstring inventory も移す）→ ③`CHECK_SOURCE_FILES` 登録 → ④`verify` で 45/70/105 緑を確認 → ⑤`docs/files` mirror（Check 108）。
- **不動点**: Check 45 自身（`_aggregate_check_numbers` を呼ぶ集約器）は monolith に残す。main.js/html 等の共有 global を直接読む Check は ctx へその content を足すか module 側で re-read する。

したがって「本 increment は分割を行わず」は Phase 0（#253 準備）時点の記述であり、現在は **段階分割を実行中**（sustained track・一撃分割は依然厳禁）。
