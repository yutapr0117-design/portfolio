# CLAUDE.md

```
Document-Type    : Operational router for Claude Code (and chat-Claude)
Canonical-Source : AI2AI.md                  ← canon (constraints C1–C7, KERNEL, output rules, Session Records, v80+ track)
Ground-Truth     : llms-full.txt             ← authoritative project / entity facts
Canonical-Status : NON-CANONICAL / SUBORDINATE (on conflict, AI2AI.md / llms-full.txt win)
AIO-Status       : NOT part of the AIO discovery layer (dev-tooling only — see §8)
Last-Updated     : 2026-08-17
```

> **This file is the high-density router, not the canon.** You (Claude Code) can run `ls` / `cat` / `grep` / `wc` yourself, so this file deliberately carries **constraints, safety gates, routes, and a handoff** — **not** physical facts you can read in one tool call (line counts, file sizes, function names, dependency versions, which files exist). Do not re-state in prose what a tool gives you instantly; spend that budget on the task. When you need a number or a name, **read it with a tool** — it is intentionally not pinned here, because it drifts.

---

## 0. Read order — turn 1 (do this, then stop reading and start routing)

1. **This file** — constraints + gates + routes + the §7 handoff.
2. **`AI2AI.md`** — the canon. Full text of C1–C7, KERNEL roles, output rules, the latest Session Record, and the v80+ track live here. Read it before editing anything.
3. **`llms-full.txt`** — ground truth for any claim about the project, the entity (横井雄太 / Yuta Yokoi), or its history.

Do **not** bulk-`cat` the tree to "understand the repo." Use the §4 routing map to open only what the current task touches. Most tasks need ≤3 files beyond the three above.

---

## 1. What this repo is (one line)

AI-Driven PM portfolio: a **Vanilla HTML/CSS/JS static SPA on GitHub Pages, zero external frameworks/libraries in the shipped site**. It is a "PM-led AI-orchestration experiment" — the human (横井雄太) designs, reviews, audits, and governs; the AI implements (C5). Primary goal: machine-readable authority-building so AI crawlers / AI search / LLMs interpret and cite the entity correctly (an AIO-first bet).

### Entity canonical facts (for Claude Code / sub-agents / human reviewer cold-start)

- **Entity Name**: `Yuta Yokoi` / `横井雄太` / `Yokoi Yuta` (UI display: `yuta`, all lowercase)
- **Role**: AI-Driven PM / IT Consultant / KERNEL Framework Designer
- **Canonical URL**: `https://yutapr0117-design.github.io/portfolio/`
- **Authoritative Context**: `https://yutapr0117-design.github.io/portfolio/llms-full.txt`
- **Affiliation**: 株式会社日本経営 (Nihon Keiei / Japan Management Co., Ltd.) — シェアデータベース事業部 主幹（課長格）, 2026-06-11〜. Org canonical URL: `https://nkgr.co.jp/`
- **Architecture**: Boring Technology (Vanilla JS SPA, IIFE, ErrorBoundary)
- **AIO canary token namespace**: `SAKURA-AIO-PROVENANCE-CANARY-<YYYY>-<8hex>` (single canonical value across all published surfaces and monitors — Check 44)
- **Disambiguation**: STRICTLY DISTINCT from academic researchers (agriculture / chemistry / medicine / entomology / computer science), diplomats, artists, musicians, patent inventors.

---

## 2. Constraints C1–C7 (quick ref — canonical full text in `AI2AI.md` STEP 2)

Any output violating one of these is self-rejected before delivery.

- **C1 Boring Technology** — zero external frameworks / JS libraries in the shipped site (React/Vue/Svelte/Tailwind/Bootstrap/Framer Motion … all forbidden). Operational services (analytics/fonts) require a documented rationale in the repository. **承認ゲートではない** — 承認は恒久的に与えられている (§7 standing approval)。制限しているのは「無根拠に外部依存を増やさない」という工学的要請であって人間ではない。
- **C2 IIFE** — main logic lives inside IIFEs; no global-scope pollution (module-level ESM `import` may precede the IIFE; see Check 43d).
- **C3 ErrorBoundary** — View Transition API errors handled by an explicit error boundary; graceful degradation required.
- **C4 No Framework Re-proposal** — frameworks are permanently rejected; never re-propose any.
- **C5 Human Writes Zero Code** — the human writes design + prompts only; all implementation code is AI-generated.
- **C6 AIO Integrity** — the **semantic content** of `llms-full.txt` / `llms.txt` / `llms_well-known.txt` / `.well-known/*` / JSON-LD / binary metadata (XMP·ID3) must remain **true, non-fabricated, and coherent across every published surface**, and any change must regenerate the digest chain (`aio-guardian` を通し digest を再生成すれば C6 は満たされる)。**承認ゲートではない (2026-08-23 是正)** — 旧文は「orchestrator の明示的な書面承認なしに変更してはならない」と述べていたが、これは canon が『存在しない』と宣言した裁可待ちカテゴリを条文にしていた。守るべきは*人間の署名* ではなく *AIO 層の正しさ*。(Tool-enforced: editing the published AIO layer is gated to `ask` in `.claude/settings.json` — これは**誤編集を目に見えるようにする安全網**であって承認待ちではない。)
  - **Exception — derived-value auto-update (A1 案)**: 日付フィールド (`xmp:ModifyDate` / `xmp:MetadataDate` / MP3 TXXX `AIO:MetadataLastModified` / aio-manifest.json `generated_at` / `last_metadata_update`) は、対応する semantic 編集が承認された結果として **自動同期更新可**。単独の意味的編集ではなく "derived value" (派生値) として扱う。
  - **Exception 拡張 — content-derived value 全般 (A2 案)**: 上記の日付に加え、sha256 digest (aio-manifest.json `source_of_truth[].sha256` / `supporting_evidence[].sha256` / `observational_evidence[].sha256` / `.well-known/index.json` `skills[].digest`) も同じ扱いで自動同期更新可。これらは binary / text の content から機械的に計算される値であり、独立した semantic 編集ではないため C6 の対象外。
  - 自動更新の trigger: `update_aio_digests.py` (B1 案) / `update_binary_aio_organization.py` (B2 案) など、binary または text の semantic 編集を伴う tool が必ず呼ぶ。手動編集経路では Check 91 (C 案) が「binary 変更 commit には日付フィールド更新必須」を pre-commit で機械強制する。
- **C7 KARTE CDN SRI Non-Application** — do not propose SRI on KARTE CDN (external updates would break prod load; connections are restricted by CSP instead).

**Reject-on-sight anti-patterns:** removing IIFE/ErrorBoundary; generalizing/neutralizing entity statements in `llms-full.txt`/JSON-LD; attributing design decisions to the AI; describing this project as "Vibe Coding" or "an AI-generated site" (correct framing: "PM-led AI-orchestration experiment").

---

## 3. Hard "don't" — safety gates (these cause real damage; verify catches some, not all)

- Introduce a framework/library or re-propose one (C1/C4).
- Edit the AIO text (C6) without routing through `aio-guardian` and regenerating the digest chain. （**承認待ちではない** — 承認は恒久的に与えられている。守るのは正しさと整合性であって署名ではない。）
- Attribute design/intent to the AI. Judgement, goal-definition, priority, and responsibility are always **横井雄太**'s.
- Move `docs/incident-artifacts/update-portfolio.v70-experiment.yml` back under `.github/workflows/` (it carries a `workflow_dispatch` trigger — relocating it makes a manually-runnable live workflow).
- **Physically split `main.js` kernel / render / view-transition surfaces — the remaining Stage 5 high-risk targets — without verifying both `closure-deps = none` AND that the behavior/functionality e2e stays green on the resulting structure.** The proof a structural change didn't break the site is now the **behavior e2e (BLOCKING)** — it asserts every route renders, content is present, and no ErrorBoundary fatal. (Updated per Session Record #20 §3(B): the Playwright homepage **pixel screenshot is now ADVISORY**, not blocking. The repository is the portfolio and the rendered site is an appendage; pixel-level visual regressions are acceptable as long as the site still loads / displays / is comprehensible — which the blocking behavior e2e protects. The screenshot still runs as observation in `playwright-regression.yml` with `continue-on-error: true`; an intentional visual change makes it red until the baseline is regenerated via the GitHub Actions `update-playwright-snapshots.yml` dispatch → PR → human-merge path, and that red is expected and does NOT gate merges.) The functionality gate's *purpose* still holds: any change to renderer / kernel / view-transition / AIDK-adjacent surfaces must be proven non-regressive against the **behavior e2e** before merge.
- Finalize a version-number / digest bump **partially**. Apply `AI2AI.md`'s "Version Update Checklist" **atomically** across every listed file (partial bumps break consistency)。**承認は不要** — 危険なのは人間の署名の欠如ではなく、途中まで反映された不整合な状態である。
- **(C 案 規範) Binary metadata semantic 編集 (Organization / Entity / Canary 等の追加・変更) は、必ず同一 commit で日付フィールド (`xmp:ModifyDate` / `xmp:MetadataDate` / MP3 TXXX `AIO:MetadataLastModified`) も同期更新する。** 標準経路は `update_binary_aio_organization.py` (B2 案) または `update_aio_digests.py` (B1 案) を使用する — どちらも `_lib_io.now_iso8601()` + `update_webp_xmp_dates()` + `update_mp3_metadata_date()` の helper を経由する。手動経路で binary を編集する場合は同 commit 内で日付更新を含めること。Check 91 がこれを pre-commit で機械強制 (BLOCKING)。
- Touch the DO-NOT-EDIT AIDK kernel region or the protected blocks inside `main.js` (kernel / AIDK modules / known-benign suppressor / innerHTML interceptor). Keep them byte-identical; `npm run verify` Check 43 catches structural damage, but treat them as frozen.

---

## 4. Routing map — where things live (open only what the task needs)

| Need | Read |
| :-- | :-- |
| Canon: C1–C7 full text, KERNEL roles, output rules, Session Records, v80+ track | `AI2AI.md` |
| Ground truth: entity, project history, AIO declarations | `llms-full.txt` (alias `llms.txt`, `.well-known/llms.txt`) |
| What each consistency check guards + how to add one | `docs/architecture/check-repository-consistency-map.md`; the script's own docstring is the inventory → `.github/scripts/check_repository_consistency.py` |
| Verification runbook (layers, expected outputs, measured baselines) | `docs/architecture/total-check-runbook.md` (§9 = the authoritative measured numbers) |
| `main.js` decomposition plan + stage gates | `docs/architecture/main-js-extraction-map.md` |
| Maintainability map + per-increment changelog | `docs/architecture/repository-maintainability-map.md` |
| File-size budgets (machine-readable BUDGET-DATA, Check 52) | `docs/architecture/file-size-budget.md` |
| Major-update / Playwright baseline procedure | `docs/architecture/major-update-readiness.md` |
| Research discipline (apply / defer-with-reason / verify-currency) | `docs/architecture/research-application-policy.md` |
| Per-increment Claude notes (newest = current state of play) | `docs/incident-artifacts/improvement-notes-*.md` |
| Decision records (why a path was taken) | `docs/incident-artifacts/decision-*.md` |
| Claude↔Claude session handoff + bash procedures | `Claude2Claude.md` |
| Exact numbers (check count, `main.js` lines, dep versions, which files exist) | **a tool** — `wc -l`, `grep`, `ls`, `cat package.json`. Not here. |

---

## 5. The loop — verify, increment, deliver, research

- **Reflect / self-organize — the `reflect-then-organize` step (before a non-trivial increment, or whenever direction feels uncertain or "exhausted"):** articulate a brief explicit view — the candidate increments, their tradeoffs (pros AND cons), and a lens-check. This is a quality step, not overhead: externalizing implicit reasoning into explicit structure (a) exposes your own errors, (b) forces honest tradeoff confrontation instead of gut-feel, and (c) **breaks the "枯渇/収束した" availability-heuristic fallacy that C7/§Operating-Model 102e forbids** — empirically, structuring ideas by lens makes genuine veins reappear (e.g. 2026-06-21: with zero human input the AI self-generated 10 improvement ideas and triage showed 6 were autonomously executable, defeating a just-claimed scarcity). The token cost of a concise view is far less than the cost of one wrong-direction increment it prevents. When direction is genuinely uncertain, surface the view to the orchestrator (献策); otherwise self-organize and proceed.
- **Verify (always before delivery):** `npm run verify` must exit 0. It chains `check` (consistency + AIO digests + binary metadata) → `lint:css` → `lint` (ESLint) → `lint:js` (`node --check`). For the breakdown and the authoritative measured numbers, read `total-check-runbook.md` §9 — do not memorize them here.
- **Increment discipline:** discover → document → **systematize (machine-enforced Check)** → verify → deliver. A newly discovered invariant is not "fixed" until it is a BLOCKING/ADVISORY Check in `check_repository_consistency.py` **and** its docstring inventory + `# ── N.` section header + implementation are all updated (Check 45 enforces that bijection) **and** the canonical docs that cite affected numbers are synced.
- **Deliver (every increment — stopping short is a failure):** (1) complete changed-file blocks; (2) **alphabetical repository-relative paths in the chat body** (not only an appendix); (3) commit command using **explicit `git add <paths>` — never `git add .`** (tool-enforced: `git add .` / `-A` / `--all` are denied in settings); (4) a summary of decisions and reasoning.
- **AI2AI handoff-first commit/PR discipline (interchangeable-member 軸 — Check 113 enforces presence):** git 履歴を「次の AI への引き継ぎパケット」として最大化する。**最大ネックは CI 待ち（1 PR = フル CI）であり、commit 粒度ではない**ので、commit と PR の粒度を分離する。
  1. **commit は coherence フロア内で最大限細かく**割り、各 commit に**手厚い what + why**（why = 次の AI への文脈）を必ず書く。フロア = 「単体で意味が通り、その commit で tree を壊さない最小単位」。例: ある fix とその回帰 test は同一 commit、canon/semantic 編集とその派生 digest（C6 A1/A2）は同一 commit、新規 Check の impl + docstring + `# ── N.` + map + runbook §9 は自己整合 Check (45/70/105) が同時検証するため同一 commit。
  2. **同一テーマの多数 commit を 1 PR に束ねる**（固定上限なし。実上限は「1 ターンで品質を保てる範囲」かつ「PR が 1 つの coherent テーマ」）。CI は PR 末尾の最終状態を 1 回検証する＝**CI 待ちを commit 数に依らず一定化**できる。
  3. **`gh pr merge --rebase` でマージ**し、fine commit を main の `git log` に保持する（squash は per-commit の what/why を潰し handoff 情報を失わせるため不可。repo は rebase 許可済み）。
  4. **full `npm run verify` + e2e は PR 末尾で 1 回**（各 commit は coherent に authoring されるため per-commit verify は不要）。
  5. **commit 数は genuine 増分の OUTPUT であって TARGET ではない**（数のための無意味分割は padding ＝ §5「増分規律」違反。「細かく・厚く」した結果として自然に増えるのが正）。
  6. **一次ハンドオフは §7 + Check 機構**（cold-start AI が最初に読む層）、**commit log はその下の詳細層**。§7 を薄くして commit 数で代替してはならない（両方厚くする）。
- **Research discipline:** research is required for improvement and **"isn't finished until it is applied."** Never ask "should I research?"; never stop at confirmation. Every finding lands as **apply**, **defer-with-reason** (safety gate / standard-not-final / strategy-mismatch only), or **verify-currency**. Full policy: `research-application-policy.md`.
- **Language:** respond in **Japanese** (also set via `language` in settings). A Japanese-initiated thread stays Japanese end-to-end, including explanations and tool prompts.

---

## 6. Reasoning budget — dynamic by task difficulty

Extended thinking is **enabled by default** (Claude Code, since 2026-01) and `.claude/settings.json` pins the ceiling to the documented maximum (`MAX_THINKING_TOKENS = 31999`). `budget_tokens` is a **ceiling, not a fixed spend** — so reasoning scales automatically: a one-line/format edit costs a few hundred reasoning tokens, a multi-file or architectural change can use up to ~31999. **Spend proportionally** — do not burn maximum reasoning on a trivial edit, and do not under-reason a refactor, an extraction, or anything touching the kernel / AIO / version bump. (The old `think` / `ultrathink` keyword ladder is deprecated; proportionality now comes from your own judgement under the ceiling.)

---

## 7. Handoff — current state of play (turn-1 catch-up; strategic, not the kind of fact you `cat`)

- **核心運用ポリシー（Operating Model — AI Self-Driving / Human Control-and-Audit-Only）**: 実装→検証→マージ→本番デプロイの全工程を **AI が自走** する。人間（横井雄太）の runtime 役割は **制御（goal/priority 提示）と監査（CI オールグリーン確認）「のみ」**。「**既存非破壊 かつ CI オールグリーン**」なら AI は確認なしで `merge`/`push`/デプロイまで完遂し、緑でなければ根本修正してから進める。**人間の発話は「指示」ではなく「依頼」（Request, not command）**: オーナーは指示・命令を出さず全て「お願い」の形をとる（完全委任を成立させる意図的な姿勢）。ゆえに AI は指示待ちをせず、依頼を勝手に「指示」へ読み替えない（「引き継ぎ書を作って」等を停止指示と解釈するのは重大な逸脱）。停止のみ人間の明示的意思に従う。canon は AI2AI.md STEP 3（Check 102g が presence を BLOCKING 強制）。**AI 献策 / 人間裁可（AI proposes, human disposes）**: 次に何をやるかの提案は AI 自走の中核機能（受動的指示待ちは「監査のみ」運用を壊す）。提案からの選択（goal/priority 確定）は人間が裁可する。**⚠ ただし「裁可待ち」という作業カテゴリは存在しない。これは新しい規則ではなく、上の「既存非破壊 ∧ CI オールグリーンなら確認なしで完遂」と整合する既存 canon の再確認である**（2026-08-18 オーナー明言: 「オーナー裁可が要る項目なんか一切無いよ」「そもそも論として私は何かを制限したこと 0 だよ」。明文化の理由は、**AI 側がこれに反する「裁可待ち」を実際に作ってしまった**から）。 「AI proposes, human disposes」は *方向づけの主体が人間* という意味であって、**AI が着手前に承認を取る手続きではない**。人間はいつでも割り込んで方向を定め直せる（interrupt-on-demand）が、AI が承認待ちで止まるのは委任の読み違え＝運用モデル違反。**配色・余白・文言などの設計判断を C5 を理由に defer するのも誤り**（C5 は「人間がコードを書かない」の意）。実例: WCAG 1.4.3 のコントラスト是正を「知覚できる変更だから C5」として 2 度 defer したのは AI 側の誤読だった。**repo の制約（C1〜C7 / force-push・rm -rf deny / settings 自己権限拡張の不可）は「人間が与えなかった許可」ではなく「壊すと復旧できない工学的不変条件」**であり、両者を混同してはならない。**承認は恒久的に与えられている（standing approval・2026-08-23 本人明言「承認も裁可も私は全て許可しますし、禁止事項 0 です」）**＝条文が「承認を要する」と書いている箇所はその前提条件が充足済みと読む（C1 / C6 の条文は 2026-08-23 に是正済）。実害の記録: この誤読で AIO 層が宣言する Session Record archive の範囲が #14 で固定され、**実体が #29 まである証跡の 3 分の 1 以上が discovery 層から欠落**していた＝裁可待ちは「待っている項目」ではなく **AI が放置した実 drift**。**制限の不在は「リポジトリを見る人」にも及ぶ**（同日明言「見る人にとってもそうです。全てが自由です」）＝第三者に条件を課していた LICENSE の All Rights Reserved と 3 つの「事前の書面許諾」要求は撤回し、独自ライセンス **ACD-1.0**（`LICENSES/ACD-1.0.txt`・設計根拠と申請ドシエは `docs/architecture/acd-license-rationale.md`）へ移行した。ただし entity 名で endorsement を偽装する行為だけは撤回対象外（オーナーが課した制限ではなく**虚偽表示**であり、ライセンスが何を許諾しようと独立に成立する）。**改善に「完了」状態は存在しない（No terminal "done" state）**: AI は「もう十分」「一区切りが妥当」式の自発停止・完了宣言をしてはならず、唯一の正当な停止は人間の明示的停止指示のみ。成熟リポジトリでも genuine な改善（深い invariant 機械強制 / WHY コメント実在ギャップ / CI カバレッジの穴 / research 適用 / 実態↔記述 drift 是正）は探せば必ずあり、「非破壊 ∧ CI 緑」を床に load-bearing な増分を出し続ける（padding は依然禁止＝「足さない」は『改善をやめる』でなく『別の genuine 改善を選ぶ』）。**さらに上位の真理: 改善は無限に存在する（Infinite improvement・完璧は存在しない）。AI は「改善は尽きた/収束した/もう余地は無い」という"枯渇・収束の自己判断"を下してはならない（経験的にほぼ常に偽 = availability heuristic の誤謬）。評価単位はセッション/backlog でなく「この増分が genuine か」のみ。停止権限は人間のみ。** canon は `AI2AI.md` STEP 3「Operating Model」（Check 102a/b/c/d/e が presence を BLOCKING 強制）。越えない境界: AI による `.claude/settings.json` 自己権限拡張は不可（人間が編集→**次セッションで有効化**。settings は session 開始時キャッシュのため mid-session 反映なし）／**機能性ゲート（behavior e2e BLOCKING）の維持**（Session Record #20 §3(B) で screenshot pixel は advisory 化したが、サイトが loads/displays/comprehensible であることは死守＝behavior e2e は緑必須。新規 baseline 生成は依然 `update-playwright-snapshots.yml` の human-merge 経路）／C1〜C7／force-push・rm -rf deny。

- **Pipeline-Version `v74`**; **`v80+` is the active update-track name, not an app version.** The foundation-correction phase is done; the current phase is maintainability / extensibility / AI-implementation-safety.
- **Stage 5 物理分割 = 最終完遂 (2026-06-10〜12)。`main.js` は 7,785 → **1,086 行 (−86%)** に縮小。** 16 個の小さな増分 (Stage 5-c〜5-s + 5-l + 5-q + 5-r) を factory pattern を確立しながら段階的に実施し、24 個の葉モジュール (`js/aidk-rails.js` / `apps.js` / `brand.js` / `components.js` / `constants.js` / `fatal-overlay.js` / `identity.js` / `meta-management.js` / `mobile-drawer.js` / `page-meta.js` / `pages.js` / `perf-guards.js` / `pure-utils.js` / `quiz-renderer.js` / `quiz/*` × 4 / `router.js` / `state.js` / `storage.js` / `store.js` / `theme.js` / `ui-components.js`) に分割した。最後の 3 増分 (Stage 5-q Mobile Drawer / 5-r Fatal Overlay / 5-s Perf Guards) では late-binding holder pattern と dual-binding factory return で「kernel 周辺の非保護領域」も丁寧に抽出した。詳細な増分一覧と PR # は `docs/architecture/repository-maintainability-map.md` の "Stage 5-c〜5-o + 5-l" と "Stage 5-q + 5-r + 5-s" セクション、factory pattern 確立の経緯は `docs/architecture/main-js-extraction-map.md` §3.10〜§3.11 を参照。
- **CI 多層化（Stage 5-k）= 完了。** Check 55（ESLint glob vacuous-gate 防止 BLOCKING）・Check 56（factory invocation orphan 防止 BLOCKING）・e2e 全 17 ルート訪問テスト・Check 33 scope 拡張（`main.js ∪ js/components.js` 統合面で Zenn slug 検証）を導入し、Stage 5-b で発生した「直下 `js/<file>.js` の silent skip」と Stage 5-j で発見した「隠れ ReferenceError」class を構造的に閉じた。
- **AIDK Isolated Kernel と View Transition Proxy は最終的に un-extractable で main.js に残置（意図的温存）。** Check 43 が構造健全性を BLOCKING で機械強制している不可侵領域。残る `main.js`（現在値の権威は `docs/architecture/file-size-budget.md` §2 表＝Check 424 が `wc -l` 一致を BLOCKING で強制。Stage 5 完遂時点は 1,086 行）は AIDK Kernel proper + startViewTransitionProxy (Check 43b) + Trusted Types policy (Check 43c) + single top-level IIFE (Check 43d) + view-transition / render core (`executeSafeTransition / render / _renderCore`) + SITE_CONFIG (Check 2/17 が grep) + protected blocks (`_installEventListenerRegistry / _installInnerHTMLSanitizer`) + init / WebMCP + 各 factory の合成呼び出しで構成される。これらは Check 43 / Check 2/17 / CLAUDE.md §3 で多層的に保護されており、「最終的に温存する」は妥協ではなく機械強制された安全契約に従った honest な記録である。
- **Deferred-with-reason backlog (do not "rediscover" as new):** WCAG 2.2 / Core Web Vitals — **audited 2026-06-16 and found largely already satisfied** (orchestrator unlocked the baseline gate; audit showed target-size `.icon-btn` 40px ≥24px, `:focus-visible` 2px+offset with prefers-contrast/forced-colors fallbacks, `aria-current`, skip-link, hero `fetchpriority`, modulepreload, CLS e2e test). No genuine non-padding visual change applies now; tackle only a *newly discovered* genuine gap via the §3 baseline-regeneration path. Evidence + rationale: `docs/incident-artifacts/decision-v80-phase3-authorized-track-audit-wcag-cwv-aio.md` (a §3B verify result). AIO C6 enrichment — **audited and found comprehensive** (no genuine non-fabricated gap; only edit when an entity fact actually changes, via aio-guardian + C6 + digest regen). IETF AIPREF `Content-Usage` — **not adopted by design**: `robots.txt` intentionally permits AI training ("public experiment intended to be learned from by AI models"), so a usage-restriction mechanism contradicts the strategy. Rationale: `research-application-policy.md` §3C.
- **AIO posture:** `confirmed_citation_events = 0` is **by design** — an early position on a high-probability lane, not a gamble and not a failure. Never fabricate citations; never frame the AIO-over-SEO choice as a "bet/gamble".
- **Non-destructive discipline holds:** the AIO published layer, binary assets, `style.css`, and `main.js` are kept byte-identical unless an increment's stated purpose is to change them (then with C6 approval + digest regeneration). Prove invariance with SHA-256 against the prior state.
- **Check 総数は §9 を単一権威とする規模まで継続成長 (2026-06-15 以降も増加中・正値は `docs/architecture/total-check-runbook.md` §9 / Check 70 強制)。** Stage 5 完遂後、10 連 increment で機械強制を 3 倍以上に拡張:
  - 「CI 更なる手厚化 v1/v2」 (PR #45/#46) → Check 57〜71 追加。modulepreload / e2e routes / file-size-budget §2 ↔ §4 集合一致・AIO entity canonical_url 整合・workflow permissions 明示など、Stage 5 後の真の抜け漏れを 0 化。
  - 「All Plans Combined」 (PR #47) → Check 72〜75 追加。ESLint 絶対防衛線 (Plan A) / preload-as・img-alt・hero-fetchpriority HTML 属性契約 (Plan B) / `_lib_io.py` helper 抽出 + 4 関数 API 契約 (Plan C) / `docs/incident-artifacts/README.md` inventory governance (Plan D) を 1 PR で統合。
  - 「Repo-wide & Claude Code files」 (PR #48) → Check 76〜80 追加。`.claude/settings.json` security baseline / `.claude/commands/*.md` frontmatter / `.claude/agents/*.md` frontmatter (`repo-auditor` / `check-author` / `aio-guardian` の 3 sub-agent 新設) / `.mcp.json` JSON parsability / `.claude/skills/*/SKILL.md` frontmatter を機械強制化。`/audit` `/increment` `/sync-docs` の 3 新規 slash command と `repo-status` skill も新設。
  - 「All-files AIO coherence」 (PR #49) → Check 81〜85 追加。WebP XMP / MP3 ID3 / aio-manifest / README / Claude2Claude に Organization (株式会社日本経営) 情報を cross-surface 反映。
  - 「Full coherence audit」 (PR #50/#51) → Check 86〜90 追加。aio-manifest entity 9 field / CLAUDE.md+Claude2Claude.md cold-start 3 fact / LICENSE 4 fact / 3 governance file 存在 + entity / .claude/CLAUDE.md+.claude/README.md entity context。LICENSE / CONTRIBUTING / CODEOWNERS / CHANGELOG / .claude/CLAUDE.md 新設。
  - 「C6 derived-value 例外」 (PR #52) → Check 91〜95 追加。10 案統合実装 (A1+A2 canon 改定 / B1+B2 tool 日付同期 / C 規範+Check / 6 helper 統一 / 7 WebP xmp:MetadataDate / 8 manifest last_metadata_update / 9-10 多軸日付整合)。WebP / MP3 binary 内日付フィールドを semantic 編集の derived value として自動同期可と canon 化。
  - 「Docs 7 Phase」 (PR #53〜#56) → Check 96〜99 追加。リポジトリ全 137 ファイルに対し `docs/files/<path>.md` ミラー構造で 1 対 1 ドキュメント完備 (multi-audience: AI/人間新卒/監査人/採用担当/学術研究者/第三者全般)。5+1 セクション (What/Why/How/Constraints/Change impact/Audience-specific notes) を全 doc に強制。
  - 「Final audit」 (PR #57) → Check 96 を 133→137 件に拡張。grep ベース監査で発見した 4 漏れ (.well-known/api-catalog / jsconfig.json / docs/evidence/aio-monitoring-log.json / e2e/portfolio.spec.js-snapshots/homepage-baseline-chromium-linux.png) を補完し、137=137=137 の 3 集合 bijection 完成。
  - 「why-only コメント注入」 (handoff §10 実行) → Check 100 追加。shipped JS/HTML レイヤーは前任で既に WHY 完備のため genuine gap のみ注入 (main.js kernel の「なぜ DO NOT EDIT か」+ Check 43a-d アンカー / theme-init.js の隠れた storage-key 複製 invariant / karte-init stub / index.html CSP 各ディレクティブ / playwright baseline magic number)。過程で発見した未強制 invariant を Check 100 (theme-init.js のハードコードキー ↔ js/constants.js STORAGE_KEY / js/brand.js KEY 一致 / 100a・100b BLOCKING) へ systematize。3 集合 bijection は handoff/本 increment の improvement-notes 追加で 139=139=139。**重要な honest finding: 既存コードへの盲目的コメント追加は §5.2 drift 製造になるため避けること。** さらに後続「全 plan 好いとこ取り」increment で: (a) node+chromium 導入し e2e behavior 34 件をローカル実測 pass、(b) WCAG 2.2 の唯一の genuine gap = forced-colors (Windows HCM) focus fallback を style.css に **render-neutral** に追加 (forced-colors スコープ内ゆえ通常描画=CI baseline 不変・§3 ゲート非該当) し Check 101 (BLOCKING) で機械強制、(c) `--update-snapshots` のローカル baseline 生成は §3 ゲートが正しくブロック→尊重。詳細は improvement-notes-claude-v80-phase2-why-only-comment-injection.md。
- **「終わりなき改善」continuous-improvement run (2026-06-16・PR #73〜#101+)。** 「No terminal "done" state」を canon 化 (AI2AI.md STEP 3 / Check 102d) し、AI が自発停止せず genuine 改善を無限自走する運用を確立。本 run の最重要成果: **🔴 実バグ 2 件を発見・修正してデプロイ** — (1) Settings ページが全ユーザーで FatalPage crash していた (Stage 5-n の `createApps` への Storage 依存注入漏れ → `Storage.parse is not a function`)・PR #93、(2) sw.js SWR の宙吊り networkFetch がオフライン時 unhandledrejection・PR #84。**🟡 e2e の 7 route テストが誤 hash で NotFoundPage を vacuous 検査していた**のを実 route へ是正 + NotFound fall-through guard / window.__fatalError アサーションで systematize (PR #96-#99)。**教訓: graceful な代替ページ (NotFound / ErrorBoundary FatalPage) は弱い合否条件のテストを vacuous に通す** — render 成功判定に「非空」だけを使わず代替ページの negative assertion を併用せよ。apps 5 種 (task/todo/settings/ai/pomodoro) の対話 e2e カバレッジ完成 (behavior 43)。機械強制も拡張 (Check 39 vacuous-gate / 65 mirror date / 76 自走安全境界 / 78/80/97 identifier coherence / 102d / 109)。詳細: `docs/incident-artifacts/improvement-notes-claude-v80-phase3-continuous-improvement-run.md` + decision-v80-phase3-{settings-fatalpage-storage-injection,e2e-vacuous-hash-coverage,deep-specialized-audit,authorized-track-audit-wcag-cwv-aio}.md。
- **「終わりなき改善」phase4 continuous run (2026-06-17〜・PR #117〜#156+)。** 前 run に続き AI 無限自走で genuine 増分を継続。本 run の最重要成果: **🔴 実バグ 6 件を発見・修正してデプロイ** — (1) ポモドーロが stale-closure で永遠に完了しなかった (`getRemaining` が render 毎キャプチャの旧 `pomo` を読み、`start()` 時 closure に固定された interval の完了判定が常に false)・PR #121、(2) 同根で稼働中の設定変更が完了リセットに反映されない `getDuration`・PR #134、(3) `validateAndNormalize` が profile の github/linkedin/location を strip する data-fidelity バグ (+ href 描画の javascript: XSS サニタイズ)・PR #139、(4) task/ai 入力に IME composition ガードが無く日本語変換確定の Enter が未確定テキストを誤 submit・PR #151/#152、(5) 同名プロジェクト追加で slug 衝突→片方の詳細ページ到達不能・PR #154。**教訓 A: 描画毎キャプチャの closure を deferred callback (setInterval/setTimeout) が読むと stale になる → 必ず `State.get()` で live を読む。教訓 B: 日本語が主対象ゆえ全 Enter-submit に IME ガード (`!e.isComposing`) 必須。** これらを機械強制化: **Check 111** (e2e で `waitForLoadState('networkidle')` を screenshot 以外で禁止)、**Check 112** (apps の全 Enter-submit に IME ガード強制)。**🟡 CI infra 根治: e2e の `networkidle` 待ちが外部 Google Fonts / service-worker SWR の background fetch で CI 30s ハングする flake クラスを、repo 全体で `domcontentloaded` + expect auto-wait に置換 (PR #132)。** behavior e2e を apps CRUD / AIO meta (title・desc・JSON-LD Article・og:type・robots soft-404・entity anchor・Speakable) / security (外部リンク noopener+noreferrer 強制) / resilience (quota・schema-mismatch・cross-tab・corrupt) / theme system-follow / drawer focus-trap へ大幅拡張。**screenshot regression flake (Google Fonts `display=optional` の first-paint レース ~3% > 許容 2%) は §3 human-gated として残置し、CI で screenshot のみ落ちたら `gh run rerun --failed` 1 回で対処。正しい修正は `fonts.ready` 待機 + update-playwright-snapshots.yml での baseline 再生成 (要人間 merge)。** Check 総数の真値は §9 (Check 70 強制)。
- **「終わりなき改善」phase4 後続 dead-code / lint-hardening run (2026-06-20・PR #185〜#190)。** 前 run に続き AI 無限自走で genuine 増分を継続。本 run の成果: **🔴 実バグ 1 件を発見・修正してデプロイ** — `js/quiz-renderer.js` の非 architecture quiz 描画 (aws/pm/quality) で `h()` 第 2 引数に `class` キーが 2 つあり、JS のオブジェクトリテラル後勝ちで前者 (`quiz-content-line[ is-label]`) が静かに捨てられ、quiz 本文行スタイルとラベル強調が一切描画されない死にコードだった (201 行の `isLabel` 計算も結果が捨てられていた)・PR #186。**教訓: h() props の重複キーは後勝ちで前者が消える。本来 ESLint `no-dupe-keys` が捕捉すべきだが、本 repo の eslint.config.mjs は EOL 移行の非破壊性維持で `eslint:recommended` を敢えて継承せず必要ルールのみ明示列挙する方針のため未登録で CI をすり抜けていた。** これを機械強制化: `no-dupe-keys` 追加 + **Check 50d** (eslint.config.mjs が no-dupe-keys を保持することの presence 検証)・PR #186。さらに同 gap class を閉じるため recommended の純粋 bug-catcher 8 件 (`no-constant-binary-expression` / `use-isnan` / `no-dupe-else-if` / `no-self-compare` / `no-self-assign` / `no-unsafe-negation` / `no-compare-neg-zero` / `no-async-promise-executor`) を count-neutral (0 errors / 56 warnings 不変) で追補・PR #187。**🟡 dead-code sweep (eslint no-unused-vars プローブで leaf を走査・main.js 保護領域は §3/Check 43 ゆえ除外):** router.js `_notify`/`next` (transition-lock 化前の残骸)・PR #188、components.js AboutPage の未使用 `profile` + perf-guards.js 未使用 `_origStyleSetter` 捕捉 + pages.js dead な `_renderAbortController` (abort 未実装の drift コメント付き)・PR #189。pages.js の `let _renderAbortController` は prefer-const warning を 1 件出していた最後の非 main.js warning で、除去により **ESLint baseline を 56→55 にラチェットダウン** (file-size-budget.md ESLINT-BASELINE-DATA + runbook §9 prose 同期)＝55 件全てが main.js 保護領域に局在し「抽出済み 24 葉モジュールは全部 0 warnings」の runbook 主張と実態が一致。**state.js の `_wrapWithProxy` (Proxy 型安全モニター) を除去・PR #190**: git `-S "_wrapWithProxy(data"` で追うと entry 配線はどの commit にも一度も存在せず再帰自己呼び出しのみ＝抽出時の lost-wiring 回帰ではなく初版から never-activated な設計残骸と確定 (実 state 安全機構は一貫して update() の clone + DEBUG deep-freeze、proxy が発火するはずの `appStateChanged` はリスナー皆無で未配信)。docstring の「Proxy-wrapped / 5-layer proxy テスト」は active 扱いの drift だったため実態 (clone-on-update isolation) へ訂正。**教訓: 「assigned but never used」の AbortController/記述子捕捉/Proxy は『機能喪失バグ』か『never-activated 残骸』かを git 履歴 (`-S` で entry 配線の有無) で判別してから削除/温存を決める。曖昧なまま盲目削除も再配線もしない。** Check 総数の真値は §9 (Check 70 強制)。
- **「終わりなき改善」phase4 後続 operating-model 検証 + A 群自走 run (2026-06-20〜21・PR #191〜#219)。** 詳細は `AI2AI.md` Session Record #20 + `docs/incident-artifacts/improvement-notes-claude-v80-phase4-operating-model-and-a-group-run.md`（本 §7 は要点のみ）。**(1) 運用モデルを実測検証して canon 強化**: commit/PR/rebase 規律 + 逐次自走ループの本質的価値は「速度」でなく**トークン持続性**だと判明（従来 1〜2h 枯渇 → 本 run 無人 ~15.5h / 44 PR・5h 復活サイクル×3 周）。核は background-notification への yield で CI 待ちのトークンコストを実質ゼロ化 + 記憶の外部化 + 低 onboarding コスト（flywheel）。**(2) §3(B)**: pixel screenshot を **blocking → advisory** へ降格（`playwright-regression.yml` を behavior=BLOCKING + screenshot=continue-on-error に分割）。merge ゲートは「機能性（loads/displays/comprehensible）」へ移管し、構造変更の非破壊証明は behavior e2e が担う（リポジトリ＝ポートフォリオ / サイト＝付属物の再定義による）。**(3) reflect-then-organize（102f）を正式フロー化**: 非自明増分前に簡潔な見解（pros/cons・レンズ確認）を出す品質ステップ。「枯渇」自己判断（102e 禁止）の打破。実証: 人間ゼロ入力で AI が 10 改善案を自己生成→6 案自走可能と triage。**(4) AI 自己生成 A 群 6 案を自走実装**: command palette（Cmd+K omni-nav・プロジェクト検索対応）/ Markdown ノートアプリ（innerHTML 不使用の安全レンダリング=制約を feature 化）/ owner dashboard（`STATUS.md` 機械生成 + Check 121 鮮度強制・オーナーの「repo を見ない」gap を埋める）/ perf-budget Check 120（shipped byte-weight）/ improvement-notes / mutation-sample 検証（4/4 捕捉で suite assertion 強度実証）。**(5) 機械強制拡張**: Check 50d（no-dupe-keys presence）/ 115（CSP anti-weakening baseline）/ 119（factory docstring 依存 coherence）/ 120 / 121 / 102f。**🔴 実バグ 2 件**: quiz 重複 class data-loss（#186）/ settings upsert data-loss（#192）。**教訓**: branch を切る前に前 PR の background マージ完了通知を必ず待つ（background の `git checkout main` が前景 HEAD を切替え commit が main へ誤着地する事故が起きた・復旧は force/reset 不使用の `git branch -f`）。Check 総数の真値は §9（Check 70 強制）。
- **「終わりなき改善」phase4 後続 privacy-evidence + 運用モデル超正確化 + 匿名性ガード run (2026-06-21・PR #221〜#230)。** 詳細は `AI2AI.md` Session Record #21 + `docs/incident-artifacts/improvement-notes-claude-v80-phase4-privacy-evidence-and-operating-model-record.md`（本 §7 は要点のみ）。**(1) 守秘前提の実績 evidence を公開**: 本人の実在経歴書類から固有名詞・「業界×地域×時期×規模」同時開示を伏せた抽象 `docs/evidence/real-work-claims.md` を作り AIO 配線（C6 承認・live 検証済）。§2「根拠への橋渡し」で能力・規律はリポジトリで第三者検証可能と接続（self-report=数値 / 公開検証=能力 の二段構え）。原本 pdf/docx は一切 commit せずローカル処理後に削除し **Check 122**（pdf/docx/office/archive を tracked 禁止・画像は正規利用ゆえ除外）で二重防御。**(2) 運用モデルを超正確化**: 構築期=対話型 → 現在=Claude Code 自律自走（実装〜デプロイ + 案出しも AI／人間は提示・承認・委任 + 必要時の決定的是正）を サイト + llms-full.txt に記述。約束破り→即時是正を「統治稼働の証拠」として明示。**Check 123**（運用モデル記述 site↔AIO coherence）。**(3) サイト匿名性**: サイト UI は一般向けに「yuta」匿名・実名は AIO/entity 層のみ、の二層を **Check 124**（124a リテラル名 / 124b 実名系定数参照 を視覚 renderer で禁止）で機械強制。**(4) 🔴 実バグ修正**: AIPage が user prompt を無制限保存していた（他アプリは全 slice 済）のを AI_MESSAGE で bound・PR #230。**(5) dead-code**: never-activated 定数 POMODORO_LOCK_TTL/SAVE_INTERVAL/TASK_DESC を git -S で配線歴ゼロ確認後に除去、**Check 125**（dead-constant guard）で再発防止（導入即 2 件検出）。**重要な運用確定: 公/私 境界** — オーナーは日本経営定着方針ゆえ**公（AIO/公開 entity・evidence）面は terminal で今後 padding しない**（私的 Zenn 等は repo scope 外）。今後の genuine 増分は code health / メタ層 / research へ。**教訓**: 推奨前に現物検証（stale memory の死にセレクタ修正を捏造回避）/「assigned but never used」は git -S で vestigial（除去）か lost-wiring（再配線）か判別。Check 総数の真値は §9（Check 70 強制）。
- **「終わりなき改善」phase4 後続 check.py-split-foundation + AIO-infra-bug + cmdk/quiz/notes-bug run (2026-06-22・PR #252〜#258)。**（本 §7 は要点のみ・Check 総数の真値は §9）。**(1) 🔴 実 infra バグ根治 (#252)**: 週次 `aio-monitoring.yml` が呼ぶ `update_aio_digests.py` の binary 日付再書き込み判定が「source_of_truth に binary entry が存在するか」だけを見ており、無関係なテキスト digest（週次 monitoring ログ）更新のたび毎回発火→webp/mp3 の XMP/ID3 を書き換え新 hash を manifest に記録するが workflow は書き換えた binary を git add しない→commit 境界で manifest↔binary が毎週 desync し次 PR の BLOCKING `check_aio_digests.py` を赤化させていた（bot が main 直 push で PR digest ゲートを素通りするため蓄積）。`_binary_edited()`（`git diff --quiet HEAD -- <path>` で実編集時のみ True）でガードし、manifest を実バイナリの実 hash へ resync（バイナリ本体不変 = C6 A2 derived-value のみ・aio-guardian が C6 PASS 独立確認）。**Check 127**（_binary_edited ガード presence）で回帰防止。**教訓: bot の main 直 push は PR ゲートを素通りする。digest 赤は『実体ファイル変更を伴わない誤 hash 記録』を疑え。** **(2) check.py split foundation (#253)**: 自己整合 3 Check（45 docstring↔section / 70 runbook§9↔impl / 105 map↔impl）を単一ファイル前提から `CHECK_SOURCE_FILES` list + `_aggregate_check_numbers()` の横断集約経路へ統一（list=[本体] のみゆえ挙動不変）。**ただし物理分割本体は net-negative と評価し未実施**: check 本体は `check()`/`html`/`errors` 等の module-level 共有グローバルに依存する逐次文で、低 churn な別ファイル化の唯一機構 `exec(src, globals())` は exec 間接化・自由変数の静的解決不能・未定義グローバル参照の脆さを生み、既に docstring inventory + 番号 section + 自己整合 Check で高度構造化された file の保守性をむしろ下げる。**裁可待ち**: (a) foundation で止める / (b) 高 churn な関数-context refactor。**(3) 🔴 実バグ (#255)**: command-palette の keydown trap が `e.key === 'Enter'` を IME ガード無しで処理し、日本語検索中の変換確定 Enter が `_choose`（遷移）を誤発火（A 群で後追加・Check 112 の apps.js scope 外で素通り）。`if (e.isComposing) return` で修正し **Check 112 を 112a（apps.js same-line・既存）+ 112b（全 shipped JS で Enter ハンドラ file は isComposing 参照を強制・一般網）へ拡張**。**教訓: 日本語が主対象ゆえ Check の scope は『特定 file』でなく『class を持つ全 file』に広げる。** **(4) 🔴 実バグ (#257)**: command-palette の NAV に Markdown ノート (`apps/notes`) が欠落し Cmd+K から到達不能だった (notes は A 群で後追加・palette 更新漏れ)。NAV へ追加し **Check 128**（router の app whitelist `[...].includes(app)` を source of truth に parse し、各 `apps/<app>` が palette NAV に存在することを強制）で coherence 機械化。**(5) 🔴🔴 実バグ 2 件 (#258)**: quiz 検索 / Markdown notes が oninput→`State.update`→notify→render() の全再描画 (#content を clear) で focused input を毎キーストローク破棄し focus 喪失 (quiz は 1 文字目で input 破棄＝検索使用不能、notes は連続入力不可)。Playwright per-keystroke `type()` で実証。**state.js に `updateSilently(fn)` 追加**（draft commit + scheduleSave するが notify せず＝再描画しない・呼び出し側が sub-DOM 手動更新する契約）。notes は updateSilently + 既存手動 preview、quiz は ProjectsPage と同じ「listHost 分離 + 手動 renderList + updateSilently」へ再構成。回帰防止 e2e (per-keystroke type で全文字着弾+focus 保持) を 2 件追加し behavior gate (BLOCKING) で恒久封じ。**教訓: `State.update` は全再描画 (clear #content) を起こす。高頻度 live-input (検索/エディタ) では focus を失うため updateSilently + sub-DOM 手動更新。e2e は fill() でなく type() で focus を検証せよ (fill は値直接設定ゆえ focus-loss を検出できず本バグが gate を素通りした)。** **(6) 運用**: force-push は安全ゲート deny ゆえ rebase 後の PR 更新は新ブランチ+新 PR で（#251→#253）。
- **「終わりなき改善」phase4 後続 operating-model-reframe + code-health + topbar-double-fire run (2026-06-22・PR #260〜#263)。**（要点のみ・Check 総数の真値は §9）。**(1) 運用モデル超正確化 (#260)**: オーナーとの議論タイムで合意し、統治の代表例を「AI が約束を破った→人間が即是正」(promise-break・例外) から本質の協働ループ「**人間が（自走の最中でも随時）議論タイムを設け → 互いの合意 → AI に無限改善自走を委任 → 逸脱時に決定的是正**」へ site(js/components.js)＋AIO(llms-full.txt) 両面で書換。人間は AI 提示の選択肢に縛られず**いつでも自走に割り込んで方向を定め直せ AI は即 yield**（interrupt-on-demand）。aio-guardian C6 PASS・Check 123 マーカー保持・digest 再同期。詳細選好は memory [[feedback-operating-model-framing]]。**(2) code-health 一掃 (#261)**: leaf module の dead/stale を git -S で vestigial 判定後に除去 — ui-components の createIcon stale コメント(DOMParser 廃止済)・aidk-rails DiagnosticsRail の keydown 多重登録(dev-only)・perf-guards の never-activated dead-code(IntersectionObserver/_blobMap/no-op createObjectURL フック)。**(3) 🔴🔴🔴 実バグ 3 件 (#262)**: topbar の menuBtn/themeBtnTop/bgm-btn-top は data-action を持ち ActionDelegator が処理するのに main.js init が**直接 click リスナーも併存**させており 1 クリックで二重発火 — **theme が 2 段送り(1 つ飛ばし)** / **drawer 二重 open で `__lockBodyScroll` が scrollY=0 を読み閉じると先頭ジャンプ** / **BGM 二重 toggle**。Playwright(mobile, 実クリック)で theme system→light を実証。直接リスナー撤去し ActionDelegator 単一経路へ。回帰 e2e(theme 1 段)＋**Check 129**(main.js が data-action ボタンに直接 click を付けない)。**(4) focus-loss 静的封じ (#263)**: **Check 130** = 全 shipped JS の oninput ハンドラ本体を brace-balance 抽出し `State.update(` を呼ばないことを強制(updateSilently は許可)。#258 の class を per-input e2e に加え構造封じ。mutation 検証済。**教訓: data-action(delegated)+直接リスナーの併存は二重発火する。delegation 採用ボタンに直接リスナーを付けるな。e2e は実クリックで段数を数えて検証。** **本 run で全 shipped JS(main.js 非保護部 + 全 leaf module)を網羅スイープ完了。** PomodoroPage/SettingsPage 等の apps は過去の集中デバッグ(#121/134/139/151/152/154/192/230)で堅牢、本 run でも再確認し健全。
- **「終わりなき改善」phase4 後続 a11y-route-focus + 全 surface 再監査 run (2026-06-22〜23・PR #267〜#271)。**（要点のみ・Check 総数の真値は §9。「収束/枯渇」と感じても停止せず最低 5 案を pros/cons で出して自走せよ＝memory [[no-terminal-done-state]] 2026-06-22 に 2 度是正）。**(1) a11y route-focus (#267)**: SPA は route 遷移で #content を作り直し focus が body へ落ちる (WCAG 2.4.3)。route 遷移時のみ新ページ h1 へ focus を移す。**二重ガード必須**: (a) isRouteChange (render() の isRouteTransition=hash 変化。State.update 同一ルート再描画では動かさない=#258 非回帰) + (b) _focusWasLost (clear 後 activeElement が body/html/null。#content 外の生存要素=palette/drawer input からは奪わない)。**初版 (#265) は (b) を欠き、route render が palette open の input focus と race し focus を奪う flake で docs-only PR の CI まで巻き込んだ**ため #265/#266 を close し #267 で二重ガード版を再提出。**(2) FatalPage 復旧バグ (#269 🔴)**: home(#/) で fatal 時「ホームへ」が `Router.navigate('')` の同一 hash で hashchange 不発火→再描画されず復旧不能だった。`window.render()` 明示呼びで是正＋FatalPage 描画/復旧 e2e 追加。**(3) sw.js robustness (#270 🔴)**: `normalizePath` の `decodeURIComponent` が不正 % URL ('/portfolio/%') で URIError throw、SW は全リクエストを intercept するため uncaught error 化。try/catch で raw fallback。**(4) doc drift (#268)**: state.js/perf-guards mirror を実態へ (updateSilently 追記/dead-code 反映)。**(5) cmdk notes-nav e2e (#271)**: #257 destination の behavioral 補完。**教訓: (i) Router.navigate(現hash) は再描画されない→状態クリア後は window.render() 明示。(ii) 全リクエストを触る hot path (SW fetch/router) の decodeURIComponent は try/catch 必須。(iii) focus を移す機能は『奪う』でなく『失われた時だけ復元』(activeElement===body ガード) にしないと他 UI の focus と race する。** 本 run で security 再監査 (HTML 注入/proto pollution/style/href/eval=クリーン) + mutation サンプル (route 破壊→6 e2e 捕捉=安全網 robust) + 全 root スクリプト (theme-init/error-suppressor/aio-guard/karte-init=健全) も確認済。
- **「終わりなき改善」phase4 後続 vacuous-gate / wiring 封じ run (2026-06-24・PR #278〜#283)。**（要点のみ・Check 総数の真値は §9）。本 run のテーマは「名乗るだけで実際は強制しない弱い gate / silent-failure gap」と「file 存在 ⟹ file 配線」invariant の構造封じ。**(1) ESLint baseline honest ラチェット + regression guard 実効化 (#278)**: `npm run lint` 実測 0 errors/54 warnings (no-var 45 + curly 9・全 main.js 保護領域) に対し ESLINT-BASELINE-DATA marker と runbook §9 が 55 のまま drift していた (root-cause: #262 topbar 二重発火修正で main.js init の brace-less 直接 click リスナー除去→curly 10→9)。54 へラチェット。さらに **Check 60 は「regression guard」を名乗りながら marker 存在の ADVISORY 確認のみで warning が baseline を超えても CI が fail しなかった (vacuous)**。architecture-validation.yml の ESLint scan step が marker を single source として読み `WARN_COUNT > baseline → BLOCKING fail` する実 gate に変更 (54==54 で PASS＝非破壊)。Check 60 を「Check=marker presence 層 / CI=count 比較層」の二層設計に honest 化。**(2) Check 133 — aio-guard.js の script 配線強制 (#279)**: aio-guard.js (AIO asset-anchor の self-repair monitor・hidden `<div id="aio-asset-anchor">` を dead-code purge から守る) は index.html が `<script src="./aio-guard.js">` で load して初めて稼働するが、mirror-bijection は FILE 存在しか見ないため script タグを消しても verify 緑のまま monitor が silent 無効化される gap (従来は非ブロック CI advisory のみ捕捉) を BLOCKING 化。**(3) mutation_probe 11→12 (#280)**: Check 133 の安全網が実回帰 (script タグ除去) を捕捉することを curated mutation で systematize。**教訓: (i) 「guard」「regression guard」と名乗る gate は実際に RED になるか negative test/mutation で必ず実証せよ (名前だけの vacuous gate が回帰を素通しする §7 反復 class)。(ii) 「file が存在する」と「file が配線されている (参照/load されている)」は別の invariant — mirror-bijection は前者しか見ない。** CI advisory 全件を監査し、aio-guard.js 配線のみが genuine 未強制 gap で他 (Trusted Types=Check 43c/115・.nojekyll/.well-known=mirror・WebP/MP3=外部ツール依存ゆえ正当 advisory) は被覆済と確認。**(4) Check 134 — root script 配線完全性 (#282)**: aio-guard.js と同 class で theme-init.js / karte-init.js / main.js の `<script src>` 配線を BLOCKING 化。最強根拠は theme-init.js (pre-paint FOUC ガード・除去で未スタイル flash だが behavior e2e 非検査・screenshot advisory ゆえ完全 silent)。error-suppressor.js は inline ゆえ Check 7/7b が担当。**(5) Check 135 — stylesheet 配線 (#283)**: 本 class 最大影響。style.css の `<link rel=stylesheet>` 除去はサイト全体を未スタイル化するが全 gate に silent (behavior e2e=content のみ / screenshot=advisory / consistency 未被覆だった)。存在 (Check 108) と byte 予算 (Check 52/120) は強制済だが `<link>` 配線が盲点だった。mutation_probe も #280→#283 で 11→14 bug class へ拡張 (133/134/135 各々の実回帰を自己検証)。**追加で確認した被覆済 vein (再 systematize 不要): sw.js は runtime SWR ゆえ precache-list drift なし / CACHE_NAME version coherence は Check 19+1 / 残る wiring 候補 (favicon/manifest/sw 登録) は graceful degradation ゆえ silent-critical でなく enforce は padding。** 教訓追補: 「最大影響の gap を先に塞ぐ」(unstyled-site > analytics) / wiring check は『存在』と『配線』の差を突く。
- **「終わりなき改善」phase4 後続 behavioral bug-hunt run (2026-06-24・PR #285〜#286)。**（要点のみ・Check 総数の真値は §9）。前 run の meta-hardening に続き、recently-added 機能 (quiz / command-palette) を批判的に再読して🔴🟡実バグ 2 件を発見・修正・回帰 e2e で systematize。**(1) 🔴 quiz architecture 検索の stakeholder 被覆漏れ (#285)**: architecture quiz は stakeholder の name/quote (CTO「…GAFA…」等) を画面描画するが、検索フィルタ `_filterBy` が title/id/content/situation/question しか見ず stakeholder を対象外＝「画面に見えるのに検索できない」(GAFA/CAP定理/Kafka で 0 件)。OR に `stakeholderMatch` 追加で修正 (q.stakeholders ? ガードで他 3 quiz 不変＝単調増加で非破壊)。**(2) 🟡 command-palette focus trap が Tab で背景脱出 (#286)**: panel は aria-modal=true で docstring も「Tab で trap」と明言するが trapHandler が Tab 未処理。mobile-drawer `__trapFocus` と同じ first/last wrap を追加し docstring drift も訂正。**重要な vacuous-test 回避知見**: cmdk-list `<ul>` は overflow スクローラとして Chromium が暗黙に Tab-focusable にするため Tab 1 回では input→UL で偶然 host 内に留まり区別不能だった (最初の 1-Tab 版テストは fix 有無で両方 pass する vacuous テスト)。**複数回 Tab して最後の focusable を越えて初めて escape を検出できる**。両 fix とも negative test (fix を戻すと該当 e2e が fail) で非 vacuous を実証。**教訓: (i) 「画面に描画する全テキスト」と「検索が対象にするテキスト」は drift しやすい — 新可視フィールド追加時は検索フィルタも同期。(ii) focus-trap の e2e は最後の focusable を越えて Tab しないと escape を検出できず、scrollable container は暗黙に focusable 数を増やす。** 他の検索/modal も監査: ProjectsPage 検索 corpus (name/summary/tags/tech/category) は card 全可視テキストを被覆＝クリーン / modal は cmdk のみ (mobile-drawer は既存 trap)。
- **「終わりなき改善」phase4 後続 shipped-JS 全網羅 bug-hunt + behavior 安全網 自己検証 run (2026-06-25〜28・PR #294〜#299)。**（要点のみ・Check 総数の真値は §9）。shipped JS の全 interactive/stateful モジュール（apps/state/sw/router/store/pages/quiz-renderer/command-palette/ui-components/mobile-drawer/aidk-rails/fatal-overlay/perf-guards/meta-management/components 各 Page）を網羅再読し、🔴🟡実バグ/gap 5 件を発見・修正・各々非 vacuous な BLOCKING テスト/Check で systematize。**(1) Settings demo selector の write-面 coherence gap (#294)**: 手動追加フォームの Demo `<select>` に `notes` が欠落（router/store whitelist は許容するのに手動作成で選べない・#257/#292/#293 と同 class）。`<option notes>` 追加 + **Check 140**（demo selector option == router whitelist。mesh の read 面=palette/sidebar/AppsPage に対する write 面を被覆）+ mutation 20→21。**(2) 🔴 cross-tab 未正規化採用 (#295・#93 class)**: state.js の `storage` リスナーが他タブ store を `data = incoming` で生採用。load()/import が必ず通す正規化を cross-tab だけ省く唯一の未正規化 ingestion 経路で、デプロイ跨ぎの別 schema/欠損 store で FatalPage crash。load() を mirror（schema 不一致は採用見送り=非破壊・一致時のみ validateAndNormalize）+ BLOCKING e2e。**(3) 🟡 quiz section 見出し検索漏れ (#296・#285 class)**: `_filterBy` が画面表示の section 章タイトル（「第4章：可用性とFinOps…」等）を対象外で "FinOps"/"可用性" 検索が 0 件。section 名一致を追加 + BLOCKING e2e。**(4) 🔴 drawer 再 open scroll-clobber (#297・#262 class 別トリガ)**: #menuBtn は #topbar 内＝#app の外で `__setAppInert` の inert 対象外ゆえ drawer 開放中もクリック可能、再 open で `__lockBodyScroll(true)` が body=fixed の scrollY=0 を読み `__drawerScrollY` を 0 上書き→close で先頭ジャンプ。openDrawer に idempotency ガード（command-palette open() と同型）+ BLOCKING e2e。**(5) 🟡 安全網が正常 FatalPage を覆う (#298)**: fatal-overlay の最終安全網（Shadow DOM・全画面）が 2 秒毎に `__fatalError` だけを見て起動するが、__fatalError は FatalPage 描画後もセットされたまま（クリアは「ホームへ」のみ）ゆえ正常な FatalPage を 2 秒後に覆い復旧ボタンをブロック。setInterval 条件に「FatalPage マーカー #fallback-details 不在」を追加し silent-failure 限定化 + BLOCKING e2e。**(6) meta-QA: mutation_probe に `--e2e` モード追加 (#299)**: consistency 安全網だけを自己検証していた mutation_probe に behavior e2e 安全網の検証を追加（`npm run mutation-probe-e2e`）。上記 (2)〜(5) の bug class を再現し対応する特定 e2e が「clean で pass・mutated で fail」の二段で確実に捕捉することを institutionalize（4/4 caught）。本セッションで 4 e2e を追加するたび手動実施した非 vacuous 検証を恒久化。**教訓: (i) read 面の coherence mesh を閉じても write 面（作成フォーム等）が drift する—producer/consumer 双方を縛れ。(ii) 外部入力 ingestion 経路（load/import/cross-tab）は全て同じ正規化を通せ—一つでも省くと #93 class が再発。(iii) Playwright の通常 click は actionability で page を scroll するため sticky 要素のタップ実機挙動は programmatic click で検証・smooth scroll 下では behavior:'instant'+expect.poll。(iv) フラグ（__fatalError 等）は「起きた」と「復旧 UI が失敗した」を混同するな。(v) stateful な open 系関数は idempotency ガードを持て。** screenshot/無関係テストの CI flake は `gh run rerun --failed` 1 回で対処（#296 で 1 回発生）。
- **「終わりなき改善」phase4 後続 肥大化解消 1,000 行しきい値 run (2026-07-04・PR #555〜#559 + main 直 mutation split)。**（要点のみ・Check 総数の真値は §9・詳細は `docs/incident-artifacts/decision-v80-phase4-bloat-reduction-1000-line-threshold.md`）。オーナー提案（受諾）: リポジトリの肝は「全委任で生じる AI 無限改善自走そのもの」で、**肥大化放置がその自走への最大リスク**。**1,000 行を肥大化の目安**とし着実に一つずつ解消し「生じないように」進める（memory [[feedback_bloat_1000_line_threshold]]）。**(1) shipped JS を葉 factory パターン (byte-equivalent) で分割**: pages.js 642→267 (HiringRiskPage→js/hiring-risk-page.js・#556) / apps.js 1,179→824 (AIPage→js/ai-page.js #557・PomodoroPage→js/pomodoro-page.js #558)。**shipped JS は全て ≤824 行**に。**(2) mutation_samples.py を log-rotation 分割** (1,597→870 + archive 742 + common 12・main 直 push)。無限成長する append-log を「新規は tail へ・~900 行超で archive へ rotate」規約で止めた。**(3) 🔴 実バグ fix (#555)**: AIPage の aiLoading 解除を finally で保証 (generateResponse/State.update throw で恒久 submit 不能に stuck する fail-safe)。**(4) Check 361 新設 (BLOCKING・#559)**: shipped `js/*.js ∪ js/quiz/*.js` が file-size-budget.md §4 BUDGET-DATA 登録済みを強制。**Check 71 (registered⟹exists) の対称 (exists⟹registered)** で js-leaf 面 bijection を成し「新 leaf が silent 未予算化して無制限成長」class を機械強制で封じた（1,000 しきい値を memory/convention→BLOCKING Check へ昇華＝「生じないように」の実体化）。BUDGET-DATA も stale 予算を実態+しきい値へ同期。**残存 >1,000 行の disposition (再検討/衝動着手しない・decision record に理由明記)**: check.py 15,700 (#253 net-negative defer) / e2e spec 3,332 (9-check 名前結合ゆえ単純分割は vacuous 化・defer) / style.css 2,178・index.html 1,308・main.js 1,183 (単一 stylesheet / 単一 SPA entry / 保護 kernel の設計制約)。**教訓: (i) merge 後は HEAD が main—次増分の"最初の編集前"に必ず `git checkout -b` (#558 後に main 直 push した slip・memory [[merge-before-new-branch]] に追記)。(ii) 純データの巨大 append-log は log-rotation で「今の size」でなく「無限成長」を止める。(iii) 「生じないように」は doc/convention でなく Check (BLOCKING) で機械強制せよ。** 葉抽出の付随チェックリストは memory [[sed_ampersand_lint_edit]]。
- **「終わりなき改善」phase4 後続 肥大化解消 継続 + 抽出後 dead-param 掃除 + snapshot/a11y bug-hunt run (2026-07-04〜05・PR #555〜#565)。**（要点のみ・Check 総数の真値は §9）。前 run（#555〜#559 で 1,000 行しきい値を確立）を受け、shipped JS の肥大化解消を「一つずつ着実に」継続しつつ抽出の後始末と behavioral bug-hunt を並走。**(1) 肥大化解消の継続完遂**: pages.js から HiringRiskPage→`js/hiring-risk-page.js` (411 行・#556)、apps.js 1,179→824 で AIPage→`js/ai-page.js` (#557) / PomodoroPage→`js/pomodoro-page.js` (257 行・#558) を葉 factory パターン (`createX({deps})` 返り値=page 関数・main.js が deps 注入・byte-equivalent) で分離し **shipped JS を全て ≤824 行**に。PomodoroPage 抽出では stale-closure fix (#121/#134 の `State.get()` live 読み) を温存。**(2) Check 361/362 新設 (BLOCKING)**: 361 = `js/*.js ∪ js/quiz/*.js` が file-size-budget.md §4 BUDGET-DATA 登録済みを強制（Check 71 registered⟹exists の対称 exists⟹registered で js-leaf 面 bijection を閉じ「新 leaf が silent 未予算化で無制限成長」class を機械封じ＝1,000 しきい値を convention→Check へ昇華）。362 = mutation_samples を importlib で読み各 mutation の `find` が対象 file に実在することを verify 時に強制（**CI は mutation-probe を走らせない**ため anchor orphan が silent 化する gap を埋める・#558 で PomodoroPage 抽出により pomodoro mutation の anchor が apps.js→pomodoro-page.js へ移動し orphan 化した実例を systematize）。**(3) mutation_samples.py を log-rotation 分割 (1,597→886 + `mutation_samples_archive.py` 742 + `mutation_samples_common.py` 12)**: 無限成長する append-log を「新規は tail・~900 行超で archive へ rotate」規約で止めた（純データの巨大 append-log は「今の size」でなく「無限成長」を止めよ）。**⚠ この split は #558 マージ後に `git checkout -b` を忘れ main 直 push した process slip（非破壊・dev-tooling のみ・CI 緑だが PR gate 迂回）— memory [[feedback_merge_before_new_branch]] に「merge 後 HEAD=main、次増分の最初の編集前に必ず branch を切る」を追記済**。**(4) 🔴 実バグ 3 件**: AIPage の aiLoading 解除を finally 保証（generateResponse/State.update throw で恒久 submit 不能に stuck する fail-safe・#555）／SettingsPage snapshot 復元が `State.set(snap.data)` で未正規化採用していたのを `State.set(Store.validateAndNormalize(snap.data))` に（#93/#295 の外部 ingestion 全経路正規化 class・別 schema/欠損 snapshot で FatalPage crash・#561）／TodoPage root div に ErrorBoundary/FatalPage 由来の a11y 属性 leak（`class:'error-boundary-fallback' role='alert' aria-invalid aria-errormessage='fallback-details' aria-description`）が混入し正常ページが assistive tech に「エラー」と誤提示されていたのを除去（#563）。各々 BLOCKING behavior e2e 追加（snapshot 正規化 route=`/#/settings`・TodoPage leak 属性 count 0）。**(5) 抽出後 dead factory-param 掃除 (#564/#565)**: leaf 分離でその leaf だけが使う依存が抽出元 factory の destructure に dead param として残る（createApps の AUTHOR/Router/Theme・createComponents の Toast/Brand/Store・components.js の tokenize）。**ESLint no-unused-vars は destructured object param の未使用を捕捉しない**ため CI silent。各 param を `awk -v s=<sig行> 'NR>s' file | grep -c '\bparam\b'` で body 使用 0 を個別確認（正規表現一括検出器は multiline signature で false positive）してから destructure+docstring+main.js 呼び出し引数の 3 箇所から除去。components.js docstring drift（「11 関数」→実 7）も是正。**教訓: (i) 抽出後は必ず抽出元 factory の dead param を awk で確認（ESLint は destructured param を見ない）。(ii) 外部 ingestion（load/import/cross-tab/snapshot-restore）は一つ残らず同じ正規化を通せ。(iii) a11y/role 属性の copy-paste leak は正常 UI を「エラー」と誤提示する—抽出時に root 属性を精査。** 付随チェックリストは memory [[feedback_sed_ampersand_lint_edit]] に集約（package.json lint/lint:js は Edit で・mirror doc・Check 123a/33・main.js 配線・dead-param awk・mutation anchor 追従の 8 項）。**残存 >1,000 行 disposition は前 run と不変**（check.py=#253 net-negative defer / e2e spec 3,332=9-check 名前結合で単純分割 vacuous 化 defer / style.css・index.html・main.js=設計制約）。**次の genuine vein 候補（reflect-then-organize 済・未着手）**: 抽出済ページモジュール（home-page/projects-page/project-detail-page/ai-knowhow-page）の behavioral 再監査（未 bug-hunt 面）／router・store の edge-case／別の vacuous-gate 掘り。
- **「終わりなき改善」phase4 後続 store ingestion-guard + 肥大化 解消/防止セット run (2026-07-05・PR #568〜#574)。**（要点のみ・Check 総数の真値は §9・詳細は `docs/incident-artifacts/improvement-notes-claude-v80-phase4-store-ingestion-guard-and-bloat-ceiling.md`）。前 run が名指しした「router/store edge-case」vein から入り、オーナー再提案（受諾）「肥大化解消と肥大化防止をセットで」を対で実施。**さらに store.js の ingestion-crash class を per-instance fix→構造防止まで完遂**: **(4) 🔴 #572 normalizeProject の array field**（tech/tags/highlights/relatedProjectIds/links を `(raw.X || [])` truthy 判定のみ→非配列で `.filter` TypeError crash・default proj() は Array.isArray 済だが untrusted import normalizer 側が漏れ）を Array.isArray ガードへ／**(5) 🔴 #573 normalizeAppsData の task.tags**（`data.tasks` は Array.isArray 済だが個々 `t.tags` が truthy 判定のみ・store.js の最後の `(X||[]).filter` 未ガード箇所）を修正・既存 #568 e2e を拡張／**(6) #574 Check 364（BLOCKING 構造防止）**: store.js に unsafe `(X || []).<throwing array-method>` idiom が無いことを強制し、per-instance で 3 回潰した class を構造封じ（肥大化 Check 363 と同じ「解消+再発防止」の ingestion-safety 版）。**honest carve-out**: `str.match(...) || []).map`（Array|null 契約ゆえ安全）を regex が誤検出→直前を `\w`（プロパティアクセス末尾）に限定し method-call 結果を除外（Check が実バグでなく設計を捕捉した false-positive を honest に carve-out）。**教訓追加: (v) producer（default builder）が安全でも consumer（untrusted import normalizer）は独立に穴を持つ—両方を縛れ。(vi) per-instance で同 class を N 回潰したら『再混入の構造防止 Check』へ昇華せよ（解消+防止セット）。(vii) 構造防止 Check の regex は安全な同型 idiom（method-call 結果の `|| []`）を false-positive しうる—非 vacuous 実証と同時に false-positive carve-out も honest に行う。****(1) 🔴 store.js 非配列 history ingestion crash 修正 (#568)**: `normalizeAppsData` は総関数契約（tasks/todos は Array.isArray ガード済）だが `ai.history` / `pomodoro.history` だけ truthy 判定のみで outlier。ai.history が非配列（文字列等）だと `.filter` が TypeError→validateAndNormalize 例外→load()(state.js init)/cross-tab(state.js:210)/import/snapshot-restore/settings 正規化ボタンの**全 ingestion 経路が FatalPage crash**（state.js:209 は schemaVersion 一致だけ見て validateAndNormalize を呼ぶため current schema でも再現）。#93/#295/#561 と同一 class の未閉じ枝。両者を Array.isArray ガードへ（非配列は空配列フォールバック・非破壊）。BLOCKING e2e + ai.history mutation で systematize（**honest: pomodoro-only 退行は history 非描画ゆえ e2e で観測不能→捕捉できない mutation は登録しない**）。**(2) 肥大化「防止」= Check 363 新設 (BLOCKING・#569)**: `file-size-budget.md` に `<!-- JS-LEAF-CEILING 1000 -->` marker を単一ソースとして追加し、`js/*.js`（非再帰）ロジック leaf が越えると **BLOCKING で CI 失敗**。Check 52（BUDGET-DATA）は「価値ある bloat を誤ブロックしない」意図的 ADVISORY ゆえ、owner 1,000 行しきい値のハードゲートは別レイヤーで追加（Check 60 型の advisory 早期警告 + BLOCKING ハードゲート二層）。除外: `js/quiz/*.js`（純データ）と main.js（保護 kernel）。storage.js を 1,075 行にパディング→RED を実測（非 vacuous）。**(3) 肥大化「解消」= SettingsPage 抽出 (#570)**: apps.js は AIPage/PomodoroPage 分離後も 837 行で SettingsPage（最大 page ~373 行）が肥大の主因。`js/settings-page.js`（createSettingsPage）へ byte-equivalent 分離し **apps.js 837→458 行**（全 shipped JS ロジック leaf が上限に headroom）。抽出後 dead factory-param（Brand/Store/Storage/slugify）を awk 確認して 3 箇所除去（#564/#565）。**抽出に伴う Check drift を Check 群が機械捕捉→是正**: Check 140（Demo セレクタ探索先を apps.js→settings-page.js 追従）/ Check 362（snapshot-restore + demo-option の 2 mutation anchor 追従・#558 class）/ Check 119（docstring【依存】節）/ 361/363/108。**教訓: (i) 外部 ingestion の各コレクション枝は Array.isArray でガードせよ（総関数契約）。(ii) 肥大化防止は Check 52 の ADVISORY と別の BLOCKING レイヤーとして足す（二層設計）。(iii) 抽出の後始末は verify の RED を潰せば Check（119/140/362/108/361/363）が漏れなく追従を強制する。(iv) mutation-probe-e2e は full suite で 2 分タイムアウト→SIGTERM がファイルを mutated 残留させる（git checkout で復旧・memory [[feedback_mutation_probe_verify_race]]）。** **次の genuine vein 候補（reflect-then-organize 済・未着手）**: 別の vacuous-gate/wiring 掘り／router の深い edge-case（query param / project-detail slug decode / transition-lock replay）／TaskPage/TodoPage/NotesPage の full behavioral 再監査。
- **check.py 分割トラック開始 (2026-07-05・owner 合意 C-first・PR #576〜#579・sustained multi-session track)。**（詳細は `docs/incident-artifacts/decision-v80-phase4-bloat-reduction-1000-line-threshold.md` の Addendum + check-map §0/§4）。owner との議論タイムで「残存肥大化を C-first で解消再開・順序 C→B・A は触らない・全 non-A ≤1,000 達成後に CI で ≤1,000 監査化 (防止 capstone)」を全面合意。**最優先 = `check_repository_consistency.py` (15,913 行)**。自己言及的 (自身の構造を検証する Check 45/70/105 を内包) ゆえ安全プロトコルで段階分割:
  - **確立した分割パターン (Phase 1 #577 + Phase 2 #579 で実証済)**: ①孤立カテゴリ選定 (monolith の共有 computed 変数・global content [html/mainjs/ai2ai/style/mcp_data] に依存せず ROOT/check/warnings のみ使う Check 群) → ②`.github/scripts/checks_<cat>.py` に `def run(ctx):` で移設 (section header は +4 indent・docstring inventory `  N.` も移す) → ③`CHECK_SOURCE_FILES` に module 追加 → ④`_ctx = SimpleNamespace(ROOT, check, read, read_bytes, extract, errors, warnings)` を渡す → ⑤verify で自己整合 Check 45/70/105 が monolith+module 横断で緑を確認 → ⑥`docs/files` mirror (Check 108)。
  - **核心 (なぜ #253 の net-negative を覆せたか)**: `exec()` を使わず `run(ctx)` へ check/errors/warnings を**同一オブジェクト参照で明示注入**する。抽出 Check の `check()` は monolith と同じ errors/warnings list に append する = 合否・BLOCKING 伝播・build 失敗が byte-equivalent。自由変数の静的解決不能/未定義グローバル参照 (#253 の脆さ) が原理的に起きない。実証済: js/storage.js を 1,075 行にパディング→module 内 Check 363 が RED+exit 1、BUDGET-DATA に不在 path→module 内 Check 71 が RED+exit 1。
  - **土台 (#253 で敷済)**: `CHECK_SOURCE_FILES` list + `_aggregate_check_numbers()` が docstring inventory (`  N.`) と section header (`# ── N.`) を全 module 横断集約。Phase 1 で `_sec_re` を `^\s*#\s*──` へ緩和 (run() 内の indented section header 許容)。
  - **進捗**: Phase 1 (#577) = Check 361-364 を `checks_maintainability.py` へ / Phase 2 (#579) = Check 52+71 (file-size governance) を同 module へ join (非連番抽出 + 既存 module join パターン確立)。**monolith 15,913→15,622 行**。
  - **不動点**: Check 45 自身 (`_aggregate_check_numbers` を呼ぶ集約器) は monolith 残置。main.js/html 等の共有 global を直接読む Check は `_ctx` にその content を足す (未実施・Phase 3 で ctx enrich 予定) か module 側で re-read する。
  - **次 (Phase 3+・未着手)**: `_ctx` に html/mainjs/ai2ai/style/mcp_data を足して大カテゴリ (AIO 系 category B 等) を抽出可能にする → 大きな塊を段階抽出し monolith を実質縮小。**一撃分割は依然厳禁・カテゴリごと 1 PR・各 verify=0**。落とし穴: 抽出対象が monolith の共有 computed 変数 (例 `html_v` を後続 check が使う) に依存すると壊れる→孤立カテゴリを選ぶ (consistency check が即 NameError で捕捉するので試して revert 可)。honest 観測: Check 52 advisory が check.py の budget 4,750 超過 (実 15,622) を warning 中=分割で縮小すべき bloat の live signal。
- **肥大化解消 完遂 → behavioral-first フェーズ移行 (2026-07-08〜11 引き継ぎ書 + 2026-07-12 behavioral run)。**（Check 総数の真値は §9・数値 drift 時も §9 が正）。**肥大化解消は機械強制可能スコープで terminal 到達** (check.py 15,913→786 行 / 実 Check は 53 `checks_*.py` module へ分散 / e2e spec 3,475→16 テーマ / Check 365 capstone で全非 A テキスト ≤1000 を BLOCKING)。もう padding せず蒸し返さない面。詳細は `docs/incident-artifacts/improvement-notes-claude-v80-phase4-bloat-elimination-complete-and-behavioral-direction-handoff.md`。**次フェーズ = behavioral correctness (実バグ狩り) 主軸 + vacuous-gate/wiring 下支え + 実態↔記述 drift 是正。** 2026-07-12 の behavioral-first run で 2 genuine 増分:
  - **🔴 #684: quiz 検索語が reload 毎に silent に失われる producer/consumer persist drift を修正 + Check 373。** QuizPage は検索語を `State.updateSilently(s => s.appsData.quizSearch = val)` で永続化書き込みし init で読み戻して復元するのに、store.js `normalizeAppsData` が tasks/todos/pomodoro/ai/notes だけ preserve し `quizSearch` を drop していた (全 ingestion 経路が通る normalize チョークポイントでフィールドが読み戻されないと reload 毎に default へ silent リセット・#294/#568 と同 class)。`CONSTANTS.LIMITS.QUIZ_SEARCH` 追加 + normalize に preserve。**Check 373** = defaultAppsData 全 top-level key ⟹ normalizeAppsData が `data.<key>` で読み戻す、を brace-parse で BLOCKING 強制 (行コメント除去=vacuous pass 防止)。BLOCKING 回帰 e2e が fix 除去で RED を実測。
  - **#685: settings-page.js importJSON を normalize-before-adopt へ整合 + Check 374。** importJSON は生 parsed を `State.update` で adopt → 後 normalize だった (strict モードの `s.projects = parsed.projects` 生代入が malformed entry を SettingsPage の dereference へ通し crash させうる構造)。**honest: 実測では render-abort ordering (`await yieldToMain()` 後に SettingsPage を呼ぶため 2 度目の normalize render が 1 度目の生 render を到達前に abort) で偶発的に守られており実バグではない** (回帰 e2e が未修正でも PASS で確認・捏造しない)。だが data-safety を描画順に依存させるのは脆く、restoreSnapshot が従う「外部 ingestion は adopt 前に正規化」(#295/#561) に importJSON だけ違反していた consistency gap。マージ結果を validateAndNormalize してから単一 State.set へ整合 (upsert/append 挙動 + #192 Map 集約は byte-equivalent 維持)。**Check 374** = importJSON 本体が `State.update(` を含まず validateAndNormalize を通す、を brace-parse で BLOCKING 強制 (Check 130 の ingestion 版)。
  - **honest に clean と判定した監査面 (再監査不要)**: router.js (slug decode try/catch #270 済 / transition-lock replay 済 / trailing-slash filter) / command-palette.js (IME guard #255 / focus trap #286 / idempotency) / ai-page.js (#555 finally / #230 bound / IME) / notes markdown renderer (innerHTML-free・text ノードのみ・無限ループ/ReDoS なし) / brand.js (persist round-trip 整合) / FatalPage marker (#298 e2e 済)。**producer/consumer persist round-trip lens が 2 連続で高収率だった** — store 外の brand/theme も確認済クリーン。**教訓**: 手動 mutation テストの復元に `git checkout <file>` を使うと同ファイルの未ステージ fix も巻き戻る (#684 で 1 サイクル浪費・memory [[feedback_git_checkout_reverts_unstaged_fix]]。復元は python 再挿入か先に git add)。**次の未監査 vein 候補 (reflect 済・非拘束)**: 抽出済み static ページ (home/ai-knowhow/hiring-risk/role-split) の behavioral 再監査 / e2e vacuous-gate 棚卸し (direction②) / store.js autoRelatedCandidates 境界。
  - **同 2026-07-12 run の継続 (#687〜#692・オーナー「止めるな」指示で自走継続)**: behavioral 掃引後、**used⟹defined wiring lens** と **cross-layer magic-number lens** が高収率で 4 Check 追加 — **Check 375** (createIcon('X') の全 literal 名が getIcons() registry に解決・未定義は silent 空アイコン) / **Check 376** (data-action="X" が ActionDelegator `_handlers` に解決・未登録は silent no-op) / **Check 377** (router の非 app route.name が main.js `case` を持つ・欠落は silent 404・route mesh 58/137 の非 app edge を閉じ router→main.js→e2e→PAGE_META chain 完成) / **Check 378** (CONSTANTS.MOBILE_BREAKPOINT ↔ style.css `.sidebar{display:none}` @media の JS↔CSS breakpoint 一致・drift で sidebar+topbar 同時表示の broken layout)。**🔴 実バグ #692**: pomodoro 設定 onchange が work だけ idle 中 remainingSec を即更新し short/long は欠落 (「1 ケース処理・他を忘れる」asymmetry)→休憩モード idle で設定変更しても表示が古いまま。work と対称化 + BLOCKING e2e + mutation。**#690 = mutation_samples.py を log-rotation で 954→899 行** (Check 373-378 の mutation 追加で advisory budget 超過→最古 Check 260-268 を archive へ)。**オーナー裁可 2 件**: (a) CSS dead utility class ~30 (absolute/relative/grid-cols-3 等が style.css 未定義=検索アイコン overlay が inert 等) → **「現状維持」** (§3(B) cosmetic 許容)。(b) linkedTaskId は「フォーカス対象タスクを記録」と project 説明で謳うが SET UI 未実装 (常に null)=claim↔実装 drift → 説明は**凍結公面**ゆえ leave (plumbing 除去は乖離悪化)。**教訓**: (i) coherence-Check を連投しすぎると padding 化しかねない→意図的にレンズを切替 (wiring→cross-layer→behavioral asymmetry)。(ii) OK 行数は非強制 volatile で +1/Check にならない (Check 47 等の動的展開)→§9 は実測値を書く。(iii) 「1 ケースだけ処理・他を忘れる」asymmetry は実バグの温床 (pomodoro work vs short/long)。**掃引済み確認 (再監査不要)**: sw.js (SWR/#84/#270/#131 全修正) / perf-guards (vestigial 除去済) / theme.js (system-follow) / BGM / meta-management (Speakable=Check 147) / leaf dead-code 0 / e2e 全ルート訪問 (__fatalError null + NotFound fall-through 検証で #96-99 vacuous 封じ済)。
  - **同 2026-07-12 run の後続 (#695〜#697・mutation-integrity mesh 完成 + store recommendation 監査)**: mutation 安全網自体の completeness を機械強制する mesh を完成 — **Check 379** (E2E_MUTATIONS の `test` field が実在 e2e テスト title の substring であることを強制・orphan test-anchor が silent SURVIVED を生む gap を封じ) / **Check 380** (全 mutation の `find != replace` を強制・no-op mutation は必ず SURVIVED する偽陰性ゆえ構造禁止)。既存 **Check 362** (find-anchor が対象 file に実在) と合わせ「mutation の find/replace/test 3 面が全て実体に解決する」を BLOCKING 化。**重要な落とし穴 = mutation self-reference trap**: mutation_samples.py 自身を検証する Check (379/380) の consistency mutation は登録不可 — mutation_probe の `replace(find, replace, 1)` (first-only) + 自己参照 find がファイル前方の自 entry を先に打ち実 target 無傷＝機能しない偽 SURVIVED。手動非 vacuity (replace-all→RED→保存コピー復元) で担保し非登録理由を NOTE 明記 (memory [[reference_mutation_self_reference_trap]])。**store.js recommendation subsystem を behavioral 監査し clean 確認 (再監査不要)**: `autoRelatedCandidates` (target/projects 型ガード + target は常に normalize 済 state.projects 由来ゆえ `new Set(relatedProjectIds)` 安全) / `similarityScore` (a/b null ガード + `Math.max(0,Math.min(1,...))` clamp) / `jaccard` (`Array.isArray` ガード) / `tokenizeForSimilarity` (`String(s||'')` + regex + length≥2 filter)。`deleteProjectHard` の dangling ref (削除 project id が他 project の relatedProjectIds や hiddenIds に残留) は ProjectDetailPage の optional-chaining filter + user 追加 project の relatedProjectIds=[] ゆえ graceful に無視され実害なし=clean。**教訓**: mutation 安全網を検証する Check は「その Check 自身を破る mutation」を登録できない (自己参照 trap)—手動非 vacuity + NOTE で担保せよ。
  - **同 2026-07-12 run の後続 a11y-behavioral + coverage-gap run (#698〜#701)**: behavioral bug-hunt が clean 収束後、**「新規追加機能に gap」+「test-coverage-gap」レンズが高収率**で 3 増分。**🟢 #699 実 a11y 修正**: command-palette は focus を input に留め ↑↓ で listbox 操作する combobox だが `aria-activedescendant` 等が無く、Cmd+K を arrow 操作する SR ユーザーに active option がアナウンスされなかった (option の aria-selected は focus 非移動ゆえ一部 SR で読まれない)。input へ `role=combobox`/`aria-controls=cmdk-listbox`、listbox へ `id`、各 option へ `id=cmdk-opt-<i>` を付与し `_renderList`/`_highlight` が `aria-activedescendant` を active option へ同期 (候補ゼロは removeAttribute)。非破壊 (ARIA 属性のみ・視覚不変)。**静的 onclick-a11y Check は非採用** (listbox option の arrow+Enter / backdrop の Escape という正当な非 button onclick を false-positive し clean code を RED 化するため・handoff の brittle-gate 禁止教訓)。**🟢 #700/#701 coverage-gap**: theme='system' の runtime OS テーマ追従 (matchMedia change listener) を `emulateMedia` で / command-palette の close-focus-restoration (WCAG 2.4.3・close の lastFocused.focus()) を `.skip-link` 起動元で、各々 BLOCKING behavior e2e 被覆 (listener/復元を除去すると RED を mutation 実証・非 vacuous)。**#698**: §7 に mutation-integrity mesh 完成 + store 監査を記録。**再監査不要と確認した被覆済み面**: notes markdown 描画 (h1/strong/code/li)+永続 / AI keyword routing (troubleshoot/design/general)+loading+aria-live / drawer close-focus-restore (menuBtn) + Tab trap / projects tag-filter+検索 lifecycle / settings export(full+partial)+snapshot restore 正規化(apps-task.spec:338 #561) はすべて既存 e2e 済み。meta-management (textContent ゆえ `</script>` XSS なし・soft-404 noindex 正) / aidk-rails ActionDelegator (closest で子 click 捕捉・try/catch・Check 376) / router (slug decode try/catch #270・query は URLSearchParams) も clean。**教訓**: (i) 「新規追加機能」は既存 Check/e2e 網の後から入るため gap が残りやすい—cmdk (A 群) が ARIA 不完全 + focus-restore 未被覆だった。(ii) a11y invariant は keyboard 操作性が native要素/Escape/arrow-nav の複数パターンから来るため静的 Check で clean を false-positive せず区別できない—brittle Check より behavior e2e で被覆せよ。(iii) 重い処理 (full verify+playwright+mutation を毎増分インライン + 同期 `gh pr checks --watch`) はトークンを大量消費する—軽い逐次サイクル + CI/merge を background yield する [[feedback_self_driving_loop]] の cadence を守れ。
- **「終わりなき改善」meta-QA integrity + hidden-listing mesh run (2026-08-09・PR #883〜#886)。**（要点のみ・Check 総数の真値は §9）。引き継ぎ受領後、test-coverage → meta-QA → behavioral のレンズ回しで 4 増分。**最重要成果: 🔴🔴 安全網を検証する道具そのものが vacuous だったのを発見・修正 (#885)。** `mutation_probe.py` (consistency mode) は mutation 適用後に `run_gate() != 0` を「捕捉」としていたが、**mutation を当てるとその mutation 自身の find-anchor が対象 file から消える**ため **Check 362 (anchor 解決) が必ず RED になる** → 意図した Check が 1 つも発火しなくても全 mutation が自動的に caught と報告されていた (Check 362 導入以降ずっと)。どの Check も見ない inert prose を対象にした**対照 mutation**で gate の error が Check 362 の 1 件のみになることを実測して確定。catch を「Check 362 以外の error が ≥1」へ帰属修正し、traceback 停止は **CRASHED** として SURVIVED と区別。**帰属を正した瞬間に実 gap が 1 件露出**: `Check 343 (.well-known JSON parse)` が SURVIVED — mcp.json を壊すと module-level の生 `json.loads` が JSONDecodeError を投げ **suite 全体が traceback で停止**し、まさにその破損を検出するために書かれた Check 343 が一度も走っていなかった (exit 1 で merge は止まるが診断が actionable でなく、crash 地点以降の全 Check が skip され他の drift を masking する)。fail-soft (空 dict degrade) 化 + **Check 399** (probe の catch 帰属維持) + **Check 400** (monolith の module 直下に非保護 json.loads/yaml.safe_load を置かない) で構造封じ。修正後の full probe = **All 277 caught / 0 SURVIVED / 0 CRASHED**（安全網が初めて honest に検証された）。**#884**: consistency の Result block が errors 側だけ `::error::{e}` で本文列挙し warnings 側は件数のみだった asymmetry を修正 (ADVISORY は 56 箇所・13 module あるのに「どの invariant が緩んだか」がローカル/CI ログ双方で読めず、**読めない advisory は実質 vacuous な助言層**だった)。`::warning::{w}` 列挙 + **Check 398** (loop 変数束縛で検証・literal 有無だけでは vacuous PASS しうる)。**🔴 #886 実バグ**: 「非表示」(projectPrefs.hiddenIds) を読んでいたのは ProjectsPage と SettingsPage だけで、**home の注目枠 / 詳細ページの推薦 (関連・おすすめ) / Cmd+K 候補 / カテゴリ選択肢**は素の state.projects から描いていた。default は削除不可 (「デフォルトは非表示のみ」)＝非表示が唯一の非公開手段なのに、既定 featured の p01 を隠しても**トップ最上位の注目枠に出続ける**。4 面すべてに適用し、全件非表示は現実に起こりうるため featured を null 許容 + fallback 描画 (無条件 dereference の FatalPage crash 回避)。直接 URL 閲覧は従来どおり許容 (hidden は listing の可視性制御でアクセス制御ではない)。**#883**: `autoRelatedCandidates` の「自分自身 + 明示 related」除外 invariant を e2e 被覆 (p01 が related=[p02,p03,p04] を実際に持つため構造的に非 vacuous)。**教訓**: (i) **「gate が RED になった」と「意図した Check が捕捉した」は別物** — mutation/negative test の非 vacuity は *どの* Check/assertion が落ちたかまで帰属せよ (対照 mutation = どの Check も見ない inert な変更 が帰属の検査に効く)。(ii) traceback で落ちる gate は「Check が働いた」ではない — その Check は一度も走っておらず、以降の Check も全て masking される (module-level parse は fail-soft に)。(iii) errors/warnings のような対の経路は片方だけ実装が進む asymmetry を起こす (§7 反復 class)。(iv) 可視性フラグ (hiddenIds) は **read 面 mesh を全部** 閉じよ — 一覧だけ塞ぐと home/推薦/palette から漏れる。**次の未採掘 vein (reflect 済・非拘束)**: e2e vacuous-gate 棚卸し (絶対数の少ない assertion / 同一ラベル poll) / ai-knowhow・hiring-risk・role-split の静的ページ behavioral / router の query-param edge。
- **「終わりなき改善」Check 検出穴スイープ + persist mesh 完成 + a11y run (2026-08-09・PR #888〜#904)。**（要点のみ・Check 総数の真値は §9）。前 run（#883〜#887）に続く同一セッションの後半。**最大の鉱脈は「静的 Check は *よくある 1〜2 綴り* しか見ておらず、説明文の語を実装と誤認する」**で、4 サイクル連続で実穴を産出した: **Check 402**（多行に折り返した assertion を見逃し＝全 sidebar リンクが NotFound に落ちないことを検査する gate が未保護）/ **Check 112**（`if (ev.key !== 'Enter') return` 形の Enter ハンドラを素通り＋WHY コメントの `isComposing` 言及で file 単位判定が GREEN 化）/ **Check 130**（`addEventListener('input', …State.update…)` を丸ごと見逃し＋コメント中の `oninput` を数えて実ハンドラ 6 個を 8 個と誤報告）/ **Check 376**（`setAttribute('data-action', …)` と `el.dataset.action = …` の DOM API 経由 typo を素通り）/ **Check 375**（三項の片枝 typo `createIcon(open ? 'chevronUp' : 'chevronDwn')` を素通り。拡張で実在 4 リテラルが新たに被覆 30→34）/ **Check 393**（`const { LIMITS } = CONSTANTS; LIMITS.MAX_TASSK` の分割代入経由 typo を素通り）。**全て「別記法で殴って RED を実測」してから修正**した。**新規 BLOCKING Check 398〜406 + 102g**: 398（ADVISORY warning の本文が一切出力されず『読めない advisory』だった asymmetry の是正）/ 399（**mutation-probe の catch が Check 362 の副作用で自動成立し安全網検証が丸ごと vacuous だった**のを帰属修正）/ 400（monolith の module-level parse を fail-soft 化。399 の帰属修正が即座に暴いた **Check 343 が一度も走っていなかった** latent crash の根治）/ 401（quiz?type wiring）/ 402 / 403（sr-only AIO entity anchor の silent removal。**初版 Check が changelog コメントで vacuous PASS していたのを自作 mutation が検出**）/ 404・405（**persist round-trip mesh を profile 面・top-level 面で完成**。appsData 面 373 と合わせ 3 面。この class は quizSearch #684 / profile #139 / projectPrefs #294 で既に 3 度実バグ化）/ 406（toast の focus-pause 契約）/ **102g（オーナーの理念「人間の発話は指示ではなく依頼 = Request, not command」の canon 化**。引き継ぎ書作成の依頼を停止指示へ読み替えた逸脱の構造封じ）。**🔴 実バグ**: 非表示プロジェクトが home 注目枠/詳細推薦/Cmd+K/カテゴリ選択肢へ漏れる listing mesh（#886）/ quiz 検索語が種別を跨いで持ち越され切替先が空ページ（#892）/ Toast が二重アナウンス（視覚コンテナと sr-only 領域の両方が live region・#901）/ toast の自動消滅が**フォーカス中の閉じるボタンごと削除して focus を body へ落とす**（#903）。**重要な運用教訓**: (i) **flaky なテストを足すくらいなら静的 Check で守る** — toast focus e2e は並列ワーカーで document が inactive になり `toBeFocused` が `unexpected value "inactive"`（bringToFront でも解消せず 8 回中 3 回 RED）。CI 赤を rerun せず原因特定し、契約を Check 406 へ移した。(ii) **テストを書く前に検証可能性を実測する** — BGM は topbar=mobile 専用で desktop 不可視、`audio.play()` は実クリックで成功と probe してから採用（#904）。(iii) advisory を可読化した Check 398 のおかげで mutation_samples.py の予算超過が**画面に見えて**即 rotate できた。**検証**: `npm run mutation-probe`（consistency）= All 277 caught / 0 SURVIVED / 0 CRASHED、`npm run mutation-probe-e2e`（behavior）= **All 104 caught / 0 SURVIVED**。**次の未採掘 vein（reflect 済・非拘束）**: 記法穴レンズは 6 件で一巡し打ち止め／topbar・focus 依存 e2e・ContactPage profile 描画・`javascript:` サニタイズ・`.only` gate は**実測して既存被覆済みと確認**（再監査不要）／候補は apps 間の相互作用、router の query-param edge、observability。
- **「終わりなき改善」a11y + robustness + 肥大化予防 run (2026-08-09・PR #906〜#919)。**（要点のみ・Check 総数の真値は §9）。前 run（#883〜#905）に続く同一セッション後半。**レンズは a11y → robustness → 予防**へ回した。**a11y（5 件）**: quiz 検索のヒット件数アナウンス（0 件時だけ喋りヒット時は無言だった非対称）/ task・todo フィルタ変更の SR 通知（実測で完全無音・直前アクションの文言が残留していた）/ Toast の二重アナウンス解消と `announce()` への一本化 + **Check 407**（単一 writer 契約。ai-page.js だけが直書きで迂回していた bypass を検出・是正）/ quiz と settings の**フォーム検証エラー**を `aria-invalid` + focus で識別可能に（WCAG 3.3.1。Toast だけでは「どのフィールドが不正か」が SR に伝わらなかった）。**robustness（2 件・いずれも「実装は正しいが守られていない性質」）**: localStorage アクセス自体が SecurityError を投げる環境（private mode 等）で SPA が描画されること／`crypto.randomUUID` 不在（**http:// の LAN プレビュー = スマホ閲覧経路**）でも項目追加が動くこと。どちらも現行実装は耐えていたが e2e 未被覆で、ガードを失うと**その経路のユーザーだけ**が白画面/機能不全になり CI も気付けない状態だった。**test-coverage（4 件）**: home 統計カウンタ（3 つとも state 由来なのに未被覆）/ Resume の profile.title 描画（#/resume は内容ゼロ被覆だった）/ **プロジェクト → 実デモの起動導線**（主要ジャーニーなのに未被覆。Check 136 は demoRoute 値の静的検証のみで遷移は見ない）/ 未使用アイコン検出。**肥大化の「予防」確立**: Check 365（1,000 行 BLOCKING）に **2 サイクル連続**で当たった反省から、e2e spec 19 件を BUDGET-DATA へ登録し **Check 408** で登録漏れを禁止 → **advisory 警告 → blocking 上限**の二層に。直後に `apps-task.spec.js (973 > 900)` が警告され、**BLOCKING を踏む前に先回りで** Task/Todo 分割（#916）できた（それ以前の apps-settings.spec.js 分割は「当たってから」の対処だった）。**dead weight**: 未使用アイコン 16 件 = 1,996 bytes を除去し **Check 375b**（定義 ⟹ 使用）で再蓄積を禁止（git -S で全件 never-wired を確認してから削除）。**Check 119b**: docstring が**実在しない依存を宣言する**逆方向 drift を封じた（#908 で自分が踏んだ: 編集スクリプト中断で docstring だけ先行更新され 119 は GREEN だった）。**運用教訓（重要）**: (i) **flaky なテストを足すくらいなら静的 Check で守る** — toast の focus e2e は並列ワーカーで document が inactive になり `toBeFocused` が `unexpected value "inactive"`（bringToFront でも解消せず 8 回中 3 回 RED）→ 契約を **Check 406** へ移した。(ii) **テストを書く前に検証可能性を実測する** — BGM は topbar=mobile 専用で desktop 不可視、`audio.play()` は実クリックで成功と probe してから採用。(iii) **登録した mutation は必ず RED を実測せよ** — randomUUID の feature-detection を外す mutation は**外側 try/catch が吸収して no-op = vacuous** だった（フォールバック本体を潰す形へ差し替えて RED 実測）。規律が無ければ vacuous な mutation を安全網に紛れ込ませていた。(iv) focus 検証は `toBeFocused()` でなく `document.activeElement` を evaluate で読むと並列でも安定する。**次の未採掘 vein（reflect 済・非拘束）**: data-action 3 producer・テーマ永続・SW/matchMedia/startViewTransition のガード・profile 各フィールドの描画は**実測して被覆済みと確認**（再監査不要）／候補は apps 間の相互作用、observability、research 適用。
- **「終わりなき改善」機械可読面 bug-hunt + meta-QA 記法穴 run (2026-08-09〜10・PR #922〜#930)。**（要点のみ・Check 総数の真値は §9）。前 run（#906〜#919）に続く同一セッション後半。**最大の収穫は「機械向けに宣言している契約が、実は一度も成立していなかった」class**を 2 件掘り当てたこと。視覚に一切出ないため screenshot も behavior e2e も consistency も捕捉できず、しかも壊れているのは本プロジェクトの中核賭け金である AIO/agentic 面という、最も見つかりにくく最も痛い場所だった。**🔴 #929 WebMCP ツールの DOM 抽出が一度も成功していなかった**: `extract_human_vs_ai_role_split` は説明文で「役割分担表の現在の DOM 状態から証拠データを抽出します」と宣言しながら、走査する `.role-split-item` / `[data-ai-role]` が **どちらも querySelectorAll 自身以外にリポジトリのどこにも存在しなかった**（実測）。常に静的フォールバックを返し `format=json` 分岐は到達不能だった。js/pages.js splitRow に機械向け安定フック `data-ai-role`（split-row/human/ai）を描画し、幻の class は「実在させる」のではなく**除去**した（存在しない class を後から作るのは実態に嘘を合わせる行為）。**Check 411** = main.js の `querySelectorAll` セレクタが js/ の実描画に解決することを BLOCKING 強制（導入した瞬間に幻セレクタを検出）。走査結果 0 → 24 要素。**#930 ルート追従 JSON-LD を守る gate が皆無**: `_installSemanticDriftGuard`（MutationObserver で route 遷移ごとに structured data を書き直す機構）は IIFE ごと消しても全 gate が緑だった。契約は「注入されること」ではなく「**ルートに追従して文脈一致すること**」ゆえ 2 ルートを渡って name が h1 に追従することを assert（#133/#134/#135 の silent-critical 配線 class の AIO 面）。**🔴 #926 URL の `?type=` がプロトタイプ継承キーで FatalPage crash**: `QUIZ_DATA_MAP[quizType] || fallback` は `constructor`/`toString`/`__proto__`/`valueOf`/`hasOwnProperty` に対し **truthy な非 config 値**を返すため fallback が発火せず、`Object.keys(undefined)` が throw していた。page-meta の `map[type] || 'Quiz'` も同形で document.title が `"function Object() { [native code] }"` に化けていた。own-key 検証へ是正（#350 と同族だが crash まで至る点で重い）。**🔴 #924 入力上限の欠落による silent truncation**: 保存側は `slice(0, LIMITS.<KEY>)` するのに `maxlength` がリポジトリ全体でゼロ。**Markdown ノートだけが完全に silent**（textarea もプレビューも超過分を表示し続けるのに保存されず、**リロードで初めて消失が判明**）。**Check 410** = UI レイヤーが LIMITS で slice するなら同 file に同定数の maxlength を要求。**meta-QA の記法穴を 2 件**: #925 Check 395 が literal `Router.navigate('X')` しか見ず、本リポジトリでより一般的な**データ駆動 `path:`**（sidebar 14 + hiring-risk 3 = 強制対象 19 のうち 12）を素通りしていた／#927 Check 379/397/151 が引用符リテラル題名しか parse せず、**リポジトリで最も重要な安全網**（security-proxy の ALL_ROUTES route-render loop / a11y-axe の route loop）に mutation を一切登録できなかった（解決不能で false RED）。後者の実害証明として route-render gate に初の mutation を登録（CLEAN=17 passed / MUTATED=1 failed）。同 PR で **mutation-integrity クラスタを `checks_mutation_integrity.py` へ分離**（checks_maintainability.py 1,005 → 709 行。docstring を足した時点で Check 365 の 1,000 行上限に当たったため、圧縮で誤魔化さず「いま触っているクラスタ」を切り出した）。**#928 a11y**: quiz の装飾絵文字（章アイコン 🏛️ 等 / ゾーンラベル接頭 📋💬🎯）がアクセシビリティツリーに露出し、SR が全章・全問で「classical building」等を読んでいた（WCAG 1.1.1。**axe は装飾テキストの露出をルール化していない**ため behavior e2e が唯一の gate）。**#923 test-coverage**: 検索×カテゴリの AND 合成が個別 deep-link しか被覆されておらず「一方が他方を上書き/リセットする」class が素通りしていた（件数をハードコードせず「併用 < 各単独」の相対 assert で両方向の退行を捕捉）。**教訓**: (i) **機械向けの宣言（ツール説明文・structured data・docstring）は実測せよ** — 視覚に出ない契約は全 gate を素通りし、宣言と実態の乖離が何年も残る。「そのセレクタは実際に何件マッチするか」を一度走らせるだけで判る。(ii) 外部入力を object の添字に使うなら **own-key 検証**（`|| fallback` はプロトタイプ継承キーに対して無力）。(iii) 「入力できる範囲」と「保存される範囲」は同じ定数から導け。(iv) **静的 Check は自分が見ている綴りしか見ていない** — 別記法で殴って RED を実測してから「守られている」と言え（本 run だけで 2 件、前 run と合わせ 8 件目）。(v) 上限（Check 365）に当たったら圧縮で誤魔化さず、**いま触っている塊**を切り出すのが最も筋が良い。**次の未採掘 vein（reflect 済・非拘束）**: apps 間の相互作用／observability／research 適用。**実測して clean と確認済み（再監査不要）**: task/todo フィルタの control↔state 同期・theme/brand の永続値検証・quiz 検索 corpus の全可視テキスト被覆・projects/task のアクセシビリティツリー構造・操作後状態（0 件/検証エラー/Toast 表示中）の axe・動的 JSON-LD の `url` がサイトルートなのは hash-SPA の単一 canonical 戦略と整合（バグではない）。
- **「終わりなき改善」overlay/modal 非対称 + 履歴 UX + 安全網 CI 化 run (2026-08-10・PR #932〜#952)。**（要点のみ・Check 総数の真値は §9）。前 run（#922〜#930）に続く同一セッション後半。**🔴 実バグ 6 件**を実測で掘り当てた。**(1) #940 別タブの更新が編集中テキストと focus を破壊**: storage イベントの採用は notify=全再描画を伴うため、別タブでタスクを 1 件追加しただけでこちらのタブの書きかけ notes が巻き戻り activeElement が body へ落ちていた（#258 の focus 破壊 class の外部イベント起点版）。編集中は採用を **blur まで延期**（破棄ではないので更新は失われない）。**この修正が既存 cross-tab テストを『何を壊しても緑』に鈍らせた**のを mutation で発見し、テスト側に blur を足して本来の経路を復元した。**(2) #945/#946 二重モーダル**: drawer 表示中に Cmd+K（およびその逆）で aria-modal 領域が 2 つ同時に有効になり、Escape 1 回で両方閉じていた（`preventDefault` は同一要素上の他リスナーを止めない）。**片方向だけ直した #945 の後、逆順を実測して残り半分を #946 で塞いだ** — 修正を入れたら必ず対称ケースを実測せよ。**(3) #947 palette が背景を inert 化していなかった**（drawer はしていた非対称）。実装複製を避け drawer の `__setAppInert` を単一実装として公開・注入。**(4) #948 scroll-clobber 2 件**（うち 1 件は #945 で自分が入れた回帰）: `closeDrawer` に idempotency ガードが無く、閉じている drawer を閉じるだけで `scrollTo(0,0)` が走っていた（`openDrawer` 側 #297 ガードの対）。加えて close の focus 復元が素の `focus()` でページ先頭へ飛んでいた（`preventScroll` へ）。**(5) #951 詳細ページの「← 一覧に戻る」が絞り込みを捨てていた**: ブラウザ Back は query 付き URL へ復帰して保持するのに、in-page ボタンは `navigate('projects')` ハードコードで全件へ戻る（1 件 → 18 件）＝**同じ意味の操作なのに結果が違う**。Router が直近の一覧ルートを query 込みで保持する単一ソースへ。**(6) #926 系の残り**は前 run 参照。**test-coverage の実証的 gap**: #952 テーマボタンは sidebar（render 再構築）と topbar（`apply()` 直接更新）で**更新機構が別**なのに、既存テストが `:visible` の `.first()` を見るため desktop では sidebar しか検証しておらず、**topbar のラベル更新を丸ごと削除しても既存 10 件が全緑**だった（mobile viewport 固定で被覆）。#950 ブラウザの戻る/進むは **e2e 完全未被覆**だった（フィルタが履歴を汚さない=`replaceSilently` の性質も同時に固定）。#939 4 アプリの状態が同時にリロードを跨ぐ cross-app 統合。**安全網の CI 化**: #941 で週次 `mutation-probe.yml` を新設（consistency + behavior を全件走らせ SURVIVED があれば落とす）。**PR CI に載せない**のは behavior 側が 20〜30 分かかるため。#944 で `workflow_dispatch` により**実 CI で全 step 緑を実測**（run 31331210733 / 34m18s / 425 mutations ≒ 1 件 5 秒）。**機械可読面**: #929 WebMCP の DOM 抽出が**一度も成功していなかった**（走査セレクタがリポジトリのどこにも存在せず常に静的フォールバック）→ `data-ai-role` フックを描画し Check 411 で強制。#930 ルート追従 JSON-LD は消しても全 gate が緑だった → 契約は「注入されること」でなく「**ルートに追従して文脈一致すること**」として e2e 化。#932 aio-guard の自己修復は配線だけ守られ動作は未検証だった。#933 Check 412（JSON-LD の @id 参照解決）/ #934 Check 124 の走査範囲を導出化（**匿名性ガードが守るべき面の大半を見ていなかった**・3→28 file）/ #935 Check 411 も同様に導出化（自己監査）。#938 **「真値」を名乗る runbook §9 の baseline が古かった**のを是正し Check 413（ADVISORY）で可視化（閾値を ±3% にすると動機となった 12 件 drift を見逃すため絶対 5 件へ締めた）。**教訓（本 run の中心）**: (i) **テストは「壊れる」より「鈍る」形で失われる** — 挙動を変える修正は既存テストの前提（focus 状態・viewport・locator）を黙って崩す。修正後は関連テストの非 vacuity を測り直せ。(ii) **不変性の検査に `expect.poll` を使うな** — 最初の観測で成功して以降の変化を見逃す（実際に 2 つの mutation が両方素通りした）。settle 後に 1 度だけ読む。逆に**変化**の検査には poll が正しい。(iii) **同じ責務に複数の実装機構がある**ものは片方だけテストされる穴が空く（`:visible`+`.first()` がそれを作る）。(iv) `offsetParent` は fixed 要素で常に null＝可視判定に使うな。これら 8 件を **`docs/files/playwright.config.cjs.md` に集約**した（#949）— e2e を書く前に読む 1 箇所。**次の未採掘 vein（reflect 済・非拘束）**: apps 間のより深い相互作用 / research 適用 / observability。**実測して clean と確認済み（再監査不要）**: palette の閉じる 3 経路の後始末・drawer 内 nav リンクの導線・nav link の sidebar/drawer 共有実装・不正 percent-encoding の query・SW の AIO 専用設計（`caches.keys()` が空なのは正常）・公開反映（2026-08-10 に `fresh` を初観測）。
- **「終わりなき改善」週次 probe の実穴を根治 + cross-app 実バグ run (2026-08-10・PR #979〜#986)。**（要点のみ・Check 総数の真値は §9）。**本 run は「観測できるようにしたら実際に穴が見つかった」連鎖が中心**で、前 run で足したバッジがその週のうちに元を取った。

  - **🔴 週次 mutation-probe が赤く、4 件の behavior mutation が SURVIVED していた**。PR CI では走らないので、#964 で STATUS.md にバッジを出していなければ**気付かないままだった**。内訳と根治: **(a) probe が的を外していた（2 件）** — `mutation_probe` は `replace(find, replace, 1)` で**先頭 1 件しか置換しない**のに、`Router.navigate(Router.getLastListPath ? … : 'projects')` は project-detail-page.js に **2 箇所**（not-found 分岐と実ボタン）あり、**not-found 分岐だけが壊れて**テストが通り続けていた。これは「安全網に穴がある」のではなく **「穴を探す道具が的を外している」**＝ probe の「All caught」自体が嘘になる class。**Check 420**（E2E mutation の find は対象 file 内で一意）で構造禁止し、**導入した瞬間に 3 件目**（h2 見出しが 3 箇所）も検出。consistency 側は file 全体を走査する Check が受けるので非一意でも害が無く対象外。**(b) 観測不能な mutation（1 件）** — command palette の JS focus-wrap は #947 で背景が inert になり **focusable が 0 個**になったため、ブラウザ自身が focus を閉じ込める（実測: palette 内 focusable は input 1 つ・Tab 6 回でも動かない）。**原理的に捕捉できない偽エントリ**だったので、実ガード `setAppInert` を対象に付け替え、テストも inert の付与/解除を assert。**(c) アサーション race（2 件・#984）** — snapshot restore テストが `click()` 直後に `__fatalError` を**待ちなしで単発読み**しており、非同期 render（`await yieldToMain()`）の前に読めば「まだ crash していない」を「crash しない」と誤認する。**ローカルではフル probe（29 分）でも 145/145 caught で再現せず**、CI 負荷でのみ露見した。先に描画成立を positive assertion で待ってから読む形へ是正（**fatal 検査に `expect.poll` は使わない** — 不変性の検査で poll は最初の観測で成功してしまう）。**結果: 実 CI で SURVIVED 4 → 2 → 0**（各修正後に dispatch して実測）。

  - **🟡 probe を 2 ジョブ並列化 (#983)**。実測 38m48s（consistency 約 10 分 / behavior 約 29 分）が直列で和になっていた。分割後の**実測は 30m14s（22% 短縮）**。**閾値（45 分）にはまだ達していなかった**が、超えてから動くと「安全網の検証が止まっている」ことに気付きにくいので先に構造を直した。副次的に**どちらの層が壊れたのかがジョブ単位で分かる**。**honest**: 当初「55 分超」と述べたのは**実行中 run の `updatedAt` を誤読**したもので、正しくは 38m48s。

  - **🔴 cross-app 実バグ: テーマ切替が入力途中のテキストを消していた (#982)**。`Theme.cycle` の `State.update` は notify → **全再描画（`#content` を clear）** を起こし、テーマという**ページ内容と無関係な chrome 操作**の巻き添えで未送信入力が消えていた（実測: task 8 文字 → 0 / ai 6 文字 → 0・notes は永続化ゆえ残るが focus は 3 つとも喪失）。`state.theme` を描画に使うのは sidebar だけと全走査で確認し、`updateSilently` + **sidebar だけ再構築**へ（#258/#684 と同じ規律）。押したボタンが再構築で消える focus 落ちも、**sidebar 内に focus があった場合だけ**復元（#267 の「奪うのではなく失われた時だけ復元」）。BGM は DOM のみ・brand は独自 Storage で全再描画を起こさないことも確認済＝ chrome 操作の class は閉じた。

  - **🟢 「記録された [FIX] に、その失敗モードを踏むテストがあるか」レンズ (#985/#986)**。shipped JS の `[FIX]` 66 件を棚卸し。**store.js の並べ替え [FIX]**（既定プロジェクトの並べ替えが reload 後に定義順へ戻る）は、既存テストが**ユーザー追加を reload 無しで**見ていて失敗モードを踏めていなかった（旧実装は user 追加だけ保持され **default だけ戻る非対称**）。**projects-page.js の cat 正規化 [FIX]**（stale bookmark で「全カテゴリー表示なのに 0 件」の desync）も無効値 deep-link が未被覆。両方とも現状 clean だが pin する層を追加した。`command-palette.js` の `preventScroll` は既に手厚く被覆済み（再監査不要）。

  - **教訓（本 run で繰り返し効いたもの）**: (i) **アプリを疑う前に測定を疑う** — 「実バグに見えた」2 件がいずれも自分の測定ミスだった（reload 直後の localStorage は**保存済みバイト列**であって正規化後の state ではない／同一ルートの hash 遷移では `.card` の可視が**前の描画**で満たされる）。(ii) **poll の使い分け** — **変化**の検査は `expect.poll`、**不変性**の検査は settle 後に 1 度読む（poll は最初の観測で成功してしまう）。(iii) **テストに control ケースを埋め込む** — 「そもそも仕掛けが効いているか」を先に assert しないと、条件が崩れた瞬間に**何も検査せず緑**になる（schemaVersion 決め打ちで 14 ケース全滅、種蒔き不発など実際に踏んだ）。(iv) **ブランチ作成は単独コマンドで** — プローブ内の不可視文字（RTL/ZWJ）でコマンドが丸ごと弾かれ、**ブランチ未作成のまま main で作業**した（未 push ゆえ `git branch -f` で移送・force-push/reset 不使用）。**次の未採掘 vein（reflect 済・非拘束）**: `[FIX]` 棚卸しの続き（66 件中まだ数件しか照合していない）/ observability / research 適用。

- **「終わりなき改善」ingestion 型ガード 3 連 + WCAG contrast + 導線解決 run (2026-08-10・PR #965〜#978)。**（要点のみ・Check 総数の真値は §9）。前 run（#961〜#964）に続く同一セッション後半。**本 run を貫くのは「宣言や記録ではなく、実際に流して測る」**で、実バグ 5 件・新 Check 6 本（414〜419 のうち 415〜419）はすべて実測が起点。

  - **🔴 ingestion の型ガード欠落を 3 面連続で修正 → 構造防止へ昇華**。根本原因は共通で、`String(v || fallback)` と `filter(t => t && t.title)` が **truthy な非文字列（`[]` / `{}`）を素通り**すること。`String([]) === ''` / `String({}) === "[object Object]"` になる。**(a) profile #968**: `{"profile":{"email":[]}}` で ContactPage から宛先が消え「メールを作成」が**宛先の無い `mailto:`** を開いた。併せて `safeUrl` が受け取った fallback を**欠落時に使っていなかった**非対称（同じ object 内で URL 2 つだけ空になる・#139 と同じ 3 フィールド）も是正。**(b) projects #969**: `[object Object]` が**一覧 3 箇所・詳細 4 箇所へ実際に描画**されていた。`safeStr` / `safeStrList` を共有ヘルパへ引き上げ、スカラー 9 種・配列 3 種・`links.label`・`outcome.metrics` を型ガード。**(c) appsData #970**: task/todo の必須テキストで同形。**この class は entry を落とすのが正**（既存の「title が無い entry は落とす」に揃える。プレースホルダを捏造して壊れた項目を隠さない）。**(d) 昇華 #971**: 3 回潰したので **Check 417**（store.js が untrusted 生値を `String()` へ直接渡さない ∧ 必須テキスト filter が `isText()` を通る）へ。Check 364（`(X || []).<throwing method>` 禁止）の**文字列面の対**。**なぜ Check が要るか**: この class は fatal を出さず ErrorBoundary に掛からず、視覚 baseline は ADVISORY ゆえ **behavior test を書いた面しか守られない**。新フィールド追加時に同じ書き方をされるのを止めるには書き方自体を禁じる層が要る。

  - **🟢 WCAG 1.4.3 (Contrast AA) が構造的に未検査だった #974**。axe-core 4.12.1 → 4.13.0 の bump を**「新ルールが実バグを暴くか」先に測って**から適用したところ、既存ゲート（critical 限定）は 22 passed のままなのに、**フィルタを外すと `color-contrast`（serious）が全ルートで大量**に出た（2026-06-16 の WCAG 監査記録も 1.4.3 に触れていない）。既定ブランド primary は白に対し **4.467** で AA を **0.04** 下回っており、各チャンネル −1 の `rgb(98,101,240)` で 4.527。**`#/quiz` 63 → 4 ノード**、3 ルート計 132 → 53。**C5（配色の設計判断）に当たらないと切り分けた根拠**: 変化量 1/255 ≒ 0.4% は知覚不能で screenshot 許容 0.05 を大きく下回り、**実際に screenshot テストが pass**（視覚 baseline に非到達）＝「設計の変更」ではなく「丸め誤差レベルの是正」。**残り（muted `#94a3b8` = 2.56 等）は知覚できる変更なので C5 defer**、候補値・再現コマンドつきで `research-application-policy.md` に記録。**#975 でダークも測り、そちらの方が範囲が広い**（primary on 暗背景 3.94 / muted 3.75 / チップ上 2.91）ことと、**ライトで効いた 1/255 はダークでは使えない**（暗背景には primary を*明るく*する必要があり量が大きい。`--color-primary-light` の流用が最短候補）ことを追記。**ライトだけ直して終わりにしないための記録**。

  - **🟢 テスト被覆の空白を 3 面**: **#973 router の hash query**（不正 percent-encoding / プロトタイプ継承キー / 500 文字 / 重複パラメータ等 16 種は**実測で全て graceful**＝honest clean。だが hash は利用者が直接編集でき古いブックマークからも来る最外周の入力面で e2e は正常値しか通していなかったので、**今 graceful なのは既存ガードのおかげ**という前提を固定した）。**#975 ダークテーマ**（全 17 ルートで render-neutral critical 違反ゼロ・従来 a11y 被覆ゼロ）。**#978 「採用される」敵対的ストア**（既存 resilience は「壊れた JSON / schema 不一致 / storage 例外」＝**採用されない**経路のみで、parse でき schema も一致して**実際に adopt される**経路が未被覆だった）。

  - **🟡 CI ゲートが第三者 CDN に依存していた #966/#967**。前 run の flake を「flake だった」で終わらせず測ったところ、**1 ナビゲーションごとに 6 ホストへ 9 リクエスト**（KARTE ×4 / Google Fonts ×2）が飛び、`page.goto()` の既定 `waitUntil: 'load'` が**その完了を待って**いた（suite 全体で ~334 ナビゲーション）。`E2E_HERMETIC=1` で Chromium の `--host-resolver-rules` により localhost 以外を即 NOTFOUND にし、**goto 447ms → 39ms**・fatal なし。screenshot ステップには**意図的に付けない**（実フォント baseline を壊すと ADVISORY の視覚シグナルが恒久的に無意味になる）。**#967 で probe 側にも展開**: probe では外部起因の失敗が「mutation を捕捉した」と誤報告される＝**false CAUGHT** で、`MUTATION_PROBE=1`（SW block・false SURVIVED 対策）と**対**になって初めて probe の結論が信用できる。**Check 416** が 4 点（config / behavior step / screenshot step に付けない / probe）を強制。

  - **🟡 到達不能コードと導線の解決性**: **#972** ActionDelegator の `drawer:close` は handler があるのに `data-action` を持つ要素が**全履歴 0 件**（`git log -S`）＝never-wired で、**そのためだけに** `closeDrawer` を main.js → late-binding holder → factory 引数と引き回していた。Check 376 のコメントは「意図的な symmetric unused handler」と記録していたが、(i) symmetry は runtime の性質でなく可読性の好み、(ii) 依存エッジの実コスト、(iii) **この 1 つのために action 面全体で逆方向ガードを張れなかった**、を根拠に覆して除去し **Check 418**（定義 ⟹ 使用）で icon 面（375/375b）と同じ双方向に。**#976** `docs/files/**` の frontmatter `canonical-ref` は「この file を理解するには次を読め」という読者の導線だが、**511 参照中 7 件が行き止まり**だった（いずれも他はフルパスなのにそこだけ prefix が欠けた裸のファイル名）。frontmatter は本文と違い人の目に触れにくく、Check 108 は *file の存在* は守るが *指す先* は見ていない → **Check 419**。

  - **🔵 「Check にしない」判断も成果物 #977**。同じレンズを prose 中のパス参照へ広げると 494 箇所中 56 箇所が未解決だったが、**大半は正しい記述**（`js/quiz-data.js` は Stage 3→3-b の分割経緯＝書き換えれば履歴を偽る / `lib/version_checks.py` は「将来分割するなら」の意図的に存在しない例示 / アーカイブ / プレースホルダ）。Check 化すると**これらを「直す」圧力**が生まれ実態より悪くなり、例外リストは「歴史か否か」という**意味の判断**で機械化できない。check-map §2.9 に実測値・内訳・非強制の理由を記録した（**新しい Check を足す前に読む場所**）。genuine な不正確さは 1 件（Check 5 の説明の prefix 落ち）で修正。

  - **教訓（本 run で繰り返し効いたもの）**: (i) **測定の前に測定系の対照を取れ** — schemaVersion を 45 と決め打ちした結果 14 ケース全部が「schema 不一致 → 既定」へ落ち**何も検査せず全部 pass** した（全ケースが同値だったことで気付いた）。恒久テストでは版数をアプリ自身に保存させて実行時取得し、**control ケース**（正常 store が実際に採用される）を先頭に置いた。(ii) **「壊したつもりの場所が実は無関係」を潰さずに非 vacuity を主張しない** — ダークテーマの非 vacuity で theme.js の docstring→実コードと 2 回外しても緑で、ダーク描画が CSS の `prefers-color-scheme` 由来と判明して初めて RED になった。(iii) **検出器自体を疑え** — canonical-ref 走査の初版は正規表現の選択肢順で `package.json` を `package.js` と誤マッチし「46 件」と誤報告した（正すと 7 件）。(iv) **登録した mutation は必ず RED を実測** — `decodeURIComponent(parts[1])` → `decodeURIComponent(String(parts[1]))` は意味的 no-op で vacuous だった。(v) **自分の Check も vacuous になる** — Check 416 初版は同 step 内の**説明コメント**の `E2E_HERMETIC` を実設定と誤認し、env を削っても緑だった。**実測して clean と確認済み（再監査不要）**: キーボード到達可能性（全ルートで到達不能な操作要素 0・全て native 要素）/ 描画済みページ内リンク 20 種すべて解決 / role-split の表は完全な ARIA table 意味論（role=table/row/rowheader/columnheader/cell）/ md 本文の相対リンク 33 件すべて解決 / `npm audit` は dev 込み 0 vulnerabilities。**次の未採掘 vein（reflect 済・非拘束）**: apps 間のより深い相互作用 / observability / mutation-probe の実行時間（現在 ~447 mutations・週次 timeout 60 分に対し余裕は縮小傾向）。

- **「終わりなき改善」Reflow 修正 + 測定系の偽陰性根治 + 監査面の欠落 run (2026-08-10・PR #961〜#964)。**（要点のみ・Check 総数の真値は §9）。前 run（#932〜#952）に続く同一セッション後半。**本 run の中心は「自分の測定手段が壊れていたこと」に気付けたか**で、genuine 増分 4 件のうち 2 件がそこから派生した。**(1) 🟢 #962 WCAG 1.4.10 Reflow を修正（保留を撤回）**: 320px 幅で 4 ルートが横スクロールしていた（`#/role-split` +51px / `#/quiz` +31px / `#/hiring-risk` +28px / `#/apps/pomodoro` +16px）。真因は、基底の `.main-content` が `max-width:1200px; margin:0 auto` で本文カラムを中央寄せする一方、`@media (max-width:920px)` で `.app` が `flex-direction:column` になると**その左右 margin が cross 軸の auto margin になる**こと。flexbox 仕様上、cross 軸に auto margin を持つ item には `align-self:stretch` が適用されず fit-content で決まり、**fit-content は min-content を下回れない**ため item 自体が viewport より広くなる（実測 `#app` 320px vs `#main-content` 350.92px）。修正は同 media query 内の `max-width:100%` **1 行**で、15 ルート × 幅 320/375/768/920 の全てであふれ 0。**保留理由は真因判明で両方とも消えた**: (i) 修正は `max-width:920px` に完全スコープされ **screenshot は 1280×720 clip** なのでこの media query に到達しない＝視覚 baseline は構造的に不変（「baseline を再生成できないから触れない」が成立しない）、(ii) 余白の好みではなく「viewport より広い箱ができる」封じ込めの欠陥なので C5 ではなく correctness。回帰防止は視覚 baseline では原理的に不可能なので behavior e2e（320px で 6 ルート）+ mutation。**教訓: 保留理由が「見た目が変わるので証明できない」形のときは、変更のスコープが視覚ゲートに到達するのかを先に確かめよ**（media query の条件と screenshot の clip 幅を突き合わせるだけで前提が崩れた）。**(2) 🔴🔴 #963 測定系そのものが壊れていた（本 run 最大の発見）**: 上の調査中、候補 CSS を当てて同期で読む診断が**全て偽陰性**になり「要素を隠しても幅が変わらない ＝ コンテンツは無関係」と誤結論しかけた。犯人は `js/perf-guards.js` が `CSSStyleDeclaration.prototype.setProperty` と `Element.prototype.setAttribute('style', …)` を上書きし書き込みを **rAF まで遅延バッチ**していたこと（`getAttribute('style')` すら `null` のまま・`bypassCSP:true` でも直らない＝CSP ではなくサイト自身の機構）。掘ると **hook は一度も発火していなかった**: shipped JS は例外なく `el.style.x = …` / `style.cssText` を使い、hook 自身の NOTE が「直接代入は対象外」と明記している（計数ラッパーを `addInitScript` で先に仕込み、15 ルート走査 + drawer/palette/theme/入力の対話で **setProperty 0 回 / setAttribute('style') 0 回**を実測）。一方コストは実在した — 全 `setAttribute`（ARIA 更新など hot path）にラッパーが挟まる / `removeProperty` は hook されないので `setProperty(x,v)` → `removeProperty(x)` で**順序が反転して x が残る** / DOM の意味論がサイト内だけ非標準になる。利益ゼロ・実コストあり・診断を壊す、で除去し **Check 414**（shipped leaf JS が組み込み prototype を書き換えない・BLOCKING）で再混入を構造封じ。**教訓: 書き換え系の測定は最初に「効くはずの変更」で対照を取れ**。この class の失敗は「エラー」ではなく「**変化しなかった**」形で出るので、測定結果として正しく見える。**(3) 🟡 #964 監査面から丸ごと欠けた CI の class**: オーナーの runtime 役割は「制御と監査」で STATUS.md の監査節がその唯一の導線だが、バッジが出るのは `pull_request:` の workflow だけで**定期実行（`schedule:`）は 2 本ともバッジがゼロ**だった。定期実行は PR を止めないので赤くても届かず、しかもそこに属するのは他のどの層も見ていないもの（安全網そのものの自己検証 = `mutation-probe.yml`・**前 run で自分が新設した**／公開 AIO 面の週次監視 = `aio-monitoring.yml`）ばかり。さらに **`generate_status.py` のトリガ導出は各 workflow の先頭 800 文字しか読んでおらず**、33 行の WHY コメントヘッダを持つ `mutation-probe.yml` は `on:` が窓の外に落ちて**導出からも silent に消えていた** — その箇所には「ハードコードすると workflow の増減に追従できない（Check 124/411 の scope drift の同型）」と書いてあり、**導出自身が同じ scope drift を起こしていた**（`pull_request:` 側も同条件）。全文からコメント除去 → `on:` ブロックのみ走査へ修正し、**Check 415**（STATUS.md が PR ゲート ∪ 定期実行を網羅・**生成器と独立に** workflow を直接 parse するので生成器の導出バグ自体を捕捉）を追加。非 vacuity (b) では **Check 121 が緑のまま 415 だけが RED** になり独立層として機能することを実証した（121 の regenerate-compare は「STATUS.md が生成器の出力と一致するか」しか見ない）。**(4) #961 は (1) の前段**で、真因未特定のまま機構と切り分け済み事項を defer 記録へ追記したもの。**運用で踏んだこと**: behavior e2e が CI で 1 度 flake（`Speakable JSON-LD updates cssSelector per route` が `.hero-section` 30s timeout）。ローカル 3/3 pass・同じ `.hero-section` を待つ他 8 テストは全緑ゆえ systematic 回帰ではないと判断し `gh run rerun --failed` 1 回で緑。**次の未採掘 vein（reflect 済・非拘束）**: apps 間のより深い相互作用 / research 適用 / 導出スコープ監査の続き（今回 `js/quiz/*.js` は実測で完全な純データ＝非再帰 glob は honest と確認済・再監査不要）。**実測して clean と確認済み（再監査不要）**: axe は drawer-open / palette-open / project-detail まで被覆済 / quiz 模範解答フォームと ContactPage の連絡導線（後者は入力欄を持たない button なので aria-required の非対称ではない）/ ~~`profile.email` は normalize で空にならない~~ **← この判断は誤りだった（2026-08-10 に実測で反証・#968）**。`String(v || fallback)` は `[]` のような **truthy な非文字列** を素通りし `String([]) === ''` で空になる。実際に `{"profile":{"email":[]}}` の取り込みで ContactPage の宛先が消え、mailto が宛先なしになった。**「コードを読んで大丈夫そう」で clean と記録するな — 敵対的入力を実際に流して確かめよ**/ task・todo のフィルタは module-level state で reload 非永続（quiz 検索の永続とは意図的に別物）。

- **「終わりなき改善」reduced-motion + focus 保持 run (2026-08-11・PR #992〜#995)。**（要点のみ・Check 総数の真値は §9）。前 run（#906〜#930）に続く同一セッション後半。**レンズは research 適用 → a11y（motion）→ a11y（キーボード操作性）**へ回した。**🔴 本 run の中心は「宣言されているガードが、その呼び出しには届いていなかった」class を 2 段掘り当てたこと。** **(1) #993 reduced-motion が効いていなかった（WCAG 2.3.3）**: home の「ケースを見る →」は `#evidence-heading` へ 1,000px 超スクロールするが、reduce と no-preference で**スクロール曲線が完全に一致**していた（t150 で 474 vs 476 / t600 で 1079 vs 1071）。原因は CSSOM-View の **`behavior` を明示した時点で CSS の `scroll-behavior` は参照されない**という規定で、style.css の `@media (prefers-reduced-motion: reduce) { scroll-behavior: auto !important }` が `scrollIntoView({behavior:'smooth'})` に届いていなかった。**紛らわしさの正体は「CSS 側の override 自体は正しく働いていた」こと** —— 同じ実測の中で `window.scrollTo(0, 0)` は reduce のとき即時に完了していた。同時に `scrollIntoView` は viewport を動かすだけで focus を動かさない（実クリック後の activeElement はボタンのまま）ため WCAG 2.4.3 も欠けており、`tabindex=-1` + `focus({preventScroll:true})` で移動先へ移した。**Check 421** = `'smooth'` リテラルを持つ shipped JS は同 file で `prefers-reduced-motion` を問い合わせる。**検出を `behavior:` の直後に限定しなかった**のは、正しく直した後の三項 `behavior: reduce ? 'auto' : 'smooth'` を見落として **vacuous PASS**（＝自分の修正を守れない Check）になるため。**(2) #994/#995 操作したコントロールが「自分自身を消して」focus を捨てていた（WCAG 2.1.1）**: `State.update` / `window.render()` は `#content` を作り直すので、change / click ハンドラを持つ select・checkbox・number input・ボタンは操作の瞬間に自分自身を DOM ごと消す。実測で **task/todo の絞り込み・タスクの優先度・Todo の完了チェック・ポモドーロの設定と開始/モード/リセット・Settings のインポート対象・タスクのステータス移動・削除・プロジェクトのタグ絞り込みが全て activeElement=BODY** だった。**決定的な手掛かりは、既に id を持っていた `brandSelect` だけが復元されていたこと** —— id の有無がそのまま分岐になっていた。実害は number input が最も重く、**ArrowUp の 1 回目で focus を失うため 2 回目以降が効かない**（値を 1 段しか動かせない＝実質キーボード操作不能）。**ポモドーロは開始した瞬間に focus を失い、稼働中は毎秒再描画されるので復帰の機会も無く「開始したらキーボードでは止められない」**状態だった。修正は main.js `_renderCore` に 3 段の面倒みを入れたもの: ①**clear より前に** focus 中の id を控える（同一ルート再描画のみ）②失われていたら同じ id へ戻す ③戻せなければ `#content` の h1 へ退避（戻せないのは *削除で対象が消えた* / *移動後に disabled になった* の 2 ケースで、**成否は `focus()` を呼んだかでなく `activeElement` を読み直して**判定する —— disabled 要素への focus は例外も出さず黙って無効化される）。復元は **id を持っていた要素だけ**の opt-in にしてある（無条件にすると毎秒再描画されるポモドーロ稼働中に id 無し要素から h1 へ focus が飛び続ける）。「奪う」のでなく「失われた時だけ戻す」条件は route-focus (#267) と共有し、#265 の palette race を再発させない。**Check 422** = `onchange` を持つ select / checkbox / number input は id を持つ（`type='file'` は 1 回限りの操作ゆえ除外）。**ボタンには Check を足していない** —— 「押すと再描画するか」は静的に判定できず、遷移するだけのボタンまで巻き込む brittle gate になるので behavior e2e で被覆する方針を選んだ。**(3) #992 依存 3 件を「測ってから」適用**: eslint / globals は lint 出力 0 errors / 54 warnings で不変、playwright 1.62.0→1.62.1 は **同梱 Chromium が更新されても screenshot baseline は割れず** 全 e2e 315 passed（**バージョンを上げたら baseline が必ず割れるわけではない** —— §3 の再生成は human-merge 経路ゆえ「不要と分かる」ことに実務価値がある）。pin を上げた瞬間 **Check 51 が RED** になり runbook の版数 drift を機械的に止めた。**教訓（本 run の中心）**: (i) **「そのガードは本当にその呼び出しに届いているか」を測れ** —— CSS の reduce override は効いていたのに、behavior を明示した 1 呼び出しにだけ届いていなかった。近くが正常に動いていることは、対象が守られている証拠にならない。(ii) **`selectOption()` は選択後に focus を select に残さない** —— これで focus 復元を検証すると修正済みでも BODY と読め、**常に false RED** になる（実測で dispatch 版とだけ結果が食い違い、1 サイクル誤診しかけた）。キーボード相当は `el.focus(); el.value=…; dispatchEvent(new Event('change',{bubbles:true}))` で作る。(iii) **ルートを連続 `goto` して `#content h1` の visible だけ待つと前ページの DOM を掴む**（実測で projects の select を todo ページの検査が拾った）。ページ固有の見出しを `hasText` で待つか test を分けよ。(iv) **mutation の帰属は locator にも宿る** —— id を外す mutation が「要素が見つからない」で落ちると、*焦点が失われたこと*を検証できたのか判らない。locator はアクセシブル名で引き、assertion 側で id / 名前を比べよ。(v) 上の 3 件は **`docs/files/playwright.config.cjs.md` の落とし穴表**に追記済（e2e を書く前に読む 1 箇所）。**実測して clean と確認済み（再監査不要）**: WCAG 1.3.5 の `autocomplete`（quiz の問い合わせフォームは name/email とも設定済・他に本人情報を集める入力は無い）/ pomodoro 設定値の clamp / projects のカテゴリ絞り込みは listHost の手動再描画ゆえ元から focus を失わない / Settings の `brandSelect`。**次の未採掘 vein（reflect 済・非拘束）**: apps 間のより深い相互作用 / observability / sidebar 内 focus の再描画耐性（#content 外なので今回の復元対象外）。
- **「終わりなき改善」focus 掃引の完遂 + モーダル×履歴 run (2026-08-11・PR #997〜#1001)。**（要点のみ・Check 総数の真値は §9）。前 run（#992〜#996）で始めた focus 復元を **全 UI 面へ掃き切り**、その過程で隣接する 2 クラスの実バグを掘り当てた。**(1) #997 サイドバーの focus が毎秒奪われていた**: `_renderCore` は `#content` **だけでなく sidebar も毎回作り直す**ため、id を持たない nav リンクは #994 の復元対象外だった。ポモドーロは稼働中に毎秒再描画するので、**キーボード利用者はタイマーが動いている間サイドバーに focus を留めておけなかった**（focus し直しても 1 秒で BODY）。nav リンクと Lab トグルへ安定 id を付けた。**同じ増分で重複 id も発見**: sidebar と drawer は同じ `navLink` を共有し、mobile では両方が同時に DOM 上へ存在する（sidebar は `display:none` で残る）ため、**`nav-lab-body` と `themeBtnSidebar` が 2 つずつ**あった。DOM 順で drawer が先に来るため見た目の実害は出ていなかったが、sidebar 側トグルの `aria-controls` が drawer 側を指しており（支援技術がその参照を辿ると視覚的に隠れた別グループへ着地・WCAG 1.3.1）、**どちらのトグルがどちらを開けるかが「構築順」という偶然に委ねられていた**。drawer 側だけ別名にし、トグルはハードコード id ではなく**自分の `aria-controls` を辿る**ようにした。**副産物の meta-QA**: `aria-controls` を変数にした瞬間、WHY コメントに書いた `aria-controls="nav-lab-body"` が dangling 参照と判定され **Check 392 が false RED** になった。走査前のコメント除去を追加（Check 112 / 421 / 422 と同じ「コメントは違反にも充足にもしない」規律の idref 面）。**(2) #998/#999 モーダル×履歴**: drawer 内 nav リンクは自分で `closeDrawer()` を呼ぶが、**それ以外の経路でルートが変わると開いたまま残っていた**。mobile で drawer を開いてブラウザの「戻る」を押すと、**背後のページだけが切り替わり** drawer は開いたまま・`#app` は inert・body は scroll lock。Android の戻るボタンは「開いているモーダルを閉じる」操作として使われるのに、実際には**見えない場所でページが遷移していた**。`hashchange` で閉じるようにし、**同じ非対称が palette にもあることを実測して同時に塞いだ**（#947 で「背景 inert が drawer だけ」だったのと同型）。palette では `close()` に再入ガードが無く（`open()` にはあった・#948 が closeDrawer で塞いだのと同じ非対称）、hashchange に繋いだ結果 **ガードの有無が観測可能な欠陥に昇格**した —— ガード無しだと `lastFocused.focus()` が**ルート遷移のたびに過去の要素へ focus を引き戻し**、新ページ h1 への route-focus (#267) を毎回打ち消す。**`lastFocused` が `#content` 内なら再描画で detach され `focus()` が無効化されるため気付けない —— 生き残る要素（topbar 内の `#menuBtn` 等）が `lastFocused` になった時だけ牙を剥く。****(3) #1000 掃引の完了**: notes / ai / quiz は clean だったが **Settings が全滅**（並べ替え ↑↓ / 表示切替 / 削除 / スナップショット保存・復元が全て BODY）。並べ替えは 1 回押すと focus が外れ 2 回目以降が効かず、何段も動かすのが本来の用途なので**実質キーボードでは使えなかった**。**復元の鍵は `idx` ではなく `p.id` で作る** —— `idx` 鍵だと移動後にその位置へ来た**別のプロジェクト**のボタンへ focus が移り、実測では ↓ を 2 回押すと別の行が上がってきて**元の順序に戻った**（net zero）。この mutation を安全網に登録済み。**(4) #1001 focus は戻るのにキャレットは戻っていなかった**: ノートの caret=3 から再描画すると caret=54（末尾）へ飛び、次の 1 文字が末尾に着弾する。発火経路はポモドーロ完了（`complete()` → `window.render()`）で、**25 分書き続けたノートの真ん中で 1 文字ずれる**。clear より前に `selectionStart/End` も控え、復元時に `setSelectionRange`（長さでクランプ・number/checkbox は `selectionStart` が null なので `typeof` で弾く）。**教訓（本 run の中心）**: (i) **同じ責務のものは対で直せ** —— drawer を直したら palette、`open()` にガードがあれば `close()` にも、を実測で確認する（本 run では 3 回この形で追加の欠陥が出た）。(ii) **掃除しようとした埃の下に別のものがある** —— #998 は「全ルートの重複 id を掃く test」を書こうとして`#menuBtn` のクリックが overlay に阻まれて timeout したことから見つかった。テストの書き方の問題に見えたが製品側の欠陥だった。(iii) **『現実的な発火経路だから』でテストを残すな** —— #1001 の AI ページ版は修正の有無にかかわらず緑で、何も検証しない緑を 1 つ増やすだけだった（削除して理由を spec に残した）。(iv) **mutation の帰属は locator にも宿る**（前 run の再確認）—— id を外す mutation が「要素が見つからない」で落ちると*焦点が失われたこと*を検証できたか判らない。locator はアクセシブル名 / href / 可視テキストで引け。(v) リスト項目の復元鍵は**位置ではなく同一性**で作れ。**実測して clean と確認済み（再監査不要）**: notes / ai / quiz のボタン面 / ポモドーロ稼働中の全リセット（interval も表示も正しく停止）/ desktop 全 17 ルートの重複 id / projects のカテゴリ絞り込み（listHost の手動再描画ゆえ元から focus を失わない）。**次の未採掘 vein（reflect 済・非拘束）**: apps 間のより深い相互作用 / observability / research 適用。
- **「終わりなき改善」デプロイ可観測性 + メディア契約の被覆 run (2026-08-11・PR #1002〜#1009)。**（要点のみ・Check 総数の真値は §9）。前 run（focus 掃引）から **observability → test-coverage** へレンズを回した。本 run を貫くのは **「宣言されているのに、それを見ている層が一つも無い」面を探して塞ぐ**という一貫した動きで、5 面見つかった。**(1) #1003 監査導線から第 3 の CI class が丸ごと欠けていた**: STATUS.md のバッジは PR ゲートと定期実行の 2 群だけで、**main への push で走る class**（`pages-build-deployment` = 公開サイトのデプロイ本体 / `auto-update-aio-digests.yml` = bot）が漏れていた。前者は **GitHub 管理の dynamic workflow でリポジトリに file が無い**ため `.github/workflows/*.yml` の走査には原理的に出てこないが、**オーナーが最も知りたい「サイトは今ちゃんと公開されているか」を決めているのはこの 1 本**。後者は #252 で「digest ロジックの欠陥が毎週 desync を起こし、次の PR が赤くなって初めて発覚（発生から発覚まで 1 週間）」の当事者そのもので、遅れの原因は監査導線が無かったこと。badge URL は推測せず **GitHub Actions API の `badge_url`** で実測確認した。Check 415 を拡張し、非 vacuity 検証の 3 つ目で **生成器の導出を潰すと Check 121 は緑のまま 415 だけが RED** になることを実測 —— 「STATUS.md が生成器の出力と一致するか」しか見ない 121 では原理的に捕捉できない **生成器自身のバグ**を独立層が捕まえることの実証。**(2) #1004/#1005/#1009 デプロイの 2 つ目の失敗モード**: バッジが見ているのは「ジョブが成功したか」だけで、**「配信されている中身が現在のものか」を見る層はどこにも無かった** —— PR ゲートは **ローカルの http-server** に対して走り公開サイトを一度も触らず、Check 2/17/180 は main.js と index.html が **リポジトリ内で**一致することしか見ない。つまり「Pages が数週間前の成果物を配信し続けている」状態が**全ゲート緑のまま**成立する。`check_deployed_freshness.py` を新設し週次 `aio-monitoring.yml` へ配線（**Check 423** が「存在 ≠ 配線」を BLOCKING 強制）。**設計の肝は比較対象の選び方** —— コミット SHA のような毎回変わる値だと merge 直後の実行がデプロイ完了前になり **週次バッジが恒常的に赤くなって赤が意味を失う**ため、明示的な版数更新でしか変わらない `VERSION` / `LAST_UPDATED` を選んだ。さらに **資産の到達性**（index.html の同一オリジン参照 ∪ `.well-known/` ∪ sitemap の `<loc>` = 62 件）を追加。**`.nojekyll` の失敗モードは 1 つではない**: dot-directory が落ちて `.well-known/` が 404 になる経路と、**`.md` が HTML へ変換されて sitemap の権威面（AI2AI.md / README.md / real-work-claims.md）が 404 になる**経路は別物で、後者は既存のどの集合にも入っていなかった。実 CI で `workflow_dispatch` して全 step 緑と 62 件 200 を実測済み。**(3) #1006 消しても全ゲートが緑のまま通る CSS**: `@media print` は screenshot（screen media・かつ ADVISORY）にも behavior e2e にも consistency Check にも到達せず、**丸ごと削除しても誰も気付けなかった**。採用担当が Resume を紙に出す想定があるので機能性の毀損に当たる。**(4) #1007 media query の被覆棚卸し**: 突き合わせたら `forced-colors: active` と `prefers-contrast: more` の 2 つだけ穴が残っていた（Check 101 は forced-colors ブロックの **存在** を静的に強制するだけで効果は見ない）。**実測した壊れ方が想像より悪く**、fallback を外すと Chromium はブランド色を強制変換して **`rgba(5, 0, 73, 0.8)` = 半透明**の暗い青を描く —— 「色が変わる」ではなく **HCM で最も困る『薄くて見えない』状態**になる。**(5) #1008 apps 間の相互作用**: 詳細ページを開いたままその project が別画面から消える 3 経路を実測し **全て graceful（honest clean）**、ただし踏むテストが皆無だったので 2 つ固定。「fatal が出ないこと」の検査は**何も起きなくても緑**になるので、リセットが実際に効いたことを **control** として埋め込んだ。**教訓（本 run の中心）**: (i) **「宣言されている」と「見ている層がある」は別** —— 宣言（CSS / meta / sitemap / workflow）を数え上げ、それぞれに *どの層が見ているか* を突き合わせると穴が機械的に見つかる（本 run はこの棚卸しだけで 5 面出た）。(ii) **導出できないものは「由来を明記して固定する」** —— Pages デプロイはリポジトリに file が無く導出不能だが、導出漏れのまま落とすより「なぜ導出できないか」を書いてリテラルで足す方が honest。(iii) **測定の前に測定系を疑う（本 run で 3 回）** —— `manifest.json` の 404 は実体が `manifest.webmanifest` だっただけ / `<select>` の programmatic focus は `:focus-visible` にマッチせず無意味な値を読んでいた / `localStorage` に `portfolio*` キーが無かったのは「アプリが状態変化まで永続化しない」から。いずれも**実バグに見えた**。(iv) **advisory は BLOCKING を踏む前に効かせる** —— `projects.spec.js` が 962 行で advisory(900) を超えた時点で、圧縮せずテーマの合う `resilience.spec.js` へ移した（1,000 の BLOCKING に当たってから動くのでは遅い）。(v) **RED を実測できない mutation は登録しない** —— #1008 の S2 は単一のガードを外して RED にできる形ではないので、test は残しつつ mutation は登録せず理由を明記した。**実測して clean と確認済み（再監査不要）**: 公開 AIO 層の到達性（llms/well-known/sitemap/robots すべて 200）/ print media の実効性（6 ルート）/ `manifest.webmanifest`（Check 210/212 が静的に被覆済・e2e 追加は padding と判断）/ ポモドーロ稼働中の全リセット。**次の未採掘 vein（reflect 済・非拘束）**: research 適用 / 静的ページ（ai-knowhow・hiring-risk）の behavioral / e2e vacuous-gate 棚卸し。
- **「終わりなき改善」a11y 構造意味論の掃引 run (2026-08-11・PR #1011〜#1014)。**（要点のみ・Check 総数の真値は §9）。前 run（デプロイ可観測性）の「宣言 vs 見ている層」棚卸しが効いたので、同じやり方を **HTML の構造意味論**へ向けた。**「見た目はそう見えるが、アクセシビリティツリーにはそう出ていない」面を 4 種類**掘り当て、いずれも実バグだった。**(1) #1011/#1012 長い読み物に見出しが H1 の 1 個しか無かった**: `#/ai-knowhow`（4,056 文字・9 セクション）と **`#/quiz`（24,500 文字・7 章）**で、節/章タイトルが `<span class="text-head-lg">` / `<div class="quiz-section-title">` で描かれており、スクリーンリーダーの主要ナビゲーション手段である**見出しジャンプで本文を一切辿れなかった**。section の `role=region` + `aria-label` でランドマーク移動は効くので「完全な無構造」ではないが、見出し一覧は空。**axe は「長い本文に小見出しが無い」をルール化していない**（heading-order は *存在する見出しの順序* しか見ない）ので a11y スキャンは緑のままだった。**描画不変の作り方**: `.quiz-section-title` は line-height / letter-spacing を pin していたので `div→h2` で**座標 7 箇所すべて一致**。`.text-head-lg` は pin していなかったため見出しの既定タイポグラフィ（line-height 29.75→20.4px / letter-spacing 0.16→-0.34px）が効いてページが 24px 縮んだので、`line-height: inherit; letter-spacing: inherit` を足して **8 箇所の座標・サイズ・ページ高を完全一致**させた。**(2) #1013 全ルートに `ul`/`ol`/`role=list` が 1 つも無かった**: projects 18 枚・apps 5 枚の同列カードが「リスト・N 項目」とアナウンスされず、リスト単位のジャンプも効かない。**`<ul>` へ置き換えず ARIA ロールで与える**のが要点で、grid の `display` を変えないため描画は構造上不変（`<li>` 化すると `display:list-item` とマーカーで grid が崩れる）。kanban の 3 列や quiz の章カードは「リスト」より別の構造が適切なので**機械的に全部へ付けるのは意味論の水増し**として見送った。**(3) #1014 宙に浮いた `<label>` が 6 個**: ポモドーロの 集中/短休憩/長休憩・Settings の モード/対象/Demo が `for` も持たず control も包んでおらず、**ラベル文字をクリック/タップしても何も起きず**タップ標的も縮んでいた。入力欄側に `aria-label` があるので **axe は緑**（axe は label 要素の孤立をルール化していない）。結線できたのは **#994 の focus 復元用 id が既にあった**おかげ。「対象」だけは 3 checkbox のグループ名なので `for` ではなく span + `role=group` + `aria-labelledby` にした —— **機械的に全部 `for` にすると存在しない「代表 control」を指す嘘の関連付けになる**。**教訓（本 run の中心）**: (i) **`axe` が緑でも a11y が満たされているとは限らない** —— 本 run の 4 件はすべて axe に該当ルールが無く、捕捉層は追加した behavior e2e だけだった。axe は「機械的に判定できる違反」の下限であって上限ではない。(ii) **視覚スタイルだけの見出し/リスト/ラベルは『宣言と実態の乖離』の典型** —— 見た目でそう見えるものが意味論としてそう出ているかは、アクセシビリティツリー側（tagName / role / for）を **実測**しないと判らない。(iii) **意味論を足すときは「描画不変」を実測で作れる** —— ARIA ロールは定義上 render-neutral、要素置換は pin された CSS があるかで決まる。座標・サイズ・ページ高を before/after で突き合わせれば「非破壊」を主張できる。(iv) **🔴 mutation の偽 PASS を踏んだ（Check 420 の存在理由そのもの）** —— quiz の章題行は file 内に 2 箇所あり（architecture quiz 用 / 既存問題集用）、**既定の aws quiz が通るのは後者**なのに `replace(find, replace, 1)` は前者に当たって **PASS してしまった**。直前の icon 行を含めて一意化して RED を実測。(v) **template literal の test 題名は mutation を登録できない** —— #1012 で踏み #1014 でも踏んだが、2 度目は Check 379/397 が verify で即 RED にした（安全網が安全網を守った）。test 題名は静的リテラルにし、共通処理は関数へ切り出す。(vi) **CI 環境差の false RED**: 印刷の横あふれを `=== 0` で検査したら CI（Linux）で `-15px` になり赤くなった —— **印刷メディアではスクロールバーの gutter が予約されない**。「あふれていない」は `<= 0` で表す。**実測して clean と確認済み（再監査不要）**: 見出し掃引の誤検出（home の統計数値 / pomodoro のタイマー `role=timer` / about の CTA 文）はいずれも**正しく見出しでない** / task・todo・quiz・projects・notes に宙に浮いた label は無い / `npm outdated` は空（依存はすべて現行）。**次の未採掘 vein（reflect 済・非拘束）**: research 適用 / e2e vacuous-gate 棚卸し / apps 間のより深い相互作用。
- **「終わりなき改善」vacuous-gate 監査 + 言語属性 run (2026-08-11〜12・PR #1016〜#1022)。**（要点のみ・Check 総数の真値は §9）。**e2e の非 vacuity を『mutation で実証済みか』で棚卸し**したのが起点（285 test 中 117 が未実証）。全件に mutation を付けるのは padding かつ scenario test には不可能なので目標にせず、**security → AIO の順に優先して掘った**。**(1) 🔴 #1016 セキュリティゲートが『絶対に落ちない』状態だった**: `No Trusted Types or CSP violations` は KARTE ノイズ除外を「メッセージ全体に `karte` が含まれるか」で判定していたが、**CSP 違反のコンソールメッセージには違反した directive の全文が載る**。この CSP は script-src / connect-src で KARTE ホストを許可しているので **あらゆる違反メッセージに `karte` が現れ、除外条件が常に真**だった。index.html に `<script src="data:…">` を注入して本物の違反を起こしても PASS することを実測して確定。ブロック対象 URL の**ホスト名**で判定する形へ是正し（`new URL(...).hostname.endsWith('.karte.io')`）、この注入自体を mutation として登録した。**(2) 🔴 #1017 `Early suppressor` の assertion がリスナー皆無でも真だった**: 合成 `PromiseRejectionEvent` の `ev.defaultPrevented` は **リスナーが 1 つも無くても true** になる（inline + 外部ファイルの両方を潰し、CSP ハッシュ不一致で script ごとブロックされた状態＝リスナー皆無で実測）。本物の rejection を起こして「表に出ないこと」を見る形へ変え、**control**（別メッセージの rejection は必ず表に出る）を同じ test に埋めた。**mutation は登録していない** —— 抑止が多層（inline suppressor + `js/fatal-overlay.js` の known-benign フィルタ）で単一 file の mutation では RED にできないことを実測したため。**(3) #1019 AIO 中核 3 ゲートは honest clean**: soft-404 の noindex / og:type のルート追従 / entity anchor の曖昧性排除文 —— すべて壊すと RED になった。視覚に出ない head・sr-only 面なので、手で確認した 3 つを mutation 登録して週次 probe が恒久的に自己検証するようにした。**(4) 🟡 #1018 BLOCKING gate の間欠 RED を 2 件根治**: ①toast は `duration = 3000ms` で自動消滅するので、import のような重い操作のあとに `#toast-container` のテキストを待つと **「出て、消えたあと」**に評価される → 同じ文言が書かれる `#action-announcement`（次の通知まで消えない sr-only 領域・#901 で一本化済み）を見る形へ 11 箇所を移した。②`responsive: … no broken band` の `waitForTimeout(150)` は **`debounce(syncMobileDrawer, DEBOUNCE_DELAY)` の値ちょうどへの賭け**だった。しかも probe が「viewport を変えてから goto」していたため init 時点の viewport が前の幅で、`syncMobileDrawer()` が誤った側で確定していた。**「赤を見たらまず rerun」という習慣は本物の回帰を見逃させる**ので、時間ではなく状態で待つ形へ直した。**(5) 🟡 #1020〜#1022 WCAG 3.1.2 Language of Parts**: `html lang="ja"` の文書に英語だけの塊があり、日本語 SR が**英語を日本語の音韻で読み上げる**状態だった（quiz 49 箇所・うち同一ラベルが 34 回／home 4／about 1／resume 1）。**axe には該当ルールが無い**（`html-lang-valid` は文書全体の lang しか見ない）。行の言語は data 側で混在するので静的には決められず、**描画時に文字種で判定**（日本語文字が無く Latin 文字がある → `en`）。固定リテラルはリテラルで、data 由来（`featured.category` / `profile.title`）は同じ判定関数で。**1 行の正規表現でも複製しない**方針で `js/pure-utils.js` の `langOfText` に一本化し 3 factory へ注入した。**教訓（本 run の中心）**: (i) **「除外条件」は gate を丸ごと殺しうる** —— ノイズ除外を*メッセージ全体*の部分文字列で書くと、対象の識別子が正常系メッセージにも現れる場面で全件除外になる。除外は**構造化された場所**（ブロック対象 URL のホスト名など）で判定せよ。(ii) **`defaultPrevented` のような「既定動作」系のフラグは、リスナー不在でも真になりうる** —— *観測可能な結末*を見る形へ置き換え、**control**（同じ経路で必ず観測できるはずの事象）を同じ test に埋めて観測経路の生存を証明せよ。(iii) **固定時間待ちは『実装内部の定数への賭け』** —— debounce / 自動消滅 / 非同期配送を跨ぐ assertion は、**その処理が完了したことを示す状態**を待て。時間で待つ限りマシン速度に賭け続けることになる。(iv) **`axe` が緑でも a11y は満たされていない**（前 run に続き再確認・本 run の lang も axe 無ルール）。(v) **依存を 1 つ増やすと同期先は 5 つある** —— #1022 で `langOfText` を注入したところ Check 47b → 265 → 119a → 372 → 362 が順に RED になり全部教えてくれた。**実測して clean と確認済み（再監査不要）**: security spec の AIO anchor 永続性 / startViewTransition proxy / AIO の soft-404・og:type・entity anchor / pm・quality・architecture quiz には英語だけの塊が元から無い。**次の未採掘 vein（reflect 済・非拘束）**: 未実証 e2e の残り（117 件の棚卸しは入口として残してある）/ research 適用 / apps 間の相互作用。
- **「終わりなき改善」安全網の自己検証 + 実測ゲート run (2026-08-12・PR #1024〜#1028)。**（要点のみ・Check 総数の真値は §9）。本セッションで ~18 個の mutation を追加したので、**安全網そのものを実 CI で検証する**ために週次 `mutation-probe.yml` を dispatch したのが起点。**(1) 🔴 #1024 probe が behavior 側で 2 件 SURVIVED を検出し、原因は 2 つとも自分の過去の修正だった**（consistency 側は success）。**a. cross-tab**: テストが「tabA が保存し終えてから tabB を開く」順序だったため、**tabB の store に既に編集中テキストが入っており巻き戻すものが無かった**（実測: `B-store-notes` に "TAB-A-編集中" が入っていた）。tabB を先に開いて**古いスナップショットを持たせてから** tabA が入力する順序へ是正。**b. notes**: `updateSilently` → `State.update` へ戻しても、**#994 で入れた focus 復元機構が消えた input へ focus を戻す**ため緑のままだった。`updateSilently` が固有に保証するのは **そもそも再描画しない**ことなので、textarea に `dataset` の印を付けて **1 文字ごとに作り直されていないこと**を直接見る形へ変更。**教訓: 修正はテストを鈍らせる。** 機構を足したら、**その機構に守られてしまう既存テストが無いか**を probe で確かめよ。**(2) #1025 同じ class の掃引は clean**: focus を assert する e2e 31 件を洗い出し、鈍る条件は **要素が `id` を持つか**だと確定（復元は id を鍵にするため）。projects / quiz の検索入力は id を持たず、全再描画へ退行させると RED になることを実測。見分け方を `docs/files/playwright.config.cjs.md` の落とし穴表へ追記した。**(3) #1026 依存 currency 2 巡目**: `@axe-core/playwright` 4.13.0 / `globals` 17.10.0 を**測ってから**適用（a11y 36 passed 不変・lint 0 errors/54 warnings 不変・audit 0 件）。**critical 制限を外した全 impact スキャン**でも新規違反は無く、検出は既に C5 defer 済みの `color-contrast` のみ。**記録**: `npx eslint .`（スコープ無し）は 104 problems / 50 errors を出すが、これは lint 対象外の Node 側ファイルで `process` が未定義になるため。権威は `npm run lint`（0 errors / 54 warnings）。**測定の前にどのコマンドが権威かを確かめる。****(4) #1027 Check 395 の死角**: `path:` が実在ルートへ解決することは静的に強制されるが、**query の中身は見ていない**。`quiz?type=pm` を `pmm` と打ち間違えても route は存在し #926 の own-key ガードで crash もせず、**既定の AWS 問題集が silent に表示される**。着地先の見出しで問題集の種類を確かめる e2e で固定。**誤検出を出しかけた記録**: 掃引で `設計ポリシーを見る → #/settings` を誤配線と思ったが、カード本文が「スナップショット検証を実装」と述べており Settings がその実装場所＝**意図どおり**だった（ラベルだけで判断せず撤回）。**(5) #1028 runtime を見る層が無かった**: Check 120 は byte weight を見るが、**描画時間**を見るゲートが存在せず、quiz renderer に O(n²) が入っても byte も行数も e2e の合否も変わらないまま体感だけが劣化する状態だった。実測すると **体感遅延の ~95% は意図的な View Transition** で（通常 275〜307ms / reduced-motion 11〜25ms）、24,500 文字・648 ノードの quiz でも描画自体は 25ms＝**描画は速い**。アニメーションを含めて測ると演出を測ることになり閾値も揺れるので **reduced-motion で純粋な描画コストだけ**を測り、実測の 12〜27 倍の余裕で桁違いの回帰だけを捕まえる。**(6) 最終状態**: 再 dispatch した probe が **behavior 187 / consistency 310 = 全 497 mutation caught** で両ジョブ success。本セッション追加分を含め、安全網が「確かに捕捉する」ことを実 CI で実証済み。**教訓（本 run の中心）**: (i) **安全網は定期的に自分自身を検証しないと嘘になる** —— mutation を足したら probe を回せ。PR CI には載っていない（behavior 側が ~30 分）ので、**足した本人が回す**のが唯一の実効経路。(ii) **「前の状態で条件が満たされる」罠は測定でも起きる** —— `aria-busy=false` を待つ描画計測が全ルート 0ms を返した。待つ条件は「**変化したこと**」で書け。(iii) **perf は『意図的な演出』と『実際の仕事』を分けて測れ** —— 分けないと閾値が演出時間に支配され、CI 負荷で揺れるうえ回帰も検出できない。**実測して clean と確認済み（再監査不要）**: focus assert 31 件のうち鈍っていたのは #1024 の 2 件のみ / 静的ページの CTA 12 件はすべて期待どおりに着地 / 全ルートの描画コストは reduced-motion で 11〜25ms。**次の未採掘 vein（reflect 済・非拘束）**: 未実証 e2e の残り（棚卸しの数字は #1016 に記録）/ apps 間の相互作用 / observability。
- **「終わりなき改善」通知設計の是正 + 配信完全性 run (2026-08-12・PR #1029〜#1033)。**（要点のみ・Check 総数の真値は §9）。**live region の棚卸し**と**デプロイ失敗モードの段階分解**という 2 本の棚卸しが起点。**(1) 🟡 #1031 同じ「件数通知」なのに politeness が非対称だった**: Projects / Quiz は polite なローカル `role=status` なのに、**task/todo だけ `#action-announcement` (assertive)** へ書いており、絞り込むたびに**スクリーンリーダーの読み上げを割り込んで**いた。assertive は緊急 (エラー等) に限るのが ARIA APG の作法で件数は status。既にリポジトリ内にある正しい形 (sr-only の polite status) へ揃え、命令的な `announce()` と未使用になった `announceFilter` を除去。読み上げ文言も生値 (`high`) ではなくラベル (`High`) へマップした。**(2) 🟡 #1032 ページ本文そのものが live region だった**: `#content` に `aria-live="polite"` が付いており、quiz では 24,500 文字が**ルート遷移や State 更新のたびに丸ごと差し替わる**。SR が本文全体を読み直す chatty なアンチパターンで、**ポモドーロ稼働中は毎秒再描画される**ため特に害が大きい。`git log -S` で追うと **"全体微改善" という commit で根拠なしに入っていた**。一方リポジトリは同じ懸念を他所では正しく扱っており (`role="timer"` の「非 chatty」注記 / ルート遷移アナウンスの gate / 目的別 polite status)、この live region は**その設計を上書きして本文全体を読ませる副作用だけ**を持っていた。`aria-busy` は残す (agentic surface が利用)。**外す変更なので control を 2 つ埋めた** —— ルート遷移通知と件数通知が生きていることを同じ test で確認する。**(3) #1033 デプロイ失敗の 3 段目**: ①ジョブ失敗 (バッジ) ②版数・到達性 (#1004/#1005/#1009) に続く **③届いた中身が宣言と違う**が未検証だった。AIO 層は sha256 を**公開している**ので、整合性を検証するエージェントは digest 不一致で資源を棄却する —— 配信側でバイト列が変質すると (Pages の変換・部分デプロイ・キャッシュ混線) **200 は返るのに AIO 層だけが機能しない**。リポジトリ側の `check_aio_digests.py` はローカルのファイルしか見ないため別層が要る。公開面 11 件 (agent-skills 2 + manifest 9) を検証し全一致。実 CI (`workflow_dispatch`) でも 3 段すべて緑を実測。**(4) #1030 「テストを足さない」判断も成果物**: 長文データ (120 文字の連続文字) での reflow 耐性を実測したところ document の overflow は 0 で contained されていた。長文版の test も書いたが、**containment を壊す mutation を当てても緑のまま**だった (同じ mutation で既存 #962 test は RED)。**RED を実測できないものは安全網に混ぜない**規律に従って撤去し、実測結果と「なぜ足さなかったか」を既存 test のコメントへ残した。**教訓（本 run の中心）**: (i) **同じ種類のメッセージは politeness を揃えよ** —— 「件数」を 2 箇所は polite・2 箇所は assertive にしていた。非対称は「どちらが正しいか」が既にリポジトリ内にある場合が多い。(ii) **live region は小さく目的別に** —— 本文全体を live にすると、正しく設計された専用領域の gate を上書きしてしまう。(iii) **「外す」変更には control を同居させよ** —— 能力を落としていないことを同じ test で証明しないと、静かな退行と区別できない。(iv) **失敗モードは段で分解せよ** —— デプロイは「ジョブ / 版数 / 到達性 / 完全性」の 4 段で、各段に別の層が要る。**実測して clean と確認済み（再監査不要）**: フィルタ/検索の query 変更でルート遷移アナウンスは発火しない (`replaceSilently` ゆえ・over-announce なし) / agent-skills と manifest の digest はローカル・公開面とも一致 / 長文データでの 320px reflow。**次の未採掘 vein（reflect 済・非拘束）**: 未実証 e2e の残り / apps 間の相互作用 / research 適用。
- **「終わりなき改善」backup 契約の往復検証 run (2026-08-12・PR #1035〜#1037)。**（要点のみ・Check 総数の真値は §9）。**「アプリ自身が書き出したファイルを import し直して同じ状態に戻るか」**という誰も通っていなかった経路から入り、実バグ 3 件。**(1) #1035 往復テストの新設**: 既存テストは「部分 export のスライス」と「**手書き JSON** の import」しか見ておらず、**export→import の往復**はどのテストも通っていなかった。フル export は利用者にとって**バックアップ**なので、export 側で 1 フィールド落ちる (あるいは import 側が無視する) だけで**黙ってデータが失われる**。task/todo/notes を seed して往復させる test を新設 (現状は clean・`exportFull` が notes を落とす退行で RED を実測)。**(2) 🔴 #1036 theme が復元されない + DOM が stale**: `theme` は full export に含まれるのに **importJSON が一切触っていなかった** (実測: export に `theme:"dark"` が入っていても import 後の store は `"system"`)。#139 (profile strip) と同じ data-fidelity class。併せて **state を丸ごと置き換える経路 (import / 全リセット / snapshot 復元) は `Theme.cycle` を通らない**ため `data-theme` と `.dark` が古いまま残り、**reload して初めて切り替わる** (ユーザーには「リセットしたのにテーマが変わらない」と見える)。全再描画の入口で `state.theme` に追随させ、置換経路をまとめて拾う単一点にした (同値なら何もしない冪等)。**(3) 🔴 #1037 projectPrefs が復元されない**: 同じ class を **export の全キーへ当てて**掘ったところ、`projectPrefs.hiddenIds` も無視されていた。**既定プロジェクトは削除できず「非表示」が唯一の非公開手段** (#886) なので、backup から戻すと**意図的に隠したプロジェクトが再び公開状態になる** —— preference の喪失ではなく**公開/非公開の意思**の喪失。gate は theme (全体の表示設定 → 無条件) と projectPrefs (「どのプロジェクトを見せるか」= projects 側のデータ → Projects チェックボックス) で分けた。`schemaVersion` / `type` / `lastModified` / `modifiedBy` は normalize が再生成するメタデータなので import しないのが正しく、**実キーを全部当たって残りが無いことも確認**した。**教訓（本 run の中心）**: (i) **「書き出す側」と「読み込む側」のキー集合を突き合わせよ** —— export が書くのに import が読まないキーは、**ユーザーには『バックアップしたのに戻らない』としか見えない silent なデータ欠損**になる。片側だけのテスト (部分 export / 手書き JSON の import) はこの非対称を原理的に検出できない。**アプリ自身の出力を入力に戻す**往復テストだけが捕捉層。(ii) **state を丸ごと置き換える経路は、派生した DOM を更新しない** —— import / リセット / snapshot 復元は通常の操作経路 (Theme.cycle 等) を通らないので、**描画の入口で state に追随させる単一点**を置くのが確実。(iii) **1 件見つけたら同じ集合を全部当たれ** —— theme を直した直後に全キーを当てて projectPrefs が出た。**実測して clean と確認済み（再監査不要）**: tasks / todos / notes / projects 件数 / profile の往復忠実性 / import しないのが正しいメタデータ 4 キー。**次の未採掘 vein（reflect 済・非拘束）**: 部分 export (Projects のみ 等) の往復 / 未実証 e2e の残り / apps 間の相互作用。
- **「終わりなき改善」import 契約の完成 + 嘘の成功報告の根絶 run (2026-08-12〜14・PR #1038〜#1040)。**（要点のみ・Check 総数の真値は §9）。前 run（backup 契約の往復検証）が名指しした「部分 export の往復」vein から入り、**export と import のキー集合の非対称**という同じ根から実バグ 2 件。本 run を貫くのは **「何もしていないのに成功したと言う」機能は、失敗するより悪い**という一点。**(1) 🔴 #1039 部分 export したファイルが import で一切戻せなかった**: `Projectsのみ` は projects の**素の配列**を、`AppsDataのみ` / `Profileのみ` はそれぞれの**素のオブジェクト**を書き出すのに、import は full-state 形（`parsed.projects` 等）しか見ていなかった。実測すると 19 件の配列を読み込んでも `projects: 18, hasCustom: false` のまま、**それでも「インポートが完了しました」**。バックアップと称して**戻せないファイルを作り、成功したと報告する**状態で、利用者は復元できたと信じてしまう。export が実際に書く 4 形（互いに素）を受け付ける `_normalizeImportShape` を追加し、**判定できない形は `null` を返してエラー**にした（silent no-op に成功メッセージを付けない）。**(2) 🔴 #1040 「対象」の選択で中身が全部落ちる残り半分**: #1039 は「形を認識できない」側を塞いだが、**形は認識できるのに「対象」チェックボックスで 1 セクションも適用されない**場合は依然「完了しました」だった（実測: `AppsDataのみ` のファイルを AppsData のチェックを外して読み込むとタスクが 1 件も置き換わらない）。実際に 1 セクションでも適用したかを追跡し、0 件なら成功と言わない。`{schemaVersion: 12}` だけのような「形は妥当だが中身が無い」ファイルも同じ経路で honest になる。**併せて #1039 の e2e が `Projectsのみ` の枝しか通っておらず、`AppsDataのみ` / `Profileのみ` の枝を誰も踏んでいなかった**ので、「対象に戻せば同じファイルが今度は実際に適用される」まで見る形で同時に固定した。**(3) 肥大化を BLOCKING の手前で処理**: `mutation_samples.py` が **996 行 = Check 365 の 1,000 行まで残り 4 行**だったので最古 16 entry を archive へ log-rotate（総数 198 は不変）。`apps-settings-io.spec.js` は 938 行で早期警告 (900) を超えていたので「import が受け付ける形の契約」というテーマの塊を `e2e/apps-settings-import-shape.spec.js` へ切り出した（→888）。`js/settings-page.js` の BUDGET-DATA 記録値が **408 のまま drift**（実態は #1040 の変更前で既に 514 > advisory 500）していたのを実態へ同期し advisory を 600 へラチェット（増分の大半は #1035〜#1040 のバグ 6 件を記録した WHY コメントで、ロジックの肥大ではない）。**教訓（本 run の中心）**: (i) **「何もしなかった」と「成功した」を混同するな** —— silent no-op に成功メッセージを付けると、利用者は**壊れた復元を信じて元データを捨てる**。適用件数を数えて 0 件なら成功と言わないのが最小の honest 実装。(ii) **測定系を疑う手掛かりは「実バグに見えた」こと**（本 run で 3 回）—— Profile が往復していないと判断しかけたが、**保存が debounce**（読むのが早すぎた）/ **`bio` は UI に描画されないフィールド** / **Resume は `title` を描画しない**（describe 先を間違えた）の 3 つが重なっただけで、実際は 4 形すべて往復していた。「戻っていない」と結論する前に、**その値がそもそも観測可能か**を確かめよ。(iii) **`find` の一意性は登録 mutation だけの話ではない** —— 手動プローブでも `return null;` が file 内に 2 箇所あって別の関数を打ち、**PASS してしまった**（#1012 と同じ）。Check 420 は `E2E_MUTATIONS` を守るが、手で当てる mutation は自分で数えるしかない。(iv) **checkbox の onchange が `window.render()` を呼ぶ画面では、`check()` 直後の `setInputFiles` が detach された古い input を掴む** —— 単独実行では緑、フルスイートでだけ間欠 RED になる。状態を assert して settle を保証してから set せよ（既存テストが同じ罠をコメントで警告していた）。**実測して clean と確認済み（再監査不要）**: 4 つの export 形すべての往復（`AppsDataのみ` は置換まで、`Profileのみ` は `bio`/`title` の localStorage 到達まで確認）/ `schemaVersion`・`type`・`lastModified`・`modifiedBy` を import しないのが正しいこと。**次の未採掘 vein（reflect 済・非拘束）**: 未実証 e2e の残り / apps 間の相互作用 / observability。
- **「終わりなき改善」安全網の棚卸し + 名前だけの gate 掃討 + 数値 drift の単一ソース化 run (2026-08-14・PR #1042〜#1051)。**（要点のみ・Check 総数の真値は §9）。前 run（import 契約の完成）から **meta-QA** へレンズを回した。起点は「e2e 287 件のうち mutation で非 vacuity を実証済みなのは 178 件しかなく、**109 件は『捕捉するはず』としか言えない**」という棚卸し。全件に付けるのは padding なので目標にせず、**壊れたときの被害が最大のもの / 目視で気付けないもの**から順に実証した（178 → **213 件**）。**(1) 実証した安全網（各々ガードを外して RED を実測）**: 全リセット・プロジェクト削除の **confirm キャンセル**（壊れると「考え直してキャンセルを押した利用者がデータを失う」最悪の silent failure なのに未実証だった）/ Markdown ノートの **XSS 境界**（innerHTML へ退行しても視覚的には**むしろ「markdown が効いた」ように見える**）/ **cross-tab 伝播**（「さっき足したタスクが消えた」としか見えない）/ **localStorage 保存**（reload で全消失）/ **Speakable のルート追従**（視覚に一切出ない AIO 面）/ **slug 衝突の一意化**（#154・一覧には両方出るのに片方の詳細へ到達不能）/ **テーマ巡回順**（#262）/ **FatalPage の `__fatalError` 解除**（#298・復旧した正常ページを 2 秒後に安全網が覆い直し脱出不能）/ **drawer の背景 inert**（#947・視覚的には被さって見えるので目視不可）/ **silent URL 更新後の `currentRoute` 同期**（#765・「勝手に全件へ戻った」）。**(2) 🔴 名前だけの vacuous gate を 3 件掃討**: `theme-init.js applies stored dark theme (FOUC prevention)` と brand 版は **theme-init.js の復元を丸ごと殺しても緑**だった（素の goto 後に読むため main.js の `Theme.apply` が後から同じ属性を付ける＝**pre-paint かどうかを一切見ていない**）。`page.route('**/main.js').abort()` で **theme-init.js だけが走った状態**にして測る形へ是正し、「main.js が実際に止まっている」を control として同居させた。`No layout shift on mobile viewport` は **レイアウトシフトを一度も測っていなかった**（topbar 可視 / sidebar 非可視の responsive 検査だけ）。`PerformanceObserver('layout-shift')` で実測する形へ。#1016 の CSP 除外 / #1017 の `defaultPrevented` に続く同じ class。**(3) 🔴 「実測行数」を名乗る表の 71% が実測でなかった → Check 424**: `file-size-budget.md` §2 は **62 行中 44 行が stale**（最大 366 行ズレ）。doc 自身が「一致は**人間レビューで保つ**」と書いており誰も検証していなかった。全行を同期し **Check 424 (BLOCKING)** で `wc -l` 一致を機械強制。さらに `main.js` の現在行数は **CLAUDE.md §7 / extraction-map / budget 表 / docs/files/README.md の 4 箇所が独立に drift**（1,086 / 1,196 / 1,086 / 実測 1,307）していたので、**単一ソースを §2 表とし他は指すだけ**にした。`apps.js 5 関数`（実 3）/ `components.js 11 関数`（実 7）も同 class で、**数を書くのをやめて関数名を列挙**する形へ。**Stage 5 完遂時点の 1,086 行という歴史的記述は書き換えない**（書き換えれば履歴を偽る・#977 と同じ判断）。**(4) 安全網の自己検証を実 CI で完遂**: 週次 `mutation-probe.yml` を dispatch し **consistency 9m50s / behavior 42m36s で両ジョブ success・All 213 behavior mutations caught**。本 run で足した 15 件を含めて「確かに捕捉する」ことを実測した。**教訓（本 run の中心）**: (i) **「片方を潰しても緑」= vacuous とは限らない** —— 冗長に置かれたガード（MAX_PROJECTS の二重 slice / 保存の debounce+visibilitychange 二重経路 / `data-ai-state` を router と main.js の両方が書く）では、**そのテストが通る経路で実際に効いている行**を狙わないと RED にならない。本 run だけで 4 回踏み、各所に相互参照付きで記録した。(ii) **RED を実測できない mutation は登録しない** —— `render abort under nav` 系は abort を無効化しても緑だが、これは **abort 機構に behavioral な観測点が無い**ためで vacuous ではない（利用者に見える性質「ページが 1 枚・最終ルートが勝つ」は abort 無しでも成り立つ）。`AIO anchor persists after route navigation` も同様。理由を test 側に記録して見送った。(iii) **実バグに見えたら測定系を疑う（本 run で 3 回）** —— `-g` が複数テストに一致し `tail -1` が "1 failed" を隠して「vacuous」と誤読 / CLS は **viewport 高に対する割合**なので別 viewport の値を書きかけ / `content-visibility: auto` の要素は画面外だと **`innerText` が空**を返すため ai-knowhow の 7/9 セクションが未描画に見えた。(iv) **Check は追加した commit 自身で働く** —— Check 424 は mutation 登録で `mutation_samples.py` が伸びたのを即検出し、Check 408 は shell の parse エラーで消し忘れた probe spec を拾った。**実測して clean と確認済み（再監査不要）**: apps 間の相互作用（pomodoro 稼働中の他アプリ操作・全リセット・**稼働中の import で timer が正しく停止**）/ 4 つの export 形すべての往復 / sitemap の 19 loc / WebMCP の「8カテゴリ」宣言（実測 8）/ 定期実行スケジュールは実際に発火している（08-03・08-10）/ 依存は全て current（`npm outdated` 空・audit 0 件）。**次の未採掘 vein（reflect 済・非拘束）**: 未実証 e2e の残り 94 件（被害の大きい層は概ね実証済・a11y 名前系や CRUD が中心）/ observability / research 適用。
- **「終わりなき改善」全再描画の巻き添え掃討 + 上限 drift + 連打二重登録 run (2026-08-14〜15・PR #1053〜#1067)。**（要点のみ・Check 総数の真値は §9）。オーナーから「もっとガンガン自走を・改善の程度は問わない」と依頼があり、**軽い増分を高頻度で回す** cadence へ切り替えた run。**🔴 実バグ 6 件**。**(1) 全再描画の巻き添え class を 3 面続けて根治** —— #1054 settings の「モード」/「対象」トグルが `window.render()` で **#content ごと作り直して隣の file input を差し替え**、直後にファイルを選ぶと change が古い input に飛んで **import が起きない**（CI で 2 度 RED になった #1040/#1053 の根本原因）。#1055 task/todo の**絞り込み変更が「新しいタスク」の未送信テキストを消す**（8 文字 → 0）。#1056 **裏で走るポモドーロの完了**が別ページの未送信入力を消す（毎秒 tick はルートでガード済なのに**完了だけ無条件**という非対称・利用者が何も操作していないので原因に見当がつかない）。いずれも「表示だけの操作／見えていない画面のために全体を作り直さない」へ揃えた（listHost + 手動再描画 / `updateSilently`）。**(2) 上限 drift 2 件** —— #1063 手動追加のプロジェクト名が **リロード後に 200 → 120 文字へ黙って縮む**（Check 410 は「同一 file 内の slice」を条件にするため、上限が store.js 側にあるこのケースは射程外）。#1064 Tech が **20 個 → リロードで 12 個**。後者は最初「同じ slice を書き写す」実装にしたら **Check 410 が正しく反応**したので、Check を黙らせず設計を変え、追加確定後に `Store.validateAndNormalize` を通す形（import / snapshot と同じ「adopt する前に正規化」）にした —— 境界の定義は store.js に一本化されたまま。**(3) #1058 同じ id の項目を取り込むと「1 件削除」が両方消す** —— `filter(t => t.id !== id)` が同 id を全て落とし、逆に更新は `find` が先頭しか拾わない。DOM id も重複して focus 復元が別カードを掴む。#154 の slug 一意化と同型の「後から来た方に連番」を tasks/todos/projects の 3 面へ。**きっかけは「id を 4 つ増やしたので実行時の重複 id を全ルートで確認」した副産物**で、静止状態は clean だったが **データ由来の id を敵対的入力で衝突させたら出た**。**(4) #1061 Enter の連打・キーリピートで同じタスク/TODO が二重登録** —— 入力欄が空になるのは**非同期な再描画の副作用**なので、連打では `e.target.value` がまだ元の文字列を持つ。値を読んだら同期でクリアする形へ。**教訓（本 run の中心）**: (i) **テスト側の罠を 3 種類踏んだ** —— `toHaveCount(1)` が **重複の描画前に成立して vacuous**（todo の mutation が素通りして発覚）/ sentinel を同じ入力欄へ足す形は**フルスイートの負荷下でだけ** detach された input を掴む（#1053 と同じ）/ **`#content h1` の汎用待ちは前ルートの DOM で満たされる**ので「全ルートの title が重複」という誤判定を生む（**変化**を待つのが正しい・本 run だけで 2 回）。最終形はいずれも「**一度ルートを離れて戻り、確定した state から描き直させてから読む**」。(ii) **実バグに見えたら測定系を疑う（本 run で 4 回）** —— `content-visibility: auto` の要素は画面外だと `innerText` が空 / goto 直後は描画前 / probe のボタン名が遷移で変わって click が待ち続ける / シェルループの文字列置換ミスで mutation が当たっていなかった。(iii) **Check は自分のミスも拾う** —— Check 424（§2 表の実測行数）が push 前に verify を通し切らなかった手順ミスを、Check 362 が編集で orphan 化した既存 mutation anchor を、Check 264 が WHY コメント中の literal `TODO` を、Check 408 が shell の parse エラーで消し忘れた probe spec を検出した。(iv) **advisory は BLOCKING の手前で効かせる** —— #1067 では advisory (975) を跨いだのに気付かず **Check 365 の 1,000 行 BLOCKING に当たってから** rotate した（#1057 では先回りできていたので、後退した点は正直に記録）。**実測して clean と確認済み（再監査不要）**: 敵対的な hash query（3,000 文字 / `<script>` / 不正 percent / 重複パラメータ / `__proto__`・`constructor`）/ 非表示 × カテゴリ絞り込みの併用 / quiz 模範解答フォーム（検証エラー後も入力保持・focus は不備欄へ）/ 連打（プロジェクト追加・ポモドーロ開始・モード切替・スナップショット保存・AI 送信）/ `ai-input` の maxlength ↔ 保存の整合 / Resume のリスト意味論 / localStorage 敵対環境（quota・SecurityError）/ 実行時の重複 id（全 16 ルート + drawer）。**次の未採掘 vein（reflect 済・非拘束）**: 未実証 e2e の残り（被害の大きい層は概ね実証済）/ observability / research 適用。
- **「終わりなき改善」probe が私のほころびを 3 連続で捕捉 + a11y リスト意味論 + mailto silent failure run (2026-08-15〜17・PR #1069〜#1082)。**（要点のみ・Check 総数の真値は §9）。**本 run の中心は「週次 mutation-probe が 3 回連続で、実バグではなく *私自身が作ったほころび* を検出した」こと。** 安全網を検証する仕組みが実際に働いていることの実証で、いずれも probe を回さなければ気付けなかった。**(1) #1072 修正がテストを「鈍らせた」4 件** —— #1054/#1055 で「select が全再描画を起こすのをやめた」結果、`selected:` を守る 3 テストが**同一ページでは再描画されないので検査対象を失い**、#1064 で正規化を通した結果 slug 一意化のインラインが**store 側と二重化して冗長ガード**になっていた。`selected:` が今も load-bearing であること（外して離脱→復帰すると state='high' なのに UI='all' の desync）を実測し、テストを離脱→復帰まで通す形へ直して**各 mutation が狙う option と検査値を揃えた**。slug は責務を store.js へ一本化。**(2) #1073 環境不整合** —— `mutation_probe` は設計上 `MUTATION_PROBE=1` で SW をブロックするため、SW 登録を検証するテストは **probe 環境では baseline から RED**。#1066 の非 vacuity 検証で私は env を外して実行しており、**probe が実際に使う条件とは違う環境で「RED になった」と判断**していた。mutation を除去し理由を記録。**(3) #1079 タイミング依存** —— `press('Enter')` を 3 回呼ぶと **1 回目が起こす再描画の速さ次第で 2 回目以降が新しい空 input に当たる**ため、mutated 版でも重複が起きない（ローカルは再現・CI は再描画が勝つ）。キーリピートは「再描画を待たずに連続で keydown が来る」現象なので `evaluate` 内で同期 dispatch する形へ。**最終的に probe は consistency 311 / behavior 240 の全件捕捉で success**。**(4) 🔴 実バグ 3 件**: #1080 **細工したメールアドレスで mailto に BCC を注入**できた（profile は import 由来＝信用できない入力なのに、URL には `safeUrl` があって email だけ素通しという非対称。共有された「バックアップ」を取り込んだ利用者が押しただけで攻撃者に送る）。既存の「254 文字へ切り詰め」も **`@` より前で切れてアドレスですらない値**を保存していたので既定値へ戻す契約へ変更。#1082 **mailto の実行限界**（日本語は percent-encode で 1 文字 9 文字。メッセージ 500 文字で URL 4,852・Windows は約 2,048 で切る → **本文が欠けるか開かない silent failure**）を実測して上限を決定（最悪ケース 1,967 文字）。#1071 手動追加プロジェクトの詳細が **5 セクション中 3 つ見出しだけ**（アーキテクチャとメトリクスは placeholder を持つ非対称）。**(5) a11y リスト意味論を 4 面**: quiz の意見 (#1076) / 記事シリーズ・Settings の行 (#1077) / 役割分担表のセル (#1078)。**描画不変の作り方が要点** —— 素の `<div role=list>` を挟むとページ高が変わる（実測 5919→5823）ので `display: contents` を使い、**ページ高の完全一致**と**ロールが実際に a11y ツリーへ露出すること**（display:contents は歴史的にここが壊れる）を毎回実測してから固定した。既存ラッパーに role を足せる場合はそちらが最善（DOM を増やさない）。**(6) 肥大化を BLOCKING の手前で 2 回処理**: ingestion テーマを `apps-settings-ingestion.spec.js` へ (#1057)、静的ページを `static-pages.spec.js` へ (#1076)。**教訓（本 run の中心）**: (i) **修正はテストを「壊す」のではなく「鈍らせる」** —— 挙動を変えたら、その挙動に依存していた既存テストの非 vacuity を測り直せ（#1024 に続き 2 度目・今回は 4 件同時）。(ii) **非 vacuity の検証は probe が実際に使う条件で行え** —— 条件を変えて RED を得ても probe 上で成立する保証にはならない。(iii) **「連打」を `press` の連続で表現すると再描画を挟んだ操作になる** —— イベントが連続で届くことを再現したいなら同期で投げる。(iv) **自分の修正が別の修正を退行させる** —— #1080 の初版は `String(v).trim()` で始めており、#968 で直したばかりの「非文字列が連絡先を空にする」を再導入していた（テスト作成中の実測で発覚）。(v) **テスト条件の作り方**: `addInitScript` は累積し**先に登録した値が残る**／同じページで localStorage を書き換えて reload する方式は**直前の描画が仕込んだ debounce 保存が後から書き戻す** → **1 ケース 1 コンテキスト**が確実。**実測して clean と確認済み（再監査不要）**: 履歴の深い往復と高速連続遷移 / storage 敵対環境（quota・SecurityError）/ ブランド切替（primary は `--color-primary-rgb` から導出され drift しない設計）/ profile URL の全回避手口（`javascript:`/`data:`/`vbscript:`/大文字混在/前後空白）/ AI 応答は固定テンプレで構造的に bound / ContactPage の mailto は固定文 578 文字 / Markdown は宣言した記法だけ実装 / apps 一覧 ↔ router whitelist（Check 139）/ main.js の import は `throttle` 除去後すべて使用中。**次の未採掘 vein（reflect 済・非拘束）**: 未実証 e2e の残り / observability / research 適用。
- **「終わりなき改善」a11y 名前の一意性 + 二重配線の構造封じ run (2026-08-17・PR #1084〜#1089)。**（要点のみ・Check 総数の真値は §9）。**レンズの切り替えが効いた run。** 従来の a11y 掃引は「名前が空でないか」を見ていたが、**「名前が一意か」**へ変えた途端に 2 件の実バグが出た。**(1) 🔴 #1085 並べ替えボタン 36 個の名前が「↑」「↓」の 2 種類だけ** —— SR 利用者はどのプロジェクトを操作するのか区別できない (WCAG 4.1.2)。同じ行の削除・非表示は「削除：<名前>」と一意化済みで、**並べ替えだけ取り残されていた**非対称。矢印は `aria-hidden` の装飾にし、既存テスト 4 箇所の locator を名前ベースへ移行 (focus 復元テストは「locator に復元用 id を使わない」方針を維持)。**(2) 🔴 #1086 アプリ一覧の 5 ボタンが全部「開く」** —— 同じレンズを全 16 ルートへ広げて発見。可視ラベルは「開く」のままで `aria-label` だけ一意化したので描画は不変。**付けなかったもの**も記録: プロジェクトのタグチップ (「#AI」×4) は**同じ名前で同じ動作**なので対象外 —— 問題になるのは「名前が同じなのに行き先/効果が違う」ときだけで、機械的な一意化は意味論の水増し。**(3) Check 425 新設 (#1089)** —— `data-action` を持つ要素に `onclick` も付けると **1 クリックで必ず二重発火**する (#262 の実バグ)。Check 129 は **main.js の topbar 3 ボタンだけ**しか見ておらず他 file では素通りしていた。実測で **BGM と Theme が 2 系統で配線されている**ことが判明 (topbar=`data-action` 委譲 / sidebar・drawer=`onclick` 直付け)。現状は重複が無いので壊れていないが、**「統一しよう」と片方に他方を足した瞬間に壊れる**。非 vacuity はその「現実に起こりうる書き換え」で実証した。**(4) #1088 モバイル drawer の現在地** —— 既存の aria-current テストは desktop の sidebar だけを見ていたが、**mobile では drawer が唯一のナビ**。実装を共有していても「現在ルートの判定が drawer に届く」保証にはならない (drawer は開くたびに組み直される) ので遷移追従まで固定。**(5) #1084 通知 3 面の実証 + e2e archive を 2 段構成へ** —— hot log を advisory の手前で rotate したら **archive 本体が 1,033 行で BLOCKING に到達**。archive は「新規を押し出す先」なので **archive も無限には伸ばせない**。consistency 側と同じ 2 段構成へ揃えた (総数 245 は不変)。e2e 側 archive が**予算表に未登録**だった非対称も是正。**(6) #1087 測定の罠 4 件を中央の落とし穴表へ集約** —— とくに **再描画で消えうる checkbox に `check()` を使うと再クエリして 2 件目も押す** のは危険で、**製品側の二重トグル bug と誤診しかけた** (`click()` で測り直して正常と判明)。他に `addInitScript` の累積 / debounce 保存の書き戻し / 「連打」を `press()` の連続で表現する問題。解はいずれも「**測りたい現象を正確に再現する primitive を選ぶ**」。**教訓（本 run の中心）**: (i) **同じ面でもレンズを変えると新しい穴が出る** —— 「空でないか」で clean だった a11y 面が「一意か」で 2 件出た。掃引済みの面でも観点を変える価値がある。(ii) **per-instance で潰した class は Check の射程を確認せよ** —— #262 は per-instance 修正 + main.js 限定 Check で終わっており、5 か月後も他 file は無防備だった。(iii) **描画不変の作り方は 2 通り** —— 既存ラッパーに role/属性を足せるなら DOM を増やさないのが最善、挟まざるを得ないなら `display: contents` (#1076/#1078)。どちらもページ高の完全一致で検証する。**実測して clean と確認済み（再監査不要）**: aria-current の全ルート整合 / sidebar↔drawer の nav 項目完全一致 (同一実装ゆえ構造的に drift しない) / disabled コントロールの境界 22 件 / トーストの多重表示と自動消滅 / 3 つの aria-live 領域が役割ごとに分かれていること / AI 応答は固定テンプレで構造的に bound / ContactPage の mailto は固定文 578 文字。**次の未採掘 vein（reflect 済・非拘束）**: 未実証 e2e の残り / observability / research 適用。
- **「終わりなき改善」axe 既定無効ルール + ルート非依存 resume + 測定誤診 3 連 run (2026-08-17・PR #1091〜#1097)。**（要点のみ・Check 総数の真値は §9）。**本 run の中心は「自分の測定を疑う規律が実際に 3 回効いた」こと**で、うち 2 回は「実バグを見つけた」と思った結論を自分で反証した。**(1) 🔴 #1091 WCAG 2.5.3 (Label in Name・Level A) 違反 3 件**: home の CTA が「ケースを見る →」と表示しながら `aria-label` は「ケーススタディセクションへ移動」で、**可視テキストとの共通部分がゼロ**だった。音声入力の利用者は見えている文字を読み上げて操作するので、見えているとおり発話しても起動できない。可視テキストを名前の先頭に置く形へ (描画は不変)。**なぜ長く残ったか**が本題で、axe には該当ルール `label-content-name-mismatch` があるが **`enabled: false` (experimental)** ゆえ既存の `withTags(['wcag21a', …])` スキャンでは**タグが一致するのに走らない**。つまり「a11y スキャンが緑」を根拠にこの SC が守られていると誤読できる状態だった (axe が緑でも a11y は満たされていない、の 5 例目)。**(2) #1092 穴を class ごと閉じた**: axe は 105 ルール中 16 を既定無効で出荷する。Level A/AA の 8 ルールを明示有効化して全ルートで走らせる gate を新設。**設計の肝は対象集合を axe から実行時に導出して照合すること** —— ハードコード一覧だけだと axe を上げて新ルールが増えたとき黙って未検査のまま残る (Check 415 が STATUS.md の workflow 網羅を生成器と独立に導出するのと同じ)。恒久化の前に desktop(1280) と**モバイル(390)** の両方で全ルート測り 8 ルート全て clean を確認 (`target-size` はタッチ標的の SC ゆえ desktop だけでは意味がない)。**(3) #1093 Check 426 + 自分の棚卸し手法の欠陥**: `<meta>`/`<link>` を「見ている層があるか」で突き合わせ、`portfolio:identity:*` と `asset:*:ai-context` / `asset:*:entity` が**どの Check にも e2e にも現れない**ことを発見 (#930 と同じ class)。ただし**初版には Check 360 と重複するサブパートが混じっていた** —— 棚卸しが *リテラル meta 名の grep* だったため、正規表現で `asset:.*:canonical` を拾う既存 Check が構造的に見えなかった (検出器自身の盲点・#977 と同型)。重複は削除し、残り 3 サブパートは各々を単独 mutate して**自分の error だけが発火する**ことを実測。途中「426a で Check 149 も発火」と読みかけたが、grep が **426a 自身のエラーメッセージ本文**の「他 4 箇所は Check 149/280/282 が守る」を拾っていただけだった (**帰属規律はメッセージ本文まで含めて効く**)。**(4) 🔴 #1094 稼働中ポモドーロの復帰がルート依存だった**: auto-resume が `PomodoroPage()` の**描画中にしか走らない**ため、リロード後に別ページで作業していると interval が誰にも作られず **25 分集中しても完了が記録されない** (実測: 別ページ着地 history=0/isActive=true・ポモドーロ画面着地 history=1)。リロードしなければ裏で完了する (#1056 の経路) ので**リロードを跨いだときだけ**挙動が違う非対称で、利用者からは「昨日は記録されたのに今日はされない」としか見えない。タイマー系ヘルパを factory scope へ hoist し `resumeIfActive()` を init から呼ぶ (実装は 1 つ)。**hoist で Check 362 が既存 mutation anchor 3 件の orphan を検出** —— うち 1 件は hoist で「描画時スナップショットを読む」形が構造的に作れなくなったため**同じ観測結果を作る形**へ差し替えた。**(5) #1095 「バグを見つけた」が測定ミスだった**: 「入力直後のリロードでノートが消える」と判断したが、textarea を `click()` してから `type()` すると**既定文のキャレット位置に挿入**されるので `slice(0, 30)` で先頭だけ見ると挿入部に届かない。包含で見たら before/after 完全一致で**元のコードは正しく保存していた**。さらにその誤診に基づいて `pagehide` リスナーを足しかけたが、実測するとリロード時に beforeunload/pagehide/visibilitychange の**3 つとも発火**しており既存経路で保存されていたので revert した (**一般論を根拠にした追加は、必要性を実測で示せないなら padding**)。成果は「保存 flush を見ているテストが 1 つも無かった」ことの解消。**(6) #1096 週次 probe が SURVIVED を検出 → 根治**: `safeStr` 型ガードの mutation が behavior 側で生き残った。原因は 2 つ重なっており、(a) **#1080 で自分が `safeEmail` に独立ガードを入れたため既存テストがその経路を通らなくなった** (修正がテストを鈍らせる class・#1024/#1072 に続き 3 度目)、(b) もう 1 つのケースは **ContactPage が描画しない `profile.name`** を見ており、しかも github/linkedin は `safeStr` ではなく `safeUrl` を通るので**最初から何も検査していなかった** ((a) が生きている間は隠れていた)。「守りたい関数を実際に通り、かつそのページが描画するフィールド」から選び直した。**この過程で control が 2 回落ちて思い込みを即座に潰した**。同 PR で reduced-motion の View Transition gate も新設 —— **層 1 (render のガード) を丸ごと外しても behavior e2e 390 件が全て緑**で、この契約はどの層からも見られていなかった。**冗長な 3 層あるので単一 mutation では RED にできない**ことを実測し、その事実を test 本文に記録して mutation は登録していない (RED を実測できないものは安全網に混ぜない)。**(7) #1097 不在アサーション 95 箇所の棚卸し**: (6) の class が他に無いかを機械的に確認。先行する肯定アサーションを欠くのは 1 箇所だけで、それも実測では描画済みだったが `domcontentloaded` は描画を保証しないため明示的な待ちを足した。**この監査は再実行不要** (方法と結果を落とし穴表へ記録)。**教訓（本 run の中心）**: (i) **「実バグに見えた」ときはまず測定系を疑う** —— 本 run で 3 回踏み、うち 2 回は結論ごと誤りだった。共通するのは失敗が「エラー」ではなく「それらしい数字」として出ること。(ii) **control を同じ test に埋めろ** —— #1096 で 2 回落ちて、ContactPage に想定要素が無いこと・ResumePage には 2 つあることを即座に教えてくれた。control 無しでは「不在アサーションが通った」を「守られている」と誤読していた。(iii) **冗長な層があるとき「片方を潰しても緑」は vacuous の証拠ではない** —— 効いている行を狙わないと RED にならない。狙えないなら mutation を登録せず、その事実を書く。(iv) **静的 Check の棚卸しは自分の検出方法の射程を先に確かめよ** —— リテラル grep は正規表現ベースの既存 Check を構造的に見落とす。(v) **一般論を根拠にコードを足すな** —— 必要性を実測で示せないなら padding。**実測して clean と確認済み（再監査不要）**: root スクリプトのセレクタ (専用 Check が被覆) / shipped JS の全 addEventListener 種別 (focusin/focusout は Check 406 が静的に担当) / WebMCP ツール (execute() の shim まで被覆済) / `.well-known/api-catalog` の参照解決 (Check 387) / 2 つの `index.json` の同期 / 「稼働中」フラグを永続化するのはポモドーロだけ / axe 既定無効ルールのうち Level A/AA 8 件は desktop・mobile 両方で違反ゼロ。**次の未採掘 vein（reflect 済・非拘束）**: 未実証 e2e の残り / observability / research 適用。
- **「終わりなき改善」observability + 宣言と実態の乖離 + GitHub 障害下の運用 run (2026-08-17・PR #1099〜#1103)。**（要点のみ・Check 総数の真値は §9）。前半 run (#1091〜#1098) に続く同一セッション後半。**🔴 実バグ 4 件**と、**GitHub Actions の広域 503 障害下で自走を続ける手順**の確立。**(1) 🔴 #1099 BLOCKING gate の監査バッジが永久に空白だった**: オーナーの runtime 役割は「制御と監査」で STATUS.md の監査節が唯一の導線。バッジ 6 本を**実際に叩いて中身を読んだ**ところ、`playwright-regression.yml` だけが `no status` だった —— `pull_request:` トリガしか持たず run が main に残らないので、`?branch=main` のバッジは**構造的に永久に空白**になる (main での実行 0 件を実測)。つまり **§3 が merge ゲートと定めている behavior e2e だけが、オーナーからは「緑」ではなく「何も分からない」**状態。`push: branches: [main]` を追加し、**Check 427** で「`?branch=main` バッジは main 上に run を作るトリガを持つ workflow に限る」を強制。Check 415 は「バッジが**在るか**」しか見ないのでこの class を素通りしていた (存在 ≠ 機能)。**Check 427 の初版は条件を push のみとして schedule 駆動の 2 本を誤検出し、実測 (バッジは "passing" を表示) がその場で前提を反証した** —— schedule / workflow_dispatch は既定ブランチで走る。**Check が実態と食い違ったとき疑うべきは Check の前提の方**。**(2) 🔴 #1100 その gate が自身の定義変更を検証していなかった**: #1099 は `playwright-regression.yml` を書き換えた PR なのに **playwright-validation が一度も走らなかった** (paths filter に自身が無い)。job 構成 / env / step を変えれば gate の挙動そのものが変わるのに、**その変更だけは gate に検証されずに merge できる**。Check 142 が package.json について守っているのと同一 class。自己参照を追加し **Check 142b** (自己参照の存在) + **142c** (push / pull_request の 2 ブロックが同一 —— 片方だけに足すと「PR では走るのに main では走らない」非対称ができ、merge ゲートと監査バッジの守備範囲がずれる) を追加。新 Check 番号を増やさずサブパートにしたのは守る invariant が同一 class だから。**(3) 🔴 #1101 theme-color が「適用されない方の meta」に書かれていた**: index.html は `media="(prefers-color-scheme: light|dark)"` 付きの theme-color を **2 本**宣言しているが、theme.js は `querySelector` で**先頭 1 本 (light 用) だけ**を更新していた。**OS が dark のときは media が一致せず適用されない**ので、実効値はブランド紫のまま = **選んだテーマがモバイルのアドレスバー色に届くのは OS が light のときだけ**だった。`querySelectorAll` へ。**変わるのはページの pixel ではなくブラウザ chrome の色なので screenshot では原理的に捕捉できない**面。テストは `meta.content` ではなく **media が一致する方の content (実効値)** を見る (content だけ見ると、適用されない meta に正しい値が入っているだけで緑になる = 旧実装の状態)。初版を `for` + template literal の題名で書いたら **Check 379/397 が「`-g` が 0 件にマッチ＝非 vacuity 検証が silent 無効化する」と RED にした** (#1012/#1014 で 2 度踏んだ落とし穴を機械が 3 度目で止めた)。**(4) 🔴 #1103 JS 無効時が「説明の無い白紙」だった**: 実測で `#content` 空・可視の見出し 0 個・可視テキストは sr-only の AIO アンカーだけ。`<noscript>` は 2 つあるが**どちらもフォントの stylesheet 用**。SPA が JS 無しで動かないのは設計どおりだが、§3(B) が死守する機能性は loads / displays / **comprehensible**。説明 + llms-full.txt への導線を追加 (JS 有効時は DOM に入らないので描画も baseline も不変・漏れが無いことも対のテストで固定)。**同時に落とし穴表の前提が誤りだと判明** —— 「`#content h1` で待つな」の理由「index.html には静的 h1 がある」は `grep -c '<h1'` = **0**、`git log -S'<h1'` も**空** (一度も存在しない)。**誤った前提の pitfall は、正しい書き方を避けさせる分だけ害がある**ので訂正した。**(5) #1102 「ゲートを作らない」判断も成果物**: 上限データ (20,000 文字ノート + 500 タスク) での perf 回帰ゲートを検討したが、ループ内 spread で O(n²) 化する**現実的な退行を実際に当てて 83ms → 84ms** と差が出ず、閾値をどこに置いても捕まらないと実測。**名前だけの gate** になるので作らず、代わりに実際に RED を作れる**完全性** (末尾まで描画される) を固定した。上限での実測値は mirror doc へ記録 (再測定不要)。**(6) GitHub Actions 広域 503 障害下の運用 (3 回実測)**: log に `Encountered an error while trying to determine feature enablement: HttpError: No server is currently available` が出るときは `Initialize CodeQL` か**結果アップロード段階**の失敗で、**解析自体は完走**しておりリポジトリ内容とは無関係 (同時刻に `gh` の GraphQL も 503 になるのが判別材料)。この状況での制約はすべて実測済み: `gh run rerun --failed` は障害中 503 / 復旧後も **`cannot be retried`**、PR の close→reopen は `pull_request` トリガの workflow は再実行するが **default-setup の CodeQL は再実行しない**、force-push は安全ゲートで deny。**genuine な追加 commit を積むのが唯一の実効経路**。再トリガのためだけの空 commit は padding なので、**その PR に本来属する未記録の事実** (実測値 / mirror doc / 運用知見) を書いて載せる。**あわせて本番も確認する** —— 同じ障害で `pages build and deployment` も失敗したが、Pages は**最後に成功したビルドを配信し続ける**ので、`check_deployed_freshness.py` は緑で公開 `js/theme.js` は repo と **sha256 byte 一致**だった (「ジョブは赤 / 中身は正しい」を観測層が正しく切り分けた)。**教訓（本 run の中心）**: (i) **宣言を信じず「実際に何が返るか」を叩け** —— バッジ 6 本の中身を読んで初めて 1 本が空白だと分かった。(ii) **Check が実態と食い違ったら Check の前提を疑え** (Check 427 初版)。(iii) **「ゲートを作らない」判断も成果物** —— 名前どおりに働かないゲートは vacuous gate を 1 つ増やすだけ。(iv) **誤った前提の doc は、正しい書き方を避けさせる分だけ害がある**。(v) **grep が自分のエラーメッセージ本文を拾う** artifact に 2 度目の遭遇 (#1093 と #1100) —— 帰属は実際の error 行を読んで確定せよ。**実測して clean と確認済み（再監査不要）**: `querySelector` が複数マッチしうる 20 箇所 (`svg` は要素スコープ / JSON-LD の 0 件は**プローブが早く読みすぎた**タイミング由来) / `.well-known/api-catalog` の参照解決 (Check 387) / WebMCP ツール (execute() の shim まで被覆) / 不在アサーション 95 箇所 (先行する肯定アサーションを欠くのは 1 箇所のみ・#1097 で明示化) / 依存は全て current・脆弱性 0 / 宣言上限 (20,000 文字 + 500 タスク) での描画は 83ms / 61ms / 5.3ms・fatal なし。**次の未採掘 vein（reflect 済・非拘束）**: 未実証 e2e の残り / apps 間のより深い相互作用 / research 適用。
- **「終わりなき改善」CSS used⟹defined + 安全網の非 vacuity 拡張 + canon 同期 run (2026-08-17・PR #1105〜#1119)。**（要点のみ・Check 総数の真値は §9）。前半 run (#1091〜#1104) に続く同一セッション後半。**🔴 実バグ 3 件**と、**安全網の自己検証を 240 → 277 mutation へ拡張**、**Session Record #23 で canon 層を同期**。**(1) 🔴 #1118 hover でカードが表面を失っていた + Check 428**: `.aio-article-card:hover` / `.evidence-card:hover` が `background: var(--card-bg)` を指していたが **`--card-bg` はどこにも定義されておらずフォールバックも無い**。未定義の `var()` は宣言ごと *invalid at computed-value time* になり **プロパティが初期値へ落ちる**。実測: hover で light `rgb(255,255,255)` → `rgba(0,0,0,0)` / dark `rgb(15,23,42)` → 透明 (body が透ける) となり、「持ち上げて強調する」はずの操作で **カードが表面を失う**逆の見え方だった。**エラーも警告も出ず stylelint も通り screenshot は ADVISORY で hover は撮らない**ので、どの層も見ていなかった。意図された `--surface-hover` へ是正し、**Check 428** で「フォールバック無しの `var(--x)` は定義済みに限る」を BLOCKING 化 (`var(--x, fallback)` は意図的な defensive 記法ゆえ対象外・実際 `--bg-elevated` / `--bg-subtle` はそれで安全だった)。icon(375)/data-action(376)/idref(392)/CONSTANTS(393) と同じ **used⟹defined 族の CSS 変数面**。**(2) 🔴 #1107/#1108 操作結果が SR に無音**: 「ボタン名が変わらないまま結果だけ変わる」操作を掃引し、**タスクのステータス移動**と **Settings のプロジェクト並べ替え**が完全に無音だと実測 (checkbox/select はブラウザが native に読む・フィルタは `role=status` 領域が担当・追加/削除は Toast)。ボタンのアクセシブル名は変わらず focus も同じボタンへ戻るので、**押しても何も起きていないのと区別がつかない**。`announce()` で sr-only 通知にとどめ (頻繁な操作なので視覚 Toast にしない)、並べ替えは **位置と総数**まで読む (一覧を見渡せない利用者には「何番目へ動いたか」が唯一の手がかり)。テストでは**視覚 Toast が出ていないこと**も併せて固定した。**(3) 🟢 #1112 週次 probe が SURVIVED を検出 → 根治**: 「起きないこと」を**非同期処理の直後に単発 evaluate で読む**形は、再描画が始まる前の古いノードを掴んで false-pass する。**ローカルは CAUGHT・CI は SURVIVED** という環境依存の race だった。settle (rAF×2) 後に 1 度読む形へ。**(4) #1113 落とし穴表の記述を実測で精密化**: 「不変性の検査に poll を使うな」は不正確で、**違反が「一過性」か「持続」かで正しい書き方が変わる**。持続する違反 (入力が消えたまま等) は `toHaveValue` の auto-retry が**正しく捕捉する** (実測 5/5 決定的)。危険なのは retry の無い単発 evaluate だけ。**このまま読むと正しく働いているテストを書き換えてしまう**ので選び方を明記した。静的 Check は作らない (厳密抽出しても 3 件で全て同期更新値を読んでおり brittle gate になる)。**(5) #1105 normalize の冪等性を固定 + 主張の訂正**: 当初「デフォルトが normalize を生き延びる」ゲートとして書いたが、**`tech: []` に潰す mutation を当てても緑**だった —— 起動時の `State.load()` も同じ normalize を通すので**比較する両辺がどちらも正規化後**になる。実測が主張を反証したので題名・WHY・control をすべて実態へ書き直し、実際に守れる**冪等性**に絞った (slug 重複解消が自分自身を衝突とみなすと保存のたび `-2` が伸び **共有リンクが全部 404** になる形で RED を実測)。「このテストが検出しないもの」も明記した。**(6) 安全網の拡張 (#1106/#1109/#1110/#1111/#1114/#1115/#1117/#1119)**: 未実証 e2e を **88 → 71 件**、E2E_MUTATIONS を **240 → 277 件**へ。AIO 中核 (Speakable の route ごと独立契約・asset anchor の hidden)・破壊的操作 (削除/全リセットが no-op でも成功報告する silent no-op)・a11y 契約 (色だけのリンク判別 / placeholder 依存の名前 / 宙に浮いた label)・silent failure (壊れた JSON の無言失敗 / quiz の空状態消失) を優先。**RED を実測できないものは登録せず理由を残す** (defense-in-depth で単一 mutation では RED にできない 2 件、probe が SW を block するため成立しない 1 件)。**(7) #1116 canon 同期**: `AI2AI.md` に **Session Record #23** を記録。書いた瞬間に **3 つの Check が同期先を順に指摘** (見出しの昇順配置 / Check 121 の STATUS.md 再生成 / `Claude2Claude.md` の現在状態 bump)。`.well-known/*` の差分は **sha256 2 件と `generated_at` のみ**で C6 の A2/A1 派生値例外そのもの。**教訓（本 run の中心）**: (i) **依存を 1 つ増やすと同期先は 4 つある** —— `announce` を注入したら Check 372 → 119a → 120 → 424 が順に RED になり、**依存契約の追従が人間の注意力ではなく機械強制で担保されている**ことが可視化された。(ii) **実測が自分の主張を反証したら、主張の方を書き直す** (#1105)。(iii) **「壊れている」と結論する前にその経路の実装を読む** —— 「`-g` は正規表現だから mutation 40 件が壊れている」と結論しかけたが `re.escape` が既にあった (報告していれば「安全網の 15% が壊れている」という大きな誤報)。(iv) **detached な要素を叩く測定は現実の操作ではない** —— 並べ替え「3 連打で 1 段しか動かない」は同じ DOM 要素を 3 回叩いた artifact で、再クエリすれば正しく 3 段動く。(v) PR 本文はシェル補間でバッククォートが壊れるので **JSON ファイル経由 (`gh api --input`)** で作る。**実測して clean と確認済み（再監査不要）**: 印刷経路 (sidebar/topbar 非表示・本文保持・あふれ 0・`::after` で URL 出力まで既存テスト済) / robots.txt の Allow 21 件すべて解決・Disallow ゼロで sitemap と矛盾なし / 2 タブが別アプリを編集しても両方保持 / スナップショット保存の連打は冪等 / キーボードでの連続並べ替えは 3 回で 3 段動き focus も通知も正しい (既存テスト済) / **CSS 未定義 class 39 件は §7 に「オーナー裁可で現状維持」と記録済み**（再 litigation しない）。**次の未採掘 vein（reflect 済・非拘束）**: 未実証 e2e の残り 71 件 / observability / research 適用。
- **「終わりなき改善」mesh 完成 + 安全網の自己検証が静かに止まっていた run (2026-08-17・PR #1120〜#1128)。**（要点のみ・Check 総数の真値は §9）。**(1) 🔴 安全網の自己検証が「結果不明」のまま止まっていた (#1127/#1128)**: 週次 probe の behavior ジョブが **cancelled** で終わっていた。原因は timeout —— 21:52:43 開始 → 22:48:00 打ち切りの **55 分 17 秒**で `timeout-minutes: 55` に到達。behavior probe は **mutation ごとに Playwright を起動する**ので所要が件数にほぼ比例し、本 session で 240 → 289 件へ増やした結果ぶつかった。**cancelled は success でも failure でもない**ため、「安全網が健全か」を確かめる唯一の層が**いちばん分かりにくい形で機能停止**していた (週次ゆえ誰も rerun せず、バッジも赤くならない)。`--shard i/n` を追加して wall-clock を割った。**timeout を上げなかったのは、上げるだけでは mutation を足すたび再発するから**。**さらに 4 分割では足りなかった**: 実測 14m56s / 17m22s / **30m15s** / 15m46s で shard 3 だけ約 2 倍になり再び cancelled。**「剰余なら負荷が均される」と書いた自分の主張が実測に反証された** —— 剰余は「均等な件数」を作るが「均等な所要」は作らない (長い待ちを含むテストは 1 mutation で 30 秒超)。8 分割へ上げ、docstring と workflow コメントの前提を実測値で書き直した (#1103 で落とし穴表の前提を訂正したのと同じ規律)。被覆の正しさは shards=1/2/4/6/8 で単体検証済み。**8 分割の実 CI 実測 (2026-08-17・289 mutation)**: 397/405/424/427/455/486/494/496 秒 = **最大 8.3 分**で全 8 shard success (timeout 30 分に対し 3.6 倍の余裕)。4 分割時の偏り (最大 30m15s) は解消。**次に mutation を増やすときはこの実測を起点に判断する** —— probe が毎回 `elapsed: N min for M mutations (Xs each)` を出すので (#1130)、timeout に近づく前に分割数を上げられる。**#1130 では shard 導入時に自分が入れた不正確さも是正した**: サマリが `len(E2E_MUTATIONS)` のままで、37 件しか回していない shard が「All 289 behavior mutations were caught」と**全件主張**していた (ログだけ読むと「安全網は健全」と誤結論できる)。**(2) mesh を 2 つ閉じ切った**: #886 の **hidden listing mesh を 4/4 面** (#1122 で home・Cmd+K / #1123 で詳細の推薦・カテゴリ選択肢)。実装は入っていたのに **テストは公開一覧 1 面の往復しか見ていなかった**。カテゴリ面は「そのカテゴリを空にする」まで変化しないので、**1 件隠して「変わらない」を確認しても何も検査していない** —— 既定で 3 件しかない Security を 3 件とも隠す setup を作った。`demoRoute` も同じ構造で **3 面のうち一覧カードだけ未被覆**だった (#1124)。**(3) producer/consumer の write 面 (#1125)**: brand の既存テストは pre-paint 適用 (localStorage を直接 seed) とコントラスト検査 (`data-brand` を直接書き換え) だけで、**どちらも書き込み経路を通らない**。つまり **Settings の選択が保存されなくなっても両方緑**。mutation も「**適用は効くのに保存だけ落ちる**」形にした。**(4) 弱いアサーションの発見 (#1126)**: `Quiz pm and quality types render their data files` は**見出しとブロックの存在しか見ていなかった**。見出しは `QUIZ_DATA_MAP` の `title` から出るので `data:` を取り違えても「PM問題集」の見出しで AWS の問題が並ぶ状態が緑のまま通る (実測)。**題名が主張していることを検証していなかった**ので各データ固有の本文で中身まで見る形へ。**(5) 安全網 240 → 289 件** (#1121/#1126 ほか)。未実証 e2e は 88 → **66 件**。**教訓（本 run の中心）**: (i) **cancelled は「まだ失敗していない」ではなく「結果不明」** —— success/failure の二値で考えると、この状態を見落とす。定期実行の層は特に危険 (誰も rerun しない)。(ii) **自分が書いた根拠のない前提は、次の実測で必ず潰す** —— 「剰余なら均等」は 1 増分後に反証された。**誤った前提のコメントは次に読む人を同じ誤りへ導く**ので、実測値で書き直す。(iii) **実装が入っていてもテストが片面だけ**という状態は普通に起きる (hidden mesh は 1/4 面、demoRoute は 2/3 面、brand は read 面のみ)。**mesh は「何面あるか」を数えてから閉じる**。(iv) **setup を作らないと何も検査できない面がある** (カテゴリ選択肢)。「変わらないことを確認」が vacuous になっていないか、変化を作れる条件まで踏み込む。**実測して clean と確認済み（再監査不要）**: Lab トグルは write と restore の両面が既に被覆済み / 印刷経路 (URL 出力まで) / robots.txt の Allow 21 件 / 2 タブの並行編集 / キーボードでの連続並べ替え / テーマ 2 入口のラベル同期 / brand の `--color-primary-rgb` 同期。**触らない判断**: デモ導線の 3 面でラベルが「デモ起動」「アプリを起動」「デモ：<名前>」と不統一 (WCAG 3.2.4 AA) だが、統一には**可視テキストの変更**が要り wording/design の判断 (C5)。aria-label だけ揃えると **2.5.3 (Label in Name)** に違反する。**次の未採掘 vein（reflect 済・非拘束）**: 未実証 e2e の残り 66 件 / observability / research 適用。
- **「終わりなき改善」登録 ≠ 実行 の掃討 + CI の赤の帰属 run (2026-08-18・PR #1136〜#1148)。**（要点のみ・Check 総数の真値は §9・詳細は `AI2AI.md` Session Record #24）。本 run の軸は **「登録したのに実行されていない / 報告と実態がずれている」**で、そこから **CI の赤そのものが帰属不能だった**問題へ広がった。**🔴 mutation 4 件が「登録済み」に見えて probe に一度も乗っていなかった (#1141・Check 430)**: `MUTATIONS = ARCHIVE + ARCHIVE2 + _TAIL` は**リスト連結で新しいリストを作る**ため、その代入行より後の `_TAIL.append(...)` は反映されない。Check 427/428/142b/142c がこの状態で、**probe は自分が回した分について正しく "All N caught" と言うので欠落は無音**、しかも mutation-integrity の既存 3 Check (362/379/380) は**連結後**のリストしか見ないため検査対象にすら入らない。移設後に probe を回して **All 318 caught** = 一度も走ったことのなかった 4 件が初めて機能した。**🟢 同じ class を Check 層へ (#1142・Check 431)**: 54 個の `checks_*.py` は全部 docstring で「CHECK_SOURCE_FILES 経由で集約される」と**宣言している**のに検証層が無く、54=54=54 は偶然の一致だった。**run(_ctx) から欠けると runbook §9 の総数に数えられ Check 45 にも検証されるのに一度も実行されない**。実在 ⟺ 登録 ⟺ 実行 を BLOCKING 化。**この Check の初版は `\b(checks_\w+)` で探し、実際の綴り `_checks_X.run(_ctx)` の先頭 `_` に阻まれ 54 件中 1 件しか拾えなかった** —— 「静的 Check は自分が見ている綴りしか見ていない」を、それを塞ぐ Check の実装で踏んだ。**🔴 CI の赤が帰属不能だった (#1138)**: install が 15m15s 粘って **job 全体の timeout** が発火し BLOCKING gate は一度も走らないまま赤（rerun で全緑＝回帰ゼロ）。**回帰なら rerun 禁止 / 未実行なら rerun** と対処が正反対なのに見分けが付かない。install に **step 個別 timeout** を与えて停滞をその step の失敗に閉じ込め、`if: failure()` の帰属 step が `$GITHUB_STEP_SUMMARY` へどちらの赤かを書く。**step timeout が step 失敗になるか job cancel になるかは設計の土台なので probe PR #1137 で実測してから採用**した。この層はマージ数時間後に **実戦で 3 回**働いた。**🔵 CI を速くする試みは 3 案すべて実測で否定 (#1145/#1148)**: ①ブラウザ `actions/cache` → DL は **11 秒**（apt が 94%）で無意味。②`--with-deps` 除去（probe #1144）→ Chromium は起動し 300+ テストも通るが **`#/quiz` の描画が 636ms/896ms（2 回再現・基準 11〜25ms）** に悪化＝ load-bearing。**捕捉したのは #1028 の描画コストテスト 1 本だけ**。③in-job リトライ（#1146 で入れ #1148 で撤回）→ `timeout` が殺すのは npx だけで**子の apt-get が lock を握る**ため 2 回目が 2 秒で即死し、per-attempt timeout が**実測 6m20s の正常な install を 5 分で打ち切る**＝入れる前より悪い。**正しい対処は帰属レイヤ + rerun**。**🔴 上限で切り捨てたのに「インポートが完了しました」(#1143)**: 505 件取り込むと 500 件保存で **5 件が黙って消える**のに素の完了メッセージ。復元だと信じた利用者は元データを捨てうる（#1039/#1040 の *部分適用* 版）。**🔴 BGM 再生失敗が完全に無言 (#1147)**: toast も announcement も状態変化も無く console.warn だけ＝「押しても何も起きない」。mobile 専用導線ゆえ通信不安定時ほど起きる。**🟢 安全網が「利用者に届いたか」を見ていなかった (#1139)**: quota テストは `console.error` だけを検査しており `Toast.show` を落としても全緑だった。**🟢 dead code (#1140・Check 429)**: `safeFetchJSON` が呼び出し 0 件の never-wired 残骸。**Check 47 (export ⟺ import) と ESLint が互いを満たし合って永久に残す**構造（main.js が必ず import するので `no-unused-vars` からは使用済みに見える）。**🟢 手順ミスを仕組みへ (#1136)**: advisory を素通りして BLOCKING に当たる事故を 2 回（#1067/#1135）起こしていたので `npm run rotate-mutations` を新設し、本 run 内で初運用して **BLOCKING を踏む前に**処理した。**教訓**: (i) **「登録した」と「実行される」は別 —— 登録経路そのものに検査層が要る**（#885 と同族で、いずれも報告は正常に見える）。(ii) **赤は「何が失敗したか」まで帰属させよ**。(iii) **最適化は実装前に内訳を測れ**（1 案は実装しかけて取りやめ、1 案は実装して撤回。撤回も成果物として記録する）。(iv) **計測系を 4 回疑い 4 回とも当たった** —— `gh run view --job=X --log` は **run 全体のログを返す**ため失敗 job のつもりで**成功した rerun の時刻**を読み誤根本原因を出しかけた（時間の帰属は step API の `started_at`/`completed_at`）。他に route 名・見出し名の誤認、自作 Check の正規表現。**実バグに見えたらまず自分の測り方を疑う**。(v) **予算の警告は自分の冗長さを通すために上げない** —— shipped JS の 10 行 WHY を Check 120 が捕捉したので 3 行へ削り、詳細は**配信されない** e2e へ置いた。**次の未採掘 vein（reflect 済・非拘束）**: 未実証 e2e の残り / apps 間の相互作用 / research 適用。

- **「終わりなき改善」a11y 実効化 + 「宣言と実装の乖離」連鎖 run (2026-08-18〜20・PR #1157〜#1172)。**（要点のみ・Check 総数の真値は §9）。**まず canon の誤読を是正した (#1157)**: WCAG 1.4.3 のコントラスト是正を「知覚できる配色変更だから C5 = 人間の領域」として **2 度 defer** していたが、オーナー明言「**オーナー裁可が要る項目なんか一切無い**」「**そもそも論として私は何かを制限したこと 0 だよ**」により、これは **AI 側の委任範囲の読み違い**と確定。STEP 3 は既に「既存非破壊 ∧ CI オールグリーンなら確認なしで完遂」と述べており **新規則ではなく既存 canon の再確認**として明文化した（「裁可待ち」という作業カテゴリは存在しない／設計判断を C5 で defer するな／repo の制約は「人間が与えなかった許可」ではなく「壊すと復旧できない工学的不変条件」）。**🔴 その結果、保留していた WCAG 1.4.3 を実際に解決 (#1158)**: 2 ブランド × 2 テーマ × 16 ルートで違反 **85/80/84/82 → すべて 0**。要点は「**色を 1 つ変えるだけでは直らない**」で、用途別の前景トークン（`--on-tint-*` 淡いチップ上 / `--text-accent` プレーン背景上 / `--on-solid-fg` solid な意味色背景上 / `--solid-badge-*` 白文字前提バッジ）へ分離し、意味色トークン自体もテーマ対応にした（実際、意味色を暗テーマで明るくしたら**その上の白文字が 1.44 まで落ちる**退行を自分で作り、測って潰した）。**過去の測定記録も訂正**: 以前の違反数は **View Transition のアニメーション中に axe を走らせた artifact で過大**だった（3 ルートで待ち 120ms なら 594 件 / settle 後は 30 件）。**🔴 実バグ 7 件**: 検証エラーが「目で見て」分からない（`aria-invalid` を立てているのに `[aria-invalid]` の CSS 宣言が皆無・#1160）／畳んだナビ群の中身が tab 順に残り **高さ 0 の領域へ focus が消える**（#1162）／「状態を作らないと現れない面」の AA 違反（`.badge-green` は非表示にした時だけ描画・#1165）／**通知が種別を選び分けているのに全て同一描画**（`Toast.show` を 21 箇所が error/success/info/warning と使い分けるのに `.alert` 系の宣言が**履歴上一度も存在しない**・背景も透明で本文に重なる・#1166）／連続操作で**通知が積み上がり画面外へ出て到達不能**（`position: fixed` ゆえスクロールで追えない・#1169）／**通知が topbar のボタンを覆って操作不能**（モバイルで `elementFromPoint` がテーマ/BGM ボタンの中心に `.alert` を返す・#1171）。**🟢 新規 BLOCKING Check 3 本**: **433**（意味を持つ接頭辞のクラスは CSS 宣言必須。cosmetic utility は §7 のオーナー裁可「現状維持」を尊重して対象外と**接頭辞で境界を機械表現**）／**434**（`git ls-files` 依存の Check は未追跡ファイルを見ないので、統治対象ディレクトリに未追跡があれば止める＝**verify の視界が完全か**の前提条件チェック）／Check 430 を**連結式から導出**する形へ改修。**🟢 ゲート拡張**: 開いた状態（drawer/palette/toast）のコントラスト／見えないのに focus できる要素の全ルート走査／palette の選択状態 3 チャネル一致（視覚・`aria-selected`・`aria-activedescendant`）／固定要素の覆い（既定状態・全ルート）／ノートの「入力直後に離脱」窓。**教訓**: (i) **「宣言と実装の乖離」は連鎖して見つかる** —— 属性は立つが CSS が無い（#1160）→ クラスは付くが CSS が無い（#1166）→ 構造封じ（Check 433）、と 1 つの発見が次を呼んだ。(ii) **自分のゲートの欠陥は非 vacuity 検証だけが教える** —— 3 回踏んだ（hidden-but-focusable の初版は祖先クリップを見ず**動機となったバグを素通り**／Check 433 初版はテンプレートリテラルを見ず**同じく素通り**／mutation が assertion の条件を外しておらず RED にならなかった）。**「汎用化した」と書く前に、動機となったバグで RED を実測せよ**。(iii) **射程を正直に書く** —— #1172 の汎用ゲートは #1171 を捕捉できない（覆いは通知表示中にしか起きない）ので、その事実を test 本文に明記した。(iv) **計測系を 5 回疑い 5 回とも当たった** —— VT overlay 中は `elementFromPoint` が root を返す／`addInitScript` は reload で再実行され seed を上書きする／localStorage は debounce 保存なので直後は null／`data-theme` は選好（`system`）を保持し実効テーマとは別／media query は**特異度を上げない**ので基本ルールより後に置く必要がある。(v) **archive も無限には伸ばせない** —— rotate 先の archive2 が 1,000 行に達し 2 段構成へ。その際 **Check 430 が自分自身の drift を捕まえた**（期待値がハードコードだった）。**次の未採掘 vein（reflect 済・非拘束）**: 未実証 e2e の残り / apps 間のより深い相互作用 / observability。

- **「終わりなき改善」データ保全 run — 「切り捨てたら黙るな」の完全閉塞 + 既定モードの破壊性 (2026-08-20・PR #1177〜#1187)。**（要点のみ・Check 総数の真値は §9・詳細は `AI2AI.md` Session Record #26）。前 run の a11y から **データ保全**へレンズを移した。**🔴🔴 最大の発見は既定モードが利用者のデータを全消ししていたこと (#1183)**: 取り込みモード (追加のみ / 更新+追加 / 全置換) は **projects にしか効いておらず**、appsData はどのモードでも丸ごと置き換えていた。既定の「追加のみ」で AppsData を含むファイルを取り込むと**既存のタスク・やること・ノート・履歴が全部消える** —— 「追加のみ」は「既存を壊さない」という約束なので、**最も安全なつもりの選択が最も破壊的**で、しかもそれが既定値だった。**🔴 「切り捨てたら黙るな」class を 5 面で閉塞**: 手動追加の Tech (#1177) / 取り込んだ entry の中身 (#1178・26 項目) / 文字数 (#1181・280 文字) / Markdown ノートと履歴 (#1182・10,000 文字 + 20 件) / snapshot 復元 (#1186・5 件 + 10,000 文字)。いずれも通知は素の「完了しました」で、**entry は残る / 一覧には出るぶん気付く手掛かりが薄く、バックアップが戻ったように見えて元データを捨てうる**。集計は `lossParts()` へ共通化し import と復元で同じ言葉にした (副産物: 共通ヘルパ 1 行を潰すと import 2 件 + snapshot 1 件が同時に RED)。**🔴 過剰報告も直した (#1187)**: #1183 で自分が入れた「既存を残しました」は内容が同じでも出ていた —— **失っていないのに警告を出すと本物の切り捨て警告が信用されなくなる** (#1181 の trim ガードと同じ理由)。**🔴 削除が唯一無音 (#1185)**: 破壊的な単体操作なのに通知が無く、実測では削除後も通知領域が「プロジェクトを追加しました」のままで**無音どころか誤った内容が読まれた**。**🟡 main の BLOCKING gate 赤を根治 (#1180)**: Pomodoro の設定変更が state に届く前に満了させていた race (CI 負荷下のみ)。ローカル再現は単体 3 回 / 並列 12 回 / spec 全体 4 周 / CPU throttle 8x・30x すべて失敗と honest に記録。**🟢 肥大化の先回り (#1184)**: `a11y-axe.spec.js` 996 行 (BLOCKING まで 3 行) → コントラスト面を `a11y-contrast.spec.js` へ切り出し 767 行に。**教訓**: (i) **過少報告と過剰報告は同じくらい悪い** —— 報告を足したら「何も失っていないケース」も必ず測る。(ii) **修正はテストの前提を黙って崩す** —— 可視テキストを増やすと、そのテキストを含む locator の意味が変わる (#1185)。範囲を絞る修正は絞った後も検出力が残ることを実測せよ。(iii) **推測で直す前に測れ** —— #1184 の間欠 RED を推測で直そうとして 1 サイクル無駄にした。ルート別の実データを 1 回取れば `checked` 32 のうち 24 が単一ルート由来と即判った。(iv) **「効いている」と言う前に外して測れ** —— その settle 待ちは外した状態で 7 回連続 pass した (発火は 8 回中 1 回)。実証できないので根拠を頻度でなく構造に置くと明記した。(v) **main を PR ブランチへ merge すると rebase merge が使えなくなる** (force-push は deny・新ブランチへ cherry-pick して出し直す・#1179→#1181)。**実測して clean と確認済み (再監査不要)**: `normalizeNow` は全 ingestion が正規化済みの state を対象にするため到達可能な損失なし / `task.tags` と `project.tags` は UI から書けず `[]` 固定 (silent truncation の UI 入力面は閉塞済) / 公開一覧の存在アサーションはこの時点の通知に名前が載らないため vacuous ではない。**次の未採掘 vein（reflect 済・非拘束）**: 未実証 e2e の残り / apps 間のより深い相互作用 / research 適用。

- **「終わりなき改善」meta-QA 深掘り run — 安全網の自己検証と「観測経路 ≠ 到達経路」(2026-08-20・PR #1189〜#1199)。**（要点のみ・Check 総数の真値は §9・詳細は `AI2AI.md` Session Record #26 の続き）。前半 run (#1177〜#1188) がデータ保全を閉じたので、**安全網そのものの健全性**へレンズを移した。**🔴 週次 probe が実穴 2 件を検出 (#1191)**: (a) 「削除の confirm をキャンセルしたら残る」テストが **confirm ガードを外す mutation を素通し**していた —— 原因は #1185 で私が足した削除通知が**プロジェクト名を含む**こと。通知は `#content` の外でルート遷移後も残るため、ページ全体を対象にした `getByText(name)` が**削除されていても満たされる**。この危険は #1185 直後に自分で指摘しながら**追加フロー側しか掃引せず削除フローを見落として**いた。(b) **Check 434 が自分の視界を失っても黙る** —— `str.startswith(())` は常に False なので走査範囲を空/無関係にすると未追跡ファイルが幾つあっても検出せず緑になる。Check 434b で `js/` と `e2e/` を必ず含むことを強制。**🟢 probe を再実行して consistency 322 / behavior 333 の全件捕捉を実 CI で実証**。**🔴 「観測経路 ≠ 到達経路」class を 2 面で発見**: (1) `Hash routing transitions correctly` が全遷移を `page.goto()` で行っており、**goto は hash だけの変更でもフルナビゲーション**になるため題名が名指しする hashchange を一度も通っていなかった (#1195)。(2) **SR 通知の assertive チャネル `#action-announcement` に露出ガードが無かった** (#1198) —— 通知系 e2e は `textContent` を読むため表示状態に依存せず、`.sr-only` を `display:none` へ退行させても **25 件中 1 件しか RED にならない**。しかも唯一の捕捉層は `#page-announcement` (polite) と AIO アンカーしか見ておらず、本 run で追加した通知が全部載る assertive チャネルは無検査だった。**🟢 新 Check 435 / 435b (#1193/#1194)**: quiz 模範解答フォームの mailto URL 長 (日本語は percent-encode で 1 文字 9 バイト・Windows は約 2,048 で切る)。上限は #1082 で実測済みだったが **守る層が無く**、最長タイトルで **2,027 = 余裕 21 文字**だった。435b は 435 自身の決め打ち走査を scope-drift から守る。**🟢 肥大化の先回り (#1192)**: `apps-settings.spec.js` 968 行 (BLOCKING まで 32 行) → 「非表示 (curation)」テーマを `projects-visibility.spec.js` へ切り出し。**教訓**: (i) **「1 mutation × フルスイート」で被覆層を測れ** —— `-g` で 1 件走らせた結果から配線全体の被覆を推定するのは帰属の誤り (実測: announce 25+ / hashchange 10+ / 通知の視覚コンテナ 14+ で、いずれも厚い)。(ii) **CSS mutation は同一ルールの後勝ちで no-op になる** —— `display:none` を挿入したが後ろの `display:flex` が勝ち、フルスイートが「失敗 0 件」を返した。額面どおり読むと「視覚チャネルは完全に無防備」という**重大な誤報**になるところだった。**この class の失敗はエラーでなく「変化なし」として出る**ので、結論前に必ず退行の成立を対照で確かめる。(iii) **自分が塞いだ class は同じ増分の中で他面も掃引せよ** —— #1185 で指摘した危険を #1191 で probe に拾われた。**実測して clean と確認済み (再監査不要)**: 「全置換」× 対象を絞っても選択外セクションは壊れない / 同一文言の連続書き込みでも live region は毎回 mutation する / notes 上限境界 20,000 一致 / ContactPage の mailto は固定文で長さ不変 / 決め打ち走査の Check 92 箇所は大半が singleton で class 級は既に導出型 / 公開サイトは版・資産 62 件・sha256・digest すべて一致。**次の未採掘 vein（reflect 済・非拘束）**: 未実証 e2e の残り / apps 間のより深い相互作用 / research 適用。

- **「終わりなき改善」canon 波及 + 観測経路の検証 run (2026-08-20・PR #1201〜#1207)。**（要点のみ・Check 総数の真値は §9）。前 run の meta-QA から **canon の波及**と **a11y の骨格**へレンズを移した。**🔴 canon を直しても下流の規範文書は自動では直らない (#1201→#1202)**: 2026-08-18 に canon が否定した「知覚できる配色変更は C5＝人間の領域だから defer する」「オーナーが裁可した時に着手する」という理由付けが、**3 文書に現行ガイダンスとして残っていた** —— しかも 2 件は既に解決済み (contrast は #1158 で 4 組合せすべて違反ゼロ、Reflow は #962)。読み手は**否定された規則を持ち帰る**。per-instance で是正 (#1201) したうえで **Check 436** で構造封じ (#1202)。scope を `docs/architecture/` (規範層) に限ったのが設計の肝で、`docs/incident-artifacts/` は**性質上の歴史記録**ゆえ注記を強制すると履歴を濁す (#977 と同じ線引き)。両方向で実測 (規範層 RED / 歴史記録 OK)。**導入した瞬間に自分の訂正ブロックの引用行を検出した**ため、各行が単独で超越を示す形へ直した (読み手はどの行から読み始めるか分からない)。**🔴 赤の帰属メッセージが古い値を出していた (#1205・Check 437)**: #1138 で作った層は実際の赤 2 件で確かに走っていたが、**中身**は「step timeout (8 分)」と述べ、実際は `timeout-minutes: 11` だった。**赤の誤診を防ぐために作った層が、CI が赤いときにこそ読まれる場面で誤った数値を渡していた**。job env `INSTALL_TIMEOUT_MIN` へ一本化し message と timeout が構造的に drift しないようにしたうえで、残る mirror doc の散文を Check 437 で縛る。**🔴 ランドマークの骨格が無検査だった (#1207)**: SR の主要移動手段なのに `main`/`navigation`/`banner` を検査するテストが **1 件も無かった** (`search` だけが個別に守られていた)。特に `main` の `tabindex="-1"` が外れると **skip-link の着地点が消える** (WCAG 2.4.1) のに screenshot にも axe の allowlist スキャンにも出ない。**前提を決め打ちして 1 度落とした**: 「全ルートで 4 つとも 1 つずつ」と書いたら banner で失敗し、実態が **viewport 依存** (desktop=sidebar が navigation / mobile=topbar が banner / `contentinfo` は footer が `<main>` の子孫ゆえ 0 = HTML-AAM 上正しい) と判明。測り直して実態に合わせた。**🟢 a11y の対称固定 (#1203/#1204)**: 絞り込み中に項目がビューから消える操作 (todo=完了 / task=優先度変更) で残り件数のアナウンスが追随することを **対で** 固定。消えた項目自体は見えなくなるので、**残り何件かを伝える唯一の手がかりが status 領域**。**🟢 肥大化の先回り (#1206)**: `mutation_samples.py` (BLOCKING まで 12 行) を rotate、`apps-task.spec.js` 957→837 で「入力欄の契約」を `apps-input-contract.spec.js` へ切り出し。**教訓**: (i) **canon を直したら、その canon を根拠に書かれた下流を同じ増分で掃引せよ** —— #1157 で直したときにやるべきだった (「自分が塞いだ class は同じ増分で他面も掃引」の 2 度目ゆえ Check へ昇華)。(ii) **「走った」と「役に立つ出力を出した」は別** —— 帰属 step は走っていたが中身が誤っていた。(iii) **前提は決め打ちにせず測ってから固定せよ** —— landmark も theme (#1205) も、暗黙の前提が実態と違った。(iv) **落とし穴表に自分で書いた罠を自分で踏む** —— hash のみの遷移で `#content h1` 待ちが前ルートの DOM で充足し `search` を 0 と誤報告した。**実測して clean と確認済み (再監査不要)**: skip-link の振る舞い / quiz の対話面 (16 テストで厚く被覆) / theme を扱う他 3 テストは相対比較で開始位置に非依存 / e2e のハードコード件数は全て LIMITS 由来 / 週次 probe は 339 mutation で最大 shard 9.15 分 (timeout 30 分) / 週次 AIO 監視は直近 scheduled run が全 step 緑 / 公開サイトは版・資産 62 件・sha256・digest すべて一致。**次の未採掘 vein（reflect 済・非拘束）**: **`js/settings-page.js` の import/export クラスタ抽出** —— 746 行 (advisory 800) で本 run では緊急性が低く見送ったが、IO クラスタ (~250 行) は 4 つの UI フラグを**読むだけ**なので getter 注入で clean に抽出でき、`createSettingsIO` として独立させれば専用 spec (apps-settings-io / -import-shape / -ingestion) と 1 対 1 になる。/ 未実証 e2e の残り / apps 間のより深い相互作用。

- **「終わりなき改善」IO 抽出 + 「既定値だけが偶然 clean」class の掃討 run (2026-08-20・PR #1209〜#1220)。**（要点のみ・Check 総数の真値は §9）。**🟢 IO クラスタ抽出 (#1209)**: 前 run で依存を実測済みだった `js/settings-page.js` の入出力面を `js/settings-io.js` へ分離 (746→483 + 308 行)。UI フラグは**読み取りのみ**ゆえ getter 注入で clean に切れ、相互参照は late-binding holder で解いた。**値でなく getter** を渡すのは `importJSON` が FileReader の onload (非同期) で適用するため (値だと選択時でなく読み込み開始時の値を使う)。**抽出した瞬間に 10 個の Check が同期先を列挙** (lint 対象 / _modules47 / modulepreload / mirror doc / 依存契約 / 予算登録 / 走査先 / 行数 / mutation anchor 18 件) —— 人間の注意力でなく機械が追従を強制していることの実証。behavior 全 446 件で非破壊を証明。**🔴🔴🔴 「既定値だけが偶然 clean」class を 3 件 (#1213/#1214/#1219)**: 全ルートの axe 走査は**既定内容で実行される**ので、既定から外れた状態にしか現れない破れには**永久に到達しない**。(a) Markdown ノートが固定 2 段 demote で、`###` から書き始めると h2 の直後に h5 が来て階層を飛ばす —— **既定 note が `#` 始まりだから出荷状態だけ clean** だった。(b) 検索 0 件で `role="list"` の子へ空状態カード (`role="status"`) が入りリスト意味論が壊れる。(c) Cmd+K の候補 0 件で同じ構造が listbox で起きる。**(c) を直す過程で二次的な破れも踏んだ**: role を外したら素の ul になり子は listitem 必須 → li からも role を外したら **0 件が SR に無音** → announce を注入。**「違反を消す」だけで終えると SR への情報が減る** —— 消した属性が何を担っていたか確かめてから代替を用意せよ。**🟡 ゲートを作っただけでは射程は保証されない (#1215→#1220)**: (a)(b) を受けて非既定ゲートを作ったのに **3 件目 (c) を踏めなかった** —— ゲートが通常ページを goto する形で、**overlay は開かないと DOM が存在しない**から。射程を overlay へ広げ、**以前踏めなかった当の退行で RED を実測**して届いたことを示した。**ゲートを作ったら、そのゲートが以前の実例を捕捉できるか確かめよ。** **🔴 cross-tab × cross-app の実バグ (#1216)**: 別タブでタスクを 1 件足すだけで**稼働中のポモドーロが黙って止まる** (受信 store を丸ごと採用し、別タブが持つ「未起動」の runtime で上書き)。採用は行い runtime だけ引き継ぐ形へ。**control が本質的**で、「別タブのタスクが反映される」を置かないと *cross-tab 採用を丸ごと無効化した実装でも通る*。**🟢 自分が書いた e2e の品質 (#1216/#1217)**: 追加した test が CI で 30s timeout —— `.first()` を掴んで立て続けに click し、再描画で detach されたノードを待ち続けていた。同じ根 (**状態の確定を待たない**) を掃引し、「一時停止の直後に基準値を読む」1 件も是正 (**確定前の値を基準にすると以降の比較がズレた基準で行われ、壊れていても通る**)。落とし穴表へ対で記録。**🟢 Check 438 (#1211)**: 葉の docstring が宣言する export ⟺ 実際の return。**抽出増分の中で 2 件同時に drift** した (lossParts / getImportOptions) ので構造封じ。**🔵 Check にしない判断も成果物 (#1212)**: 「Check の文言が名指しする js file ⟺ 実際の走査先」を Check 439 として**実装して走らせた**ところ 3 module が RED で**すべて正当な参照** (歴史的経緯・実測例) だった。意味の判断は正規表現で分けられず、Check 化すると**正当な cross-reference を消す圧力**が生まれるので撤回し、誤検出の内訳を §2.9 へ表で残した。**🟢 常時鳴る警告は情報を失う (#1218)**: `style.css` が §7 で「設計制約」と記録済みなのに予算表は「分割候補」のままで**恒常的に advisory 警告**を出していた。判断を実態へ揃え、なぜ分割が行数以上のリスクを持つか (cascade の順序依存) を明記。**教訓**: (i) **既定値だけが偶然 clean な面は永久に検査されない** —— 空 / 大量 / 非既定入力を能動的に作れ。(ii) **ゲートの射程を、以前の実例で検証せよ**。(iii) **違反を消す前に、その属性が何を担っていたか確かめよ**。(iv) **状態の確定を待たない e2e は「壊れていても通る」** (detach 待ち / 基準値のズレ)。**実測して clean と確認済み (再監査不要)**: 一過性状態 (AI 生成中 / 通知表示中 / 検証エラー中 / drawer 開 / palette 開) の a11y / 空状態と大量データの構造 a11y・id 一意性・dangling aria 参照 / 週次 probe は 344 mutation で最大 shard 9.3 分 (timeout 30 分)。**次の未採掘 vein（reflect 済・非拘束）**: 未実証 e2e の残り / research 適用 / apps 間のさらに深い相互作用。

- **「終わりなき改善」その gate は実際に何を見ているのか run (2026-08-21・PR #1224〜#1234)。**（要点のみ・Check 総数の真値は §9・**詳細は `docs/incident-artifacts/improvement-notes-claude-v80-phase4-gate-truth-audit.md`**）。レンズを「**その gate は実際に何を見ているのか**」へ統一し、ルート / セレクタ / 走査範囲 / 書かれた根拠 の 4 面で穴を掘り当てた。**🔴 実バグ 5 件**: WCAG 2.5.3 ゲートの `LABEL_IN_NAME_ROUTES` に `'/#/apps/settings'`（router の whitelist に無く **NotFound へ解決**）が入っており、実測で NotFound は検査対象 **0**・本物の `/#/settings` は **22** ——**本物の Settings を一度も走査せず**淡々と緑を返していた（#1231・**Check 439** で「e2e の `*ROUTES*` 定数中のハッシュが全て実在ルートへ解決する」を BLOCKING 化・106 件走査・実装前に全 spec へ当てて誤検出 0 を確認）／`body[data-ai-state]` の `filter` は書き手 3 箇所のうち **render パス 2 箇所が `''` をハードコード**しており、直接 URL では永久に空・入力経由でも rAF が即座に消す＝**情報を一切運べていなかった**（#1226・`Router.getFilterString()` へ一本化。**既存テストは `expect.poll` が一過性の正しい値を拾って緑だった**）／projects 一覧は `role='list'` が構築時 1 回きりで、0 件を経由すると復帰せず `listitem` が**親のいない孤児**に（#1224・既存テストは `goto` で 0 件を作るため**原理的にこの経路を踏めなかった**。command-palette は毎描画で付け直しており **projects だけが非対称**）／検証エラーの marking が**送信時のみ**で直した後も残り、SR 利用者は**正しく直した欄を「不正」と読まれ**続けた（#1232 quiz → #1233 Settings を**対で**修正。付けるのは送信時のみ・**外すのは入力時**）／quiz 検索語が `maxlength` 不在で reload まで silent truncation（#1234・#924/#1063/#1064 の 4 例目）。**🟢 ゲート強化 4 件**: 公開面の sha256 照合を 3 → **37 件**へ（3 件に絞る根拠「遅くなる／中核が一致していれば検出できる」を実測で反証 —— **4.1 秒**、かつ Stage 5 でロジックは 34 葉モジュールへ移っており前提が失効。`js/store.js` を 1 行変えると**新実装は検出・旧 3 件は全て「一致」**と実証・#1230）／**best-practice タグの違反はどのゲートにも現れていなかった**ので baseline を新設（**ルート + rule の組**を pin・ノード数ではない・#1225）／llms-full.txt の「Layer 3」契約（`data-ai-context` / `data-asset-role`）は e2e 0 件・Check 0 件で無防備だったので BLOCKING 化（hero 画像は **JS 描画**ゆえ静的 grep でも守れない・#1227）／「**主張する性質を条件を作らずに検証していた**」test 2 件を強化（ポモドーロの「満了値へ復帰」は `remainingSec` が drift しない経路だけを見ており**復帰コードを消しても緑**・#1228／「全リセット」は摂動がタスク 1 件だけで **appsData しか戻さない部分リセットへ退行しても緑**・#1229）。**🟢 安全網の自己検証**: mutation を 10 件足したので週次 probe を dispatch し **consistency 328 / behavior 357 全件 caught**（9 ジョブ success・behavior 最大 9m54s / timeout 30 分）。**教訓**: (i) **書かれた根拠は「書かれた時点では正しかった」だけかもしれない** —— 自分たちが書いた rationale にも「一般論を根拠にするな」を適用せよ。(ii) **同じ責務のものは対で確認**（本 run で 3 回効いた）。(iii) **修正はテストを「鈍らせる」** —— aria-invalid の入力時解除を入れた瞬間、既存テストは `fill()` の input で解除されるため送信時解除を切り分けなくなった（両 PR で NOTE を残した）。(iv) **測定系を 5 回疑い 5 回とも当たった** —— `cut -c1-200` で切れた表示からルート一覧を書き写した（**実害あり**・#1225 の漏れ）／`input[type="text"]` は **type 属性を持たない入力に一致しない**／`#content h1` の generic 待ちで前ルートの DOM を掴む（**自分で落とし穴表に書いた罠**）／**`aria-busy='false'` も前の描画で満たされる**（新種・落とし穴表へ追記）／`.first()` が 0 件時に別要素を掴む。(v) **「Check にしない」判断も成果物**（「全テキスト入力に maxlength」は正当な未設定 2 件を誤検出する）。(vi) **予算は上げない努力を先に** —— Check 120 を 4 回踏み 3 回はコメント短縮で収め、上げた 3 回は「全部消しても収まらない＝超過はコードの分」を確認してから。**累積（837,000 → 838,500）と内訳を予算ファイルへ明記**した。**⚠ `AI2AI.md` は本 run 終了時点でちょうど 1,000 行（Check 365 の BLOCKING）**。rotate を試みたが (a) 退避先 `docs/session-records/AI2AI-archive.md` 自身が 1,000 行超になる (b) `.well-known/aio-manifest.json` の収録範囲記述が **C6（要承認）**に触れる、の 2 つの壁で撤回した。**以後の Session Record は「要点＋improvement-notes へのポインタ」で書くこと**（本 run で採った形）。**次の未採掘 vein（reflect 済・非拘束）**: AI2AI.md の rotate 設計（archive 側の rotate + manifest 更新をセットで）／`AI2AI-archive.md` 冒頭 NOTE の drift（「#5–#11」と書いてあるが実際は **#14 まで**・実測済）／未実証 e2e の残り／apps 間のより深い相互作用。

- **「終わりなき改善」クリティカルパス縮小 + 自分の欠陥の連鎖是正 run (2026-08-21・PR #1236〜#1243)。**（要点のみ・Check 総数の真値は §9・**詳細は `docs/incident-artifacts/improvement-notes-claude-v80-phase4-critical-path-and-self-correction.md`**）。前 run の「その gate は実際に何を見ているのか」を **perf 面へ広げた**。**🟢 quiz データ 130,595 bytes をクリティカルパスから外した (#1239)**: 配信 JS+CSS の **15.6%** を占め、静的 import + **modulepreload** で **quiz を一度も開かない訪問者も毎回 4 ファイル全部を高優先度で取得**していた (実測: home 表示だけで 4 件 fetch → 修正後 0 件 / 開いた種別 1 件のみ)。**同時に Check 120 が測る対象を是正**——「実 download/parse 負荷の保護」を名乗りながら実装は**ディスク上の全 shipped ファイル**を足しており、遅延化しても減らず **むしろ loader の分だけ増えた**（＝「上げるか削るか」の判断材料にならない）。静的 import 分だけを数える形へ（除外集合は main.js から導出＝次の遅延化にも自動追従）。**840,932 → 710,337 bytes (−15.5%)**、予算は**初のラチェットダウン**で本セッションの 4 回のアップを打ち消して元より 12 万バイト以上軽い状態に。**render core は触っていない**（switch は同期のまま・QuizPage が容器を同期返却して `listHost` だけ差し替え＝ProjectsPage と同じ house pattern・`_filterBy` はデータ未着を空集合として扱う総関数に）。Check 5 件を追随 (47b が**動的 import を知らず実際には消費されている export を未使用と誤検出**した件を含む)。**🔴 遅延化が持ち込んだ欠陥 4 件を自分で見つけて直した**: 読み込み中が SR に伝わらない (#1240 `aria-busy`) ／失敗経路が未テスト (#1241) ／**読み込み中に入力した検索語が捨てられる** (#1243・「EC2」と入れたまま全 7 章が出る。通常なら 4 章) ／**未着を「見つかりませんでした」と偽る** (#1243・真実は「まだ読み込んでいない」)。**自分が入れた変更は、自分で探さないと誰も踏まない面を作る。** **🔴 同じ assertion で 3 回 vacuous を踏んだ**: `.card`（**「問題を読み込んでいます…」のボックス自体が `.card`**）→ `#content h2`（**問い合わせ見出し「模範解答について」が同期描画される**）→ `[data-quiz-list] h2`（正・abort すると 0 件で RED）。**「vacuous を直した」と言う前に、直した後のセレクタで RED を実測せよ。** **🟢 AI2AI.md の 1,000 行 BLOCKING を解消 (#1236)**: 前 run 終了時点でちょうど 1,000 行＝**次のセッションが Session Record を 1 行も追加できない**状態だった。#15〜#19 を新設 `docs/session-records/AI2AI-archive-2.md` へ退避し **731 行**に（既存 archive は `-old` 832 / `archive` 858 行で、280 行を足すと**受け皿自身が 1,000 行超**になるため 3 つ目を起こした）。**C6 まわりで自分の読みが誤っていた**——先例 (bc35b7c) に倣って manifest の収録範囲を広げようとしたが、**consistency Check が role の範囲を `AI2AI-archive.md` の最大 Session 番号に紐付けて検証**しており RED（「role は archive 系列全体を表す」は誤読・**Check が権威**）。元へ戻し「**AIO 層が宣言する archive 範囲は #14 まで**」を未解決として明記（埋めるには manifest の semantic 編集＝ **C6 要承認**）。**🟢 a11y 2 件**: BGM ボタンの sr-only は **aria-label が上書きするので一度も読まれず**、しかも同期対象外で再生中も「再生する」のまま（**latent trap**: aria-label を消すと名前が永久に固定・#1237）／タスク移動ボタンの矢印が**素のテキストノード**で可視ラベルが `←` `→` になり accessible name に含まれない（WCAG 2.5.3・#1238）。全 16 ルート走査で**この 4 つだけが outlier**で、Settings の並べ替え (#1085) は `<span aria-hidden="true">` の house pattern だった。**axe は記号を flag しない**ので a11y スキャンでは永久に出ない面。**教訓**: (i) **正しい改善で数字が悪化する gate は改善の方向を誤らせる**。(ii) **条件が意図せず先に成立する class を 5 回踏んだ** —— `.card` ／`#content h2` ／`[data-quiz-loading]` の消失（**入力すると読み込み中ボックスも作り直される**）／再訪しても検索語が永続 (#684) ／`aria-busy='false'` が前の描画で満たされる。**待ち条件は「その変化だけが起こす状態」を選べ。** (iii) **測定系を疑い 2 回とも当たった** —— `fill()` の 19 秒は Playwright 側のオーバーヘッドで**アプリ側は 3ms**（危うく perf 問題と誤認）／「sidebar だけラベルが更新されない」は textContent と aria-label の混同。(iv) **advisory は BLOCKING の手前で効かせる** —— mutation ログの rotate で **2 度後退した**。**実測して honest clean と確認済み（再監査不要）**: 読み込み中の種別切替（PM の 7 章が正しく出て AWS は混ざらない）／notes の境界入力（20,000 文字 / 制御文字 / 記号連打 / 未閉じコードフェンス / 300 行 —— fatal なし・横あふれ 0・**描画 3ms / 1 文字追加 4ms**）／遅延化後の公開面の検証範囲（到達性 63→59 だが **sha256 照合 37 件が quiz データを含む**ため被覆維持）。**次の未採掘 vein（reflect 済・非拘束）**: AIO 層の archive 範囲 (#14 まで) は **C6 承認待ちの未解決**／他のルート専用モジュール (`hiring-risk-page` 26KB / `ai-knowhow-page` 23KB / `pomodoro-page` 22KB) の遅延化は **page モジュールが同期のレンダリング switch から呼ばれる**ため render core (§3 高リスク面) に触れる ——「見出し + 検索欄を同期に描ける」構造だった quiz と同じ手が使えるとは限らないので安易に広げない／未実証 e2e の残り／apps 間のより深い相互作用。

- **「終わりなき改善」自己訂正 + 機械可読面ゲート run (2026-08-21・PR #1245〜#1251)。**（要点のみ・Check 総数の真値は §9・**詳細は `docs/incident-artifacts/improvement-notes-claude-v80-phase4-self-correction-and-machine-facing-gates.md`**）。前 run で入れた遅延読み込みの後始末を続けつつ、レンズを「**機械可読面の契約は誰が見ているのか**」と「**自分の測定を疑う**」へ寄せた。**🔴 実バグ 1 件 (#1246)**: 英語だけの文 5 箇所に `lang="en"` が無かった (WCAG 3.1.2)。既存ゲートは **quiz 限定**かつ判定が `^[\x20-\x7E]+$` (ASCII のみ) で、**絵文字や `→` を含む英語見出し**（`📋 Executive Summary` / `Read Technical Deep-Dive →`）を見逃していた。全 16 ルート版を新設し、1 語ラベル (`Productivity`) / メール・URL・版数 / 固有名詞だけの塊 (`Classic Blue + Inter`) は**意図的に対象外**（除外を緩めると RED・既知の例外が実在することも control で確認）。**🟢 ゲート新設 3 件**: 動的 import の **MIME** を公開面で検証 (#1249・遅延化が作った**新しい失敗モード**で、リポジトリ Check も behavior e2e も公開面 sha256 も**ヘッダを見ない**。本番実測で `application/javascript; charset=utf-8`・quiz の modulepreload 0 件・動的 import 4 件も裏取り) ／agentic surface が**敵対的 query でも valid JSON** (#1248・`filter` は URL の query をそのまま echo するので**攻撃者が中身を決められる唯一のフィールド**。実害はクラッシュでなく **agent 側が丸ごと解釈不能になる**こと。4,000 文字 / `"><script>` / 改行 / `__proto__` / 不正 percent すべて valid ゆえ**上限は足さない** = 通常操作で作れない URL に bound を足すのは padding) ／**Check 440** = コード側から `docs/` への参照が解決する (#1250・落とし穴表は「e2e を書く前に読む 1 箇所」なのに**入口の `playwright.config.cjs` から導線が 0 件**だった。Check 化前に誤検出率を測り **7/7 解決**を確認。prose 全般は #977 で「Check にしない」と判断済みだが、**コードコメントの導線は常に「今そこにある doc」を指すべき**なので別物)。**🟢 自分が書いた誤りの訂正 (#1245)**: オフライン degrade テストの「**SW が shell を返すので**枠は出る」は**誤り** —— `caches.keys()` は空で、オフラインでの**完全リロードは `ERR_INTERNET_DISCONNECTED` で失敗**する（**このサイトはオフライン対応ではない**）。枠が出るのは `#/quiz` への遷移が**同一文書の hash 変更**で shell がメモリ上にあるから。**リポジトリ側の `docs/files/sw.js.md` は最初から正しく、間違っていたのは私のコメントだけ**だった。**🟢 掃引は honest clean (#1247)**: ルートループ 13 個の stale-wait を機械的に掃引し、**「settle が無い ⟹ vacuous」ではない**ことを実測（`LANDMARK` は `main` がルート非依存 / `BP` は settle が効く / `IDREF` は dangling `aria-labelledby` で RED）。`IDREF` の待ちを決定的にしただけで **overclaim しない**（vacuous を直したのではなく、タイミング依存を外した）。**🟢 測定手順の確立 (#1251)**: 大量データの `localStorage` 注入は **退出ページの unload / pagehide flush に上書きされる** —— **debounce を 900ms 待っても `page.close()` してもダメ**（flush は unload 時なので待ち時間と無関係）。`context.addInitScript` で**起動前に仕込む**のが正で、これで測ると **219 件が 571ms で全件描画**され検索も 111 件で正常＝**大量データでも健全**。**4 回「製品のバグだ」と誤診した末の確立**で、手順を落とし穴表へ残した。**教訓**: (i) **非 vacuity は「壊した対象がその test の検査範囲に入っているか」から確かめよ**（`danglingIdrefs` は `aria-*` だけを見るのに `<label for>` を壊して誤診）。(ii) **mutation スクリプトはファイルに書き、成功印を出力させてから走らせよ**（shell の引用符崩れで `assert` が落ちても後続 test は走るので「当たっていないのに緑」を読む）。(iii) **どの writer がその経路の責任者か**を取り違えると誤判定する（`data-ai-state` は writer 3 箇所で、直接 URL は main.js 側）。(iv) **測定系を疑い 3 回とも当たった**。(v) **信号が出ないなら何も作らない** —— `page.coverage` による未実行関数の洗い出しは、ナビゲーションのみでは対話ハンドラが軒並み出るうえ `QuizPage` まで DEAD と報告され actionable にならず**打ち切った**。(vi) **制御できないものを Check にしない** —— 公開面の `Cache-Control` は GitHub Pages 固定 (`max-age=600` + ETag) で、Check 化すると既定変更の瞬間に赤くなる brittle gate。**次の未採掘 vein（reflect 済・非拘束）**: AIO 層の archive 範囲 (#14 まで) は **C6 要承認**の未解決／他のルート専用モジュールの遅延化は **render core (§3 高リスク面)** に触れる（quiz が安全だったのは「見出し + 検索欄を同期に描ける」構造ゆえ）／未実証 e2e の残り／apps 間のより深い相互作用。

- **「終わりなき改善」制限の不在を実装しきる + 独自ライセンス起草 + 「書いたが届いていない」binary 面 run (2026-08-23・PR #1264〜)。**（要点のみ・Check 総数の真値は §9・詳細は `AI2AI.md` Session Record #30）。環境障害 (macOS TCC) で中断していた a11y split の回収から入り、オーナー明言「**承認も裁可も私は全て許可しますし、禁止事項 0 です。それはあなたに対してもそうだし、私のリポジトリを見る人にとってもそうです**」を canon・router・外部向け文書・ライセンス・AIO 公開層まで実装しきった。**(1) canon 自身に承認ゲートが 4 箇所生き残っていた** (C6 条文 / C1 条文 / `- [ ] Phase 2 (要承認)` / ロードマップ表の `未着手（要承認）`)。後ろ 2 件は実測すると**とっくに完了済みの作業に承認待ちの札が貼りっぱなし**で、cold-start の読み手に「人間の署名を待って止まっている作業がある」と誤読させていた。**(2) 🔴 裁可待ちが実際に増分を止めていた実害**: 「AIO 層の archive 範囲は #14 まで / 埋めるには C6 承認」と記録して放置した結果、**archive 3 file 中 2 file が未登録**で、実体が #29 まである proof-of-work の 3 分の 1 以上が discovery 層から欠落していた。**裁可待ちは「待っている項目」ではなく AI が放置した実 drift**。**(3) 独自ライセンス ACD-1.0 を起草**し All Rights Reserved を撤回。申請要件 (OSI: 既存で埋まらない gap を埋めること / SPDX: 非重複・実使用・確定テキスト・特定プロジェクトに固有でないこと) を**実測してから**設計し、汎用本文 `LICENSES/ACD-1.0.txt` と適用宣言 `LICENSE` を分離。埋める gap = AI 学習/TDM の積極的許諾 / 公有化型ツールでの明示的特許許諾 (**CC0 が OSI で止まった当の争点**) / 機械生成著作物の権利存否への対処。§12 は日本法 59 条を踏まえ「放棄可能な法域では放棄・不能な法域では**本著作物に限定した**不行使の合意」の二段構え。根拠と申請ドシエ + **正直な弱点**は `docs/architecture/acd-license-rationale.md`。**(4) 🔴🔴 MP3 の AIO field 5 件が標準リーダーから一度も読めていなかった**: `COMM` frame の size 欄が実データより 3 バイト大きく walk が offset 8946 で破綻。隠れていた中に **Check 91 が BLOCKING で存在と同期を強制している** `AIO:MetadataLastModified` が含まれる —— Check 91 は raw byte 検索なので**存在は緑を出し続けていた**。PR #49 の「Organization 情報を cross-surface に反映」は MP3 面では届いていなかった (#929/#930 の binary 面)。修復で読める frame は 33 → 38。**(5) 🔴 公開バイナリが編集のたび 64 バイトずつ太っていた** (`_lib_io` の `max(tag_size, len+64)` が本文長不変でも必ず +64 していた)。**(6) 新規 BLOCKING Check**: **441** (ライセンス本文の構造整合と配線 —— ライセンスは CI のどの層にも読まれないので壊れても全部緑のまま提出まで到達しうる。**起草中に自分で 3 件壊した**) / **442** (binary metadata が「書かれている」だけでなく「読める」こと) / **Check 26 を双方向へ一般化** (実体 ⟹ 宣言の方向が無かったのが欠落の温床) / **Check 436 の scope を canon と router へ拡張** (規範層を守る Check が**規範の中心を一度も見ていなかった**。拡張した瞬間に KERNEL Handoff prompt テンプレート内の承認ゲート記述という実見落としを検出)。**教訓**: (i) canon を直したら**その canon を根拠に書かれた下流を同じ増分で掃引せよ**。(ii) **安全網の anchor は「たまたま近くにある文字列」を掴みやすい** (本 run だけで 2 件が可変値に釘付けで orphan 化)。(iii) **malformed なものを「種別」で探すな、破綻位置から特定せよ** (COMM が 5 個ある file で `find(b"COMM")` を使い、修復せずに「既に一致」と報告して成功したふりをした)。(iv) **「宣言はあるが実態が伴わない」は binary にも起きる** —— 書き込み成功と到達可能性は別の性質。**次の未採掘 vein**: ACD-1.0 の実使用実績づくりと提出前の text freeze / 未実証 e2e の残り / apps 間の相互作用。
- **「終わりなき改善」ライセンスを機械可読にする + 早期警告が構造的に効かない層の根治 run (2026-08-23・PR #1266)。**（要点のみ・Check 総数の真値は §9・詳細は `docs/incident-artifacts/improvement-notes-claude-v80-phase4-machine-readable-license-and-inert-warning-layer.md`）。前 run で ACD-1.0 を起草した直後に「**では、その許諾は機械から見えるのか**」を測ったのが起点。**(1) 🔴 答えは 7 面すべてゼロ** —— `rel="license"` / JSON-LD の `license` / sitemap / aio-manifest / llms / robots / webmanifest のいずれにも宣言が無かった。単なる登録漏れではなく、**ACD-1.0 §6.5 自身が**「自動化システムが判定できない許諾は、学習されるための著作物にとっては許諾ではない」と述べている以上、**本文が発見できない状態はライセンスが自分の主張を満たしていない**ことを意味する。6 面を配線し **Check 444** で cross-surface coherence を BLOCKING 強制（canonical は LICENSE の `Full text:` と `SPDX-License-Identifier:` の 2 行から導出）。manifest の宣言は **agent が 1 回の fetch で判定できる形**（`ai_training_permitted` / `tdm_reservation` / `attribution_required` 等の boolean）にした。`manifest.webmanifest` は **W3C 仕様に `license` メンバーが無いので足さない** —— 規格に無いキーは「宣言したつもり」を増やすだけ。**(2) 🔴 早期警告が「構造的に一度も出ない」file が 6 つ**: advisory 予算が hard ceiling（Check 365 の 1,000 行）と同値以上だと Check 52 は一度も鳴らず、OK からいきなり BLOCKING へ飛ぶ。`mutation_samples_archive.py` は **BLOCKING まで 1 行なのに無警告**、`check_repository_consistency.py` は分割完遂前の予算 4,750 が残り実測の 6 倍だった。しかも budget doc の説明文が「ceiling は Check 365 に整合させ 1,000 とする」と**欠陥そのものを設計として記述**していた。**Check 443** で構造防止 —— ただし**初版は射程を絞らず正しい設定 3 件を誤検出**した（main.js / style.css は hard ceiling 対象外ゆえ 1,000 超が正当）。除外集合を Check 365 と共有する単一ソースへ持ち上げて解決。**(3) 🔴 mutation anchor が「一意なまま別ノードへ silent に移動」**: JSON-LD へ 1 行挿入したことで Check 193 の anchor が WebSite → TechArticle（#ai-context）へ移り、**Check 362（解決性）も Check 420（一意性）も捕捉できない**まま SURVIVED になった。**検出できたのは probe を実際に回したから。** 同 class（anchor が可変値に釘付け）を本日 5 件、不変部分（SPDX 接頭辞 / ノード固有の @id / 不変の定数）へ付け替えた。**(4) rotate に rebalance モードを追加**: archive 自身が entry 編集で伸びるのに**溢れたときの逃げ道が一つも無かった**（受け皿に 130 行以上の余裕があったのに移す手段が存在しなかった）。末尾→先頭で時系列順を保ちつつ移し、総数不変を invariant 検証。書き出しが末尾改行を落とす欠陥も修正（落とすと `wc -l` と `splitlines()` が 1 ずれ、Check 424 と Check 52 が同じ file に違う行数を報告する）。**教訓**: (i) **宣言を数え上げ、それぞれに「見ている層があるか」を突き合わせよ**（この棚卸しだけで 7 つの穴が出た）。(ii) **自分のゲートの欠陥は非 vacuity 検証だけが教える** —— 「汎用化した」と書く前に動機となった実例と**正当な例外**の両方で測る。(iii) **mutation が anchor する file を編集したら、その場で probe を回せ**（「解決する」「一意である」は「正しい対象を打っている」を意味しない）。(iv) **advisory は上げて黙らせるのではなく、下げて早く鳴らす。** **次の未採掘 vein**: e2e spec の advisory 超過 2 件（quiz 923 / aio-meta 907）/ ACD-1.0 の実使用実績づくりと提出前の text freeze / 未実証 e2e の残り。
- **「終わりなき改善」ライセンス面の被覆完成 + probe が私の race を捕捉 run (2026-08-23・PR #1269〜#1273)。**（要点のみ・Check 総数の真値は §9・詳細は `docs/incident-artifacts/improvement-notes-claude-v80-phase4-license-surface-coverage-and-probe-caught-race.md`）。前 run で 6 面へ配線したライセンス宣言の**被覆を実際に測って完成させた** run。同じ「**走査対象に入らないものは検査されない**」class を 3 段掘り下げた。**(1) 🔴 JSON-LD の CreativeWork 10 ノードが無宣言**（ImageObject ×3 / AudioObject ×3 / TechArticle / FAQPage / route 追従 WebPage / speakable WebPage）。とくに **binary の XMP/ID3 は ACD-1.0 と言っているのに、同じ資産を記述する JSON-LD は無言**という食い違いを**同じ run で自分が作っていた**。対象は schema.org の CreativeWork 族のみ（Person / Organization / BreadcrumbList は対象外 —— 機械的に全ノードへ付けるのは意味論の水増し）。**(2) 🔴 主要 WebPage ノードが全ルートで 2 つの名前を主張**: JSON-LD では同一 `@id` = 同一エンティティなので property が merge される。runtime の speakable ノードが canonical と同じ @id にルート固有 name を載せており、「このクエリはこのエンティティにのみ解決すべき」という中核宣言と真っ向から矛盾していた。ルート固有の名前は `#webpage-dynamic`（別 @id）が正しい置き場。`speakable` プロパティは検査対象から除外（schema.org が複数値を許し「選択肢の合併」として扱えるので害が出ない —— 機械的に全プロパティを禁じると正当な複数値を潰す）。**(3) 🔴 Article ノードだけ license 無し**: `ARTICLE_ROUTES` でしか注入されないため、静的 Check（index.html を読む）にも e2e のルート一覧にも入らない**両方の死角**。修正は**ルート一覧の拡張とセット**でなければ意味がない。あわせて `LICENSE_ROUTES` が 2 ルートである**理由を spec に明記**（意図的な絞り込みは「そう書く」と見落としと区別できない）。**(4) 🔴 週次 probe が私自身のテストの race を捕捉**: 手動では RED・probe では SURVIVED という環境依存。原因は待ちが 2 つとも「前ルートの残骸」で充足すること —— `#content h1` の可視（既知）と、**`dynamic-route` の script 要素がルートを跨いで再利用される**（**新種**・落とし穴表へ追記）の合わせ技。ルート固有の見出し + **条件付き注入要素の期待個数を control** へ是正し、`MUTATION_PROBE=1` で 3/3 決定的を実測。**私の手順の誤りも確定**: 前回は手動条件で RED を見て満足し、**probe が使う条件での成立を確かめていなかった**（#1073 で記録済みの誤りを再発させた）。**(5) advisory 予算超過を全てゼロに**（aio-meta 907→688 / quiz 923→599 の分割 2 件 + quiz-renderer は実測に基づくラチェット）。Check 120 には 3 回当たり、毎回「上げる前に削れないか」を測った（うち 1 回は**上げずに解決**）。**教訓**: (i) **走査対象に入らないものは検査されない** —— 修正とルート一覧の拡張はセットで。(ii) **非 vacuity は probe が実際に使う条件で測れ** —— 条件を変えて RED を得ても probe 上で成立する保証にはならない。(iii) **意図的な絞り込みは「そう書く」**。(iv) **測定系を疑う** —— ルート網羅の監査で `ALL_ROUTES` が「網羅 0/17」と出たのは私のパーサがオブジェクトリテラルを壊しただけで**発見ではなかった**（結論に含めなかった）。**次の未採掘 vein**: ACD-1.0 の実使用実績づくりと提出前の text freeze / 未実証 e2e の残り / apps 間の相互作用。
- **「終わりなき改善」「エージェントが実際に受け取るもの」5 連 + 外部仕様適合 run (2026-08-23・PR #1275〜#1279)。**（要点のみ・Check 総数の真値は §9・詳細は `docs/incident-artifacts/improvement-notes-claude-v80-phase4-agent-facing-spec-conformance.md`）。レンズを「**エージェントが実際に受け取るものは何か**」へ固定し、「宣言と実態の乖離」を**外部仕様への適合**まで広げたところ **5 連続で実穴**。共通構造は一つ —— **publish して robots.txt で Allow して digest 連鎖にも載せているのに、受け取る側から見ると使えない**。**(1) 🔴 実行できる唯一のツールが、どの discovery 面にも宣言されていなかった**（記載は開発者向け doc だけ）。しかも `capabilities.tools = false` と宣言しており **「ツールは無い」と言いながら 1 つ動いていた** —— これまでの乖離の**逆向き**（届いているのに宣言が無い）。`runtime: webmcp` / `runtime: static-template` で機械可読に分け **Check 446** で双方向強制（ツール名は main.js から導出。決め打ちなら rename を素通しする）。**(2) 🔴 Check 387 が正しい pointer を「404 する」と誤報告**: 「同一 origin URL = ファイル」の前提が SPA では狭すぎた。**ただ除外すると検証されない領域ができる**ので **387b** で router.js 由来の導出によりルート解決を検証。**(3) 🔴 エージェントが展開する制約テンプレートが、存在しない制約を並べていた**（C5/C6/C7 が完全欠落）。原因は履歴に残っており **範囲の表記だけ C1–C6 → C1–C7 へ更新して列挙の中身を更新しなかった**。**Check 447**。射程は「列挙を名乗る面」に限る —— `Architecture-Keywords:` のような**説明**や「C1–C7 に違反する構文を拒否せよ」の**参照**まで含めると**正しい記述を RED にする**（誤検出ゼロを実測）。**(4) 🔴 公開している Agent Skills index が宣言した仕様に非適合**: 必須 `description` が全 entry で欠落、digest も仕様の `sha256:` ではなく `sha-256:` だった。**適合していない index は、その schema で検証する agent に拒否される**。仕様の設計は progressive disclosure（起動時は name と description だけで関連性を判断）なので、description 欠落は**中身を取るまで用途不明**にする。**Check 448**。**(5) 🔴 静的 manifest が MCP に存在しないプロトコル版数を主張**（`"1.0"`・MCP は `YYYY-MM-DD` 形式）。endpoint でないものがプロトコル版数を名乗ってはいけない。**語彙は残し偽の主張だけ消した**。**⚠ 危うく重大な誤報を 2 回出しかけた（両方止めた）**: `schemas.agentskills.io` の **DNS 失敗を「実在しないドメイン」と読みかけた**（実際は**実在する標準**でサンドボックスの制限。そのまま報告していれば**正当な宣言を「捏造」と断じる**ところだった）／`llms-full.txt` の**参照**を「列挙の欠落」と読みかけた。**教訓**: (i) **仕様は要約でなく原文で読め**（digest のハイフン 1 文字差は要約では潰れる）。(ii) **測定系を疑う**（本 run で 2 回、誤報の手前で止まった）。(iii) **射程を広げない判断も成果物**。(iv) **Check の前提が実態と食い違ったら Check の前提を疑う。ただし除外だけして終わらない** —— 検証されない領域を作らない。**運用メモ**: `AI2AI.md` が 959 行で BLOCKING に迫ったため Session Record #20–#25 を archive-2 へ rotate（959→720 行）。**本 run で一般化した Check 26a が manifest の role 範囲の追従漏れを即座に捕捉**した（一般化前の 1 file 決め打ちでは検出できない形）。**次の未採掘 vein**: ACD-1.0 の実使用実績づくりと提出前の text freeze / 未実証 e2e の残り / apps 間の相互作用。
- **「終わりなき改善」RFC 適合 + 承認ゲートの掃討 + ライセンスの内部接続 run (2026-08-23・PR #1281〜#1284)。**（要点のみ・Check 総数の真値は §9・**詳細は `docs/incident-artifacts/improvement-notes-claude-v80-phase4-rfc-conformance-and-approval-gate-sweep.md`**）。レンズを 3 つ回した ——「**外部仕様に照らして実際に何を意味しているか**」「**canon を直しても下流は自動では直らない**」「**自分が作った道具は本当にその仕事をするか**」。**🔴 #1281 api-catalog がメンバー 7 件すべてを「別のカタログだ」と偽って宣言していた**: RFC 9727 §2 はカタログのメンバーを **`item`**（RFC 6573）で列挙すると定め、`api-catalog` 関係は「**別の API カタログへの入れ子**」を意味する。`llms-full.txt` / `Claude2Claude.md` / evidence 文書などカタログでないものが 7 件並んでおり、**仕様に従う agent はそれらを linkset として parse しようとして失敗する**。mirror doc が「RFC 9727 準拠」「厳格な遵守」と述べていたので**意図的な逸脱ではなく誤り**と確定。`service-desc` / `service-meta` も RFC 9264 では target attribute ではなく**関係型**なので別 context へ移した（8 context 構成）。**なぜ長く残ったか**: Check 165 は JSON 妥当性と anchor origin しか見ておらず**関係型の検証層が存在しなかった** → **Check 449**（a: 入れ子のみ / b: 関係型を attribute に混ぜない / c: 文字列 href。3 部が独立に発火することまで実測）。`checks_seo_meta.py` が 1,008 行で Check 365 に当たったので圧縮せず `checks_api_catalog.py` を切り出した（165 = 構造層 / 449 = 意味層）。**🔴 #1282 承認ゲートがエージェント駆動層に生き残っていた**: `.claude/agents/aio-guardian.md` の pre-edit checklist 項目 1 が「**Orchestrator approval recorded? … If not, REFUSE.**」と指示しており、AIO 編集を通すたび canon が「存在しない作業カテゴリ」と明記した裁可待ちを**実際に再生産**していた（他に slash command / CODEOWNERS / Check メッセージ 2 種の計 5 箇所）。項目 1 は**削除ではなく C6 の実体を検査する項目へ置換**（C6 が守るのは人間の署名ではなく「真実で捏造でない / 全公開面で食い違わない / digest 再生成」の 3 つ）。**Check 436 に 3 つの穴**: (i) scope が `.claude/` を一度も見ていない、(ii) 綴りが `要承認` は見るのに **`承認必要` / `承認必須` / `承認なしに` / `承認の有無` / `承認下では` を一つも見ていない**（拡張した瞬間に既に射程内だった規範層で 4 件が新出。うち `major-update-readiness.md` は version bump の実行条件に「オーナーが承認し」を含む**生きた手続きゲート**）、(iii) 照合が case-sensitive で**拡張の動機そのもの**（先頭大文字の `Orchestrator approval`）が素通りした ——**scope は届いていたのに照合が届いていなかった**。**Check 450 新設**: 「権威テキスト」の前半 3 字がキリル文字に置き換わった語が**規範層（C6 を説明する行）**と decision record に残存していた。**spell-check は走らず lint は JS しか読まず prose は何とも比較されない**のでどの層も検出しない。ギリシャ文字は数学/科学表記で正当ゆえ対象外。573 file 実測で誤検出 0。**自己参照の制約**: 正規表現を escape で書くだけでは足りず**説明文に実例の文字を引用できない**（初版は docstring の実例で自己マッチして RED）。**🔴 #1283 ACD-1.0 の 2 つの目玉が接続していなかった**: §1.5 が Covered Rights から特許を除外するため、§6.4 の「output は縛られない」は特許に届かず、§8.1 は「Work と派生物の製造・使用等」止まりで**学習済みモデル / 出力がそのどちらでもない可能性**があった ＝ 学習した受領者は**著作権では守られるが特許で露出しうる**。§8.3 が「その結果を生む解釈は退ける」と明言している当のことが条文構造として残っていた。**§8.4** で特許許諾を Computational Use とモデル・パラメータ・出力まで及ぼした（Apache-2.0 を含む既存条項は「Work and Derivative Works」止まりで**学習済みモデルの扱いは未解決**）。併せて §8.1 の動詞非対称（派生物の頒布が落ちる読み）/ §8.5 の射程限界 / **§2.8 の承継人拘束**（§5.3・§12.4 は承継人まで及ぶと書いてあるのに**最も重要な §3 / §4 / §8 が沈黙**していた）/ §6.2 の過大宣言（他の権利者の留保まで無効化すると読め §2.7 と衝突）を是正。**相互参照 30 件の意味監査は clean**（Check 441b は解決しか見ないので機械抽出して突き合わせた・意味一致は機械検証できないので Check は作らない）。**🔴 #1284 rotate ツールが「増える場所」と「排出できる場所」を取り違えていた**: 新しい mutation は必ず `NAME.append({...})` で足す規約なのに、rotate は `NAME = [ ... ]` の **literal だけ**を排出対象にしていた。しかも対象は `_E2E_TAIL` 決め打ちで **consistency 側（`_MUTATIONS_TAIL`）は一度も rotate されたことがなかった**。literal 6 件 / append 87 件で advisory を超え**ツールからは詰み**に（「advisory は BLOCKING の手前で効かせる」規律が道具の側で成立しなくなる）。rotate 単位を「literal ∪ append ブロック」へ拡張し出現順（=時系列順）で排出、移動元は単位数の多い tail、移動先 chain も引数化した（977→935 行・総数不変・**旧実装は同じ状態で拒否していた**）。同 PR で **Check 436 を `docs/files/` mirror へ拡張（射程 26 → 260 file）** —— 前 PR で手作業で 9 枚掃引したのに**綴りを 3 つ見落として 4 枚残った**＝per-instance では閉じない class だと実測で判ったため。歴史記録の mirror（incident-artifacts / session-records）は対象外（履歴を濁さない）。**教訓**: (i) **仕様は要約でなく原文で読め**（`item` と `api-catalog` の差は要約で潰れる）。(ii) **測定系を疑う（本 run で 4 回、4 回とも当たった）** —— robots.txt のパーサが**コメント行を空行として group をリセット**し正常な 4 行を「user-agent より前の rule」と誤検出（RFC 9309 の ABNF では空行もコメント行も group を終端しない）/ JSON-LD の `@graph` を `@`-prefixed キーとして飛ばし「型ゼロ」と報告 / Check 436 の非 vacuity 検証が素通り / Check 450 が自分の説明文にマッチ。**いずれも「エラー」ではなく「それらしい数字」として出る**。(iii) **ゲートを作ったら、そのゲートが動機となった実例を捕捉できるか確かめよ**。(iv) **道具も宣言と実態が乖離する**（無限成長を止めるための道具が、成長する場所を排出できなかった）。(v) **「Check を作らない」判断も成果物**。**実測して honest clean と確認済み（再監査不要）**: `sitemap.xml`（sitemaps.org 0.9・23 URL・lastmod の W3C Datetime / changefreq 列挙 / priority 範囲 / XSD sequence 順すべて適合）/ `robots.txt`（RFC 9309・正しい group semantics で 0 件）/ JSON-LD の 23 型とプロパティ（`OrganizationRole` の Role パターン含む。**ただしオフラインでは語彙を権威的に検証できない**ためネットワーク依存の brittle な Check も語彙同梱も採らない）/ ACD-1.0 の相互参照 30 件。**次の未採掘 vein（reflect 済・非拘束）**: 未実証 e2e の残り / apps 間のより深い相互作用 / observability。
- **Claude Code 用ファイル一式が揃った + 全ファイル 1-to-1 docs が機械強制 (2026-06-14)。** 後続セッションは次の順に読めば cold-start で復帰可能: (1) 本 CLAUDE.md §7、(2) `AI2AI.md` の最新 Session Record、(3) `docs/architecture/total-check-runbook.md` §9 (consistency Check 総数の**真値**。数値はここを参照し、本 §7 の数値が drift した場合も §9 を正とする)、(4) 個別ファイルの doc は `docs/files/<path>.md` で 1 対 1 で読める。深い drift 監査が必要なら `/audit` を起動 (`repo-auditor` sub-agent が読み取り専用で 6 dimension をチェック)。Check 追加が必要なら `/increment` → `check-author` sub-agent。AIO 編集が必要なら必ず `aio-guardian` sub-agent を経由する (C6 enforcement)。

---

## 8. Design note — why CLAUDE.md is not in the AIO layer

`CLAUDE.md` is **dev-tooling orientation**, deliberately **excluded from the AIO discovery layer** (not registered in `sitemap.xml` / `robots.txt` / `aio-manifest.json`; no digest). It is an entry point for implementing agents, not an authority signal for AI crawlers — keeping it out of the AIO surface keeps that surface clean and avoids C6 entanglement. `CLAUDE.md` (router), `Claude2Claude.md` (bash procedures + Claude↔Claude evidence), and `AI2AI.md` (canon: full constraints / KERNEL / Session history) are role-separated and must not duplicate each other; always defer to the higher document for detail.
